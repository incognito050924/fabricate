# GOAL — 돌아가는 인터뷰 하네스 하나

> **이 문서는 비준 후 읽기 전용이다.** 진척·발견·다음 조각은 `STATE.md`에 적는다.
> 이 파일을 고치려면 §9를 밟는다. 고치고 싶어질 때가 §3을 읽을 때다.
>
> 상태: **비준됨 — 2026-07-27, 개정 5 반영**(훅이 둘이 아니라 셋 — 모델 호출 경로를 덮는 `PreToolUse`(매처 `Skill`) 추가와 원문 비대칭에 따른 IP-2ⓚ·ⓜ 강화 · IP-0ⓐ 증거를 4단으로 강화 · §4-7의 적용 범위를 §8에 명시). 개정 4(훅 매처 문자열 정정 · §8 훅 봉쇄 잔여를 실측으로 갱신)를 포함한다. 이 줄 아래 본문은 §9 없이 고치지 않는다.
> 본문 해시(sha256, 9행 이후): 2a4ba701d13dbf3670871d840224c86f66f1cb237cfc0772a62c230ca71c088f
> 검증: `sed '1,8d' GOAL.md | shasum -a 256`

---

## 0. 재진입 규약

이 루프에 들어오는 모든 세션과 서브에이전트는 **이 문서 전문 + `STATE.md` 요약**을 받는다.

- **이 문서를 요약해서 전달하는 것은 금지다.** 짧으니 전문을 붙인다.
- **이전 조각의 산출물을 시드로 주지 않는다.** 의도가 변질되는 기제는 쪼개는 것 자체가 아니라, 다음 걸음의 입력이 원 목표가 아니라 직전 걸음의 산출물일 때다. 매 걸음은 여기로 돌아와서 시작한다.
- 사용자가 쓴 원 문장이 필요하면 `contract/original-request.md`를 **직접 읽는다**. 인용도 원문으로 한다.

---

## 1. 목표 세계

이 루프가 끝난 세계에서 다음이 참이다.

**사용자가 명령 하나로 인터뷰를 시작한다.** 대화가 오가고, 끝나면 잠긴 의도 레코드가 디스크에 남는다. 그 레코드는 사용자가 실제로 쓴 문장과 "무엇이 달성돼 있으면 완료인가"를 담는다. 요약본이 아니다.

**그 레코드가 다음 걸음의 시드다.** 새 세션이든 다른 에이전트든, 그 파일을 읽으면 이전 대화를 복원하지 않고도 같은 뜻 위에 선다.

**그 레코드가 완료를 통치한다.** 잠긴 의도 없이는 "다 됐다"를 선언할 수 없고, 잠긴 의도의 충족 상태가 실제로 관측되지 않으면 완료가 부적격이 된다.

**판단은 모델이 하고, 검사는 기계가 한다.** 좋은 질문을 만드는 것도, 모순을 알아보는 것도, 무엇이 중대한지 재는 것도 모델의 일이다. 기계는 **그 절차가 실제로 밟혔는지와 산출물의 형상**만 본다 — 끝내도 되는가, 무엇이 남았는가, 무엇이 잠겼는가, 합의된 말을 쓰고 있는가.

### 왜 이것이 필요한가 — 4줄짜리 인터뷰 스킬이 못 하는 것 셋

모델은 이미 인터뷰를 잘한다. laddering도, 삼원 유도도, pre-mortem도 아는 기법이다. 그걸 코드로 다시 만드는 것은 이 루프의 목표가 아니다. 프롬프트만으로는 **구조적으로** 안 되는 것이 셋 있고, 이 하네스는 정확히 그 셋이다.

1. **끝을 모델이 정한다.** "충분히 이해했다"를 모델이 선언하면 끝난다. 자기 확신은 자기가 못 깬다.
2. **남지 않는다.** 대화가 끝나면 사라진다. 다음 세션이 못 읽는다.
3. **구속력이 없다.** 인터뷰 결과가 나중의 완료 판정에 아무 힘을 못 쓴다.

### 아키텍처 — 네 부분, 그 이상 없음 (2026-07-26 사용자 확정)

**fabricate는 Claude Code를 대상으로 하는 하네스이고, Claude Code 안에서 돈다. fabricate가 Claude Code를 호출하지 않는다.** 모델은 호스트가 낸다.

| 부분 | 매체 | 하는 일 |
| --- | --- | --- |
| 스킬 | 프롬프트 | 인터뷰를 어떻게 하는가 + 매 턴 CLI를 부르라는 지시 + 서브에이전트 위임 지시 |
| 에이전트 정의 | 프롬프트 | 서브에이전트의 역할·경계·반환 형식 |
| CLI | 코드 | 턴 기록 · 게이트 · 잠금 · 거부. 대화하지 않는다 — **상태 관리자다** |
| 훅 (세 사건) | 코드 | 스킬의 지시를 **안 지켰을 때** 잡는다. 디스크를 읽고 판정한다 |

따라서 **모델 어댑터 · API 키 · 스트리밍 · 재시도 · 호스트 추상화는 존재하지 않는다.** Codex 대응도 이중 호스트 계층도 짓지 않는다(§5).

#### 훅이 이 설계의 급소다 — 사건 셋

fabricate가 호스트를 부르지 않으므로, 에이전트가 CLI를 건너뛰면 장부가 비고 기계는 그것을 모른다. 대화창에서 "다 파악했습니다"로 끝나면 §1의 셋 중 무엇도 성립하지 않는다. **그래서 강제력은 프롬프트가 아니라 훅에 있다** — 프롬프트는 질문을 잘하게 하고, 훅은 안 지킨 것을 잡는다. §4-1 판별식의 실제 착지 지점이다.

| 사건 | 언제 | 무엇을 한다 |
| --- | --- | --- |
| `UserPromptExpansion` | **사용자가 타이핑한 슬래시 명령**이 프롬프트로 확장될 때, 모델에 닿기 전. 매처는 **네임스페이스가 붙은 `command_name`**(`fabricate:deep-interview`)과 비교된다 — 대소문자 구분, 메타문자 없으면 완전 일치(§6-6) | **활성 표식을 디스크에 만든다. 사용자 원문(`command_args`)을 함께 앉힌다** |
| `PreToolUse` (매처 `Skill`) | **모델이 스스로 `Skill` 도구로 같은 스킬을 부를 때.** 매처는 `tool_name` 정규식일 뿐이라 `Skill(name)` 형식이 안 먹는다 — 스킬 이름은 스크립트가 `tool_input.skill`로 검사한다(§6-6) | **같은 활성 표식을 만든다. 단 원문은 없다** — `tool_input.args`는 모델이 지어낸 것이다 |
| `Stop` | **Claude가 응답을 끝낼 때** — 턴마다 | 활성 표식이 있는데 **이번 턴에 장부가 늘지 않았으면** 막고 사유를 돌려준다 |

**왜 셋인가.** `Stop`만 있으면 `deep-interview start`를 건너뛴 세션은 활성 표식이 없어서 일반 대화와 구분되지 않는다 — 훅이 판정할 근거 자체가 없다. 그 근거를 모델 밖에서 만드는 자리가 **둘**이다. **`UserPromptExpansion`은 사용자가 타이핑한 슬래시 명령에만 발화하고, 모델이 `Skill` 도구로 같은 스킬을 호출하면 발화하지 않는다**(2026-07-27 실측, 4회 재현. UPE 훅 실행의 유일한 호출 사슬이 슬래시 명령 처리이고 `expansion_type` 열거형에 스킬 도구용 값이 아예 없다). 그 경로를 덮는 것이 `PreToolUse`(매처 `Skill`)이고, 그 입력에도 **`prompt_id`가 오므로** `Stop`의 턴 회계와 같은 경계를 공유한다. 두 표식 생성기는 **멱등해야 한다** — 사용자 타이핑 경로에서도 모델이 확장 직후 `Skill` 도구를 한 번 더 부르는 것이 관측됐다(한 턴에 UPE 1회 + `PreToolUse` 1회가 정상 경로다).

**받아야 할 비대칭: 사용자 원문은 `UserPromptExpansion`에만 있다.** `Skill` 도구 호출에는 원문이 없다. 따라서 **모델 호출 경로로 시작한 세션은 "원문 없음" 상태로 표식만 서고, `close`가 원문 부재를 이유로 거부한다.** 봉쇄는 두 경로 다 서지만 잠금 자격은 원문이 확보된 경로에만 준다 — IP-1ⓕ와 §4-3이 그 자리를 지킨다.

Stop이 **턴마다** 도는 것이 규칙의 형태를 정한다. "잠긴 의도가 없으면 막는다"로 쓰면 인터뷰 진행 중 매 턴이 막힌다. 옳은 규칙은 **"활성 표식 + 이번 턴에 장부 증가 없음 → 막는다"**이고, 이것이 "모델이 CLI를 건너뛰었다"를 직접 잡는다.

**실측으로 닫힌 둘** (2026-07-27, §8): 사용자가 인터럽트하면 `Stop`은 **발화하지 않는다.** 연속 차단에는 **기본 8회의 캡**이 있고 넘긴 차단은 **조용히 버려진다.** 즉 인터럽트 한 번으로 `Stop`을 완전히 우회할 수 있다 — 봉쇄는 원리적으로 불완전하다. 그래서 훅은 **완벽한 봉쇄가 아니라 우회를 비싸고 눈에 보이게 만드는 장치**로 쓴다. 훅이 뚫려도 IP-4가 마지막 방어선이다.

#### 서브에이전트

**스킬이 띄운다 — CLI가 아니다.** 스킬이 호스트의 Agent 도구를 쓰라고 지시하고 호스트가 띄운다. `fabricate` 실행 파일은 어떤 에이전트도 프로세스도 부르지 않는다. 에이전트 정의는 이 저장소의 산출물이지만 **프롬프트이고 코드가 아니다**(§4-1).

- **위임이 필수인 지점은 이번 루프에서 하나다** — 질문이 사용자에게 가기 전의 **세션-맹검 판단**. 드라이버는 대화 서사를 알기 때문에 자기 질문이 서사 없이도 답할 수 있는지 판정할 수 없다. 자기 채점이 구조적으로 불가능한 유일한 자리이고, 그래서 IP-5가 된다.
- 그 밖의 위임(반론·조사)은 **허용이고 필수는 아니다.** 필수로 만들면 증명할 수 없는 것을 증명한다고 주장하게 된다(§4-5).
- **서브에이전트는 사용자에게 묻지 않는다.** 사용자와 말하는 자리는 드라이버 하나다. 서브에이전트는 읽기 전용 판단만 하고 돌려준다.
- **서브에이전트의 산출은 CLI를 통해 장부에 들어간다.** 대화 안에만 있으면 기계가 못 본다 — 턴 건너뛰기와 같은 구멍이다.

### 출처

| 문서 | 지위 |
| --- | --- |
| `contract/original-request.md` | **원 요청. 사용자가 쓴 문장 그대로. 요약 금지.** |
| `contract/research-report.md` | 의도 형성 품질 조사 — 기법의 출처. 대부분 프롬프트로 착지한다 |
| `contract/criteria.json` · `criteria.md` · `contract-draft.md` | **참고 신호로 강등.** 완료 정의가 아니다(§3) |
| `contract/reviews/` | 착수 전 심의 세 건 — 위험 목록으로 유효 |
| `audit/2026-07-26-piece3-audit.md` | 1차 시도가 어떻게 실패했는지의 실측. §3의 근거 |
| `PLAN.md` · `NEXT.md` | **삭제됨** (2026-07-27). 1차 시도의 계획·인수인계였고 이 문서와 `STATE.md`가 대체했다. git 히스토리에만 남는다 |

---

## 2. 통합 술어 — 이것이 완료 정의다

**개별 단위 테스트의 초록은 완료가 아니다.** 아래 일곱이 전부 통과할 때, 그리고 그때만 이 루프가 끝난다. **무엇이 참이어야 하는지만 고정한다 — 어떻게 그렇게 만드는지는 정하지 않는다.**

**모든 술어는 대칭 증거를 갖는다** — 통과해야 하는 경우와 거부해야 하는 경우가 **둘 다** 있다. 한쪽만 적으면 아무것도 안 하는 구현이나 항상 거부하는 구현이 통과한다. 1차 시도에서 빈-껍데기 반박이 69개 중 43개를 잡았던 것이 이 실패다.

| id | 무엇을 증명하는가 | 증거 (전부 필요) |
| --- | --- | --- |
| **IP-0** | **남의 기계에서 즉시 쓸 수 있다** | ⓐ **설치 → 실행 → 재설치 → 재실행 4단**을 통과한다. 깨끗한 체크아웃에서 **문서에 적힌 설치 명령 하나**를 돌리고 Claude Code를 재시작하면 `/fabricate:deep-interview "<요청>"`이 실행되고, 같은 설치를 한 번 더 돌린 뒤 다시 실행해도 같다. **증거는 설치 명령의 성공이 아니라 새 프로세스에서 슬래시 명령이 실제로 훅을 발화시켰다는 관측이다** — 프로젝트 스코프 설치는 전역 `known_marketplaces.json`에 의존해서, 다른 프로젝트에서 마켓플레이스를 지우면 **프로젝트 설정은 살아 있고 슬래시 명령만 사라지며 종료 코드 0에 stderr가 비어 있다**(2026-07-27 실측). 설치 성공은 증거가 아니다.<br>ⓑ 두 번 돌려도 같은 결과다 — 훅 중복 등록 없음<br>ⓒ 기존 Claude Code 설정이 유실되지 않는다<br>ⓓ **음극**: 설치가 실패하면 변경 전 상태가 보존된다 |
| **IP-1** | 끝까지 돈다 | ⓐ **사용자 표면**: `/fabricate:deep-interview "<요청>"`이 실제로 인터뷰를 연다 — 활성 표식이 생기고 첫 질문이 사용자에게 간다. 저수준 CLI만 도는 것으로는 못 닫는다<br>ⓑ **내부**: `start` → `turn record` ×N → `close`가 장부에 남는다<br>ⓒ `close`가 통과하면 `.fabricate/intent/<id>.json`이 생기고 exit 0<br>ⓓ **음극**: 스킬이 질문을 하나도 하지 않은 세션은 `close`가 거부한다<br>ⓔ **고아 차단**(원 요청 G2): 어느 목표 술어를 위한 질문인지 안 적힌 질문은 `turn record`가 **거부**한다. 목표에 안 닿는 질문은 발사 자격이 없다<br>ⓕ **확정 바인딩**(원 요청 A3): 저장된 레코드가 **사용자에게 보여준 문안의 해시**와 묶여 있다. 보여준 것과 저장되는 것이 다르면 거부<br>ⓖ **되말하기 상태기계**(원 요청 B1): 답은 `candidate`로 들어오고, **다른 말 + 구체 사례**로 재진술해 사용자가 확인해야 `confirmed`가 된다<br>ⓗ **음극**: 사용자 말과 너무 겹치는 재진술(에코)은 확인으로 안 쳐준다 — 앵무새는 검사가 아니다 |
| **IP-2** | 못 끝내면 **실제로 막힌다** | **`close`를 막는 것 — 인터뷰 내용에서 여섯**(개시 형상에서 하나 더는 ⓜ). 각각 무엇이 왜 남았는지 출력하고 레코드를 만들지 않는다:<br>ⓐ 미해소 차원이 남았다<br>ⓑ **미커버 조각**(원 요청 A1): 사용자 말의 조각 중 **어느 질문에도 안 걸린 것**이 있다. 차원 목록이 사용자의 실제 말에 묶이지 않으면 ⓐ는 공허하다<br>ⓒ **무증거 해소**(원 요청 A2): 근거와 사용자-답 마커 없이 닫힌 차원은 `unevaluated`(닫힘 아님)로 남는다. 모델의 "이건 해결됐다" 선언만으로는 안 닫힌다<br>ⓓ **stale 노드**(원 요청 A5): 전제가 뒤집혀 다시 열린 것이 남아 있다<br>ⓔ **미교정 불일치**(원 요청 B1): 되말하기가 `confirmed`에 닿지 못한 항목이 남았다<br>ⓕ **모순 패스 미실행/미해소**(원 요청 B2): 잠금 전 교차-답변 검사가 **한 번도 안 돌았거나**, 돌아서 찾은 모순이 안 풀렸다. **안 돌았으면 안 돌았다고 정직하게 기록된다** — 끝난 척할 수 없다<br>**준비도가 실제 신호를 읽는다**(원 요청 A4):<br>ⓖ 준비도 판정의 입력이 B2가 센 모순 개수 · "잘 모르겠다" 답 수 · 강등된 판정 수다. **음극**: 상수를 읽는 구현은 통과로 세지 않는다 — 1차 시도의 하한이 사실상 0이어서 아무것도 안 막았다<br>**전제 뒤집힘 전파**:<br>ⓗ **양극**: 기존 전제를 뒤엎는 답이 오면 그 전제에 기대던 하류 전부가 stale이 되어 **다시 인터뷰 대상이 된다**<br>ⓘ **음극**: 전제와 무관한 답은 하류를 건드리지 않는다<br>**우회를 잡는 것**:<br>ⓙ 활성 표식이 있는데 장부가 늘지 않은 턴 → **Stop 훅이 막고 사유를 돌려준다**<br>ⓚ 표식이 **모델 재량 밖의 두 자리**에서 생긴다 — 사용자 타이핑 경로는 `UserPromptExpansion`, 모델 호출 경로는 `PreToolUse`(매처 `Skill`). 그래서 모델이 `start`를 건너뛴 세션도 잡힌다. 두 생성기는 멱등하다 — 한 턴에 둘 다 발화해도 표식은 하나다<br>ⓛ **음극**: 정상 진행 턴(장부가 늘었음)은 막지 **않는다**. 인터뷰가 진행 불가가 되면 안 된다<br>**개시 형상 — 일곱째 거부**:<br>ⓜ **원문 없이 시작된 세션**: 모델 호출 경로(`PreToolUse`)에는 사용자 원문이 없다(`tool_input.args`는 모델이 지어낸 것이다). 표식은 서지만 **`close`가 원문 부재를 이유로 거부한다** — 잠긴 레코드는 사용자가 실제로 쓴 문장 없이는 만들어지지 않는다(§4-3) |
| **IP-3** | 세션을 넘어 **시드가 된다** | ⓐ 새 프로세스에서 `deep-interview show <id>`가 사용자 원문과 목표 술어를 낸다, exit 0<br>ⓑ **원문 대조**: show 출력의 사용자 문장이 세션 파일의 바이트와 같다 — 요약이 끼어들지 않았다(§4-3)<br>ⓒ **주입 경로**: 그 출력이 새 세션의 컨텍스트로 실제로 들어가는 경로가 있고 도달 가능하다. 출력만 되고 아무도 안 읽으면 저장을 증명한 것이지 시드를 증명한 것이 아니다<br>ⓓ **음극**: 없는 id·잠기지 않은 세션 → exit ≠ 0 |
| **IP-4** | 완료를 통치한다 | ⓐ **양극**: 잠긴 의도 + 유효한 충족 증거 → **exit 0**<br>ⓑ 잠긴 의도 없음 → exit ≠ 0<br>ⓒ 증거 없음 · 검증 명령 실패 · 오래된 증거 → exit ≠ 0<br>ⓓ **다른 intent의 증거 재사용** → exit ≠ 0 |
| **IP-5** | **스스로 채점하지 않는다** | ⓐ 질문이 사용자에게 가기 전, 세션-맹검 판단이 장부에 기록으로 남는다<br>ⓑ **음극**: 그 판단이 거부하면 질문은 그대로 사용자에게 가지 않는다<br>ⓒ 판단자와 드라이버가 다른 컨텍스트다 — 같은 컨텍스트의 자기 채점은 통과로 세지 않는다 |
| **IP-6** | **물을 값어치가 있는 것만 묻고, 물은 것은 구체화된다** | **중대성 라우팅**(원 요청 B5):<br>ⓐ 모호한 지점마다 해석 k개의 산출물 diff를 보고 라우팅한다 — 안 달라짐 → 가정하고 **보이게 기록** / 달라짐 → 분기점을 질문 / 고위험·비가역 → 무조건 질문<br>ⓑ **다양성 바닥**: 해석을 하나만 만들어 놓고 "안 달라진다"로 가는 것은 거부한다<br>**증거로 도전**(원 요청 B4):<br>ⓒ 답이 **현재 코드**와 모순되면 **근거 인용과 함께** 다음 라운드 질문이 선다 — 권위는 코드다<br>ⓓ **음극**: 인용 없는 "도전"은 `turn record`가 비승인한다<br>**예시-판정 구체화**(원 요청 B6):<br>ⓔ hard 항목마다 **사용자가 판정한 예시 ≥1**에서 판정 기준을 만든다 — 합의와 검증이 갈라질 수 없다<br>ⓕ hard/soft 타이핑 — soft는 가짜 기준을 만들지 않고 사람 판정으로 남긴다<br>ⓖ **면제 금지**: 다른 증거가 있다고 예시를 건너뛸 수 없다 |

일곱 문장으로 요약된다:

> **설치된다 · 돈다 · 못 끝내면 막힌다 · 남는다 · 구속한다 · 자기 채점 안 한다 · 값어치 있는 것만 묻는다**

**합의 용어집은 이번 루프에서 짓지 않는다**(2026-07-27 사용자 확정, §5). 따라서 사용자에게 나가는 말을 기계가 대조할 기준이 없고, **역방향 언어 품질은 프롬프트에만 맡겨진다**(§8 잔여).

IP-5는 **질문의 질을 판정하지 않고**, IP-6은 **중대성·모순·예시의 내용을 판정하지 않는다**(전부 §8 잔여다). 기계가 보는 것은 절차가 밟혔는가와 산출물의 형상뿐이다 — **판단은 모델이, 검사는 기계가.**

### 기계 통과를 닫는 명령은 하나다

`bun run verify` 하나가 아래를 전부 돌린다. **에이전트는 이 명령 하나로 자기 상태를 확인하고, 이것이 초록이 아니면 `기계 통과`를 주장하지 않는다.**

- IP-0~6의 fixture 전부 (양극·음극 둘 다)
- 실제 `Stop` · `UserPromptExpansion` · `PreToolUse`(매처 `Skill`) 훅 통합 검사 — 훅 스크립트를 모사한 것이 아니라 설치된 경로로
- 운영 모듈 도달 가능성 검사 (§6)
- `typecheck` · `lint` · `test`
- 설치된 스킬·에이전트 정의·훅 경로가 이 저장소를 가리키는지

### 완료 상태 둘 — 섞지 않는다

| 상태 | 뜻 | 누가 판정 |
| --- | --- | --- |
| `기계 통과` | `bun run verify`가 초록 — IP-0~6의 양극·음극 증거 전부 | 에이전트가 닫는다 |
| `사람 검증 대기` | 실제 사람과 인터뷰 1회를 돌려 대화의 질을 확인 | **사람만** 닫는다 (§7 마지막 관문) |

에이전트는 `기계 통과`까지 혼자 간다. **`기계 통과`를 완료라고 보고하지 않는다** — 남은 상태의 이름을 함께 말한다.

---

## 3. 왜 하나의 단위인가 — 쪼개고 싶어지면 여기를 읽어라

**1차 시도는 이 목표를 조건 69개로 쪼갰고, 조건 하나에 모듈 하나씩 지었다. 결과는 실측돼 있다.**

- 조건 33개가 초록이 됐다. 독립 감사 여덟이 되짚은 결과 **파손 4 · 의심 22 · 기록만 7**.
- 부정행위는 0이었다. 동결 무결, 계약 무수정, 옛 코드 유입 0. **문제는 전부 "테스트는 통과하는데 문안은 못 지킨다"였다.**
- 서로 모르는 감사자 넷이 같은 결론에 도달했다: **지은 것 대부분이 비-테스트 호출자가 없는 모듈 섬이다.**
- 초록 33개로 **돌아가는 것은 0개**였다.

원인은 조건 원문을 안 줘서가 아니다. 각 조각은 계약 원문을 그대로 받았다. **원인은 "이게 다 서면 무엇이 돌아가는가"를 아무도 보고 있지 않았다는 것**이다. 그래서 각자 자기 조건만 만족하는 최소 형상을 지었고, 최소 형상은 스키마 한 줄이었고, 호출자가 없었다.

> **목표는 부품 목록이 아니라 도는 경로다. 경로는 나눌 수 없다.**

작업을 여러 걸음으로 나누는 것은 허용된다 — context rot 때문에 어차피 나뉜다. 나누면 안 되는 것은 **의도**다. 실무 규칙:

- 매 걸음은 직전 걸음이 아니라 이 문서를 시드로 삼는다 (§0).
- 원자성의 단위는 컨텍스트가 아니라 **병합**이다. **§2가 전부 초록일 때만 `main`에 착지한다.** 중간 커밋은 브랜치/워크트리에서 자유롭게 한다. — 이건 규칙이다. 당신의 "한 방에"가 실제로 뜻하는 바가 이것이고, 이걸 놓으면 미완의 조각이 `main`에 쌓이면서 다시 조각으로 관리되기 시작한다.
- **조건 목록을 다시 만드는 것은 금지가 아니라 경고다.** 구현하다가 하위 사례를 열거하는 것은 정상이다. 위험한 것은 **그 목록이 §2를 대신해 완료 정의가 되는 순간**이다. 목록이 생기면 그것이 IP를 전진시키는지 확인하고, 아니면 버린다.

---

## 4. 불변식 — 위반하면 그 작업은 실패다. 되돌린다

1. **더 좋은 모델로 바꿨을 때 개선되는 것은 코드로 쓰지 않는다.** 개선되면 프롬프트 자리, 모델과 무관하면 코드 자리다. laddering 품질은 모델이 낸다. "잠긴 의도가 세션을 넘어 남는가"는 모델과 무관하다.
2. **뿌리(CLI 진입점 · 훅 진입점)에서 도달할 수 없는 모듈은 완료로 세지 않는다.** "임포터가 하나 이상"으로 읽지 않는다 — 그 표현은 진입점 자신을 실패로 만들고 죽은 모듈끼리의 순환을 통과시킨다. 검사는 §6에 있다.
3. **사용자가 쓴 문장은 요약되지 않는다.** 저장할 때도, 다음 걸음에 넘길 때도, 서브에이전트에게 위임할 때도 원문이다. 요약이 원문을 대체하는 것이 이 하네스가 막으려는 실패 그 자체다.
4. **완료는 §2로만 판정한다.** 단위 테스트가 몇 개 초록인지는 완료와 무관하다.
5. **기계가 못 판정하는 것은 잔여로 적는다.** 판정하는 척하는 스키마를 짓지 않는다. `.length(3)`은 세 개를 세지만 세 개를 내놓게 만들지는 못한다.
6. **게이트는 실제 경로 위에 선다.** 검사를 가진 함수와 실제로 쓰이는 함수가 다르면 그 게이트는 없는 것이다. **잠금을 우회하는 쓰기 경로가 없어야 한다** — 몇 개의 모듈로 짓는지는 자유다.
7. **거부는 관측 가능해야 한다.** 조용히 넘어가는 실패를 만들지 않는다. 거부는 종료 코드와 사람이 읽을 문장으로 나온다.
8. **설치는 멱등이고 기존 사용자 설정을 유실시키지 않는다.** 두 번 돌려도 같은 결과이고, 훅이 중복 등록되지 않으며, **실패하면 변경 전 상태를 보존한다.** 사용자의 Claude Code 설정을 덮어쓰지 않는다.
9. **검증이 사용자의 실제 설정을 오염시키지 않는다.** `bun run verify`는 격리된 위치에서 돈다.
10. **절대경로에 의존하지 않는다.** 저장소를 옮기면 깨지는 설치는 설치가 아니다.

8·9·10은 §7-3(비가역 위험)에 걸리는 것들이라 불변식으로 둔다 — 되돌림이 작동하지 않는 유일한 부류다.

---

## 5. 비-목표 — 이번 루프에서 짓지 않는다

- **ditto의 자산**(ACG · e2e · hosts · memory-graph · worktree · journey-authoring · CodeQL). 손대지 않는다. 인터뷰 하네스가 실제로 쓸 만해진 뒤 증거를 보고 정한다.
- **1차 시도 fabricate 코드의 승계.** 읽는 것은 허용, 가져오는 것은 금지. 나쁜 코드까지 딸려 와 끼워 맞추는 비용이 다시 쓰는 비용보다 크다 (2026-07-26 사용자 결정).
- **모델 어댑터 · API 키 · 스트리밍 · 재시도.** 모델은 Claude Code가 낸다(§1 아키텍처).
- **호스트 추상화 계층 · Codex 대응 · 이중 호스트.** 대상은 Claude Code 하나다. ditto의 `src/core/hosts`를 재현하지 않는다. 나중에 필요해지면 그때 짓는다 — 지금 짓는 것은 쓰이지 않을 이음매 하나를 미리 지는 것이다(§4-5).
- **오케스트레이션 루프 · autopilot · 노드 그래프.** 스킬이 서브에이전트를 띄우는 것은 여기 해당하지 **않는다**(§1 아키텍처) — 금지되는 것은 코드로 짓는 상태 기계이고, 스킬의 위임 지시는 프롬프트다.
- **dialectic 3역할 · 멀티 모델 적대 검토.** 다음 루프. 서브에이전트 능력 자체는 §1이 허용하므로, 이번 루프에서 필요한 만큼(편향 분리)만 쓰고 3역할 심의 절차는 짓지 않는다.
- **인터뷰 기법을 코드로 재현하는 것** — laddering · 삼원 유도 · bipolar · pre-mortem 항목 · 유도질문 탐지 · 질문 모드 다양성. **기법 자체는 스킬(프롬프트)로 이번에 지어진다.** 금지되는 것은 기법을 코드 모듈로 다시 구현하는 것이다(§4-1).
- **합의 용어집 · 역방향 언어 렌더링의 기계 검사**(원 요청 묶음2 일부). 2026-07-27 사용자 확정. 용어집이 빠지면 사용자에게 나가는 말을 대조할 기준 자체가 없어지므로 **언어 품질은 프롬프트에만 맡겨진다**(§8 잔여). 함께 배제되는 것: i18n 카탈로그(키→문자열 표) — **문구를 만드는 것이 LLM인데 기계적 번역표를 들고 갈아끼우는 것은 앞뒤가 안 맞는다.** 1차 시도는 578줄을 썼고 관리 대상 문구는 10개였다.
- **EARS lint.** `the system shall` 영어 정규식인데 사용자 표면은 한국어다. IP-6의 예시-판정은 남고 lint만 뺀다.
- **B3 이견 비교쌍**(원 요청 묶음4). 반론 위임에 붙어 있는데 §1이 그 위임을 "허용이고 필수는 아니다"로 정했다. 필수로 만들려면 §1을 먼저 고쳐야 하므로 이번엔 뺀다.
- **Tier U 본체(U1~U10)**(원 요청 묶음2). 연구 보고서가 **"deep-interview 밖 — 모든 턴에 적용"**이라 스스로 규정했고, 얹는 자리도 스키마·게이트가 아니라 헌장 + per-turn 훅이다. 이번 루프는 인터뷰 명령 하나이므로 **대상이 다르다.** 역방향 언어 렌더링도 위 항목에 따라 함께 빠진다.
- **묶음7 Tier C**(C2 세-장부 · C3 충실도 사다리 · C4 보정+회귀 하네스). 원 요청이 이미 **"전부 design 노드"**로 적었다 — 애초에 구현 대상이 아니었다.
- **조건 목록 · 판정표 · 테스트 동결 절차의 재구축.**

---

## 6. 살아있는 경로 규약

완료로 세려면 아래 **여섯이 전부** 참이어야 한다. **무엇이 참이어야 하는지는 고정이고, 어떻게 그렇게 만드는지는 자유다.**

| # | 무엇이 참이어야 하는가 | 왜 |
| --- | --- | --- |
| 1 | 진입점이 실재한다 — `fabricate --help`에 `deep-interview`·`turn`·`check`가 뜬다 | 설치했는데 부를 수 없으면 없는 것이다 |
| 2 | **운영 모듈 전부가 뿌리(CLI · 훅)에서 도달 가능하다** | 1차 시도의 실패를 만든 단 하나의 자리 |
| 3 | **잠금을 우회하는 쓰기 경로가 없다** | `.fabricate/intent/`에 쓰는 권한이 갈라지면 게이트를 지나지 않은 레코드가 생긴다. 물리적으로 한 모듈일 필요는 없다 — **우회 경로가 없으면 된다** |
| 4 | 거부가 셸에 보인다 — 종료 코드 ≠ 0 + 사람이 읽을 사유 | 조용한 실패는 실패가 아니라 거짓 성공이다 |
| 5 | 훅이 설치된 경로로 실제 발화한다 | 설정 파일만 있고 안 도는 훅은 없는 훅이다 |
| 6 | **훅이 실제 명령 이름에 발화한다** — 매처가 그 이름과 맞거나, 매처에 기대지 않는다 | 같은 교훈이 **두 사건**에 걸린다. `UserPromptExpansion`의 매처는 **네임스페이스가 붙은 `command_name`**(`fabricate:deep-interview`)과 비교되고, **대소문자를 구분하며 정규식 메타문자가 없으면 완전 일치다**(2026-07-27 실측). 그래서 네임스페이스를 뺀 `deep-interview`를 적으면 훅은 **조용히 한 번도 발화하지 않으면서 설치 검사는 통과한다.** `PreToolUse`의 매처는 **`tool_name`에 대한 정규식일 뿐이다** — `Skill`은 매칭되지만 **`Skill(deep-interview)` 형식은 매칭되지 않는다.** 스킬 이름 판별은 매처가 아니라 스크립트 안에서 `tool_input.skill`(네임스페이스 포함, `fabricate:deep-interview`)로 해야 한다. 이름을 바꿀 때 함께 바꿔야 하는 유일한 자리. **더 안전한 형상은 매처를 생략(또는 `fabricate:.*`처럼 넓게)하고 훅 스크립트 안에서 이름을 검사하는 것** — 그러면 이름 변경 시 조용히 죽는 위험 자체가 사라지고, `command_name`과 `tool_input.skill`이 같은 문자열이라 정규화 함수 하나로 처리된다 |

2번이 **유일한 구조적 도달성 게이트**다 — 나머지 다섯은 진입점·쓰기 권한·거부 가시성·훅 발화·훅 매처를 각각 본다.

2번을 "모든 모듈에 임포터가 하나 이상"으로 쓰면 두 방향으로 무너진다 — CLI 진입점 자신은 임포터가 없어 실패하고, 죽은 모듈끼리 서로 import하면 통과한다. **뿌리에서의 도달 가능성**이어야 둘 다 막힌다.

여섯 다 사람의 성실성이 아니라 **`bun run verify` 안의 스크립트**로 확인된다. 언제 짓는지는 정하지 않는다 — `verify`가 초록이 될 때 거기 있으면 된다.

---

## 7. 중단 조건 — 넷뿐이다

**기술적 모호함은 스스로 정한다.** 틀렸으면 `bun run verify`와 실제 인터뷰가 되돌려 보낸다. 그게 §2가 존재하는 이유다. 멈춰야 하는 것은 **되돌림이 작동하지 않는 종류**뿐이다.

1. **`contract/original-request.md`와 이 문서가 실질적으로 충돌할 때.** 원 요청이 이긴다.
2. **사용자만 정할 수 있는 제품 의미가 갈릴 때.** 어느 쪽으로 가도 동작하지만 뜻이 달라지는 갈림길.
3. **비가역 위험이 있을 때** — 사용자 데이터·기존 설정·외부 환경을 훼손할 수 있는 변경.
4. **§4의 불변식이나 §2의 술어를 약화시키지 않고는 진행할 수 없을 때.** 술어를 목표에 맞추는 게 아니라 목표를 술어에 맞추게 되는 순간이다.

그 밖에는 멈추지 않는다. 파일 구조·내부 스키마·함수 분리·알고리즘·작업 순서는 **구현 중 발견한 사실을 보고 정하는 것이 옳다.** 이 문서가 그것까지 정하면 에이전트가 실제 문제보다 규칙 만족에 집중한다.

마지막 관문(사람): 실제 사람과 인터뷰 1회를 돌려 IP-1을 사람 손으로 확인하고, 기계가 못 닫는 항목을 §8에 대조한다.

---

## 8. 미리 선언한 잔여 — 지금 적어야 나중에 변명이 못 된다

기계가 닫지 못하는 것들. 완료 판정에서 제외되며, 이 목록에 없는 것을 나중에 잔여라고 주장할 수 없다.

- **질문의 질.** 좋은 질문인지, 삼원 대안 셋이 실제로 구체적인지, laddering이 진짜 구성개념에 닿았는지 — 기계는 형상만 본다.
- **판단의 내용 — 전부.** 계약이 조건마다 직접 선언한 잔여다. 되말하기가 정말 뜻을 옮겼는지(B1) · 모순으로 지목된 것이 진짜 모순인지(B2·B4) · 중대하다는 판정이 옳은지(B5) · 예시가 적절한지(B6) · 해소가 실질적인지(A2) · 문안이 정말 모호한지(A3, 정규식은 근사일 뿐이다). **기계는 절차가 밟혔는지와 산출물의 형상만 본다.**
- **조각 나누기 자체**(A1의 순환). 사용자 말을 조각으로 쪼개는 것은 모델이 한다. "빠짐없이 쪼갰는가"는 같은 모델이 판정할 수 없다. IP-2ⓑ가 잡는 것은 **쪼갠 조각 중 안 다뤄진 것**이지, **안 쪼개진 것**이 아니다.
- **중대성 판단의 상관 맹점.** B5의 k개 해석을 같은 모델이 같은 prior로 만든다. 놓친 해석은 k에 안 들어오므로 "안 달라진다"가 참인지 확인할 방법이 없다.
- **역방향 언어 품질** — 사용자가 직접 명명한 두 고통(잘못된 번역 · 뜬금없는 단어). 용어집을 안 지으므로 **이번 루프의 기계는 이것을 전혀 잡지 않는다.** 남는 것은 프롬프트 지시뿐이고, 연구 보고서는 *그 지시가 per-turn 훅에 이미 있는데도 고통이 지속된다*고 실측했다. **알면서 남기는 잔여다.**
- **사용자 답의 진실성.** 사용자가 자기 의도를 정확히 말했는지는 판정 불가.
- **"이해했다"의 실체.** 기계가 보는 것은 "열린 차원이 없다"는 형식이지 이해 자체가 아니다.
- **프롬프트가 실제로 좋은 인터뷰를 내는지.** 주입 여부와 산출물 형상은 기계가 보지만, 대화의 질은 사람이 §7의 마지막 관문에서 본다.
- **같은 편향.** 짓는 모델과 검사하는 모델이 같은 계열이다. 컨텍스트를 벌려도 남는다. IP-5는 컨텍스트가 다름만 강제하고 모델 계열이 다름은 강제하지 않는다.
- **훅의 봉쇄 완전성 — 실측으로 닫혔다(2026-07-27, 개정 4).** 미확인이던 둘을 Claude Code 2.1.220에서 관측했고, 결과는 잔여를 없애는 것이 아니라 **잔여의 정체를 확정한다.**
  - **인터럽트 시 `Stop`은 발화하지 않는다.** 중단된 턴에서는 `SessionEnd`만 발화했다(정상 종료 대조군에서는 `Stop` 발화 확인). 도구 호출 중·텍스트 생성 중 2회 모두 같았다. *남은 한계*: 실제 TTY에서 ESC 키를 누른 것이 아니라 SDK control-protocol의 `interrupt`(같은 abort 경로)로 근사한 관측이다.
  - **연속 차단 한계는 실재하고 기본 8회다.** `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP`으로 조정된다. **캡을 넘긴 차단은 조용히 버려진다.** 비차단 방식(`hookSpecificOutput.additionalContext`)도 턴을 재개시키지만 **같은 캡을 공유한다.** 차이는 도달했을 때뿐이다 — `decision:block`으로 캡에 닿으면 최종 응답이 **빈 문자열**이 되고, `additionalContext`로 닿으면 모델의 마지막 답이 보존된다.
  - **§4-7의 적용 범위는 우리 코드까지다.** 조용한 실패를 우리가 안 만들어도 **호스트가 만든다** — 캡을 넘긴 차단은 경고 없이 사라지고 종료 코드는 0이며, 미신뢰 워크스페이스에서 `.claude/settings.json`의 `permissions.allow`가 무시되는 것도 stderr 한 줄로 흘러간다. 따라서 §4-7("거부는 관측 가능해야 한다")은 **fabricate가 쓰는 코드에 대한 요구이고, 훅 계층 전체의 보장이 아니다.** 불변식을 약화시키는 것이 아니라 적용 범위를 정직하게 긋는 것이다.
  - **이 둘은 §8의 결론을 약화시키지 않고 증명한다.** 인터럽트 한 번으로 `Stop`을 완전히 우회할 수 있으므로 훅은 완벽한 봉쇄가 될 수 없다. 훅은 **우회를 비싸고 눈에 보이게 만드는 장치**이고, 뚫렸을 때의 방어선은 IP-4다 — 이제 이것은 추정이 아니라 실측에 근거한 판단이다.

---

## 9. 개정 절차 — 못 바꾸는 게 아니라 몰래 못 바꾼다

이 문서를 고치려면 셋을 함께 낸다.

1. **무엇이 틀렸는지** — 어느 절의 어느 문장인가.
2. **왜 틀렸는지의 근거** — `contract/original-request.md` 원문 대조, 또는 실행해서 관측한 사실. "그렇게 하는 게 나아 보인다"는 근거가 아니다.
3. **사용자 확인.** GOAL 개정은 사용자만 비준한다.

개정하면 `STATE.md`에 개정 이력(무엇을·왜·언제)을 남기고 새 해시를 이 문서 머리에 적는다. **§2의 통합 술어를 약화시키는 개정은 §7의 중단 조건이다** — 자동으로 사용자 결정으로 간다.
