# GOAL — 돌아가는 인터뷰 하네스 하나

> **이 문서는 비준 후 읽기 전용이다.** 진척·발견·다음 조각은 `STATE.md`에 적는다.
> 이 파일을 고치려면 §9를 밟는다. 고치고 싶어질 때가 §3을 읽을 때다.
>
> 상태: **초안 — 미비준.** 비준일:  · 해시:

---

## 0. 재진입 규약

이 루프에 들어오는 모든 세션과 서브에이전트는 **이 문서 전문 + `STATE.md` 요약**을 받는다.

- **이 문서를 요약해서 전달하는 것은 금지다.** 짧으니 전문을 붙인다.
- **이전 조각의 산출물을 시드로 주지 않는다.** 의도가 변질되는 기제는 쪼개는 것 자체가 아니라, 다음 걸음의 입력이 원 목표가 아니라 직전 걸음의 산출물일 때다. 매 걸음은 여기로 돌아와서 시작한다.
- 사용자가 쓴 원 문장이 필요하면 `contract/original-request.md`를 **직접 읽는다**. 인용도 원문으로 한다.

---

## 1. 목표 세계

이 루프가 끝난 세계에서 다음이 참이다.

**사용자가 명령 하나로 인터뷰를 시작한다.** 대화가 오가고, 끝나면 잠긴 의도 레코드가 디스크에 남는다. 그 레코드는 사용자가 실제로 쓴 문장과, 합의된 용어와, "무엇이 달성돼 있으면 완료인가"를 담는다. 요약본이 아니다.

**그 레코드가 다음 걸음의 시드다.** 새 세션이든 다른 에이전트든, 그 파일을 읽으면 이전 대화를 복원하지 않고도 같은 뜻 위에 선다.

**그 레코드가 완료를 통치한다.** 잠긴 의도 없이는 "다 됐다"를 선언할 수 없고, 잠긴 의도의 충족 상태가 실제로 관측되지 않으면 완료가 부적격이 된다.

**질문하는 능력은 프롬프트가 낸다. 기계는 세 가지만 판정한다** — 끝내도 되는가, 무엇이 남았는가, 무엇이 잠겼는가.

### 왜 이것이 필요한가 — 4줄짜리 인터뷰 스킬이 못 하는 것 셋

모델은 이미 인터뷰를 잘한다. laddering도, 삼원 유도도, pre-mortem도 아는 기법이다. 그걸 코드로 다시 만드는 것은 이 루프의 목표가 아니다. 프롬프트만으로는 **구조적으로** 안 되는 것이 셋 있고, 이 하네스는 정확히 그 셋이다.

1. **끝을 모델이 정한다.** "충분히 이해했다"를 모델이 선언하면 끝난다. 자기 확신은 자기가 못 깬다.
2. **남지 않는다.** 대화가 끝나면 사라진다. 다음 세션이 못 읽는다.
3. **구속력이 없다.** 인터뷰 결과가 나중의 완료 판정에 아무 힘을 못 쓴다.

### 아키텍처 — 네 부분, 그 이상 없음 (2026-07-26 사용자 확정)

**fabricate는 Claude Code를 대상으로 하는 하네스이고, Claude Code 안에서 돈다. fabricate가 Claude Code를 호출하지 않는다.** 모델은 호스트가 낸다.

| 부분 | 매체 | 하는 일 |
| --- | --- | --- |
| 스킬 | 프롬프트 | 인터뷰를 어떻게 하는가 + 매 턴 CLI를 부르라는 지시 + 서브에이전트 위임 지시 |
| 에이전트 정의 | 프롬프트 | 서브에이전트의 역할·경계·반환 형식 |
| CLI | 코드 | 턴 기록 · 게이트 · 잠금 · 거부. 대화하지 않는다 — **상태 관리자다** |
| 훅 (두 사건) | 코드 | 스킬의 지시를 **안 지켰을 때** 잡는다. 디스크를 읽고 판정한다 |

따라서 **모델 어댑터 · API 키 · 스트리밍 · 재시도 · 호스트 추상화는 존재하지 않는다.** Codex 대응도 이중 호스트 계층도 짓지 않는다(§5).

#### 훅이 이 설계의 급소다 — 사건 둘

fabricate가 호스트를 부르지 않으므로, 에이전트가 CLI를 건너뛰면 장부가 비고 기계는 그것을 모른다. 대화창에서 "다 파악했습니다"로 끝나면 §1의 셋 중 무엇도 성립하지 않는다. **그래서 강제력은 프롬프트가 아니라 훅에 있다** — 프롬프트는 질문을 잘하게 하고, 훅은 안 지킨 것을 잡는다. §4-1 판별식의 실제 착지 지점이다.

| 사건 | 언제 | 무엇을 한다 |
| --- | --- | --- |
| `UserPromptExpansion` | 사용자가 타이핑한 **명령이 프롬프트로 확장될 때**, 모델에 닿기 전. 매처는 명령 이름 | **활성 표식을 디스크에 만든다.** 모델 재량 밖이므로 인터뷰 시작을 건너뛸 수 없다 |
| `Stop` | **Claude가 응답을 끝낼 때** — 턴마다 | 활성 표식이 있는데 **이번 턴에 장부가 늘지 않았으면** 막고 사유를 돌려준다 |

**둘이 다 필요하다.** `Stop`만 있으면 `deep-interview start`를 건너뛴 세션은 활성 표식이 없어서 일반 대화와 구분되지 않는다 — 훅이 판정할 근거 자체가 없다. `UserPromptExpansion`이 그 근거를 모델 밖에서 만든다.

Stop이 **턴마다** 도는 것이 규칙의 형태를 정한다. "잠긴 의도가 없으면 막는다"로 쓰면 인터뷰 진행 중 매 턴이 막힌다. 옳은 규칙은 **"활성 표식 + 이번 턴에 장부 증가 없음 → 막는다"**이고, 이것이 "모델이 CLI를 건너뛰었다"를 직접 잡는다.

**미확인 둘** (공식 문서에 명시 없음, §8): 사용자 인터럽트 시 `Stop`이 도는지 · 연속 차단에 횟수 한계가 있는지. 그래서 훅은 **완벽한 봉쇄가 아니라 우회를 비싸고 눈에 보이게 만드는 장치**로 쓴다. 훅이 뚫려도 IP-4가 마지막 방어선이다.

#### 서브에이전트

**스킬이 띄운다 — CLI가 아니다.** 스킬이 호스트의 Agent 도구를 쓰라고 지시하고 호스트가 띄운다. `fabricate` 실행 파일은 어떤 에이전트도 프로세스도 부르지 않는다. 에이전트 정의는 이 저장소의 산출물이지만 **프롬프트이고 코드가 아니다**(§4-1).

- **위임이 필수인 지점은 이번 루프에서 하나다** — 질문이 사용자에게 가기 전의 **세션-맹검 판단**. 드라이버는 대화 서사를 알기 때문에 자기 질문이 서사 없이도 답할 수 있는지 판정할 수 없다. 자기 채점이 구조적으로 불가능한 유일한 자리이고, 그래서 IP-5가 된다.
- 그 밖의 위임(반론·조사)은 **허용이고 필수는 아니다.** 필수로 만들면 증명할 수 없는 것을 증명한다고 주장하게 된다(§4-5).
- **서브에이전트는 사용자에게 묻지 않는다.** 사용자와 말하는 자리는 드라이버 하나다. 서브에이전트는 읽기 전용 판단만 하고 돌려준다.
- **서브에이전트의 산출은 CLI를 통해 장부에 들어간다.** 대화 안에만 있으면 기계가 못 본다 — 턴 건너뛰기와 같은 구멍이다.

### 출처

| 문서 | 지위 |
| --- | --- |
| `contract/original-request.md` | **원 요청. 사용자가 쓴 문장 그대로. 요약 금지.** |
| `contract/research-report.md` | 의도 형성 품질 조사 — 기법의 출처. 대부분 프롬프트로 착지한다 |
| `contract/criteria.json` · `criteria.md` · `contract-draft.md` | **참고 신호로 강등.** 완료 정의가 아니다(§3) |
| `contract/reviews/` | 착수 전 심의 세 건 — 위험 목록으로 유효 |
| `audit/2026-07-26-piece3-audit.md` | 1차 시도가 어떻게 실패했는지의 실측. §3의 근거 |
| `PLAN.md` · `NEXT.md` | **권위 없음.** 1차 시도의 계획·인수인계다. 이 문서와 `STATE.md`가 대체했다. `NEXT.md`의 "첫 5분"·물결 순서·조건 목록을 따르지 마라 — 그 계획이 실패한 기록이 §3이다 |

---

## 2. 통합 술어 — 이것이 완료 정의다

**개별 단위 테스트의 초록은 완료가 아니다.** 아래 여섯이 전부 통과할 때, 그리고 그때만 이 루프가 끝난다. **무엇이 참이어야 하는지만 고정한다 — 어떻게 그렇게 만드는지는 정하지 않는다.**

**모든 술어는 대칭 증거를 갖는다** — 통과해야 하는 경우와 거부해야 하는 경우가 **둘 다** 있다. 한쪽만 적으면 아무것도 안 하는 구현이나 항상 거부하는 구현이 통과한다. 1차 시도에서 빈-껍데기 반박이 69개 중 43개를 잡았던 것이 이 실패다.

| id | 무엇을 증명하는가 | 증거 (전부 필요) |
| --- | --- | --- |
| **IP-0** | **남의 기계에서 즉시 쓸 수 있다** | ⓐ 깨끗한 체크아웃에서 **문서에 적힌 설치 명령 하나**를 돌리고 Claude Code를 재시작하면 `/fabricate:deep-interview "<요청>"`이 바로 실행된다.<br>ⓑ 두 번 돌려도 같은 결과다 — 훅 중복 등록 없음<br>ⓒ 기존 Claude Code 설정이 유실되지 않는다<br>ⓓ **음극**: 설치가 실패하면 변경 전 상태가 보존된다 |
| **IP-1** | 끝까지 돈다 | ⓐ **사용자 표면**: `/fabricate:deep-interview "<요청>"`이 실제로 인터뷰를 연다 — 활성 표식이 생기고 첫 질문이 사용자에게 간다. 저수준 CLI만 도는 것으로는 못 닫는다<br>ⓑ **내부**: `start` → `turn record` ×N → `close`가 장부에 남는다<br>ⓒ `close`가 통과하면 `.fabricate/intent/<id>.json`이 생기고 exit 0<br>ⓓ **음극**: 스킬이 질문을 하나도 하지 않은 세션은 `close`가 거부한다 |
| **IP-2** | 못 끝내면 **실제로 막힌다** | ⓐ 미해소 차원이 남은 세션에서 `close` → exit ≠ 0, **무엇이 왜 남았는지** 출력, 레코드 미생성<br>ⓑ 활성 표식이 있는데 장부가 늘지 않은 턴 → **Stop 훅이 막고 사유를 돌려준다**<br>ⓒ 표식이 `UserPromptExpansion`에서 생긴다 — 모델이 `start`를 건너뛴 세션도 잡힌다<br>ⓓ **음극**: 정상 진행 턴(장부가 늘었음)은 막지 **않는다**. 인터뷰가 진행 불가가 되면 안 된다 |
| **IP-3** | 세션을 넘어 **시드가 된다** | ⓐ 새 프로세스에서 `deep-interview show <id>`가 사용자 원문·합의 용어·목표 술어를 낸다, exit 0<br>ⓑ **원문 대조**: show 출력의 사용자 문장이 세션 파일의 바이트와 같다 — 요약이 끼어들지 않았다(§4-3)<br>ⓒ **주입 경로**: 그 출력이 새 세션의 컨텍스트로 실제로 들어가는 경로가 있고 도달 가능하다. 출력만 되고 아무도 안 읽으면 저장을 증명한 것이지 시드를 증명한 것이 아니다<br>ⓓ **음극**: 없는 id·잠기지 않은 세션 → exit ≠ 0 |
| **IP-4** | 완료를 통치한다 | ⓐ **양극**: 잠긴 의도 + 유효한 충족 증거 → **exit 0**<br>ⓑ 잠긴 의도 없음 → exit ≠ 0<br>ⓒ 증거 없음 · 검증 명령 실패 · 오래된 증거 → exit ≠ 0<br>ⓓ **다른 intent의 증거 재사용** → exit ≠ 0 |
| **IP-5** | **스스로 채점하지 않는다** | ⓐ 질문이 사용자에게 가기 전, 세션-맹검 판단이 장부에 기록으로 남는다<br>ⓑ **음극**: 그 판단이 거부하면 질문은 그대로 사용자에게 가지 않는다<br>ⓒ 판단자와 드라이버가 다른 컨텍스트다 — 같은 컨텍스트의 자기 채점은 통과로 세지 않는다 |

등뼈는 다섯 문장이다: **돈다 · 못 끝내면 막힌다 · 남는다 · 구속한다 · 자기 채점 안 한다.** IP-0은 그 다섯이 **개발자 기계 밖에서도** 성립하게 만드는 전제다 — 우연히 이 머신에서만 도는 것, 수동 파일 복사가 필요한 것, 재설치가 훅을 중복 등록하는 것을 막는다.

IP-5는 **질문의 질을 판정하지 않는다**(그것은 §8 잔여다). 판정하는 것은 **판단이 다른 컨텍스트에서 일어나 장부에 남았는가**라는 구조뿐이다.

### 기계 통과를 닫는 명령은 하나다

`bun run verify` 하나가 아래를 전부 돌린다. **에이전트는 이 명령 하나로 자기 상태를 확인하고, 이것이 초록이 아니면 `기계 통과`를 주장하지 않는다.**

- IP-0~5의 fixture 전부 (양극·음극 둘 다)
- 실제 `Stop` · `UserPromptExpansion` 훅 통합 검사 — 훅 스크립트를 모사한 것이 아니라 설치된 경로로
- 운영 모듈 도달 가능성 검사 (§6)
- `typecheck` · `lint` · `test`
- 설치된 스킬·에이전트 정의·훅 경로가 이 저장소를 가리키는지

### 완료 상태 둘 — 섞지 않는다

| 상태 | 뜻 | 누가 판정 |
| --- | --- | --- |
| `기계 통과` | `bun run verify`가 초록 — IP-0~5의 양극·음극 증거 전부 | 에이전트가 닫는다 |
| `사람 검증 대기` | 실제 사람과 인터뷰 1회를 돌려 대화의 질을 확인 | **사람만** 닫는다 (§7 마지막 관문) |

에이전트는 `기계 통과`까지 혼자 간다. **`기계 통과`를 완료라고 보고하지 않는다** — 남은 상태의 이름을 함께 말한다.

---

## 3. 왜 하나의 단위인가 — 쪼개고 싶어지면 여기를 읽어라

**1차 시도는 이 목표를 조건 69개로 쪼갰고, 조건 하나에 모듈 하나씩 지었다. 결과는 실측돼 있다.**

- 조건 33개가 초록이 됐다. 독립 감사 여덟이 되짚은 결과 **파손 4 · 의심 22 · 기록만 7**.
- 부정행위는 0이었다. 동결 무결, 계약 무수정, 옛 코드 유입 0. **문제는 전부 "테스트는 통과하는데 문안은 못 지킨다"였다.**
- 서로 모르는 감사자 넷이 같은 결론에 도달했다: **지은 것 대부분이 비-테스트 호출자가 없는 모듈 섬이다.**
- 초록 33개로 **돌아가는 것은 0개**였다.

원인은 조건 원문을 안 줘서가 아니다. 각 조각은 계약 원문을 그대로 받았다. **원인은 "이게 다 서면 무엇이 돌아가는가"를 아무도 보고 있지 않았다는 것**이다. 그래서 각자 자기 조건만 만족하는 최소 형상을 지었고, 최소 형상은 스키마 한 줄이었고, 호출자가 없었다.

> **목표는 부품 목록이 아니라 도는 경로다. 경로는 나눌 수 없다.**

작업을 여러 걸음으로 나누는 것은 허용된다 — context rot 때문에 어차피 나뉜다. 나누면 안 되는 것은 **의도**다. 실무 규칙:

- 매 걸음은 직전 걸음이 아니라 이 문서를 시드로 삼는다 (§0).
- 원자성의 단위는 컨텍스트가 아니라 **병합**이다. **§2가 전부 초록일 때만 `main`에 착지한다.** 중간 커밋은 브랜치/워크트리에서 자유롭게 한다. — 이건 규칙이다. 당신의 "한 방에"가 실제로 뜻하는 바가 이것이고, 이걸 놓으면 미완의 조각이 `main`에 쌓이면서 다시 조각으로 관리되기 시작한다.
- **조건 목록을 다시 만드는 것은 금지가 아니라 경고다.** 구현하다가 하위 사례를 열거하는 것은 정상이다. 위험한 것은 **그 목록이 §2를 대신해 완료 정의가 되는 순간**이다. 목록이 생기면 그것이 IP를 전진시키는지 확인하고, 아니면 버린다.

---

## 4. 불변식 — 위반하면 그 작업은 실패다. 되돌린다

1. **더 좋은 모델로 바꿨을 때 개선되는 것은 코드로 쓰지 않는다.** 개선되면 프롬프트 자리, 모델과 무관하면 코드 자리다. laddering 품질은 모델이 낸다. "잠긴 의도가 세션을 넘어 남는가"는 모델과 무관하다.
2. **뿌리(CLI 진입점 · 훅 진입점)에서 도달할 수 없는 모듈은 완료로 세지 않는다.** "임포터가 하나 이상"으로 읽지 않는다 — 그 표현은 진입점 자신을 실패로 만들고 죽은 모듈끼리의 순환을 통과시킨다. 검사는 §6에 있다.
3. **사용자가 쓴 문장은 요약되지 않는다.** 저장할 때도, 다음 걸음에 넘길 때도, 서브에이전트에게 위임할 때도 원문이다. 요약이 원문을 대체하는 것이 이 하네스가 막으려는 실패 그 자체다.
4. **완료는 §2로만 판정한다.** 단위 테스트가 몇 개 초록인지는 완료와 무관하다.
5. **기계가 못 판정하는 것은 잔여로 적는다.** 판정하는 척하는 스키마를 짓지 않는다. `.length(3)`은 세 개를 세지만 세 개를 내놓게 만들지는 못한다.
6. **게이트는 실제 경로 위에 선다.** 검사를 가진 함수와 실제로 쓰이는 함수가 다르면 그 게이트는 없는 것이다. **잠금을 우회하는 쓰기 경로가 없어야 한다** — 몇 개의 모듈로 짓는지는 자유다.
7. **거부는 관측 가능해야 한다.** 조용히 넘어가는 실패를 만들지 않는다. 거부는 종료 코드와 사람이 읽을 문장으로 나온다.
8. **설치는 멱등이고 기존 사용자 설정을 유실시키지 않는다.** 두 번 돌려도 같은 결과이고, 훅이 중복 등록되지 않으며, **실패하면 변경 전 상태를 보존한다.** 사용자의 Claude Code 설정을 덮어쓰지 않는다.
9. **검증이 사용자의 실제 설정을 오염시키지 않는다.** `bun run verify`는 격리된 위치에서 돈다.
10. **절대경로에 의존하지 않는다.** 저장소를 옮기면 깨지는 설치는 설치가 아니다.

8·9·10은 §7-3(비가역 위험)에 걸리는 것들이라 불변식으로 둔다 — 되돌림이 작동하지 않는 유일한 부류다.

---

## 5. 비-목표 — 이번 루프에서 짓지 않는다

- **ditto의 자산**(ACG · e2e · hosts · memory-graph · worktree · journey-authoring · CodeQL). 손대지 않는다. 인터뷰 하네스가 실제로 쓸 만해진 뒤 증거를 보고 정한다.
- **1차 시도 fabricate 코드의 승계.** 읽는 것은 허용, 가져오는 것은 금지. 나쁜 코드까지 딸려 와 끼워 맞추는 비용이 다시 쓰는 비용보다 크다 (2026-07-26 사용자 결정).
- **모델 어댑터 · API 키 · 스트리밍 · 재시도.** 모델은 Claude Code가 낸다(§1 아키텍처).
- **호스트 추상화 계층 · Codex 대응 · 이중 호스트.** 대상은 Claude Code 하나다. ditto의 `src/core/hosts`를 재현하지 않는다. 나중에 필요해지면 그때 짓는다 — 지금 짓는 것은 쓰이지 않을 이음매 하나를 미리 지는 것이다(§4-5).
- **오케스트레이션 루프 · autopilot · 노드 그래프.** 스킬이 서브에이전트를 띄우는 것은 여기 해당하지 **않는다**(§1 아키텍처) — 금지되는 것은 코드로 짓는 상태 기계이고, 스킬의 위임 지시는 프롬프트다.
- **dialectic 3역할 · 멀티 모델 적대 검토.** 다음 루프. 서브에이전트 능력 자체는 §1이 허용하므로, 이번 루프에서 필요한 만큼(편향 분리)만 쓰고 3역할 심의 절차는 짓지 않는다.
- **인터뷰 기법을 코드로 재현하는 것** — laddering · 삼원 유도 · bipolar · pre-mortem 항목 · 유도질문 탐지 · 앵무새 탐지 · 질문 모드 다양성. 전부 프롬프트로 간다.
- **i18n 카탈로그 · EARS lint.** 1차 시도에서 578줄이었고, 관리 대상 한국어 문구는 10개였다.
- **조건 목록 · 판정표 · 테스트 동결 절차의 재구축.**

---

## 6. 살아있는 경로 규약

완료로 세려면 아래 **여섯이 전부** 참이어야 한다. **무엇이 참이어야 하는지는 고정이고, 어떻게 그렇게 만드는지는 자유다.**

| # | 무엇이 참이어야 하는가 | 왜 |
| --- | --- | --- |
| 1 | 진입점이 실재한다 — `fabricate --help`에 `deep-interview`·`turn`·`check`가 뜬다 | 설치했는데 부를 수 없으면 없는 것이다 |
| 2 | **운영 모듈 전부가 뿌리(CLI · 훅)에서 도달 가능하다** | 1차 시도의 실패를 만든 단 하나의 자리 |
| 3 | **잠금을 우회하는 쓰기 경로가 없다** | `.fabricate/intent/`에 쓰는 권한이 갈라지면 게이트를 지나지 않은 레코드가 생긴다. 물리적으로 한 모듈일 필요는 없다 — **우회 경로가 없으면 된다** |
| 4 | 거부가 셸에 보인다 — 종료 코드 ≠ 0 + 사람이 읽을 사유 | 조용한 실패는 실패가 아니라 거짓 성공이다 |
| 5 | 훅이 설치된 경로로 실제 발화한다 | 설정 파일만 있고 안 도는 훅은 없는 훅이다 |
| 6 | **훅 매처가 스킬 이름과 같다** | `UserPromptExpansion`의 매처는 **명령 이름**이다. 매처가 `deep-interview`가 아니면 훅은 **조용히 한 번도 발화하지 않으면서 설치 검사는 통과한다.** 이름을 바꿀 때 함께 바꿔야 하는 유일한 자리 |

2번이 **유일한 구조적 도달성 게이트**다 — 나머지 다섯은 진입점·쓰기 권한·거부 가시성·훅 발화·훅 매처를 각각 본다.

2번을 "모든 모듈에 임포터가 하나 이상"으로 쓰면 두 방향으로 무너진다 — CLI 진입점 자신은 임포터가 없어 실패하고, 죽은 모듈끼리 서로 import하면 통과한다. **뿌리에서의 도달 가능성**이어야 둘 다 막힌다.

여섯 다 사람의 성실성이 아니라 **`bun run verify` 안의 스크립트**로 확인된다. 언제 짓는지는 정하지 않는다 — `verify`가 초록이 될 때 거기 있으면 된다.

---

## 7. 중단 조건 — 넷뿐이다

**기술적 모호함은 스스로 정한다.** 틀렸으면 `bun run verify`와 실제 인터뷰가 되돌려 보낸다. 그게 §2가 존재하는 이유다. 멈춰야 하는 것은 **되돌림이 작동하지 않는 종류**뿐이다.

1. **`contract/original-request.md`와 이 문서가 실질적으로 충돌할 때.** 원 요청이 이긴다.
2. **사용자만 정할 수 있는 제품 의미가 갈릴 때.** 어느 쪽으로 가도 동작하지만 뜻이 달라지는 갈림길.
3. **비가역 위험이 있을 때** — 사용자 데이터·기존 설정·외부 환경을 훼손할 수 있는 변경.
4. **§4의 불변식이나 §2의 술어를 약화시키지 않고는 진행할 수 없을 때.** 술어를 목표에 맞추는 게 아니라 목표를 술어에 맞추게 되는 순간이다.

그 밖에는 멈추지 않는다. 파일 구조·내부 스키마·함수 분리·알고리즘·작업 순서는 **구현 중 발견한 사실을 보고 정하는 것이 옳다.** 이 문서가 그것까지 정하면 에이전트가 실제 문제보다 규칙 만족에 집중한다.

마지막 관문(사람): 실제 사람과 인터뷰 1회를 돌려 IP-1을 사람 손으로 확인하고, 기계가 못 닫는 항목을 §8에 대조한다.

---

## 8. 미리 선언한 잔여 — 지금 적어야 나중에 변명이 못 된다

기계가 닫지 못하는 것들. 완료 판정에서 제외되며, 이 목록에 없는 것을 나중에 잔여라고 주장할 수 없다.

- **질문의 질.** 좋은 질문인지, 삼원 대안 셋이 실제로 구체적인지, laddering이 진짜 구성개념에 닿았는지 — 기계는 형상만 본다.
- **사용자 답의 진실성.** 사용자가 자기 의도를 정확히 말했는지는 판정 불가.
- **"이해했다"의 실체.** 기계가 보는 것은 "열린 차원이 없다"는 형식이지 이해 자체가 아니다.
- **프롬프트가 실제로 좋은 인터뷰를 내는지.** 주입 여부와 산출물 형상은 기계가 보지만, 대화의 질은 사람이 §7의 마지막 관문에서 본다.
- **같은 편향.** 짓는 모델과 검사하는 모델이 같은 계열이다. 컨텍스트를 벌려도 남는다. IP-5는 컨텍스트가 다름만 강제하고 모델 계열이 다름은 강제하지 않는다.
- **훅의 봉쇄 완전성.** 공식 문서에 명시가 없어 미확인인 것 둘 — 사용자 인터럽트 시 `Stop`이 발화하는지, 연속 차단에 횟수 한계가 있는지. 따라서 훅은 완벽한 봉쇄가 아니라 **우회를 비싸고 눈에 보이게 만드는 장치**이며, 뚫렸을 때의 방어선은 IP-4다. 실측하면 이 항목을 §9로 갱신한다.

---

## 9. 개정 절차 — 못 바꾸는 게 아니라 몰래 못 바꾼다

이 문서를 고치려면 셋을 함께 낸다.

1. **무엇이 틀렸는지** — 어느 절의 어느 문장인가.
2. **왜 틀렸는지의 근거** — `contract/original-request.md` 원문 대조, 또는 실행해서 관측한 사실. "그렇게 하는 게 나아 보인다"는 근거가 아니다.
3. **사용자 확인.** GOAL 개정은 사용자만 비준한다.

개정하면 `STATE.md`에 개정 이력(무엇을·왜·언제)을 남기고 새 해시를 이 문서 머리에 적는다. **§2의 통합 술어를 약화시키는 개정은 §7의 중단 조건이다** — 자동으로 사용자 결정으로 간다.
