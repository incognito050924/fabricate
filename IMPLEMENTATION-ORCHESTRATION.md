# Fabricate 구현 오케스트레이션 요구사항 초안

> 상태: 사용자 발화 정리본 · 미비준 초안  
> 범위: 잠긴 의도를 입력으로 받아 코드 구현을 끝까지 수행하는 다음 단계 하네스  
> 주의: 이 문서는 현재 비준된 `GOAL.md`를 변경하거나 대체하지 않는다.

---

## 1. 목적

Fabricate의 구현 오케스트레이션은 사용자가 확정한 의도를 제품 개발에 충분한 구현 스펙으로
확장하고, 실패 가능성을 구현 전에 최대한 제거한 뒤, 테스트 주도로 구현·검증·리뷰·지식
내재화·보고까지 한 번의 오케스트레이션 안에서 끝내는 하네스다.

사용자는 하나의 작업을 요청한 뒤 그 작업에서 파생되는 `A-1`, `A-1-1`, `A-2` 같은 후속 작업을
계속 발견하고 다시 지시하는 데 인지 비용을 쓰지 않아야 한다. 따라서 하네스는 최초 의도에서
직접 파생되는 명시적 후속 작업을 추적하고, 누락하거나 다음 실행으로 미루지 않고, 최초 의도가
실제로 충족될 때까지 책임진다.

이 하네스의 핵심은 단순한 장시간 실행이 아니다. 장시간 작업 중에도 최초 의도, 합의된 구현
방법, 범위, 제약, 완료 조건을 잃거나 축소하지 않는 것이 핵심이다.

---

## 2. 핵심 원칙

### 2.1 최초 의도가 통치한다

- 잠긴 의도가 모든 계획, 작업, 테스트, 구현, 검증, 리뷰의 근원이다.
- 구현 중 만든 계획이나 직전 작업의 산출물이 최초 의도를 대체해서는 안 된다.
- 작업을 분해하더라도 각 작업이 최초 의도의 어느 부분을 실현하는지 추적할 수 있어야 한다.
- “구현하기 쉬운 부분만 남겨 완료로 간주하는 것”은 실패다.

### 2.2 한 오케스트레이션에서 닫는다

- 잔여 작업, 미처리 작업, 명시적 후속 작업을 다음 오케스트레이션의 범위로 넘기지 않는다.
- 구현 중 새로 발견된 작업도 최초 의도를 충족하기 위해 필요하다면 현재 오케스트레이션에
  편입하여 끝까지 처리한다.
- 후속 작업을 분리할 수 있는 유일한 예외는 그 작업이 최초 의도 또는 기존 프로젝트의 방향을
  뒤엎는 새로운 제품 결정일 때다.
- 예외에 해당하면 하네스가 임의로 결정하지 않고 사용자에게 알린 뒤 후속 항목으로 등록한다.
- 단순한 난이도, 시간 부족, 컨텍스트 부족, 예상보다 넓어진 구현량은 이월 사유가 아니다.

### 2.3 판단은 유연하게, 통치는 필요한 곳에만 둔다

- 절차는 견고해야 하지만 모든 판단을 계약, 스키마, 결정론적 게이트로 바꾸지 않는다.
- 목표 보존, 사용자 승인, 테스트 통과, 증거 신선도, 완료 선언처럼 우회 시 피해가 큰 경계는
  강제한다.
- 설계 방법, 조사 방식, 문제 해결 전략처럼 모델의 판단력이 유리한 영역은 프롬프트와 역할에
  맡긴다.
- 게이트의 수보다 실제 실패를 막는지, 추가 시간과 토큰 비용이 정당한지가 중요하다.

### 2.4 메인 에이전트는 사용자와 오케스트레이션을 잇는 얇은 경계다

- 메인 에이전트는 구현·조사·검토를 직접 수행하지 않고 원칙적으로 서브에이전트에게 위임한다.
- 가능한 경우 오케스트레이션 지휘 자체도 컨트롤타워 서브에이전트에게 위임한다.
- 컨트롤타워가 필요한 역할과 작업을 설계하고, 메인 에이전트에게 서브에이전트 호출을 요청한다.
- 메인 에이전트는 요청받은 서브에이전트를 호출하고, 사용자와 컨트롤타워 사이에서 승인·질문·진행
  상황·완료 결과를 전달한다.
- 메인 에이전트가 직접 수행하는 일은 호스트 기능상 위임할 수 없는 최소 작업으로 제한한다.

### 2.5 장기 실행 상태는 컨텍스트 밖에 남긴다

- 메인 에이전트와 컨트롤타워 모두 장기 작업에서 발생하는 context rot의 대상이다.
- 최초 의도, 현재 단계, 남은 작업, 결정 근거, 실패한 접근, 검증 결과는 대화 컨텍스트에만
  의존하지 않고 디스크의 구조화된 상태로 남긴다.
- 새 컨텍스트 또는 대체 에이전트가 이전 대화의 요약을 신뢰하지 않고도 동일한 목표에서 재개할 수
  있어야 한다.
- 컨텍스트를 교체할 때는 직전 산출물만 넘기지 않고 최초 의도와 현재 상태를 함께 제공한다.

### 2.6 모델과 effort는 역할·위험·작업 난이도에 맞춘다

- 모든 에이전트와 스킬에 가장 비싼 모델과 가장 높은 effort를 일괄 적용하지 않는다.
- 역할뿐 아니라 개별 작업의 종류, 난이도, 실패 비용, 필요한 판단 수준에 따라 모델과 effort를
  선택한다.
- Fabricate는 별도 설정이 없어도 합리적으로 동작하는 기본 정책을 제공한다.
- 사용자는 프로젝트 설정 파일에서 작업별·역할별·스킬별 모델과 effort를 재정의할 수 있다.
- 기본 라우팅은 프론티어 모델이 아니라 해당 작업을 신뢰성 있게 완수할 수 있는 가장 저렴한
  모델과 가장 낮은 충분한 effort를 선택한다.
- 저비용 설정 때문에 최초 의도, 사용자 승인, 검증 독립성, 완료 기준을 약화하지 않는다.
- 낮은 설정으로 충분하지 않다는 실행 증거가 생기면 더 강한 설정으로 승격할 수 있어야 한다.
- 실제로 요청한 설정과 호스트가 적용한 설정이 다르면 그 사실을 실행 기록에 남긴다.

---

## 3. 역할 모델

### 3.1 사용자

- 최초 의도를 확정한다.
- RED 테스트와 구현 스펙을 검토하고 구현 착수를 승인하거나 수정을 요구한다.
- 최초 의도나 기존 프로젝트의 방향을 뒤엎는 새 결정만 직접 판단한다.
- 최종 계획 및 검토증을 받는다.

### 3.2 메인 에이전트

- 사용자와 컨트롤타워 사이의 단일 대화 창구다.
- 컨트롤타워가 요청한 서브에이전트를 호스트 기능으로 호출한다.
- 사용자 승인 게이트를 진행한다.
- 사용자 판단이 필요한 방향 전환을 보고한다.
- 진행 상황과 최종 결과를 전달한다.
- 오케스트레이션의 실질적인 계획·구현·검증 작업에는 가능한 한 관여하지 않는다.

### 3.3 컨트롤타워 에이전트

- 전체 단계와 현재 상태를 관리한다.
- 필요한 역할을 선정하고 메인 에이전트에게 호출을 요청한다.
- 병렬 가능한 작업과 직렬 의존 작업을 구분한다.
- 최초 의도와 구현 스펙의 커버리지를 계속 확인한다.
- 발견된 후속 작업을 현재 오케스트레이션에 편입한다.
- 각 전문 에이전트의 결과를 종합하되, 완료를 자기 판단만으로 승인하지 않는다.
- 컨텍스트 열화가 예상되면 상태를 디스크에 체크포인트하고 새 컨텍스트로 교대한다.

### 3.4 전문 서브에이전트

프로젝트에 따라 역할을 조합하며, 최소 범용 역할은 Fabricate가 기본 제공한다.

- 구현 스펙 설계자
- 프로젝트·코드베이스 조사자
- pre-mortem 실패 분석자
- 테스트 설계자
- 구현자
- 독립 검증자
- 코드 리뷰어
- 거버넌스·보안·제약 검토자
- 회고 및 하네스 개선 분석자
- 프로젝트 지식 내재화 담당자
- 사용자·이해관계자용 설명 및 학습 자료 작성자

같은 작업을 만든 에이전트가 그 작업의 유일한 승인자가 되어서는 안 된다.

---

## 4. 전체 절차

### 4.1 구현 스펙 형성

잠긴 의도를 제품 구현에 충분한 스펙으로 확장하고 구체화한다.

다음 항목을 포함하여 일반적인 제품 개발에서 구현 전에 점검하고 설계해야 하는 사항을 빠짐없이
다룬다.

- 프로젝트의 현재 구조와 관례
- 개발 언어, 프레임워크, 런타임
- 사용할 라이브러리와 선정 이유
- 구현 범위와 명시적 비범위
- 기능 및 비기능 요구
- 완료 조건과 관측 방법
- 아키텍처와 주요 데이터 흐름
- 기존 코드와의 통합 방식
- 호환성, 마이그레이션, 롤백
- 보안, 개인정보, 권한, 신뢰 경계
- 성능, 안정성, 접근성, 운영성
- 오류 처리와 복구
- 개발 중 지켜야 할 프로젝트 규칙과 제약
- 테스트 전략과 필요한 검증 도구
- 병렬화 가능한 영역과 직렬 의존성
- 사용자 또는 프로젝트 방향에 영향을 주는 미결정 사항

구현 스펙은 최초 의도를 확장한 것이어야 하며, 최초 의도를 더 쉬운 목표로 축소한 것이어서는 안
된다.

#### 산출물

- 구현 스펙
- 구현 범위와 완료 조건
- 제약 및 주의 사항
- 의도 항목과 구현 항목의 대응 관계
- 사용자 판단이 필요한 열린 질문

### 4.2 Pre-mortem sweep

구현을 시작하기 전에 계획이 실패한 미래를 가정하고 가능한 실패 원인을 역으로 찾는다.

검토 대상에는 다음이 포함된다.

- 요구 누락과 잘못된 해석
- 범위 축소와 불필요한 범위 확장
- 잘못된 기술 선택
- 기존 프로젝트와의 충돌
- 통합·마이그레이션·호환성 실패
- 보안과 데이터 손상
- 테스트로 관측되지 않는 실패
- 병렬 작업 간 충돌
- 배포·운영·복구 실패
- 사용성·접근성·성능 저하
- 완료처럼 보이지만 실제 기능이 빠진 stub 또는 표면 구현
- 컨텍스트 손실, 잘못된 인수인계, 작업 누락

발견된 각 실패 가능성은 다음 중 하나로 처리한다.

1. 계획이나 스펙을 수정한다.
2. 예방 또는 탐지 수단을 추가한다.
3. 테스트나 검증 조건으로 만든다.
4. 발생 가능성 또는 영향이 수용 가능한 이유를 명시한다.
5. 사용자 판단이 필요한 방향 전환으로 승격한다.

새로운 실질적 실패 가능성이 더 이상 발견되지 않고, 발견된 모든 항목의 처리 상태가 닫힐 때까지
반복한다. 단순히 같은 표현의 위험을 계속 생성하는 것은 sweep의 진척으로 세지 않는다.

#### 산출물

- 실패 시나리오 목록
- 각 시나리오의 예방·탐지·대응
- 수정된 구현 스펙
- 테스트 또는 검증으로 전환된 위험
- 남은 사용자 결정

### 4.3 TDD RED 테스트 작성

- 구현 전에 완료 조건과 pre-mortem 결과를 실행 가능한 테스트로 구체화한다.
- 테스트는 아직 구현되지 않은 요구에 대해 실제로 실패해야 한다.
- 기존 동작 보존을 위한 회귀 테스트를 포함한다.
- 단위 테스트만으로 충분하지 않으면 통합, E2E, 성능, 보안, 설치, 마이그레이션 검증을 포함한다.
- 테스트 자체가 최초 의도 전체를 충분히 커버하는지 독립적으로 검토한다.
- 테스트를 통과시키기 위해 의도를 축소하거나 테스트를 약화할 수 없도록 변경 규칙을 둔다.

#### 산출물

- 실행 시 RED인 테스트 집합
- 의도·스펙·위험과 테스트의 대응표
- 테스트로 판정할 수 없는 항목과 그 검증 방법

### 4.4 사용자 승인 게이트

사용자는 구현 스펙, 주요 설계 결정, RED 테스트, 테스트로 다루지 못하는 검증 항목을 확인한다.

사용자는 다음 중 하나를 결정한다.

- 구현 착수 승인
- 수정 후 재검토
- 구현 중단

승인 전에는 제품 구현에 착수하지 않는다. 승인 이후 최초 의도나 프로젝트 방향을 바꾸는 변경이
필요해지면 다시 사용자 판단을 받는다. 구현 세부의 가역적 변경까지 매번 승인 대상으로 올리지는
않는다.

### 4.5 구현: RED에서 GREEN으로

- 승인된 스펙, 계획, 제약, 테스트를 기준으로 구현한다.
- 병렬 가능한 작업은 병렬로 수행한다.
- 의존성이 있거나 같은 파일·상태를 충돌 가능하게 수정하는 작업은 직렬화한다.
- 각 작업은 자신이 충족하는 의도·스펙·테스트 항목을 추적한다.
- 구현 중 새 필수 작업이 발견되면 현재 오케스트레이션의 작업 목록에 추가한다.
- 합의된 구현 방법이나 제약을 바꾸려면 변경 이유와 영향을 기록한다.
- 최초 의도나 프로젝트 방향을 뒤엎는 변경만 사용자에게 승격한다.
- 테스트를 삭제·완화하여 GREEN을 만드는 것은 구현 성공으로 세지 않는다.

#### 산출물

- 구현 코드
- GREEN 테스트 결과
- 구현 중 발견·처리한 후속 작업
- 계획 대비 변경 사항과 근거
- 검증 가능한 구현 증거

### 4.6 검증 전 커밋

최초 구현과 테스트가 GREEN에 도달하면 독립 검증과 코드 리뷰를 시작하기 전에 커밋한다.

이 커밋은 검증 기준점을 제공한다.

- 검증자는 기준 커밋의 diff와 실행 결과를 검토한다.
- 리뷰에서 수정이 발생하면 수정 커밋을 추가하고 해당 변경을 다시 검증한다.
- 최종 완료 보고는 검증된 최종 커밋을 가리켜야 한다.

### 4.7 최초 의도 충족 검증 및 코드 리뷰

다음 두 검토를 함께 수행한다.

#### 제품 검증

- 최초 의도가 거짓 없이 전부 구현됐는가
- 구현 스펙과 완료 조건이 충족됐는가
- 테스트가 실제 사용자 경로와 통합 경로를 검증하는가
- stub, placeholder, 비활성 코드, 연결되지 않은 기능이 없는가
- 구현 중 발견된 후속 작업이 모두 처리됐는가
- 누락된 edge case나 회귀가 없는가

#### 코드 리뷰

- 코드 품질과 유지보수성
- 프로젝트 구조와 관례 준수
- 보안, 개인정보, 권한 경계
- 오류 처리와 복구 가능성
- 성능과 자원 사용
- 테스트 품질과 신뢰성
- 문서 및 운영 정보의 정확성
- 거버넌스와 합의된 제약 준수

발견된 모든 이슈는 현재 오케스트레이션에서 구현 단계로 되돌려 해결한다. 수정 후 테스트와 검증을
다시 수행하며, 실질적 이슈가 하나도 남지 않을 때까지 반복한다.

“나중에 할 일”, “후속 PR”, “향후 개선”, “TODO”는 최초 의도를 충족하기 위해 필요한 작업이라면
완료 판정과 양립할 수 없다.

#### 완료 예외

검증 중 발견된 작업이 최초 의도 또는 기존 프로젝트의 방향을 뒤엎는 결정이라면:

1. 현재 의도 충족에 필요한 부분과 새로운 방향 결정을 분리한다.
2. 사용자에게 영향과 선택지를 보고한다.
3. 사용자가 현재 오케스트레이션에 편입할지 별도 후속으로 등록할지 결정한다.

이 예외 외에는 잔여를 남기지 않는다.

### 4.8 회고와 지식 내재화

구현과 검증이 끝나면 다음을 점검한다.

- 하네스가 막지 못한 실패가 있었는가
- 불필요한 절차나 게이트가 시간·토큰을 낭비했는가
- 더 나은 역할 분리나 병렬화 방식이 있는가
- context rot 또는 인수인계 손실이 있었는가
- 다음 실행에서 재사용할 프로젝트 규칙·패턴·함정은 무엇인가
- 기존 지식 중 이번 구현으로 낡거나 틀린 내용은 무엇인가

검증된 교훈만 하네스 개선 후보와 프로젝트 지식으로 승격한다. 단일 실행에서 우연히 관측된 것을
성급하게 일반 규칙으로 만들지 않는다.

프로젝트 지식에는 다음을 반영한다.

- 구현된 기능과 사용자 관점의 동작
- 변경된 아키텍처와 데이터 흐름
- 새 의존성과 선정 이유
- 중요한 설계 결정과 대안
- 테스트·운영·복구 방법
- 알려진 제약이 있다면 그 성질과 사용자 승인 근거

지식 저장 대상은 추후 지식 그래프를 도입할 `~/dev/projects/palimpsest`이며, 구체적인 연동 계약은
별도로 정한다.

### 4.9 계획 및 검토증 사용자 보고

사용자와 프로젝트 이해관계자가 구현 결과를 이해하고 이후 코드베이스를 다룰 수 있도록 계획 및
검토증을 제공한다.

보고서는 단순 완료 요약이 아니라 cognitive debt를 줄이는 코드 구현 및 프로젝트 컨텍스트 학습
도구여야 한다.

최소 포함 내용:

- 최초 의도와 최종 구현의 대응
- 구현 스펙과 주요 설계 결정
- pre-mortem에서 발견한 주요 위험과 처리
- 작성한 테스트와 검증 범위
- 실제 구현 내용과 주요 코드 경로
- 계획에서 달라진 부분과 이유
- 독립 검증 및 코드 리뷰 결과
- 구현 중 발견된 후속 작업과 처리 결과
- 최종 커밋과 재현 가능한 검증 명령
- 운영·디버깅·확장 시 알아야 할 프로젝트 지식
- 회고와 하네스 개선 후보

---

## 5. 프로젝트별 에이전트 흡수

Fabricate는 범용 서브에이전트를 기본 제공하되, 사용자 프로젝트에 이미 존재하는 최적화된
에이전트를 우선 활용할 수 있어야 한다.

### 발견 시점

- Fabricate를 프로젝트에 설치할 때
- 사용자가 `fabricate:agent-scan`을 호출할 때

### 스캔 대상

- 프로젝트 로컬 에이전트 정의
- 설치된 Claude Code 플러그인이 제공하는 에이전트
- 프로젝트 스킬과 역할 지침
- 역할, 도구 권한, 모델 요구, 파일 범위, 훅, 출력 형식 같은 메타데이터

### 등록 원칙

- 발견한 에이전트를 역할 능력에 따라 Fabricate의 역할 카탈로그에 추가 등록한다.
- 이름만 보고 역할을 추정하지 않고 정의와 권한을 검사한다.
- 같은 역할의 후보가 여러 개면 프로젝트 적합성, 도구 경계, 검증 가능성을 기준으로 선택한다.
- 프로젝트 에이전트의 원본 정의를 Fabricate가 덮어쓰지 않는다.
- 신뢰되지 않은 에이전트나 과도한 권한을 요구하는 에이전트는 자동 실행하지 않는다.
- 적합한 프로젝트 에이전트가 없거나 환경이 순수한 경우 기본 범용 에이전트로 동작한다.
- 외부 에이전트 사용 여부와 선택 근거는 실행 기록과 최종 보고에 남긴다.

---

## 6. 모델·Effort·비용 정책

Fabricate는 모델과 effort를 하드코딩하지 않고 실행 정책으로 관리한다. 같은 오케스트레이션 안에서도
개별 작업, 역할, 스킬, 단계에 따라 서로 다른 설정을 사용할 수 있어야 한다. 프론티어 모델 또는
고성능 추론은 기본값이 아니라 복잡도나 실패 증거로 정당화되어야 하는 제한 자원으로 취급한다.

### 6.1 기본 정책

설정 파일이 없는 프로젝트에서도 즉시 사용할 수 있도록 Fabricate가 기본 프로필을 제공한다.
기본 프로필은 다음 원칙을 따른다.

- 모든 작업은 해당 작업을 신뢰성 있게 완수할 수 있는 가장 저렴한 모델과 가장 낮은 충분한
  effort에서 시작하는 것을 기본으로 한다.
- 파일 탐색, 단순 분류, 형식 검사, 상태 정리에는 저비용 모델과 낮은 effort를 우선한다.
- 구현 스펙 형성, pre-mortem, 아키텍처 판단, 복잡한 디버깅에는 더 강한 모델과 높은 effort를
  사용할 수 있지만, 단계 이름만으로 무조건 프론티어 모델에 배정하지 않는다.
- 반복 구현은 저비용 또는 중간 수준에서 시작하고, 같은 실패가 반복되거나 설계 판단이 필요할 때
  승격한다.
- 모델과 effort는 독립 축으로 취급한다. 더 큰 모델이 필요해도 effort는 낮게 둘 수 있고, 작은
  모델에 제한적으로 높은 effort를 배정할 수도 있다.
- 독립 검증과 코드 리뷰는 구현자보다 약한 판단 능력으로 고정하지 않는다.
- 단순한 결정론 검사는 모델을 호출하지 않고 CLI·훅·테스트로 처리한다.
- 병렬 작업 수는 모델 비용뿐 아니라 작업 충돌 가능성, 컨텍스트 중복, 검증 비용을 함께 고려한다.
- 컨트롤타워는 작업을 만들 때 예상 난이도와 위험도를 짧게 분류하고, 선택한 model·effort와 그
  이유를 작업 원장에 기록한다.

Fabricate는 최소한 다음 기본 실행 프로필을 제공한다.

| 프로필 | 목적 |
| --- | --- |
| `economy` | 저위험·반복 작업에서 비용과 속도를 우선한다 |
| `balanced` | 기본값. 품질과 비용을 균형 있게 사용한다 |
| `quality` | 고위험·복잡한 설계·검증에서 품질을 우선한다 |
| `custom` | 프로젝트가 역할별 설정을 직접 지정한다 |

구체적인 모델 이름은 Claude Code와 사용 가능한 모델의 변화에 따라 달라질 수 있으므로 Fabricate의
요구사항에 고정하지 않는다. 기본 정책은 모델 별칭 또는 capability tier를 사용하고, 실제 모델
해결 결과를 기록하는 방향을 우선한다.

### 6.2 프로젝트 설정 파일

프로젝트 루트의 `fabricate.config.json`을 커밋 가능한 프로젝트 설정 파일의 기본 후보로 사용한다.
정확한 파일명과 스키마는 구현 단계에서 확정한다.

설정은 최소한 다음 범위를 제어할 수 있어야 한다.

- 기본 실행 프로필
- 전체 기본 모델과 effort
- 작업 종류·위험도·복잡도별 모델과 effort
- 역할별 모델과 effort
- 개별 스킬별 모델과 effort
- 자동 승격 허용 여부와 최대 단계
- 작업별·단계별·전체 비용·토큰·반복 한계
- 최대 병렬 에이전트 수
- fallback 모델
- 특정 프로젝트 에이전트에 대한 override

예시 형상:

```json
{
  "execution": {
    "profile": "balanced",
    "defaults": {
      "model": "economical",
      "effort": "low",
      "routing": "cheapest-sufficient"
    },
    "tasks": {
      "search|inventory|format|mechanical-edit": {
        "model": "economical",
        "effort": "low"
      },
      "architecture|premortem|complex-debug": {
        "model": "strong",
        "effort": "high",
        "requireJustification": true
      }
    },
    "roles": {
      "control-tower": {
        "model": "capable",
        "effort": "medium"
      },
      "codebase-researcher": {
        "model": "economical",
        "effort": "low"
      },
      "implementer": {
        "model": "capable",
        "effort": "medium",
        "escalateAfterFailures": 2
      },
      "verifier": {
        "model": "strong",
        "effort": "high"
      }
    },
    "skills": {
      "premortem": {
        "effort": "high"
      },
      "agent-scan": {
        "model": "economical",
        "effort": "low"
      }
    },
    "limits": {
      "maxParallelAgents": 4,
      "maxEscalation": "high",
      "frontierRequiresJustification": true
    }
  }
}
```

`economical`, `capable`, `strong`, `inherit` 같은 값은 특정 모델명에 영구 결합되지 않는 논리
별칭의 예시다.
프로젝트가 Claude Code의 실제 모델 별칭이나 지원되는 정확한 모델명을 지정하는 것도 허용할 수
있다.

### 6.3 설정 우선순위

충돌 시 다음 우선순위를 적용한다.

1. 현재 실행에 사용자가 명시한 CLI 또는 명령 인자
2. 프로젝트 설정의 개별 작업 override
3. 프로젝트 설정의 작업 종류·역할·스킬 정책
4. 스캔·등록된 프로젝트 에이전트의 명시 설정
5. Fabricate 기본 라우팅 정책
6. Claude Code 세션 기본값

상위 설정이 하위 설정을 덮더라도 보안 경계, 사용자 승인, 검증 독립성, 완료 정의 같은 필수
불변식을 해제할 수는 없다.

### 6.4 자동 최적화와 승격

자동 최적화는 품질 요구를 낮추는 것이 아니라 필요한 판단 능력에만 비용을 집중하는 방식이어야
한다. 라우터는 최초 배정 전에 작업을 최소한 `결정론적`, `저위험 반복`, `일반 구현`,
`복잡한 판단`, `고위험 검증`으로 분류한다. 정적 도구로 끝낼 수 있는 작업은 에이전트에 배정하지
않고, 나머지는 분류에 맞는 가장 저렴한 설정에서 시작한다.

승격 신호의 예:

- 같은 검증 실패가 정해진 횟수 이상 반복됨
- 구현자가 문제를 재현했지만 원인을 설명하지 못함
- pre-mortem에서 고위험·비가역 실패가 발견됨
- 여러 하위 시스템에 걸친 설계 변경이 필요함
- 검증자들 사이에 판정 불일치가 발생함
- 컨트롤타워가 계획 커버리지 또는 의도 해석에 확신을 갖지 못함

프론티어 모델 또는 최고 effort로의 승격에는 위와 같은 구체적인 신호와 승격 이유가 필요하다.
단순히 작업이 중요하거나 컨트롤타워가 편의를 원한다는 이유만으로 승격하지 않는다. 승격은 가능하면
전체 오케스트레이션이 아니라 막힌 개별 작업에만 적용한다.

강등 또는 저비용 경로 사용 신호의 예:

- 스키마 검증, 포맷팅, 정적 검색처럼 결정론적으로 처리 가능함
- 이미 승인된 반복 패턴을 여러 파일에 적용함
- 독립적인 저위험 작업으로 실패 시 되돌림 비용이 작음
- 출력이 다른 강한 검증 단계에서 반드시 재검사됨

자동 승격은 프로젝트가 설정한 최대 모델·effort·예산을 넘지 않는다. 한계 때문에 필요한 품질에
도달하지 못하면 성공으로 축소하지 않고 막힘 또는 사용자 판단 대상으로 보고한다.

프로젝트는 자동 승격을 끄거나, 프론티어 모델 사용을 금지하거나, 명시적 사용자 승인 뒤에만
허용할 수 있어야 한다. 비용 한계는 경고용 soft limit과 초과 실행을 막는 hard limit을 구분한다.

### 6.5 스킬의 모델 정책

스킬 자체가 Claude Code에서 독립적으로 모델이나 effort를 선택할 수 없는 실행 표면일 수 있다.
따라서 Fabricate는 다음 중 호스트가 실제 지원하는 경로를 사용한다.

- 현재 세션의 모델과 effort를 상속한다.
- 스킬이 특정 역할의 서브에이전트를 요청하고 그 에이전트에 설정을 적용한다.
- 컨트롤타워가 스킬의 정책을 읽어 적합한 에이전트 실행으로 라우팅한다.

설정 파일에 스킬별 모델·effort가 있더라도 호스트가 적용할 수 없는 경우 조용히 무시하지 않는다.
상속된 실제 설정과 적용하지 못한 이유를 기록하고, 필요하면 역할 에이전트 경로로 대체한다.

### 6.6 관측성과 비용 보고

각 에이전트·스킬 실행에 대해 가능한 범위에서 다음을 기록한다.

- 역할 또는 스킬 이름
- 작업 ID, 작업 종류, 위험도 및 복잡도 분류
- 요청한 모델과 effort
- 실제 적용된 모델과 effort
- 설정의 출처: CLI, 프로젝트, 에이전트 정의, Fabricate 기본값, 세션 상속
- 해당 설정을 선택하거나 프론티어 모델로 승격한 이유
- 자동 승격·강등 여부와 이유
- 실행 시간, 반복 횟수, 가능한 경우 토큰·비용
- 산출물과 검증 결과

최종 계획 및 검토증에는 전체 비용만이 아니라 어떤 단계에 비용이 집중됐고 자동 최적화가 품질에
어떤 영향을 주었는지를 포함한다.

---

## 7. 효율성과 견고성의 균형

모든 작업에 동일한 무게의 절차를 적용하지 않는다.

### 항상 강제할 경계

- 잠긴 최초 의도의 무결성과 전문 재주입
- 작업과 최초 의도의 추적 가능성
- 사용자 구현 착수 승인
- RED 테스트가 실제로 실패했다는 관측
- 완료 전 테스트와 검증 통과
- 미처리·후속 작업이 없다는 전체 sweep
- 최초 의도나 프로젝트 방향을 바꾸는 결정의 사용자 승인
- 검증자와 구현자의 역할 분리

### 위험과 복잡도에 따라 조절할 것

- pre-mortem에 투입하는 에이전트 수와 반복 횟수
- 병렬 작업자 수
- 코드 리뷰 전문 역할의 수
- 테스트의 깊이와 종류
- 중간 체크포인트와 상태 기록 빈도
- 독립 검증 라운드 수

### 피해야 할 것

- 실제 실패를 막지 못하는 형식적 계약
- 같은 내용을 여러 파일과 에이전트에 반복 주입
- 모든 사소한 결정에 사용자 승인을 요구하는 것
- 서로 독립적이지 않은 역할을 병렬화하는 것
- 테스트·검토의 수를 품질 자체로 간주하는 것
- 컨텍스트가 길어졌다는 이유로 목표나 범위를 축소하는 것

---

## 8. 완료 정의 초안

다음이 모두 참일 때만 구현 오케스트레이션이 완료된다.

1. 잠긴 최초 의도가 최종 구현과 직접 대응된다.
2. 승인된 구현 스펙과 완료 조건이 충족됐다.
3. pre-mortem에서 발견된 실패 시나리오가 모두 처리됐다.
4. 승인된 RED 테스트가 구현으로 GREEN이 됐고 테스트 약화가 없다.
5. 구현 중 발견된 필수 후속 작업이 모두 처리됐다.
6. 독립 제품 검증과 코드 리뷰에서 미해결 이슈가 없다.
7. 최초 의도나 프로젝트 방향을 뒤엎는 별도 결정만 사용자 승인에 따라 후속 등록됐다.
8. 검증된 최종 코드가 커밋되어 있고 검증 증거가 그 커밋과 연결된다.
9. 회고와 프로젝트 지식 내재화가 완료됐다.
10. 사용자와 이해관계자를 위한 계획 및 검토증이 제공됐다.

시간·토큰·반복 상한 도달은 완료가 아니라 중단 또는 막힘이다.

---

## 9. 아직 결정하지 않은 설계 질문

다음은 요구를 구현하기 전에 별도로 결정하거나 실측해야 한다.

1. **Pre-mortem sweep의 종료 판정**  
   의미 있는 새 실패 시나리오가 없다는 것을 누가, 어떤 독립성으로 판정할지.

2. **테스트 승인 표면**  
   사용자가 개별 테스트, 테스트 묶음, 의도-테스트 대응표 중 무엇을 어떤 UI로 승인할지.

3. **리뷰 수정 이후 커밋 규칙**  
   검증 전 기준 커밋 이후 발견된 이슈를 어떤 단위로 커밋하고 최종 검증 SHA를 어떻게 고정할지.

4. **컨트롤타워 교대 규약**  
   context rot 징후, 교대 시점, 새 컨트롤타워에 전달할 정본과 금지할 요약을 어떻게 정의할지.

5. **프로젝트 에이전트 신뢰 모델**  
   스캔된 에이전트의 도구 권한, 훅, 프롬프트, 외부 연결을 어떤 기준으로 신뢰·격리할지.

6. **병렬 파일 소유권**  
   같은 저장소에서 여러 구현자가 작업할 때 파일 충돌과 부분 통합을 어떤 방식으로 막을지.

7. **필수 후속과 방향 전환의 경계**  
   현재 오케스트레이션에 반드시 편입할 파생 작업과 사용자 결정을 요구하는 새 제품 방향을 판별하는
   구체적인 기준.

8. **Palimpsest 연동 계약**  
   저장 형식, 프로젝트 식별자, provenance, 수정·폐기 규칙, 지식 그래프 도입 전 임시 저장 방식.

9. **Claude Code 실행 primitive 선택**  
   단일 메인+서브에이전트, native task list, experimental agent teams 중 무엇을 기본 경로와 선택적
   가속 경로로 둘지.

10. **현재 인터뷰 하네스와의 단계 경계**  
    현재 `GOAL.md`가 완성된 뒤 별도 비준 목표로 구현할지, 설치·명령·상태 디렉터리를 어떤 수준까지
    공유할지.

11. **모델·effort 설정의 호스트 적용 범위**  
    Claude Code에서 메인 세션, 서브에이전트, agent team teammate, 스킬 각각에 모델과 effort를
    어느 시점에 어떤 인터페이스로 적용할 수 있는지 실측하고, 지원되지 않는 조합의 fallback을
    정해야 한다.

12. **논리 모델 별칭 해석과 갱신**  
    `economical`, `capable`, `strong` 같은 capability 별칭을 실제 모델로 누가 해석하며,
    Claude Code 모델 구성이 바뀔 때 프로젝트 설정을 깨지 않고 기본 매핑을 어떻게 갱신할지.

13. **비용 한계 도달 시 상태**  
    단계별·전체 예산 또는 토큰 한계에 도달했을 때 자동 승격을 멈출지, 사용자에게 추가 예산을
    요청할지, 안전하게 막힘 상태로 전환할지.

14. **작업 단위 라우팅의 정확도**  
    작업의 복잡도와 위험도를 누가 어떤 신호로 분류하고, 저비용 모델의 불필요한 재시도로 오히려
    토큰이 증가하는 경우를 어떻게 조기에 감지할지.
