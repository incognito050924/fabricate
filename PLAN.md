# 실행 계획 — 새 하네스를 백지에서 짓고 그 위에 인터뷰를 세운다

작업 항목: wi_260724pps (ditto) → 새 저장소로 이관
결정: 2026-07-25 · 사용자 확정 — ① 새 하네스를 **진짜 백지**에서 시작한다(코드 한 줄도 안 가져온다) ② ditto의 처분은 **보류**, 새 하네스가 선 뒤에 증거를 보고 정한다

이 문서는 "무엇을 만드는가"가 아니라 **어떤 순서로, 누가, 무엇을 보고 만드는가**를 적는다. 무엇을 만드는지는 창립 계약(69개 조건)과 연구 보고서에 있다.

---

## 1. 왜 새 저장소인가 — 무엇이 사라지는가

`rebuild/`는 이미 옛 `src/`를 한 줄도 참조하지 않는 독립 코드였다(외부 의존은 zod·citty·node 기본 모듈·bun 테스트뿐). 즉 "ditto 안에 있다"는 것은 사실상 디렉터리 위치와 저장소 이름의 문제였다. 그래서 나가면 **일이 줄어드는 쪽**이다. 사라지는 것들:

- 계약을 다시 잠글 때 승인이 초기화되는 절차
- 옛 인터뷰 스킬 문서를 옛 하위 명령 16개 기준에서 다시 쓰는 일
- 플러그인·바이너리 재조립, 실기록 쓰기 스위치, `ditto` 명령 교체
- 옛 능력 24영역의 처분 의무(무엇은 유지, 무엇은 재설계, 뒤집기 전에 무엇을 끝낼지)
- 이 세션을 규율하는 훅(매 프롬프트 지시 주입, 작업 항목 게이트)

그리고 하나 얻는다. **새 하네스에서는 인터뷰가 등뼈다.** ditto에서는 오케스트레이션 층에 나중에 얹는 기능이었다. 사용자가 "사람↔에이전트 공유 이해가 하네스의 가장 중요한 단일 가치"라고 규정한 것과 코드 구조가 처음으로 일치한다.

## 2. 백지의 뜻 — 코드는 0, 계약은 원문 그대로

**코드는 한 줄도 안 가져온다.** `rebuild/`의 9,625줄(101개 모듈)도, 그 575개 테스트도 승계하지 않는다. 대신 그 코드를 **읽는 것은 허용한다** — 같은 수업료를 두 번 내지 않기 위해서다. 참고와 승계는 다르다: 읽고 이해한 뒤 새로 쓴다.

**계약은 원문 그대로 옮긴다.** 요약해서 옮기면 그 자리에서 축소가 일어난다(이 프로그램이 막으려는 바로 그 실패). 새 저장소의 창립 문서로 **글자 그대로** 복사한다:

1. 연구 보고서 — deep-interview 의도 형성 품질 조사 전문(§1~§8, 합성 지점 축소 기제와 소유권 박탈 처방 포함)
2. 원 요청 문장 + 범위 개정 기록 두 건(2026-07-24 7묶음 전부 단일 단위 / 2026-07-25 인터뷰 표면을 이 단위 안에서)
3. 69개 조건 문안 전문
4. 계약 초안(7묶음 × 조건별 근거, 406줄)
5. 심의 기록 세 건 — 설계 접근(필수 조치 6), 커버리지(40→69 조건), 충실 구현 가능성(필수 조치 8 + 남는 위험). 특히 세 번째의 필수 조치는 백지에서도 그대로 유효하다: 조건마다 판정 기준을 붙일 것, 빨간 테스트를 동결할 것, 좌표를 미리 적어 동결할 것, 물결을 실제 실행 단위로 낼 것.

옛 저장소의 결정 기록(ADR)은 **가져오지 않는다.** 그중 이 프로그램의 제약으로 살아야 하는 것이 있으면 새 저장소에서 새 결정으로 다시 기록한다.

## 3. 백지의 대가와 그것을 갚는 순서

"한 방에 손실 없이"가 성립하던 근거는 전부 코드였다 — 조건이 하나라도 사라지면 세우는 잠금, 테스트를 나중에 손대면 되돌리는 동결, 증거 종류가 안 맞으면 닫기를 거부하는 게이트, 잔여가 남은 완료를 부적격으로 만드는 게이트, 빨간 테스트 선행 검사, 외부 검사자 없으면 완료를 보류하는 규칙.

백지에서는 그게 없다. **그래서 그것을 첫 조각으로 짓는다.** 이건 우회가 아니라 순서의 문제다: 방어 장치는 이 하네스의 제품 기능이기도 하므로, 먼저 짓는 것이 낭비가 아니다. 그 구간(방어 없이 방어를 짓는 창)은 최소로 줄이고, 그 안에서는 테스트 우선 개발과 원문 대조로 버틴다.

## 4. 순서

### 조각 0 · 저장소와 창립 계약 (반나절)

새 저장소, 도구 사슬(Bun + TypeScript strict + zod + citty + 포매터), 그리고 위 2절의 다섯 문서를 원문 복사해 **읽기 전용 창립 계약**으로 커밋한다. 코드는 아직 없다.

산출: 저장소 + 계약 원문. 이 커밋 이후 계약 문안은 손대지 않는다.

### 조각 1 · 방어 게이트 (여기서 아무것도 아직 "기능"이 아니다)

테스트 우선으로, 작은 것부터. 각 항목은 이 하네스의 제품 기능이면서 동시에 우리 자신의 작업을 지키는 장치다.

1. **조건과 판정 기준의 스키마** — 조건마다 판정 기준이 **필수**다. 판정 기준 없는 조건은 파싱 자체가 거부된다. 판정 방식은 세 가지: 실행해서 확인 / 다시 스캔해서 확인 / 사람 판단. 미래를 향한 판정 기준은 코드 위치(파일:줄)에 걸 수 없다 — 변경되면 깨지니까.
2. **조건 집합 잠금** — 한 번 잠기면 제거는 거부, 추가는 허용하고 보고한다. 판정이 하나라도 붙은 뒤의 재잠금도 거부한다.
3. **증거 종류 대조** — 실행 판정은 테스트·명령 증거만, 재스캔 판정은 파일 증거만, 사람 판단은 관찰·재현 증거만 받는다. 증거가 없으면 **항상** 막는다.
4. **빨간 테스트 선행 검사** — 테스트를 쓴 주체가 루프 자신이면 무효. 빨간 상태를 실제로 관측한 기록이 없으면 거부. 내용 해시가 바뀌거나 파일이 지워지면 거부.
5. **종료 게이트** — 통과하지 못한 조건이 남아 있으면 "완료" 종료를 부적격으로 만든다. 정직한 미검증 + 재진입 조건으로만 착지한다.

산출: 위 다섯 개 + 테스트. 이 시점부터 **모든 후속 작업이 기계에 감시된다.**

### 조각 2 · 69개 판정 기준과 빨간 테스트

조건 하나에 에이전트 하나씩 부챗살로 뿌린다. 각 에이전트가 받는 것은 **계약 원문의 그 문장 그대로** + 계약 초안의 해당 구절이다(요약을 주지 않는다 — 요약이 손실 지점이다). 각자 내놓는 것: 기계가 어떻게 다시 판정하는가 · 어느 모듈에 짓는가(전부 신규다) · 빨간 테스트 파일 경로 · 증거 종류 · 먼저 서야 하는 조건.

그다음 **다른** 에이전트가 같은 원문만 보고 반박한다: "이 판정 기준이 문장을 좁게 읽었나?" 마지막에 완전성 비평가가 69개 id를 받아 "판정 기준이 비었거나 없는 id를 전부 지목하라"를 수행한다. 69개 넣고 69개 받았는지는 코드가 센다.

이어서 69개 실패 테스트를 쓰고 **실제로 돌려 빨간 것을 확인**한다. 반박자는 **테스트만** 본다(구현이 없으니 편향이 가장 적은 유일한 시점) — 질문은 하나, "빈 껍데기로도 통과하나?"

산출: 69행 판정표 + 69개 빨간 테스트 + 각각의 내용 해시.

### ▶ 경계 A · 사용자가 보는 자리 (한 번)

검토 대상: **69행 판정표**와 **69개 테스트 동결 해시**.

여기서 판단하는 것은 "어떻게 구현할까"가 아니라 **"각 조건을 기계가 어떻게 다시 판정할 것인가"**다. 조건을 좁게 읽은 자리는 이 창에서만 잡을 수 있다. 통과하면 테스트가 굳고, 그 뒤로는 약화·삭제가 거부된다. 이 승인이 곧 착수 허가다.

### 조각 3 · 인터뷰 표면 (사용자 개입 없이 한 덩어리)

물결 단위로, 같은 이음매끼리 묶어서. 조각 1의 게이트가 매 걸음을 검사한다. 물결마다 구현 → 검증 → 배리어.

여기에 계약이 이름 부르지 않은 배선 하나가 붙는다: **명령줄 진입점.** 새 저장소이므로 "갈아타기"가 없다 — 첫 명령이 곧 인터뷰다. 옛 저장소에서 필요했던 스킬 문서 재작성·플러그인 재조립·바이너리 교체가 전부 사라지고, 남는 것은 "사용자가 이 명령을 부를 수 있게 설치"뿐이다.

### 조각 4 · 검증 (새 컨텍스트)

부챗살 69갈래. 검증자가 받는 것은 원문 + 판정 기준 + **실제 변경 내용**이다(구현자의 주장은 주지 않는다). 적대적 반박, 그다음 완전성 비평가에게 "증거 없는 id를 전부 지목하라". 이 도구 사슬에서 침묵 축소를 기계적으로 잡는 유일한 단계다.

### ▶ 경계 B · 사용자가 보는 마지막 자리

새 인터뷰로 **실제 인터뷰를 한 번 돌린다**(사람 답이 필요한 조건이 여기서 채워진다). 사람만 판정할 수 있는 항목을 기록하고, 남은 것은 정직하게 미검증 + 재진입으로 착지한다.

## 5. 무엇이 운전하는가

새 저장소에는 오케스트레이션 루프가 없다(가져오지 않았으니까). 그래서:

- **순차 작업**(조각 0·1·3)은 Claude Code 세션이 직접 테스트 우선으로 짓는다. 부챗살로 뿌리면 여러 에이전트가 같은 파일을 락 없이 덮어쓴다 — 하나의 새 표면을 짓는 데는 틀린 도구다.
- **판단을 69갈래로 뿌리는 일**(조각 2·4)은 ultracode 워크플로로 한다. 스키마가 도구 층에서 강제되므로 69개 넣고 69개 받았는지가 기계적으로 세어진다. 실행 중 사용자 입력이 불가능하고 편집이 자동 승인되므로 **구현에는 쓰지 않는다.**
- **`/goal`은 쓰지 않는다.** 판정자가 대화만 읽고 파일·명령을 못 보므로, 결국 일한 쪽의 주장을 채점한다. 자율 연속이 필요해지면, 조각 1이 선 뒤에 **디스크 상태를 읽어 판정하는 정지 훅**을 새 저장소에 직접 붙인다 — 그게 이 하네스가 원래 갖춰야 하는 기능이다.

코드와 주석은 영어, 사용자에게 보이는 문구는 한국어. 도구 사슬은 Bun + TypeScript strict + zod + citty(검증된 조합이고, 새로 배울 것이 없다).

## 6. 규모 정직하게

- 방어 게이트(조각 1): 대략 600~900줄 + 테스트
- 스키마·저장: 400~600줄 + 테스트
- 인터뷰 표면(조각 3): 2,000~3,000줄 + 테스트 — 옛 인터뷰가 비테스트 3,600줄이었으나 그 누적 레이어를 이식하지 않으므로 더 작아야 정상이다
- 합계 대략 6,000~8,000줄(테스트 포함). 대신 ditto에서 승계했을 7,000줄 남짓(연동·거버넌스·배포 게이트·커버리지·메모리·지식·핸드오프·분석)은 **짓지 않는다** — 백지의 실제 이득이 이것이다.

## 7. 남는 위험 (백지에서도 그대로)

- **같은 편향.** 판정 기준을 쓴 자, 테스트를 쓴 자, 구현한 자, 검증한 자가 같은 모델 계열이다. 만든 자와 검사한 자를 벌리고 해시로 굳혀도 편향은 남는다. 조건을 좁게 읽고 그에 맞춰 굳은 테스트는 **영구히** 틀린 채 초록을 만든다. 제거 불가능한 가장 큰 위험이다.
- **경계 A가 사실상 유일한 인간 방어선인데 69행이다.** 훑고 넘기면 위 방어의 대부분이 사라진다.
- **조각 1을 짓는 구간에는 방어가 없다.** 최소로 줄이는 것 외에 방법이 없다.
- **조건 잠금은 id만 본다.** 계약 원문은 파일 해시로 굳힐 수 있지만, "그 문장을 판정 기준이 어떻게 해석했는가"는 잠기지 않은 해석물이다.
- **한 번에 통과 불가로 이미 아는 것들**: 사람의 실제 인터뷰 답이 필요한 조건, 사람만 판정할 수 있는 술어(획득 절차는 계약이 범위 밖으로 명시), 재구현되지 않은 표면에 기대는 조건 몇 개. 정직한 착지는 미검증 + 재진입이다.
- **옛 저장소에서 이미 고쳤던 결함을 다시 만들 수 있다.** 참고 읽기로 줄이되, 0이 되지는 않는다.
- 계약이 스스로 선언한 다섯 가지 순수 판단 잔여는 어떤 방법으로도 닫히지 않는다.

## 8. 옛 저장소(ditto)는 지금 어떻게 두는가

사용자 결정: **보류.** 새 하네스가 실제로 쓸 만해질 때까지 ditto는 그대로 두고, 무엇을 은퇴시키고 무엇을 살릴지는 그때 증거를 보고 정한다. 그동안 ditto는 이 세션의 생산 도구로 계속 쓴다.

이 작업 항목(wi_260724pps)은 새 저장소로 이관되므로, 옛 저장소에서는 재진입 조건과 이관 사실을 남기고 세워 둔다 — 진행 중인 것처럼 남겨 두지 않는다.

## 9. 근거 (직접 확인)

- `rebuild/`가 옛 `src/`를 참조하는 import 0건. 외부 의존은 zod·citty·node 기본 모듈·bun 테스트·zod-to-json-schema. 비테스트 101개 모듈 / 9,625줄, 테스트 80개 파일 / 575개 통과 0 실패.
- 방어 축의 실제 크기: 조건 집합 잠금 68줄, 빨간 테스트 선행 83줄, 완료 권한 83줄, 증거 대조 32줄, 잔여 게이트 56줄, 종료 161줄, 외곽 루프 406줄, 저장소 438+141줄 — 합 1,468줄. 백지에서 다시 쓸 대상의 규모 근거.
- 69개 조건 전부 판정 기준 없음(계약 파일 항목 키가 조건문·판정·증거뿐).
- `/goal`·ultracode의 한계는 공식 문서 원문(각각 code.claude.com/docs/en/goal, /workflows). 이 조사 자체가 ultracode 워크플로로 돌았으므로 그 기능이 이 기계에서 동작하는 것은 실측됐다.
- 미검증: 새 저장소는 아직 없다. 조각 1의 규모 추정은 옛 코드 밀도에서 뽑은 추정이며 계약이 규정한 수치가 아니다.
