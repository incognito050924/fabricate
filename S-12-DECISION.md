# `S-12` — 결정됨 (2026-07-29)

> **결정: (가) 적용 범위를 정직하게 긋는다.** 사용자 확인 — *"플러그인 설치는 예외로 치고 해야지"*.
> `GOAL.md` §9 를 밟아 **개정 6** 이 반영됐다(§4-9 에 적용 범위 명시, 새 본문 해시
> `560699f92f4e809e…`). 개정 이력은 `STATE.md` "`GOAL.md` 개정 6" 절에 있다.
> **남은 것은 `S-12` 를 새 문안대로 다시 구현하는 것**이고, 무엇을 검사해야 하는지는
> `STATE.md` "다른 기계에서 이어받기" 4 절에 있다.
>
> 아래는 결정에 이른 배경이다. 기록으로 남긴다.

> **이 문서는 사용자에게 올린 안건이었다.** 에이전트가 임의로 닫지 않는다.
> `bun run verify` 는 지금 **59/60 초록**이고, 빨간 하나가 이것이다.
> 근거는 `STATE.md` "세션 A가 남긴 것" 6번 미결 1 · `GOAL.md` §4-7 · §4-9 · §8 · §9.

## 무엇이 막혀 있는가

`S-12` 의 문안은 이렇다.

> **`verify` 가 저장소와 사용자 홈에 파일을 만들거나 고치지 않는다** — 임시 디렉터리 밖으로
> 안 나간다. 단 호스트(`claude`)가 자기 캐시에 쓰는 것은 제외한다.

근거는 `GOAL.md` §4-9 불변식이다.

> 9. **검증이 사용자의 실제 설정을 오염시키지 않는다.** `bun run verify` 는 격리된 위치에서 돈다.

**이 문장은 지금 참이 아니고, 참으로 만들 수 없다.**

## 왜 참으로 만들 수 없는가 — 두 기준이 상호배제다

| | 요구 | 실측 |
| --- | --- | --- |
| `S-5`·`S-6`·`S-7` | 훅이 **설치된 경로로 실제 발화**해야 인정한다. 모사는 안 된다 (`GOAL.md` §6-5) | 그러려면 플러그인을 실제로 설치해야 한다 |
| 설치 연쇄 | — | `--scope project` 설치도 **전역** `~/.claude/plugins/{known_marketplaces,installed_plugins}.json` 과 `cache/` 를 **실행 중에 고친다** |
| `CLAUDE_CONFIG_DIR` 격리 | — | **못 쓴다.** 인증을 상속하지 못해 `Not logged in` 으로 죽는다 |
| `claude -p` 자체 | — | **매 호출** `~/.claude.json` 의 `cachedGrowthBookFeaturesAt` 를 갱신한다 (2/2회 관측, 유휴 대조군 불변) |

즉 **"설치된 경로로 실제 발화만 인정한다"** 와 **"사용자 홈을 안 건드린다"** 는 동시에 참일 수 없다.
`verify` 는 정리 단계에서 잔재를 0으로 만들지만(실측 확인), *"실행 중에도 안 고친다"* 는 거짓이다.

## 왜 에이전트가 안 닫았는가

닫는 길은 `S-12` 의 문안을 느슨하게 고치는 것뿐인데, **그 순간 자기 채점이 된다.**
`GOAL.md` §7-4 가 그것을 중단 조건으로 규정했다 — *"불변식이나 술어를 약화시키지 않고는
진행할 수 없을 때"*. 그리고 §4-9 는 §9 를 밟아야 고칠 수 있다.

## 선택지

### (가) §4-9 의 적용 범위를 §4-7 처럼 긋는다 — **추천**

`GOAL.md` 는 이미 같은 일을 한 번 했다. §8 에서 §4-7 의 적용 범위를 이렇게 그었다.

> **§4-7의 적용 범위는 우리 코드까지다.** 조용한 실패를 우리가 안 만들어도 **호스트가 만든다.**
> … **불변식을 약화시키는 것이 아니라 적용 범위를 정직하게 긋는 것이다.**

같은 형태의 개정을 §4-9 에 적용한다. 대략 이런 문장이 된다.

> **§4-9 의 적용 범위는 fabricate 가 쓰는 것까지다.** `verify` 는 자기 산출물을 임시 디렉터리
> 밖에 만들지 않는다. **호스트(`claude`)가 설치 연쇄와 매 호출에서 자기 레지스트리·캐시에
> 쓰는 것은 범위 밖이다** — 그것을 막을 방법이 없고(`CLAUDE_CONFIG_DIR` 격리는 인증을 잃는다),
> 막으면 §6-5 의 *"설치된 경로로 실제 발화"* 를 포기해야 한다. `verify` 가 지는 책임은
> **고유 이름을 써서 사용자의 실제 설치와 충돌하지 않는 것**과 **끝난 뒤 자기 항목을 지우는 것**이다.

- **얻는 것**: 60/60 이 도달 가능해진다. 실제로 지켜지는 것(고유 이름 · 정리 후 잔재 0)이
  기준이 된다.
- **잃는 것**: *"verify 가 사용자 홈을 전혀 안 건드린다"* 는 강한 보장. 그런데 **그 보장은
  지금도 참이 아니다** — 선택지 (가)는 실측을 문서에 반영하는 것이지 새로 무언가를 포기하는 것이 아니다.
- 절차: `GOAL.md` §9 (무엇이 틀렸는지 · 근거 · 사용자 확인) → 새 본문 해시 갱신 →
  `S-12` 를 새 문안대로 다시 구현.

### (나) `S-5`~`S-7` 을 포기한다

훅 발화 검사를 합성 stdin 으로 내린다. **추천하지 않는다** — `GOAL.md` §6-5 가
*"설정 파일만 있고 안 도는 훅은 없는 훅이다"* 라고 못박았고, 1차 시도가 실패한 이유가
정확히 *"돌아가는 것을 아무도 안 보고 있었다"* 이다. 이쪽이 진짜 약화다.

### (다) 그대로 둔다 — `59/60` 으로 산다

`S-12` 를 영구 `n/a` 로 두고 `bun run verify` 는 영원히 exit 1 이다.
**`GOAL.md` §3 의 착지 규율(`main` 에는 전부 초록일 때만)이 영원히 충족 불가가 된다.**
`GOAL.md` §2 가 *"`bun run verify` 하나가 … 이것이 초록이 아니면 `기계 통과` 를 주장하지 않는다"*
라고 적었으므로, 이 선택은 **완료 상태의 정의 자체를 도달 불가로 만든다.**

## 결정하면 무엇이 일어나는가

- **(가)** → `GOAL.md` §9 개정 → `S-12` 재구현 → `60/60` → `main` 착지.
- **(나)** → `GOAL.md` §6-5 개정이 함께 필요하다. 범위가 훨씬 크다.
- **(다)** → 착지하지 않는다. 브랜치에 둔 채 사람 검증으로 넘어간다.

**이 셋 중 어느 것도 에이전트가 고를 수 없다.** `GOAL.md` §7-2(사용자만 정할 제품 의미)와
§7-4(술어 약화), §9(개정은 사용자만 비준)에 동시에 걸린다.
