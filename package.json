{
  "name": "fabricate",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agent work harness whose spine is intent formation",
  "bin": {
    "fabricate": "./bin/fabricate"
  },
  "scripts": {
    "verify": "bun run verify/run.ts",
    "test": "bun test verify/fixtures",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "dependencies": {
    "citty": "^0.1.6",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
