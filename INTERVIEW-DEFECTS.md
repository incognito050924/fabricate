# 인터뷰 스킬 결함 — 사용자 실사용에서 발견 (2026-07-30)

> **이 문서는 핸드오프 기능과 별개다.** 핸드오프 인터뷰를 실제로 한 번 돌리는 동안 사용자가 발견한
> `deep-interview` 자체의 결함 목록이다.
>
> 발견 경로: 인터뷰 세션 `cc9ed860-37c6-4cae-a839-4cd47f463776` (질문 14회 · 사용자 왕복 27회).
> 그 인터뷰의 산출물은 `.fabricate/intent/cc9ed860-….json` 이고 이 문서와 무관하다.

각 항목은 **사용자가 쓴 문장 그대로** → **지금 코드가 왜 그런지의 근거** → **고칠 때 걸리는 것** 순이다.

## 지금 상태 (2026-07-30 17:50)

| | 항목 | 상태 |
| --- | --- | --- |
| D-1 | 언어 고정 | **안 고침** |
| D-2 | 상태 블록 위치·형식 | **고침** — `f834e37`. 코드 + 프롬프트 |
| D-3 | 확인 게이트 | **고침** — `2ff862e`. **프롬프트만**(`src/` 무변경) |
| D-4 | 동작 방식 확인 | **답만 적힘** — 아래 표가 답이고 고칠 것은 D-5 로 넘어갔다 |
| D-5 | 의도 축소 | **안 고침.** 문서가 1순위로 꼽은 것 |
| D-6 | 어휘 | **안 고침.** 후보 넷 실험까지 됨(아래) |

**D-3 을 고치면서 이 문서의 전제 하나가 뒤집혔다.** 아래 D-3 절은 이 수정이 `GOAL.md` §9 절차를
요구한다고 적었는데 **아니었다** — `confirmed` 를 읽어 무언가를 막는 자리는 `close` 하나뿐이고
(`src/intent.ts:222`), 에코 검사는 `restate` 기록 시점에 돌아(`src/ledger.ts:333`) `confirm` 과
무관하다. 즉 왕복 비용의 출처는 기계가 아니라 `SKILL.md` 였고, `GOAL.md` 를 안 고치고 끝났다.
IP-1ⓖ·IP-1ⓗ·IP-2ⓔ 셋 다 그대로다. **D-5 의 "아마도 필요"도 같은 확인을 거치기 전에는 못 믿는다.**

확인을 없애지는 않았다 — 시점만 옮겼다(답마다 → 끝내기 전 한 번). 이번 장부에서 되말하기가
오독을 실제로 잡았기 때문이다: `confirm` 19건 중 **8건이 `rejected`** 였고, 답변 `a2` 는 원문이
`(다)` 한 낱말인데 되말하기 여섯 번 만에 닫혔다. 첫 읽기(`r3` — 집기 잠금 + 커밋 기반 낡음)와
최종(`r12` — 집으려는 순간에만 만료 계산, 막는 게 아니라 알림)이 서로 다른 물건이었다.
그리고 되돌리기(`--overturns`)는 **2건만** 쓰였다 — 아래 D-3 절이 근거로 든 "확인 게이트는 이미
있는 되돌리기 장치와 중복"은 **실측과 어긋난다.**

---

## D-1. 언어가 한국어로 고정돼 있다 — 사용자가 쓴 언어로 동작해야 한다

### 사용자 원문

> 인터뷰 스킬이 강제 한글 사용으로 고정되어 있음. 사용자가 말한 언어로 동작해야함. (SKILL.md 같은
> 프롬프트나 내부 동작 코드-사용자에게 출력되지 않는 부분 한정-는 영어를 사용할 것. 체감상 claude code
> 에서 의미적으로나 소비되는 토큰이 더 효율적으로 느껴짐)

### 지금 그런 근거

| 자리 | 무엇이 고정하고 있나 |
| --- | --- |
| `skills/deep-interview/SKILL.md:309` | "한국어로 쓴다. 사용자가 쓴 단어를 쓴다" — 스킬이 드라이버에게 한국어를 지시 |
| `SKILL.md:37` | "명령이 거부되면 **stderr에 사유가 한국어로 나온다**" |
| `README.md:51` | "코드와 주석은 영어, **사용자에게 보이는 문구는 한국어**" — 저장소 전체 규약 |
| `src/*.ts` 전체 | 거부 사유·상태 블록 문구가 한국어 리터럴로 코드에 박혀 있다 (예: `src/session.ts:101`, `src/intent.ts:224`) |

즉 고정은 **세 층에 걸쳐** 있다 — 스킬 프롬프트 · 저장소 규약 · CLI 출력 문자열.

### 고칠 때 걸리는 것

- **CLI 출력 문자열이 코드에 하드코딩돼 있다.** 사용자 언어를 따라가려면 문구를 코드에서 분리해야 하고,
  그러면 "사용자 언어"를 CLI 가 무엇으로 알 것인가가 새 문제가 된다 — 훅 payload 에 언어 정보는 없다.
  가장 싼 길은 **CLI 출력을 영어로 두고 드라이버가 사용자 언어로 옮겨 전달**하는 것이지만, 그러면
  거부 사유가 모델을 한 번 거치므로 원문 대조가 약해진다. **미결.**
- **SKILL.md 를 영어로 옮기는 것은 별건이고 더 싸다.** 사용자에게 안 보이는 프롬프트이므로 규약
  (`README.md:51`)에 걸리지 않는다. 토큰 효율은 사용자 체감이고 **실측된 바 없다** — 옮기기 전에
  같은 인터뷰를 양쪽으로 돌려 토큰을 재 보는 것이 순서다.

---

## D-2. 상태 블록이 눈에 안 들어오고 출력 맨 뒤에 있다

> **고쳤다 — `f834e37`.** 매 턴 출력이 요약줄(확정 N · 미정 M) + **이번 턴에 바뀐 것만**으로 바뀌고,
> 위치는 배경설명·질문보다 앞으로 갔다. 누적 전체는 새 명령 `fabricate deep-interview status` 로 본다.
> 아래 "고칠 때 걸리는 것"이 후보로 적은 접기 형태는 안 썼다 — 터미널이라 `<details>` 가 안 접힌다.

### 사용자 원문

> 의도 정리(의도/확인된것/미결/등) 섹션 눈에 잘 들어오지 않고, 인터뷰 본문과 구분이 모호하여 UI 개선
> 필요. 그리고 인터뷰 출력의 가장 뒤에 위치해 있어. 정작 중요한 인터뷰 내용(배경설명,질문)을 보려면
> 스크롤을 올려서 봐야함.

### 지금 그런 근거

- `SKILL.md:160` — **"턴을 끝내기 전, 그 턴의 마지막 기록에서 나온 블록을 사용자 응답에 그대로
  붙인다."** 맨 뒤에 오는 것은 사고가 아니라 **스킬이 지시한 위치**다.
- 블록 형식은 `src/status.ts:7` 의 `statusBlock` 하나가 만든다. 구분선이 `─` 한 글자이고 제목이
  없어서, 마크다운 본문과 시각적으로 갈리지 않는다.
- 블록은 **`turn record` 가 성공할 때만** 나온다 — 따로 볼 명령이 없다(`src/cli.ts:25` 의 명령
  목록에 상태 조회가 없다). 그래서 "지금 어디까지 왔지"를 보려면 무언가를 기록해야 한다.

### 고칠 때 걸리는 것

- **위치는 SKILL.md 한 줄로 바꿀 수 있다** — 질문 앞으로 옮기면 스크롤 문제가 없어진다. 대신 매 턴
  머리에 같은 블록이 반복돼 질문이 아래로 밀린다. **접을 수 있는 형태**(요약 한 줄 + 상세)가 두 문제를
  같이 푸는 후보다.
- 실제 실사용에서 블록이 30줄을 넘겼다(쟁점 13개 나열). **쟁점이 늘면 블록이 질문보다 길어진다** —
  변한 것만 보이고 나머지는 개수로 접는 것이 필요하다.
- 상태를 따로 볼 명령이 없는 것도 함께 봐야 한다. 다만 새 명령을 만드는 것이 최소 구현인지는 미결.

---

## D-3. 확인 게이트 때문에 질문이 닫히지 않는다 — 최초 답변을 바로 채택한다

> **고쳤다 — `2ff862e`. 다만 아래 절의 진단 두 개가 틀렸으니 그대로 읽지 마라.**
>
> - **`GOAL.md` §9 는 필요 없었다.** `confirmed` 를 읽어 무언가를 막는 자리는 `close` 하나뿐이고
>   (`src/intent.ts:222`), 에코 검사는 `restate` 기록 시점에 돈다(`src/ledger.ts:333`) — `confirm`
>   과 무관해서 IP-1ⓗ 는 애초에 대상이 아니었다. **기계는 인터뷰 진행을 전혀 안 막는다.** 왕복
>   비용의 출처는 기계가 아니라 `SKILL.md` 였고, 그래서 `SKILL.md` 만 고쳤다. IP-1ⓖ·ⓗ·IP-2ⓔ 유지.
> - **"확인 게이트는 되돌리기 장치와 중복"은 실측과 어긋난다.** 이번 장부에서 `--overturns` 는
>   2건만 쓰였고, 확인은 **19건 중 8건을 `rejected` 로 잡았다.** 중복이 아니라 확인이 주력이었다.
>
> 그래서 **확인을 없애지 않고 시점만 옮겼다** — 답마다 받던 것을 끝내기 전 한 번으로. 인터뷰 중에는
> 바꿔 말한 문장을 다음 질문과 같은 응답에 실어 보내고 기다리지 않는다(사용자가 아니라고 하면 그때는
> 즉시 `rejected`). 이번 세션 기준 채택 확인 왕복 11건 → 1건.
>
> **미검증**: 왕복이 실제로 줄었는지는 인터뷰를 한 번 더 돌려야 안다. 이 수정은 `src/` 를 한 줄도
> 안 건드린 프롬프트 변경이라 드라이버가 지시를 따라야만 효과가 난다.

### 사용자 원문

> 모든 질문에 대해 정리 및 예시를 출력하여 사용자의 언어로 '맞아', '확인'등을 하여야 채택되기 때문에
> 질문이 닫히지 않음. 사용자가 질문의 답변을 뒤집을 기회가 있으니 사용자 언어로 확인 받는 행위는
> 제거하고 최초 답변으로 바로 채택함(답변을 에이전트가 이해한 대로 정리 및 예시로 보여주는 것은 계속
> 유지)

### 지금 그런 근거

- `src/intent.ts:222` — `close` 가 **확인 못 받은 답변이 하나라도 있으면 거부**한다.
  실측: 이번 인터뷰의 첫 `close` 가 `확인 못 받은 답변: a6` 로 exit 1 이었다.
- 확인은 `confirm --restate <id> --verdict accepted` 로만 채워진다(`src/ledger.ts:614` 의
  허용 플래그). 즉 **사용자의 "맞아" 를 드라이버가 받아 적어야** 답이 채택된다.
- 실사용 비용: 되말하기 23건을 냈고 그중 **8건이 거부**됐다. 거부되면 고쳐서 다시 내야 하므로
  **왕복이 그만큼 늘었다.** 사용자 왕복 27회 중 상당수가 내용 결정이 아니라 확인이었다.

### 고칠 때 이것이 걸린다 — 가장 무거운 항목

**이 변경은 비준된 완료 정의 세 항목을 정면으로 건드린다.**

| `GOAL.md` | 무엇을 요구하나 |
| --- | --- |
| IP-1ⓖ (`GOAL.md:104`) | "답은 `candidate` 로 들어오고, 다른 말 + 구체 사례로 재진술해 **사용자가 확인해야 `confirmed` 가 된다**" |
| IP-1ⓗ (`GOAL.md:104`) | 음극: 에코성 재진술은 확인으로 안 쳐준다 |
| IP-2ⓔ (`GOAL.md:105`) | `close` 를 막는 것: "되말하기가 `confirmed` 에 닿지 못한 항목이 남았다" |

이 셋은 원 요청 B1 에서 온 것이고 `GOAL.md` 본문이다. **`GOAL.md` §9 절차를 밟지 않으면 못 고친다.**
그리고 지금 결정된 방침은 이번 루프에서 `GOAL.md` 를 손대지 않는 것이다 — 즉 **D-3 은 착지 이후로
미뤄지고, 미룬다는 사실이 이 문서에 남아야 한다.**

바꿀 것의 모양은 명확하다.

- **유지**: 답을 에이전트가 이해한 대로 정리하고 구체 사례를 붙여 보여주는 것 (오독을 드러내는 장치)
- **제거**: 그 정리에 사용자가 "맞아" 를 해야 답이 채택되는 것
- **근거**: 사용자는 어차피 뒤 턴에서 답을 뒤집을 수 있다(`answer --overturns`, `remark --overturns`
  가 이미 있다 — `src/ledger.ts:614`). 확인 게이트는 **이미 있는 되돌리기 장치와 중복**이다.

---

## D-4. 인터뷰의 동작 방식 확인 필요

### 사용자 원문

> 인터뷰의 동작 방식 확인 필요
> - 질문 생성은 어떻게/누가 하나
> - 답변을 이해하는 것은 어떻게/누가 하나
> - 답변에 따라 의도를 굳히거나 다음 질문 방향 설정이나 의도 파악의 범위 혹은 지도를 넓히거나 좁히는 행위는 어떻게/누가 하나
> - 인터뷰 종료 판정은 어떻게/누가 하나
> - 인터뷰 종료 후 모순 수정을 했는데. 누가/어떻게 했나
> - 인터뷰 종료 후 모순 수정과 요약 시 인터뷰에서 했던 구체적인 답변이 제거되어 의도가 축소되었는데. 어떻게 할 것인가(모순 수정하면서 내가 지시했던 대부분이 축소된 의도를 복원하는 작업이었음)

### 이번 세션에서 확인된 것 (근거 있음)

| 물음 | 지금 누가 / 어떻게 |
| --- | --- |
| 질문 생성 | **드라이버 모델이 혼자 만든다.** 기계는 형상만 본다 — `--dimension` 없는 질문을 거부하고(IP-1ⓔ), `--recommend`·`--because` 를 필수로 요구한다(`src/ledger.ts:612`). **질문의 내용 품질을 보는 것은 아무도 없다.** 세션-맹검 검토자(`agents/question-blind-reviewer.md`)는 "대화 없이 답할 수 있는가"만 판정하고 질문이 좋은지는 판정하지 않는다 |
| 답변 이해 | **드라이버 모델이 혼자 한다.** 기계는 되말하기가 사용자 표현과 너무 겹치는지(에코)만 검사한다 |
| 방향 조정·범위 넓히기/좁히기 | **드라이버 모델이 혼자 한다.** 기계가 하는 것은 전파뿐이다 — `--overturns` 가 오면 그 쟁점과 하류가 stale 이 된다(IP-2ⓗ) |
| 종료 판정 | **기계가 한다.** `close` 가 거부 사유를 전부 출력한다(`src/intent.ts` 의 `closeReasons`). 이번 세션에서 실제로 한 번 거부됐다 |
| 종료 후 모순 수정 | **드라이버 모델(나)이 혼자 했다.** `contradiction-pass` 를 돌린 것도, 모순 c1·c2 를 찾은 것도, 해소를 적은 것도 전부 나다. 기계가 검사한 것은 "패스가 한 번이라도 돌았는가"와 "찾은 모순이 해소로 닫혔는가"뿐이다 — **모순의 내용이 맞는지는 아무도 안 본다.** 실제로 **c1·c2 는 둘 다 내 오독이었고 사용자가 잡았다** |

**요약: 판단은 전부 드라이버 모델 혼자 하고, 기계는 절차가 밟혔는지와 산출물의 형상만 본다.**
이것은 `GOAL.md:2` 의 명시된 설계다("판단은 모델이, 검사는 기계가"). 문제는 그 설계의 대가가
이번 실사용에서 실제로 드러났다는 것이다 — 아래 D-5.

### 미확인

- 서브에이전트 검토가 질문 품질에 실제로 기여했는지. 이번 세션에서 **14건 전부 `pass`** 였다.
  거부가 0건이라 그 장치가 무엇을 걸러내는지 실측되지 않았다. **한 번도 안 막았다는 것이 곧
  잘 만들어진 질문이었다는 뜻은 아니다** — 실제로 낼 자격 없는 질문 4건이 그 검토를 통과해 나갔다
  (아래 D-5 목록).

---

## D-5. 목표 술어가 구체적 답변을 잃어 의도가 축소됐다 — 가장 심각

### 사용자 원문

> 인터뷰 종료 후 모순 수정과 요약 시 인터뷰에서 했던 구체적인 답변이 제거되어 의도가 축소되었는데.
> 어떻게 할 것인가(모순 수정하면서 내가 지시했던 대부분이 축소된 의도를 복원하는 작업이었음)

### 무엇이 일어났나

목표 술어를 처음 쓴 뒤 **네 번 고쳤다.** 고친 내용은 전부 **사용자가 이미 말한 것을 되돌려 놓는
작업**이었다.

| 판 | 사용자가 되돌려 놓아야 했던 것 |
| --- | --- |
| 1판 | — |
| 2판 | c1 을 사용자 언어로 다시 씀 / c2 가 모순이 아니었음을 인정하고 인터뷰 문장 삭제 |
| 3판 | 4번이 "핸드오프 커밋이 항상 있다"고 전제해 DB 경우를 잘못 덮은 것을 사용자가 잡음 |
| 4판 | 2번에 **편향 억제 원칙**이 빠져 있어 사용자가 문장을 직접 주어 넣게 함 |
| 5판 | 6번에 **읽기 절제 규칙**이 빠져 있어 사용자가 문장을 직접 주어 넣게 함 |

즉 **요약 단계에서 의도가 줄어들고, 사용자가 그것을 다시 채웠다.** 인터뷰의 목적이 의도를 보존하는
것인데 마지막 단계가 의도를 깎았다.

### 왜 일어났나 — 구조적 원인

- 장부는 원문을 다 갖고 있다. `remark` 25건 · `answer` 9건이 **사용자가 쓴 문장 그대로** 저장돼
  있다. 그런데 **목표 술어는 드라이버가 새로 쓴 요약**이고, 기계는 그 요약이 장부의 어느 기록을
  덮었는지 **대조하지 않는다.**
- `close` 는 조각 커버리지를 검사한다(IP-2ⓑ — 어느 질문에도 안 걸린 조각). 그런데 **조각은 원 요청
  한 문장을 쪼갠 것이고, 인터뷰 중에 사용자가 새로 꺼낸 `remark` 는 커버리지 검사 대상이 아니다.**
  이번 세션의 결정 대부분이 `remark` 로 들어왔다 — 즉 **가장 중요한 입력이 커버리지 밖에 있었다.**
- 그래서 목표 술어에서 `remark` 하나가 통째로 빠져도 `close` 는 통과한다. 실제로 통과했고,
  사용자가 네 번 잡았다.

### 고칠 방향 (미결 — 셋 다 후보)

1. **목표 술어에 `remark` 커버리지 검사를 붙인다.** 어느 `remark` 가 목표 술어의 어느 항목에
   반영됐는지 드라이버가 적게 하고, 안 걸린 `remark` 가 있으면 `close` 를 거부한다.
   조각 커버리지(IP-2ⓑ)와 같은 모양이므로 새 개념이 아니다.
2. **목표 술어를 드라이버가 새로 쓰지 않게 한다.** 확정된 쟁점의 해소 근거와 `remark` 원문을
   기계가 이어 붙여 초안을 만들고, 드라이버는 순서와 이음말만 손댄다. 요약이 원문을 대체하는 것을
   구조적으로 막는다.
3. **아무것도 안 하고 사용자 검토에 맡긴다.** 지금 상태다. 이번 실사용이 그 비용을 보여줬다 —
   사용자가 네 번 잡았고, 안 잡았으면 축소된 의도가 그대로 잠겼다.

**1번이 `GOAL.md` 를 안 건드리고 갈 수 있는지 확인이 필요하다.** IP-2 의 거부 사유 목록에 하나를
더하는 것이므로 §9 절차가 필요할 가능성이 높다.

---

## 부록 — 이번 세션에서 낼 자격 없이 나간 질문 4건

D-4 의 "서브에이전트 검토가 질문 품질을 안 본다"의 실증이다. 넷 다 세션-맹검 검토를 `pass` 로
통과했고, **사용자가 잡았다.**

| | 무엇이 틀렸나 | 장부 기록 |
| --- | --- | --- |
| q6 | 쪽지 본문을 암호화한다고 전제했으나 사용자가 뜻한 것은 DB 연결정보였다 | `challenge ch6` |
| q10 | 연장 발화의 셈 기준을 코드 규칙으로 못 박으려 했다 — `GOAL.md:163` 층 판별식 위반 | `challenge ch7` |
| q12 | 인터뷰 이어받기를 만들 것으로 늘렸다 | `challenge ch8` |
| q13 | 중단 인터뷰 정리 표면을 만들자고 물었다 — 사용자: "괜히 표면을 늘려가면서까지 구현할만한게 아니야" | (답변 `a8`) |

여기에 **용어 오염** 둘이 더 있다. 드라이버가 사용자가 안 쓴 말을 들여왔고 사용자가 지웠다.

- **"쪽지"** → 사용자: "쪽지라고 하지 말고 그냥 핸드오프 / 핸드오프 메시지라고 해" (`remark m16`)
- **"낡음"** → 사용자: "낡았다가 의미가 없다니까" (`remark m15`)

`SKILL.md:309` 가 이미 "사용자가 안 쓴 전문 용어를 새로 들여오지 않는다"고 지시하고 있다.
**지시만 있고 검사가 없다** — 기계가 대조할 용어 기준이 없기 때문이다. 합의 용어집은
`GOAL.md` §5 비-목표다. 그래서 이 자리는 **의도적으로 프롬프트에만 맡겨져 있고, 이번 실사용에서
두 번 실패했다.**

---

## D-6. 어휘 — 안 쓰는 말, 과도한 한글화, 과도한 영문화. **세 번째 재발**

### 사용자 원문

> 그러고보니 단어 교정도 있잖아. '맹검', '되말', 그리고 내가 지적하지는 않았지만(드러나지 않았거나
> 그냥 넘어갔거나), 부정확한 어휘나 일반적으로 사용하지 않는 어휘, '비준' 역시 흔히 사용하는 어휘는
> 아니야.

> 과도한 한글화나 영문화도 마찬가지야

### 이것은 새 발견이 아니다 — 이미 두 번 기록돼 있다

| 언제 | 무엇으로 기록됐나 |
| --- | --- |
| 2026-07-27 | 원 요청에서 사용자가 **직접 명명한 두 고통**: *잘못된 번역* · *뜬금없는 단어* (`GOAL.md:240`) |
| 2026-07-29 | 앞 인터뷰 `b63faf03` 의 **목표 술어 5번**: "인터뷰 도구가 사용자에게 보이는 말이 영어를 글자대로 옮기거나 발음만 옮긴 말이 아니라 통용되는 한국어다. **맹검·되말이·재진술·차원·드라이버·하네스·시드·사다리 타기** 같은 말이 뜻이 통하는 표현으로 바뀌어 있다" |
| 2026-07-30 | 이번 세션에서 **또 나왔다** — 드라이버가 "맹검"·"비준"·"술어"·"장부"·"조각"·"쪽지"·"낡음"을 그대로 썼다 |

`GOAL.md:240` 은 이것을 **"알면서 남기는 잔여"** 로 명기하고 있다. 그리고 같은 줄에 그 잔여가 왜
지속되는지도 적혀 있다 — *"연구 보고서는 그 지시가 per-turn 훅에 이미 있는데도 고통이 지속된다고
실측했다."* **지시만으로는 안 고쳐진다는 것이 이미 실측돼 있었고, 이번이 그 세 번째 확인이다.**

### 어디까지 고쳐졌나 — 실측

앞 인터뷰의 5번은 **표면 둘에만 반영됐다.** 지목된 여덟 단어의 출현 횟수:

| 단어 | SKILL.md | `src/*.ts` | **GOAL.md** | **STATE.md** |
| --- | --- | --- | --- | --- |
| 맹검 | 0 | 0 | **2** | **5** |
| 되말 | 0 | 0 | **3** | **8** |
| 재진술 | 0 | 0 | **2** | **4** |
| 차원 | 0 | 0 | **4** | **5** |
| 드라이버 | 0 | 0 | **3** | **3** |
| 하네스 | 0 | 0 | **6** | **5** |
| 시드 | 0 | 0 | **5** | **7** |
| 사다리 | 0 | 0 | **1** | **2** |

사용자가 새로 지목한 것과, 지목 안 됐지만 같은 부류인 것들:

| 단어 | SKILL.md | `src/*.ts` | GOAL.md | STATE.md |
| --- | --- | --- | --- | --- |
| 비준 | 0 | 0 | 4 | **15** |
| 술어 | **2** | **3** | 8 | 19 |
| 장부 | **5** | **4** | 9 | 32 |
| 조각 | **5** | **2** | 10 | 54 |
| 표식 | 0 | 0 | 12 | 22 |
| 앵무새 | **1** | 0 | 1 | 0 |
| 준비도 | **1** | **1** | 2 | 7 |
| 봉쇄 · 강등 · 고아 · 불변식 · 판별식 · 멱등 | 0 | 0 | 각 1~6 | 각 2~7 |

### 왜 표면을 고쳐도 재발하나 — 전달 경로

**`GOAL.md` 와 `STATE.md` 가 원천이고, 에이전트가 그것을 읽고 시작한다.**

- `STATE.md:31` — **"읽는 순서: `GOAL.md` 전문(짧다, 요약본을 쓰지 마라) → 이 문서"**
- 즉 모든 새 세션이 `맹검`·`되말`·`하네스`·`시드`·`비준` 을 **문서에서 통째로 흡수한 상태로 첫 말을
  꺼낸다.** SKILL.md 에서 그 단어를 지워도 원천이 그대로면 다시 나온다.
- 이번 세션이 그 실증이다. `SKILL.md` 에는 "맹검" 이 0건인데 드라이버는 사용자에게 "세션-맹검"
  이라고 말했다 — `GOAL.md` IP-5 의 문구를 그대로 옮긴 것이다.

### 정정 (2026-07-30) — `GOAL.md`·`STATE.md` 는 원천이 아니라 임시 부트스트랩 파일이다

**사용자 확인: `GOAL.md` 와 `STATE.md` 는 지금 이 구현 루프를 돌리기 위해 만든 임시 파일이다.
`deep-interview` 를 포함해 어떤 코드도 이 두 문서를 직접 앵커해서는 안 되고, 바로 지워도 되는
파일이어야 한다 — 그리고 실제로 지워질 것이다.**

코드가 실제로 앵커하고 있는지 실측했다: **앵커하지 않는다.** `src/hooks.ts:58` 에 `GOAL.md IP-2ⓜ`
을 가리키는 **소스 주석 하나**가 있을 뿐이고(코드·주석은 영어 규약이라 사용자에게 안 나간다),
`skills/`·`agents/` 아래에는 `GOAL.md`·`STATE.md` 참조가 **0건**이다. 두 파일은 git 이 추적하는
일반 파일이다(`.gitignore` 대상 아님, `git ls-files` 로 확인).

**이것이 위 "전달 경로" 절의 결론을 바꾼다.** `STATE.md:31` 의 "읽는 순서" 지시는 **그 파일이
지워지면 함께 사라진다** — 부트스트랩 루프에 한정된 전파 경로였고, 착지 후 두 파일이 삭제되면
그 경로로 인한 재발은 **저절로 멈춘다.** 다만 이것이 D-6 을 닫지는 않는다:

- **`deep-interview` 가 다음 세션에 무엇을 시드로 주는지는 이미 다른 경로다** — 잠긴 의도 레코드
  (`.fabricate/intent/<id>.json`)를 `show` 로 낸다(IP-3). 이 경로는 사용자 원문과 목표 술어만
  옮기므로 `GOAL.md`·`STATE.md` 의 어휘를 안 옮긴다 — **애초에 오염 경로가 아니었다.**
- **원인은 드라이버(나 자신)가 저장소를 조사하며 `GOAL.md`·`STATE.md` 를 직접 읽고, 거기서 흡수한
  어휘를 사용자에게 그대로 옮긴 것**이다. 이번 세션이 그 실증이다 — 인터뷰 진행 중에 `GOAL.md`·
  `STATE.md` 를 읽었고("세션-맹검"이 그 흔적이다), 두 파일이 사라져도 **드라이버가 저장소의 다른
  문서(예: 사람이 쓰는 커밋 메시지, 코드 주석, 이 결함 문서 자신)를 읽고 같은 방식으로 어휘를
  흡수할 가능성은 남는다.**
- 그래서 D-6 의 실제 원인은 "특정 파일 두 개"가 아니라 **"드라이버가 조사 중 읽은 문서의 어휘를
  검증 없이 사용자 응답에 옮긴다"**는 것으로 다시 좁혀야 한다. 아래 "고칠 방향" 1번은 이 정정을
  반영해 갱신했다.

### 기계가 못 잡는 것은 의도된 설계다

- `GOAL.md:115` — **합의 용어집을 이번 루프에서 짓지 않는다**(2026-07-27 사용자 확정). "따라서
  사용자에게 나가는 말을 **기계가 대조할 기준이 없고**, 역방향 언어 품질은 **프롬프트에만
  맡겨진다**."
- `GOAL.md:187` — i18n 카탈로그(키→문자열 표)도 함께 배제됐다. 이유: *"문구를 만드는 것이 LLM인데
  기계적 번역표를 들고 갈아끼우는 것은 앞뒤가 안 맞는다."* 1차 시도는 578줄을 쓰고 관리 대상 문구가
  10개였다.

**그래서 D-6 은 "검사를 추가하면 되는 문제"가 아니다.** 검사를 추가하는 길은 이미 두 번 평가돼 두 번
기각됐다. 남은 길은 원천을 고치는 것이다.

### 세 방향으로 갈라 물었고, 사용자가 개별 판정을 냈다

| 병 | 후보 | 사용자 판정 |
| --- | --- | --- |
| **① 안 쓰는 말** | 봉쇄 · 강등 · 판별식 · 불변식 | **대부분 기각.** "봉쇄"는 자주 쓰여서 그대로 둬도 되지만 "차단"·"막힘"도 가능. "강등"·"판별식"은 **자주 쓰는 말**이라 문제가 아니었다. 다만 **"불변식"은 문장 안에서 해석이 난해한 경우가 많다** — "변경 불가/허용되지 않는 부분" 또는 "고정" 이 문맥에 따라 더 자연스러울 수 있다 |
| **② 과도한 한글화** | 되말(이) · 맹검 · 고아 차단 · 앵무새 · 준비도 | **"대부분 문제가 됨"** — 셋 중 가장 실제 병이 짙은 항목 |
| **③ 과도한 영문화·음차** | 하네스 · 시드 · 드라이버 · 사다리 타기 | **"사다리 타기"만 확정 기각**, 나머지는 허용 범위. 단 **"드라이버"는 해석의 여지가 많아 문맥에 맞게 다른 단어를 고려**할 것 — 완전 기각도 완전 허용도 아니다 |

**② 가 가장 무겁고, ① 은 거의 문제가 아니었으며, ③ 은 "드라이버"만 걸린다.** 세 방향이 균등한
병이 아니다.

### 진짜 문제 — 개별 어휘가 아니라 문맥과 문법

사용자의 결론 문장이 이 항목 전체의 진단을 바꾼다:

> 결국은 개별 어휘의 문제라기보다는 **문맥에 맞는 어휘 선정, 문법 구조가 이상한 경우**가 많다는 거야

즉 위 세 표처럼 "이 단어는 되고 저 단어는 안 된다"는 **고정 목록으로 못 푼다.** "불변식"이 어떤
문장에서는 괜찮고 어떤 문장에서는 난해한 것, "드라이버"가 문맥에 따라 갈리는 것이 그 증거다.
**단어 하나만 보고 판정할 수 없고, 그 단어가 놓인 문장 전체를 봐야 한다.**

이것은 D-6 을 "용어집을 만들면 되는 문제"에서 "**표현이 자연스러운 한국어 문장인지를 매번 판단하는
문제**"로 옮긴다. 그런데 `GOAL.md:115` 가 이미 이 층을 정확히 지목했다 — *"역방향 언어 품질은
프롬프트에만 맡겨진다."* **표(카탈로그)로는 원래 안 풀리는 문제였고, 사용자의 이번 지적이 그것을
확인해 준 것이다.**

> 이번 세션에서 드라이버가 만든 것도 같은 부류다 — **"쪽지"**(사용자: "쪽지라고 하지 말고 그냥
> 핸드오프라고 해")와 **"낡음"**(사용자: "낡았다가 의미가 없다니까")은 둘 다 개별 단어가 아니라
> 그 단어가 실린 문장 전체가 어색했다.

### 결정됨 — 용어집 방식은 하지 않는다. humanizer 계열 스킬을 도입·차용한다

사용자가 이미 방향을 정했다: **용어집(고정 대조표)은 만들지 않는다.** 대신 **사용자에게 나가는
메시지를 교정하는 humanizer 류 도구**를 도입하거나 차용할 계획이다. 후보로 제시된 것 둘을 확인했다.

| 저장소 | 무엇인가 | 대상 언어 | 방식 |
| --- | --- | --- | --- |
| [`blader/humanizer`](https://github.com/blader/humanizer) | Claude Code **에이전트 스킬**(SKILL.md). AI 글쓰기 특유의 패턴(과잉 의미 부여·홍보 문구·em dash 남용·수동태 등)을 위키백과 "Signs of AI writing" 가이드 기준으로 탐지·수정 | 영어 | 프롬프트 — 사실은 보존하고 표현만 다시 쓴다 |
| [`hjongc/humanizer-kr`](https://github.com/hjongc/humanizer-kr) | Claude Code·Codex **에이전트 스킬**. 번역투·과한 홍보 문구·명사화·수동 표현·챗봇식 전개를 찾아 줄인다 | 한국어 | 프롬프트 — "사실, 의도, 문단 흐름, 말투는 지키고 표현만 바꾸는 것을 원칙" |

**둘 다 표가 아니라 스킬이다** — 이것이 이 항목의 진단과 정확히 들어맞는다. 위에서 "표(카탈로그)로는
원래 안 풀리는 문제"라고 적었고, `GOAL.md:115` 가 "역방향 언어 품질은 프롬프트에만 맡겨진다"고
이미 지목했다. humanizer 계열은 바로 그 층 — **문장을 통째로 보고 다시 쓰는 프롬프트 스킬** — 을
직접 만들지 않고 **차용**하는 것이다.

### 시장 조사 — 같은 유형이 매우 많다 (2026-07-30 GitHub 검색)

`humanizer` 부류 스킬은 언어별로 난립해 있다(영어·중국어·일본어·러시아어·독일어·핀란드어·말레이시아어
등 30개 이상 확인). **한국어를 다루는 것만 추렸다.**

| 저장소 | 별 | 만들어진 때 | 범위 | 비고 |
| --- | --- | --- | --- | --- |
| [`devswha/patina`](https://github.com/devswha/patina) | **280** | 2026-02-24 | **한/영/중/일 다국어** | 넷 중 별이 가장 많고 갱신도 가장 최근(2026-07-30) — 다국어라 한국어 전용보다 한국어 특이 표현 커버리지는 얕을 수 있다. **미확인** |
| [`modu-ai/cowork-plugins`](https://github.com/modu-ai/cowork-plugins) | 262 | 2026-02-15 | 한국 B2B 실무 스위트 하나에 `humanize-korean` 이 **딸린 기능 중 하나** | 사업계획서·세무·법률·HR 등 스킬 수십 개짜리 마켓플레이스라 무겁다. 라이선스가 `Other`(MIT 아님) — 차용 전에 조건 확인 필요. **묘하게도 이 저장소 설명 자체에 "하네스"가 쓰여 있다** — D-6 이 "과도한 한글화" 로 분류한 그 단어다 |
| [`hjongc/humanizer-kr`](https://github.com/hjongc/humanizer-kr) | 7 | 2026-06-30 | 한국어 전용 | 사용자가 처음 제시한 것. 가장 최근에 만들어졌고 한국어에만 집중 |
| [`dotoricode/korean-humanizer`](https://github.com/dotoricode/korean-humanizer) | 7 | 2026-04-28 | 한국어 전용 | `hjongc/humanizer-kr` 와 별 수가 같고 더 먼저 만들어졌다. 내용은 **미확인** |

**넷 다 별이 조사 시점 스냅샷이라 곧 바뀐다** — 이 표는 "지금 이런 것들이 있다"의 근거이지 순위
추천이 아니다. **어느 것을 쓸지는 실제로 인터뷰 출력 몇 개를 돌려 보고 정하는 것이 맞다** — 이번
인터뷰 문서(`INTERVIEW-DEFECTS.md` 자체, 또는 실제 `deep-interview` 질문 문안)를 표본으로 셋
정도에 태워 보고 비교하는 것을 다음 세션의 첫 일감으로 남긴다.

### 고칠 방향 (미결 — 이제 넷)

1. ~~원천(`GOAL.md`·`STATE.md`)의 문장을 다시 쓴다~~ — **정정됨(위 절 참조).** 두 파일은 임시
   부트스트랩 산출물이라 착지 후 삭제된다. 코드가 이미 그것을 앵커하지 않으므로 "문서를 다시 쓰는"
   방향은 대상이 사라진다. 대신 남는 것은 **드라이버가 조사 중 읽은 문서의 어휘를 검증 없이 옮기는
   습관 자체**다 — 이건 특정 파일이 아니라 매 세션의 행동 문제이므로 2번·3번과 겹친다.
2. **매 턴 자기 검토를 프롬프트에 강화한다** — "이 단어가 맞는가"가 아니라 **"이 문장이 자연스러운
   한국어인가"를 보내기 전에 스스로 다시 읽게** 한다. `GOAL.md:240` 이 이미 "그 지시가 per-turn
   훅에 있는데도 고통이 지속된다"고 실측했으므로, **지시를 강화하는 것만으로는 근거가 약하다.**
3. **humanizer-kr 을 인터뷰 출력 경로에 얹는다** — `deep-interview` 가 사용자에게 문장을 내보내기
   직전에 humanizer-kr 스킬을 거치게 한다. 사실을 보존하고 표현만 고치는 원칙이 인터뷰의 "요약이
   원문을 대체하지 않는다" 공리(`GOAL.md:165`)와 방향이 같다. **다만 사용자 원문을 그대로 옮기는
   자리(원 의도 인용, 되말하기의 사용자 문장 부분)에는 humanizer 가 손대면 안 된다** — 그건
   교정 대상이 아니라 그대로 남아야 하는 원문이다. 이 경계를 어디에 그을지가 남는다. **미결.**
4. **아무것도 안 하고 매번 사용자가 잡는다.** 지금 상태다. **세 번 재발했고, 세 번 다 사용자가
   잡았다.**

**용어집은 후보에서 빠졌다** — 사용자의 개별 판정(위 표)은 이번 실사용의 근거로만 남기고, 같은
방식으로 대조표를 늘리는 길은 가지 않는다.

### 후보 넷을 표본에 실제로 돌려봤다 (2026-07-30) — **넷 다 열 문장 전부 무변경**

앞 절이 제안만 하고 멈춘 자리를 실측으로 이었다. 넷을 실제로 설치하고, 이 프로젝트에서 실제로 나간
문장 10개(아래 표본)에 그대로 태워봤다. 결과부터 적는다 — **네 후보 모두 열 문장 전부를 원문 그대로
돌려줬다. 단 한 글자도 안 바뀌었다.**

#### 표본 (10개, 전부 `GOAL.md`·`STATE.md`·이번 인터뷰 장부의 실제 출력)

| # | 원문 | 출처 |
| --- | --- | --- |
| S1 | 8·9·10은 §7-3(비가역 위험)에 걸리는 것들이라 불변식으로 둔다 — 되돌림이 작동하지 않는 유일한 부류다. | `GOAL.md:174` |
| S2 | §4의 불변식이나 §2의 술어를 약화시키지 않고는 진행할 수 없을 때. 술어를 목표에 맞추는 게 아니라 목표를 술어에 맞추게 되는 순간이다. | `GOAL.md:224` |
| S3 | 위임이 필수인 지점은 이번 루프에서 하나다 — 질문이 사용자에게 가기 전의 세션-맹검 판단. 드라이버는 대화 서사를 알기 때문에 자기 질문이 서사 없이도 답할 수 있는지 판정할 수 없다. | `GOAL.md:77` |
| S4 | 기계가 보는 것은 태그 하나뿐이고, 1차에서 "Driver"·"driver "·"driver-session"·"the driver"가 전부 non-driver로 통과했다. | `STATE.md:813` |
| S5 | 질문을 자기가 채점하지 않는다 (세션-맹검 위임 기록) | `STATE.md:65` |
| S6 | 되말하기 상태기계(원 요청 B1): 답은 candidate로 들어오고, 다른 말 + 구체 사례로 재진술해 사용자가 확인해야 confirmed가 된다 | `GOAL.md:104` ⓖ |
| S7 | 사용자 확인. GOAL 개정은 사용자만 비준한다. | `GOAL.md:259` |
| S8 | 세션-맹검 위임 기록 (IP-5 문구) | `STATE.md:65` |
| S9 | 암호화 키를 어디까지 공유하는가 — 받는 사람이 키가 없으면 쪽지가 도착해도 안 열린다 | 이번 인터뷰 장부(`.fabricate/intent/cc9ed860-….json`), 사용자가 "쪽지" 대신 "핸드오프"를 쓰라고 고친 그 개념의 실사용례 |
| S10 | 여러 핸드오프가 쌓였을 때 꼬임을 무엇이 막는가 — 사람이 고르는 것만인가, 집었다는 표식과 낡음 판정까지인가 | 위와 같은 장부, "낡음" 실사용례 |

S9·S10은 부록의 "쪽지"·"낡음" 교정과 짝을 이루는 **실제 원문**이다 — 사용자의 교정 발화("쪽지라고
하지 말고 그냥 핸드오프라고 해", "낡았다가 의미가 없다니까")는 이미 부록에 있었지만, 드라이버가 그
단어를 실제로 쓴 문장 자체는 이번에 장부에서 대조해 처음 확인했다.

#### 설치 — 넷 중 셋은 실제로 설치했다, 하나는 안 했다

| 후보 | 라이선스 | 설치 방식 | 결과 |
| --- | --- | --- | --- |
| `humanizer-kr` | MIT | `claude plugin marketplace add ./humanizer-kr` → `claude plugin install humanizer-kr@humanizer-kr-marketplace` | **설치 성공** (실행 로그로 확인). 단 **이번 세션에는 반영 안 됨** — 플러그인 설치는 세션 시작 시점에 고정되는 목록이라 세션 재시작이 필요하다는 것을 이번에 실측으로 확인했다 |
| `patina` | MIT | 위와 동일 방식 | **설치 성공.** 마찬가지로 이번 세션엔 미반영 |
| `korean-humanizer` | MIT | 마켓플레이스 형태가 아니다(`.claude-plugin/marketplace.json` 없음) — README가 지시하는 공식 설치법은 `~/.claude/skills/korean-humanizer`에 직접 `git clone` | **설치 성공, 그리고 재시작 없이 바로 이 세션에서 스킬로 인식됐다** — 실제로 `Skill` 도구로 호출해 진짜로 돌렸다. 플러그인 방식과 스킬-디렉터리 방식의 반영 시점 차이를 실측으로 확인한 셈이다 |
| `cowork-plugins`(`moai-content/humanize-korean`) | **`LicenseRef-MoAI-NC-ND-1.0`**(비상업·2차저작물금지) — MIT 아니라던 원 우려가 사실로 확인됐다. 라이선스 전문: "저작물은 개인적·교육적·내부 평가 목적의 이용에 한하여 제공됩니다. 상업적 이용과 2차적저작물의 작성은 허용되지 않습니다" | 30개 플러그인 상업 스위트 전체를 설치하는 부담과 NC-ND 조건을 고려해 **정식 설치는 하지 않았다.** 대신 라이선스가 명시적으로 허용하는 "내부 평가 목적"에 한해 SKILL.md와 참조 파일을 그대로 읽어 그 지시를 따르는 방식으로만 비교했다 | 비교엔 포함하되, **"차용"(코드를 우리 스킬에 옮겨 쓰는 것) 대상에서는 애초에 제외된다** — 라이선스가 2차적저작물 자체를 금지하기 때문에, 결과 품질과 무관하게 이 후보는 채택 후보가 아니다 |

korean-humanizer만 실제 슬래시 호출로 돌렸고, 나머지 셋(재시작 필요한 둘 + 설치 안 한 하나)은 각각의
SKILL.md와 참조 파일을 그대로 읽어 그 지시를 문자 그대로 따르게 한 별도 컨텍스트 에이전트로 돌렸다.
이 넷은 전부 "프롬프트 스킬"이라 — 실행 로직 자체가 모델이 SKILL.md 지시를 읽고 따르는 것이 전부다
— 설치 경로가 달라도 실행 결과는 같아야 한다. 다만 이것은 **가정이지 재확인된 사실은 아니다** —
슬래시 커맨드 경로와 "지시문을 그대로 따르라고 시킨 별도 에이전트" 경로가 정말 동일한 산출물을 내는지,
셋 다 세션 재시작 후 재검증하지는 않았다.

#### 결과표 — 원문 / 후보별 결과

넷 다 열 문장 전부 **변경 없음**이었다. 표를 문장별로 나열하는 대신, "왜 안 바뀌었나"의 근거가
후보마다 갈리므로 그 근거를 정리한다.

| 후보 | S1–S10 결과 | 무변경의 근거 |
| --- | --- | --- |
| `korean-humanizer` | 전부 변경 없음 | **장르 배제.** SKILL.md 1단계가 "코드 / 기술 문서 / 법률 문서면 격식이 의도된 것이므로 적용하지 않는다"고 명시. 10문장 전부 이 배제 조건에 걸려 애초에 대상이 아니었다 |
| `humanizer-kr` | 전부 변경 없음 | **카테고리 불일치.** 이 스킬은 "documentation"을 배제하지 않고 오히려 대상 장르로 인정했다 — 그런데도 5개 tell 카테고리(과장된 근거·번역체·명사화-수동태·경어 격식·과잉 구조) 어디에도 안 걸렸다. 내장 감사 스크립트가 "대화"·"암호화"를 명사화 패턴으로, 괄호·인용부호 6곳을 clutter 후보로 표시했지만 전부 오탐으로 판정(고정 복합어·기능적 인용·단일 라벨) — SKILL.md 자신이 "스크립트는 스모크 테스트, 최종 판단 아님"이라 명시한 대로다 |
| `patina` | 전부 변경 없음 | **이중 원인.** (1) 표본이 전부 "1문단·2문장 이하"라 통계 분석(4.5~4.7단계)과 구조 패턴(5a — 번역체·수동태·외래어·**수사적 질문** 포함)이 스킵 조건에 걸려 통째로 안 돌았다. S9·S10은 의문형이라 수사적 질문 패턴(#30) 후보였는데 이 스킵 때문에 애초에 평가 대상에서 빠졌다. (2) 스킵 없이 실제로 실행된 5b(문장/어휘 패턴)와 AI-lexicon 매칭에서도 hot 판정이 하나도 안 나왔다 |
| `cowork-plugins`(`humanize-korean`) | 전부 변경 없음 | **카테고리 불일치.** 자체 감사 패턴(A~J, 수십 개)에서 검출 span 0건. 등급 B로 판정하며 "입력 자체가 이미 AI 티 없이 쓰인 텍스트이기 때문"이라고 스스로 밝혔다 |

**공통으로 드러난 것 하나가 더 있다 — 세 후보(`humanizer-kr`·`patina`·`cowork-plugins`)가 전부
"드라이버"·`candidate`·`confirmed`·"세션-맹검"을 스스로 "확립된 기술 용어" 또는 "코드 리터럴"로
분류해 보존 대상으로 판정했다.** 즉 이 스킬들의 눈에는 이 단어들이 "고쳐야 할 문제"가 아니라
"건드리면 안 되는 것"으로 보인다는 뜻이다. `patina` 판정 근거를 그대로 인용하면: *"'세션-맹검',
'드라이버'는 이 프로젝트의 확립된 기술 용어(에이전트 설명에도 동일 표현 사용)로 판단해 번역체/외래어
남발 대상 아님."*

#### 왜 이렇게 됐나 — 근본 원인

넷 다 **"AI 글쓰기 티"(signs of AI writing)를 잡도록 설계돼 있다** — 과잉 강조어·공허한
형용사·챗봇 잔재·번역체·3의 법칙·은유 등식·무생물 행위주 같은, **"이 글이 AI가 쓴 것처럼 들리는가"**
축의 패턴이다. `patina`는 이 계보를 스스로 명시한다("이 스킬은 위키백과:AI 글쓰기의 징후를 기반으로
하며"). D-6이 실제로 지적한 문제 — **"확립되지 않은 낯선 전문용어를 검증 없이 새로 들여오는 것,
그리고 그 단어가 실린 문장의 문맥·문법이 어색한 것"** — 은 이 넷의 탐지 카테고리 어디에도 없다.
"AI스럽게 들리는가"와 "독자가 이 단어의 뜻을 아는가"는 다른 축이고, 넷 다 앞의 축만 잰다.

여기에 용어집 배제(`GOAL.md:115`, 이미 확정)가 겹치면 문제가 하나 더 생긴다 — **humanizer 계열에게
"맹검"·"비준"·"드라이버"가 이 프로젝트에서 확립된 용어인지 사용자가 못 알아듣는 조어인지 구분할
근거를 안 주면, 위 인용처럼 스스로 "확립된 기술 용어"로 오판해 보수적으로 보존하는 쪽으로 기운다.**
넷 다 "인용부호·코드 식별자·조문 번호는 건드리지 않는다"는 보존 원칙을 갖고 있는데, 드라이버가
"맹검"·"드라이버"를 코드 밖 사용자 응답에 쓸 때도 그것을 이미 확립된 전문용어처럼 취급해 버리면 이
보존 원칙이 정확히 반대 방향으로 작동한다.

#### 결론 — 강요하지 않는다, 표본이 부족하다

- **이 열 문장만 놓고 보면 넷 다 후보에서 탈락이다** — 아무것도 안 고쳤으니 D-6을 해결하지 못한다.
  다만 이 결론을 "humanizer 계열 전체가 이 프로젝트에 안 맞는다"로 일반화하는 것은 **표본 크기(10개,
  전부 발췌 파편)로는 과하다.**
- **표본 자체의 한계가 결과에 섞여 있다.** 열 문장은 전부 원래 훨씬 긴 인터뷰 응답(여러 문단)에서
  뽑아낸 1–2문장짜리 발췌다. `patina`는 이 스킵 조건을 명시적으로 걸었고("단락≤2 OR 문장≤2"), 다른
  후보들도 짧은 입력에서는 정량 한도(문단 3곳/문장 20% 같은)가 사실상 무력화된다. **실제 인터뷰
  응답 전체(여러 문단짜리 상태 블록·질문 묶음)로 다시 돌려보지 않으면, "이 계열이 원래 이 문제에
  약하다"와 "표본이 너무 짧아서 스킵됐다"를 구분할 수 없다.**
- **용어집-없이 방침(`GOAL.md:115`)과 humanizer 채용은 지금 형태로는 서로를 방해할 수 있다.** 세
  후보가 "드라이버"·"세션-맹검"을 스스로 확립된 용어로 오판한 것은, 이 스킬들에게 "이 프로젝트에서
  무엇이 확립된 용어이고 무엇이 아닌지"를 알려줄 방법이 없기 때문이다. `korean-humanizer`의 "Personal
  list"(금지어/선호어 즉석 지정, 방식 A/B/C)나 `patina`의 `custom/lexicon/ai-ko.md`처럼 **호출 시점에
  "이 프로젝트가 새로 들여온 조어 목록"을 인라인으로 넘기는 것**은 대조표(고정 카탈로그)가 아니라
  **호출부 인자**이므로 D-6이 기각한 "용어집" 결정과 충돌하지 않을 수도 있다 — 이것도 **미확인이고
  다음 실험 대상이다.**
- **채용 후보에서 완전히 빠지는 것은 `cowork-plugins` 하나뿐**이다 — 품질과 무관하게 NC-ND 라이선스가
  2차적저작물을 금지하기 때문이다. 나머지 셋(MIT)은 이번 표본으로는 전부 무변경이었지만, 그 이유가
  "이 문제엔 원래 안 맞음"인지 "표본이 너무 짧아서"인지가 아직 안 갈렸으므로 후보에서 완전히 빼기엔
  이르다.

#### 다음 세션 할 일

1. **표본을 문단 단위로 키워 재실험한다.** 이번 인터뷰 장부에서 실제 여러 문단짜리 응답(질문 묶음 +
   근거 설명)을 통째로 뽑아 같은 세 MIT 후보(재시작 후 `humanizer-kr`·`patina`도 실제 슬래시 호출로)
   에 다시 돌린다. `patina`의 스킵 조건이 문단 수가 늘면 해제되는지가 특히 확인 대상이다.
2. **"프로젝트 조어 목록을 호출 시점 인자로 넘기는 것"이 용어집 금지 결정과 실제로 충돌하는지
   확인한다.** 위 세 후보 각각의 인라인 커스터마이징 메커니즘(personal list / custom lexicon)에
   "맹검·되말·비준·드라이버·하네스·시드·사다리 타기"를 즉석으로 넘겨 결과가 달라지는지 대조한다.
3. ~~이번 실험에서 설치한 것을 유지할지 제거할지는 사용자 확인 사항이다.~~ **제거했다** —
   `humanizer-kr`·`patina` 플러그인은 marketplace 등록까지 지웠고 `~/.claude/skills/korean-humanizer`
   도 삭제했다. 설치 전 상태로 돌아갔다.

### 다섯째 후보 — `ubiquitous-language`. 앞의 넷과 부류가 다르다

사용자가 [`queglay/matt-pocock-skills`](https://github.com/queglay/matt-pocock-skills/blob/main/ubiquitous-language/SKILL.md)
의 `ubiquitous-language` 를 제시했다. **MIT.** 상류는 `mattpocock/skills`(MIT)이고 설치는
`npx skills@latest add mattpocock/skills/ubiquitous-language` 다.

**무엇을 하나** — 대화에서 도메인 낱말을 뽑아 `UBIQUITOUS_LANGUAGE.md` 로 쓴다. 구성은 넷이다:
용어/정의/**피해야 할 별칭** 표 · 용어 사이의 관계 · 그 말들이 실제로 쓰이는 **예시 대화** ·
**모호성 지적**(한 낱말이 두 뜻으로 쓰였거나, 한 개념이 여러 이름으로 불린 자리). 다시 부르면 기존
파일을 읽어 갱신한다.

**앞의 humanizer 넷과 결정적으로 다른 점**은 **미리 만들어 들고 다니는 카탈로그가 아니라 대화에서
자란다**는 것이다. 그래서 fabricate 가 어느 프로젝트에서 돌지 모르는 채 배포돼도 성립한다 — 넷이
전부 실패한 자리(사용자 프로젝트의 도메인 용어)를 유일하게 다룰 수 있는 후보다.

**§5 비-목표와의 관계.** 기각된 것은 *"문구를 만드는 게 LLM인데 기계적 번역표를 갈아끼우는 것"*
(`GOAL.md:187`)이고, 이것은 치환표가 아니라 합의 기록이다 — 같은 이름의 다른 물건일 수 있다. 다만
§5 문구가 *"이번 루프에서 짓지 않는다"* 로 **시간이 한정돼 있어서**, 이것이 뒤집기인지 다음 루프
얘기인지는 **사용자 비준 사항이지 이 문서가 판정할 것이 아니다.**

**한계 둘.** ① 스킬이 스스로 *"도메인 전문가에게 의미 있는 말만 담아라"* 라고 못박아서, fabricate
자신의 기계 용어(맹검·비준·술어)는 **담지 않는다.** fabricate 는 하네스라 도메인이 거의 없으므로
이건 구조적이다. ② *"가장 적절한 말을 골라라"* 의 선택 주체가 에이전트인데, 인터뷰는 **사용자가 그
자리에 있는** 자리다. 고르는 권한은 사용자에게 있어야 하고, 에이전트는 사용자가 한 개념에 두 말을
썼을 때 묻기만 해야 한다.

### 이미 있는 검토자를 실측했다 — **어휘를 한 번도 안 봤다**

`agents/question-blind-reviewer.md` 는 질문이 사용자에게 나가기 전에 서는 유일한 서브에이전트다.
그 지시문의 `reject` 사유 다섯 중 하나가 **어휘**다:

> **대화에서만 뜻이 정해진 말을 쓴다.** 앞 턴에서 합의한 축약·별칭·내부 명칭이 설명 없이 나온다.
> (…) 전문 용어가 나온다고 자동으로 `reject` 가 아니다. 그 분야에서 통용되는 말이면 통과다.
> 당신이 판정하는 것은 **이 대화에서만 통하는 말인가**다.

**즉 어휘는 이미 이 에이전트 소관이다.** 그런데 장부를 열어 보면:

| 실측 | 값 |
| --- | --- |
| 질문 | **14건** |
| 그중 지목된 낱말이 들어간 것 | **13건** — 술어(q1·q11) · 표식(q2·q12·q13) · 쪽지(q3~q9 **일곱**) · 하네스(q5) · 낡음(q2·q8) · 비준·시드(q11) · 되말·장부(q12) |
| 판정 | **14건 전부 `pass`** (검토자 14건 전부 `question-blind-reviewer`) |
| **판정 사유에 어휘를 언급한 것** | **0건** |

**사유 14개 전문을 읽었더니 전부 같은 세 가지만 말한다** — ⓐ 답에 필요한 전제가 문안 안에 다 있는가
ⓑ **인용한 파일·라인이 실재하는가** ⓒ 단일 선택 질문인가. 예: *"인용된 `src/project.ts:9` ·
`.gitignore:619` 도 실재"*(q3), *"GOAL.md:165 실재 확인"*(q9), *"`src/session.ts:97`,
`GOAL.md:105-106`, 실제 확인됨"*(q12).

**`Read`·`Grep` 은 인용 앵커가 실재하는지 확인하는 데 쓰였다** — 지시문이 적은 목적(*"질문에 나온
이름이 실재하는 파일·함수인지"*) 그대로다. 질문들이 파일 경로와 명령으로 가득해서 그게 없으면
"전제가 숨어 있다" 항목을 잴 수 없다. **이 문서가 한때 "저장소 grep 이 오염된 어휘를 정당화한다"고
적었다면 그것은 틀렸다 — 그런 일은 일어나지 않았다. 어휘 판정 자체가 아예 발생하지 않았다.**

**왜 안 걸렀나 — 지시문 구조 때문이고, 그 안에서는 `pass` 가 옳다.** 어휘가 독립 기준이 아니라
**"답할 수 있는가"의 하위 조건**으로 매달려 있다. 그런데 이 질문들은 길고 스스로 전부 설명한다.
q11 의 *"완료 정의는 `GOAL.md` §2 의 통합 **술어** IP-0~6 일곱"* 은 "술어"가 무슨 뜻인지 몰라도
IP-0~6 을 보고 답할 수 있다. **답이 성립하므로 통과다.**

> **그래서 결론은 이것이다 — "답할 수 있는가"와 "알아들을 수 있는가"는 다르다.**
> D-6 이 문제 삼는 것은 뒤엣것인데 이 검토자는 앞엣것만 잰다. 어휘를 답변 가능성의 하위에 두는 한
> **스스로 설명하는 긴 질문은 낯선 말을 얼마든지 실어도 통과한다.** 고치려면 어휘가 자기 합격/불합격을
> 따로 가져야 한다.

### 병이 하나가 아니라 셋이다 — 무엇이 무엇을 잡는지

| | 무엇인가 | 이번 실사용 예 | 무엇이 잡나 |
| --- | --- | --- | --- |
| **①** | **낯선 조어** — 이 저장소 문서에서만 뜻이 정해진 말 | 맹검 · 비준 · 술어 · 되말 · 장부 · 조각 | **합의 낱말 목록.** 목록에 없으면 쉬운 말로 풀어 쓴다 |
| **②** | **사용자 말이 아닌 이름** — 뜻은 통하는데 사용자가 그 개념을 다르게 부른다 | **쪽지**(사용자: 핸드오프) · **낡음**(사용자: "낡았다가 의미가 없다니까") | **합의 낱말 목록.** 단 그 합의가 생긴 뒤부터다 — 첫 발생은 어떤 설계로도 못 잡고, 잡을 필요도 없다(합의는 인터뷰에서 만들어진다). **막아야 하는 것은 재발이다** |
| **③** | **문맥에 안 맞는 말** — 쉬운 말이고 겨루는 이름도 없는데, 그 낱말이 그 뜻을 아예 안 가진다 | **검문소** — 2026-07-30 이 논의 중 에이전트가 "질문이 나가기 전 서브에이전트가 판정하는 자리"를 가리키려고 썼다. 사용자: *"검문소라는 단어를 보고 어떻게 '서브 에이전트의 판단 시점'을 떠올리냐고"* | **목록으로 못 잡는다.** 대조할 상대가 없다. **"이 문장이 무슨 말인지 알겠는가"만 잡는다** |

**③ 이 사용자가 D-6 의 결론으로 지목한 바로 그것이다** — *"결국은 개별 어휘의 문제라기보다는 문맥에
맞는 어휘 선정, 문법 구조가 이상한 경우가 많다는 거야."* 위 표의 오른쪽 칸이 그 진단을 실물로
확인해 준다: ①②는 목록이 풀지만 **③은 어떤 목록으로도 안 풀린다.** 그리고 ③은 이미 검토자 소관이다
(`reject` 사유 *"무엇을 묻는지 자체가 갈린다"*) — 파일도 목록도 필요 없고 **실제로 걸러내기만 하면
되는데, 이번 실측(14/14 `pass`)에는 그것이 작동한 증거가 없다.**

### 이 절이 남기는 결정 셋

1. **합의 낱말 목록을 만드느냐** — ①②를 풀려면 필요하고, `ubiquitous-language` 가 그 모양을 이미
   갖고 있다. **`GOAL.md` §5 해석이 걸리므로 사용자 비준 사항이다.**
2. **어휘가 독립 기준을 갖느냐** — 지금처럼 "답할 수 있는가"의 하위로 두면 긴 질문은 계속 통과한다.
   다만 검토자의 `Read`·`Grep`(인용 앵커 실재 확인)은 **그대로 둬야 한다** — 그건 어휘와 무관한
   전제 확인이고, 빼면 "전제가 숨어 있다"를 못 재게 된다.
3. **검사를 질문 밖으로 넓히느냐** — 지금 검토자는 **질문만** 본다. 되말하기 22건 · 상태 블록 ·
   배경 설명은 아무도 안 본다. 위 표본 열 개도 대부분 질문이 아니었다.

**셋 다 새 도구를 사올 것이냐가 아니라 이미 있는 검토자를 어떻게 고칠 것이냐의 문제다.** humanizer
넷을 실제로 돌려 아무것도 못 잡은 것과, 검토자가 14건 전부 통과시킨 것이 같은 방향을 가리킨다 —
**바깥에서 가져올 수 있는 것은 목록의 모양(`ubiquitous-language`)뿐이고, 판정은 이 저장소 안에서
지어야 한다.**

---

## 우선순위 제안

| | 항목 | 크기 | `GOAL.md` §9 필요? | 상태 |
| --- | --- | --- | --- | --- |
| 1 | **D-5** 의도 축소 — 인터뷰의 목적을 정면으로 무너뜨린다 | 중 | ~~아마도 필요~~ **미확인** — D-3 이 같은 예상을 뒤집었다 | 안 고침 |
| 2 | **D-6** 어휘 — **세 번 재발.** `GOAL.md`·`STATE.md` 는 원천이 아니라 임시 파일이었다(정정됨) — 진짜 원인은 드라이버가 조사 중 읽은 문서를 검증 없이 옮기는 습관이다 | 중 | 불필요(humanizer 차용 경로에는) | 안 고침 (후보 넷 실험 완료) |
| 3 | **D-2** 상태 블록 위치·형식 | 소 | 불필요 | **고침 `f834e37`** |
| 4 | **D-3** 확인 게이트 — 왕복 비용이 가장 크다 | 소 (`SKILL.md` 만) | ~~필요 (IP-1ⓖⓗ·IP-2ⓔ)~~ **불필요였다** | **고침 `2ff862e`** |
| 5 | **D-1** 언어 — SKILL.md 영어화는 싸고, CLI 출력은 미결 | 중 | 불필요 (README 규약) | 안 고침 |

~~**D-3 · D-5 는 `GOAL.md` 개정이 얽혀 있으므로 `main` 착지 이후다.**~~ **D-3 은 `GOAL.md` 를
안 건드리고 끝났다**(위 "지금 상태" 참조) — 착지를 기다릴 이유가 없었다. D-5 도 같은 확인을
먼저 해야 한다. D-6 은 humanizer 스킬 차용 경로로 가면 `GOAL.md` 개정이 필요 없다.

**D-1 과 D-6 은 같이 봐야 한다.** D-1 이 "사용자가 쓴 언어로 동작한다"로 가면, D-6 의 "통용되는
한국어" 기준은 **한국어를 쓰는 사용자에게만 적용되는 것**이 된다. 두 항목을 따로 고치면 규칙이
갈린다.
