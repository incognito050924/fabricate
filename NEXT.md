# 지금 어디까지 왔고, 다음은 무엇인가

## 끝난 것 — 조각 0 (커밋 f3a1732)

저장소, 도구 사슬(Bun + TypeScript strict + zod + citty + biome), 그리고 `contract/`의 창립 계약
원문 복사. 검증: 69개 조건 문안이 원본과 **바이트 단위로 동일**, 원 요청 문장과 목표 문장도 verbatim
일치, 연구 보고서·계약 초안·심의 기록은 파일 비교로 동일 확인.

`src/`는 아직 비어 있다. 코드는 한 줄도 없다.

## 원칙 (어기면 이 작업의 목적 자체가 무너진다)

- **`contract/`는 읽기 전용.** 조건 문안을 고치지 않는다. 조건을 어떻게 판정하는지는 별도 산출물이다.
- **옛 저장소(`~/dev/projects/ditto`)의 코드는 승계하지 않는다.** 읽는 것은 허용한다 — 같은 수업료를
  두 번 내지 않기 위해서. 특히 참고할 가치가 있는 것: `rebuild/state/intent-lock.ts`(조건 집합 잠금),
  `rebuild/verify/red-first.ts`(빨간 테스트 선행), `rebuild/gate/oracle-satisfaction.ts`(증거 종류 대조),
  `rebuild/gate/residual.ts`(잔여가 남은 완료 차단), `rebuild/verify/completion-authority.ts`(외부 검사자).
  읽고 이해한 뒤 **새로 쓴다.**
- **테스트 우선.** 실패하는 테스트 → 최소 구현 → 구조 개선. 구조 변경과 동작 변경을 한 커밋에 섞지 않는다.
- 코드와 주석은 영어, 사용자에게 보이는 문구는 한국어.

## 다음 — 조각 1: 방어 게이트 다섯 개

이것들은 이 하네스의 제품 기능이면서, 동시에 앞으로의 우리 작업을 지키는 장치다. 이게 서기 전까지는
어떤 방어도 없다. 그래서 가장 먼저 짓는다. 각각 작게, 테스트와 함께.

1. **조건과 판정 기준 스키마** — 조건마다 판정 기준이 필수(없으면 파싱 거부). 판정 방식 세 가지:
   실행해서 확인 / 다시 스캔해서 확인 / 사람 판단. 미래를 향한 판정 기준은 코드 위치(파일:줄)에 걸 수
   없다(변경되면 깨지므로). 판정 기준의 조건 id가 조건 id와 일치해야 한다.
2. **조건 집합 잠금** — 한 번 잠기면 제거 거부, 추가는 허용하고 보고. 판정이 하나라도 붙은 뒤의
   재잠금 거부.
3. **증거 종류 대조** — 실행 판정은 테스트·명령 증거만, 재스캔 판정은 파일 증거만, 사람 판단은
   관찰·재현 증거만. 증거 없으면 항상 막는다.
4. **빨간 테스트 선행 검사** — 테스트를 쓴 주체가 루프 자신이면 무효. 빨간 상태를 실제로 관측한 기록이
   없으면 거부. 내용 해시가 바뀌거나 파일이 지워지면 거부.
5. **종료 게이트** — 통과 못 한 조건이 남아 있으면 완료 종료를 부적격으로 만든다. 정직한 미검증 +
   재진입 조건으로만 착지.

조각 1이 끝나면 `bun test`가 초록이어야 하고, 그 시점부터 모든 후속 작업이 기계에 감시된다.

## 그 다음 (미리 알아둘 것)

조각 2에서 69개 조건마다 판정 기준과 빨간 테스트를 만든다. 그 산출물(69행 판정표 + 테스트 동결 해시)이
**사용자가 검토하는 유일한 관문**이다. 승인 전에는 인터뷰 구현에 손대지 않는다.

부챗살(여러 에이전트 동시 판단)은 조각 2·4의 판단 작업에만 쓴다. 하나의 표면을 순차로 짓는 조각 1·3에
쓰면 여러 에이전트가 같은 파일을 락 없이 덮어쓴다.

## 옛 저장소 상태

`wi_260724pps`는 ditto에서 **partial로 세워 뒀다**(재진입 명령이 이 문서를 가리킨다). ditto 자체의
처분은 보류 — 이 하네스가 실제로 쓸 만해진 뒤에 증거를 보고 정한다.
