# 2회차 실증에서 나온 수정 계획 (2026-07-31)

> **딛고 선 것**: `INTERVIEW-DEFECTS.md` 의 D-1·D-2·D-3·D-5·D-6 을 고친 뒤(`30d6cea`…`1ac3424`)
> 실제로 한 번 더 돌린 인터뷰 **`e8e7e3e7-0338-454a-8eb8-b2409dfab894`**.
> 이 문서의 모든 수치는 그 세션의 파일에서 직접 센 것이고, 셈에 쓴 것은 넷이다 —
> `.fabricate/intent/<id>.json` · `.fabricate/sessions/<id>/{ledger.jsonl,hooks.jsonl}` ·
> `~/.claude/projects/-Users-ecoletree-dev-project-fabricate/<id>.jsonl`(대화 원문).
>
> **초안의 수치를 원본에서 한 번 더 셌다(2026-07-31).** 셋이 틀려서 고쳤고(§0 왕복·§0 어휘·F-5),
> 다섯을 새로 넣었다(F-1ⓒ 근거 · F-1 사슬 횟수 · F-4 제목 · F-5 원인 · §3 훅·사유 항목).
> 고친 자리마다 근거를 옆에 붙였다.
>
> **아직 착수 전이다.** 사용자가 항목을 더할 예정이고, 그 전에는 아무것도 안 고친다.
>
> **사용자 항목이 들어왔다(2026-07-31): F-7 ~ F-11.** 상시 상태 요약과 상세 보기, 최종 확인의
> UX(F-7~F-10)와 의도 확인의 어투(F-11)다. 근거는 위 넷에 더해 `e8e7e3e7` 원장을
> `analyzeLedger`+`statusBlock` 에 그대로 먹여 재현한 출력이다. 무게로는 **F-9 가 이 문서에서
> 가장 크다.** 이 항목들도 **계획에만 넣었고 코드는 안 건드렸다.**
>
> **착수 전에 이 문서 밖에서 한 번 더 검증했다(2026-07-31).** 이 대화를 안 본 별도 컨텍스트가
> 모든 수치와 `file:line` 을 원본에서 다시 셌다. **무게를 지는 주장은 하나도 빠짐없이 재현됐고**,
> 곁수치 다섯과 인용 넷이 틀려서 고쳤다 — `F-2` 함수 줄(`:364`→`:355`) · `F-7` 겹셈(여덟 쌍→
> 차원 아홉·답 여섯) · `F-2` 판별 항목 수 · `F-7`·`F-10` 확인표 행수 · `F-11` 의 `DEFECTS` 줄
> 번호 · `F-1` 훅 차단 자리 · `F-2` 의 IP-3 인용 출처 · `F-10` 의 `SKILL.md` 인용 범위.
> **셈 기준이 안 적혀 있던 자리 둘도 못박았다** — §0 의 "사람 발화"가 무엇인지, `F-9` 의 "화면에
> 안 나온다"가 무엇인지. 둘 다 고치고 나니 수치가 움직였다(§0 개선폭 · `F-9` 10종→12종).
>
> **설계도 같이 두들겨 봤다(2026-07-31).** 별도 컨텍스트가 "이 수정안이 진짜 원인을 치는가,
> 무엇을 깨뜨리는가"만 봤고, **셋이 뒤집혔다** — `F-2` 는 추천안이 고장을 안 쳐서 **ⓓ 로 갈아탔고**,
> `F-1ⓒ` 는 "IP-2ⓙ 를 안 건드린다"가 틀려서 보완이 붙었으며, **새 결함 `F-12` 가 나왔다**(장부의
> 의도 확인이 사용자가 본 문장이 아니다 — F-9·F-10·F-11 의 공통 뿌리). 미해결 질문 둘은 **코드가
> 이미 답하고 있어서 지웠다.** **착수를 막는 것 하나가 남았다 — §5-1.**

---

## 0. 무엇이 재졌고 무엇이 안 재졌나

앞 수정이 남긴 미검증 셋 중 **하나만 재졌다.**

| | 결과 |
| --- | --- |
| **왕복(D-2·D-3)** | **재졌다.** 인터뷰 구간의 사람 발화 **31 → 12**, 질문당 **2.21 → 1.33**. 기전이 원장에 보인다 — 채택 확인 10건이 **한 묶음**(`12:42:48~49`)으로 몰렸고 직전 사용자 발화 `"맞아"`(`12:41:59`) 하나가 그것을 닫았다. 상태 블록은 실린 27건 중 **25건이 응답 맨 앞**이었다 |
| **어휘 관문(D-6)** | **절반.** 구조는 고쳐졌다 — 판정 사유에 어휘가 나온 것이 **1/14 → 8/10**. 그러나 **관문 B 가 거부한 건은 0건**이고, 거부 2건은 **둘 다 관문 A** 였다. "B 가 나쁜 말을 잡아낼 수 있는가"는 그대로 미검증 |
| **remark 커버리지(D-5)** | **못 쟀다.** `remark` 가 3건뿐이고 그중 2건이 사용자가 친 `status` 였다. 실질 결정은 하나이고 그건 담겼다. `close` 거부 **0건** — 시도가 1회였고 그대로 통과했다 |

**왕복 수치를 고친 이유.** 초안은 **43 → 12** 였는데 분모가 서로 달랐다. `43` 은 앞 세션
`cc9ed860` 의 **세션 파일 전체** 사람 발화이고 그중 12건은 인터뷰가 닫힌 뒤(05:36~06:50) 나눈
다른 주제의 대화다. `12` 는 이번 세션의 **인터뷰 구간만**이다(세션 전체로는 13). 기준을 맞추면
이렇다.

| 기준 | 앞 판 | 이번 판 | 질문당 (14문 → 9문) |
| --- | --- | --- | --- |
| **인터뷰 구간** (이 문서가 쓰는 기준) | 31 | 12 | **2.21 → 1.33** |
| 세션 파일 전체 | 43 | 13 | 3.07 → 1.44 |

**무엇을 "사람 발화"로 세는지도 못박는다.** 위 수는 **사용자가 답으로 친 발화**만이다. 여기서 빠지는
것이 셋이다 — ① 서브에이전트 알림 ② 훅이 되돌려 준 문구 ③ **슬래시 명령 턴**. ③을 도로 넣으면
앞 판 31 → **33**(인터뷰를 여는 명령 2회), 이번 판 12 → **15**(여는 명령 1 + `status` 2)이고,
질문당 2.36 → 1.67 로 **개선은 1.7배가 아니라 1.4배**가 된다.

**이 갈림이 문서 안에서 한 번 어긋난다.** `F-1` 은 사용자가 친 `status` 두 번을 **사건으로 센다**.
같은 두 턴을 왕복에서는 빼고 결함에서는 세는 셈이라, `F-1` 을 고쳐 그 두 턴이 사라지면 왕복 수치도
같이 움직인다. **3회차에서는 셈 규칙을 먼저 적고 시작한다**(§3).

개선은 어느 기준으로도 남지만 **2.3배가 아니라 1.4~1.7배**다.

**어휘 수치를 고친 이유.** 앞 판이 `0/14` 가 아니라 `1/14` 다 — `cc9ed860` 의 `q12` 판정 사유가
*"사용된 어휘(장부·되말하기·활성 표식)는 GOAL.md/STATE.md 에 정의된 저장소 용어라"* 로 어휘를
직접 다룬다. 이번 판은 `review` 10건 중 **8건**이다(`goal-review` 2건까지 넣으면 9/12).
"구조는 고쳐졌다"는 결론은 그대로 선다.

**예상 밖의 신호 하나**: 목표 술어가 **5판 → 1판**이다. 이전엔 사용자가 네 번 되돌려 놨는데, 이번엔
초안 1건을 **검토자가 사용자에게 가기 전에** 잡았다.

**교란을 정직하게 적는다.** 두 인터뷰는 비교 대상으로 나쁘다 — 이번 사용자는 `A`·`3`·`1`·`맞아` 로
짧게 답했고 `remark` 가 25 → 3 으로 줄었다. **이번 판이 훨씬 덜 다퉜다.** 2.21 → 1.33 중 얼마가
수정 덕이고 얼마가 요청 성격 덕인지 이 두 판으로는 못 가른다.

---

## 1. 고칠 것

### F-1. 상태를 물어본 턴이 막히고, 막힌 자리를 쓰레기 기록으로 메운다 — **가장 무겁다**

**무슨 일이 있었나.** 한 사건인 줄 알았던 것이 다섯 걸음짜리 사슬이었다.

1. 사용자가 상태를 보려고 `/fabricate:deep-interview status` 를 쳤다. **그런 것은 없다** — 상태
   조회는 CLI `fabricate deep-interview status`(`src/cli.ts:84`)이고 슬래시 명령은 인자를 원 요청으로 받는다
2. UPE 훅이 다시 발화하고 `request_written: true` 를 적는다(`src/hooks.ts:68`)
3. 드라이버가 상태만 보여 주고 턴을 끝낸다 → **장부가 안 늘어 Stop 훅이 막는다**(판정 `src/hooks.ts:140` · 차단 반환 `:175`)
4. 훅을 만족시키려고 드라이버가 사용자의 낱말 `"status"` 를 **`remark` 로 적는다**
5. 끝낼 때 D-5 게이트가 그 쓰레기 `remark` 에 **`set-aside` + 이유를 요구한다**

**실측**: `hooks.jsonl` 의 UPE 3건 · `stop_hook_active:true` 1건 · `remark` 3건 중 `m2`·`m3` 의
본문이 `"status"` · `set-aside` 2건이 그 둘. **UPE 3건 중 2건의 인자가 `'status'` 였다는 것은
`hooks.jsonl` 이 아니라 대화 원문에서 온다** — 훅 기록에 `command_args` 필드가 없다.

**사슬이 온전히 돈 것은 한 번뿐이다.** 두 번째(`12:48:26`)에는 드라이버가 명령 5초 뒤에 `m3` 를
먼저 적고 들어가서 **막히지 않았다**(그 턴의 Stop 은 `stop_hook_active:false`). 즉 **막힌 것은
1회, `set-aside` 를 요구받은 것은 2회**다.

**드러난 설계 구멍**: Stop 훅의 "장부가 늘어야 한다"에 **적을 것이 진짜로 없는 턴을 위한 출구가
없다.** 상태 조회는 진행 턴이 아닌데 `GOAL.md` IP-2ⓛ 의 "정상 진행 턴(장부가 늘었음)은 막지
않는다"가 그 경우를 안 덮는다.

**갈래 셋.**

| | 무엇 | `GOAL.md` |
| --- | --- | --- |
| ⓐ | 슬래시 명령이 `status` 류 인자를 알아보고 상태를 낸다 | 불필요 |
| ⓑ | Stop 훅에 "적을 것 없는 턴"의 예외를 판다 | **IP-2ⓙ 를 약화시킨다 → §9 + §7-4(사용자 결정)** |
| **ⓒ** | **`deep-interview status` 가 조회 사실을 장부에 한 줄 남긴다**(`kind: status-view`) | **불필요** |

**추천은 ⓒ. 그리고 이건 추론이 아니라 실측으로 성립한다.** 대화 원문을 보면 드라이버는 그 턴에
**실제로 `fabricate deep-interview status` 를 돌렸고**(`12:20:03`), Stop 훅이 막은 것은
`12:20:39` 다. 조회 명령이 그 시점에 장부를 한 줄 늘렸다면 **36초 먼저** 늘어서 훅이 안 막았다.
장부가 실제로 늘므로 IP-2ⓙ 를 글자 그대로 만족시킨다. "사용자가 상태를 봤다"는 진짜 사건이라
기록도 정직하다. `analyzeLedger` 는 모르는 kind 를 그냥 흘리므로
(`src/interview-state.ts:188~482` 이 `if (kind === …)` 연쇄다) `close` 셈에 영향이 없다.
**ⓐ 만 하면 다른 "적을 게 없는 턴"에서 같은 일이 또 난다.**

> **착수 전 검토가 여기서 둘을 잡았다.**
>
> **① "사슬 전체가 사라진다"는 과장이다.** 사라지는 것은 3·4·5번이다. 1번(없는 하위 명령으로
> 슬래시 재진입)과 2번(UPE 재발화)은 그대로 남는다 — 다만 `request.txt` 는
> `writeFileIfAbsent`(`src/files.ts:28`)라 안 덮이므로 실제 피해는 **F-3 의 거짓 관찰 하나**다.
>
> **② "IP-2ⓙ 를 안 건드린다"는 성립하지 않는다.** `status-view` 는 `analyzeLedger` 가 흘리고
> (`src/interview-state.ts:185-493`), `close` 가 안 보고(`src/intent.ts:160-311`), `status` 가
> 안 그린다. 즉 **Stop 훅을 만족시키는 비용 0 · 흔적 0 의 경로**가 새로 생긴다. 기존 우회는 전부
> 다른 게이트에 걸린다 — 쓰레기 `remark` 는 D-5 가(`src/intent.ts:237-246`), 쓰레기 `fragment`
> 는 원문 대조가(`src/ledger.ts:192`), 쓰레기 `ambiguity` 는 `open` 목록이
> (`src/status.ts:99-103`). **`status-view` 만 아무 데도 안 걸린다.** ⓑ 와 같은 종류의 약화이고,
> 다른 점은 ⓑ 는 드러내놓고 하고 **ⓒ 는 안 드러난다**는 것뿐이다.
>
> **최소 보완**: 잔여가 **보이게** 만든다 — F-4 와 묶어 Stop 훅 관찰 기록에 "이 턴을 늘린 것이
> `status-view` 뿐이었다"를 남기거나, `statusBlock`·`close` 출력에 `status views: N` 한 줄을 낸다.
>
> **부작용 둘이 더 있다.** ① 읽기 전용이던 `deep-interview status` 가 **쓰기 명령이 된다.**
> `verify/checks/S-3.ts`("잠금을 우회하는 쓰기 경로가 없다")의 명령 목록에 `status` 가 **없어서**
> 새 쓰기 경로가 검사 밖에 놓인다 — 목록에 넣어야 한다. ② `close` 가 장부를 통째로 잠긴 레코드에
> 담으므로(`src/intent.ts:83`) 조회 기록이 시드에 실린다.

> **이번엔 §9 예상을 함부로 적지 않는다.** 앞 문서가 세 번 "§9 가 필요할 것"이라고 적었고 세 번 다
> 틀렸다. 위 표는 **읽은 근거를 적은 것이지 판정이 아니다** — 착수 전에 IP-2ⓙ·ⓛ 원문을 다시 대본다.

**크기**: 소 (ⓒ 기준 — `src/ledger.ts:30` 의 `showStatus` 한 곳 + fixture)

---

### F-2. `show` 가 목표 술어의 판본을 전부 쏟아낸다 — IP-3 직격

`goal` 기록은 쌓이고, `formatIntentForShow`(`src/intent.ts:355`, 번호를 붙이는 줄은 `:363`)가 **그것을 전부 번호를 붙여
출력한다.** 실측으로 확인했다 — `fabricate deep-interview show cc9ed860…` 은 **목표 술어 다섯 판을
0~4 로 통째로** 낸다. 판마다 자기 안에 또 번호가 있어서(0판은 0~7, 나머지 넷은 0~8) 읽는 쪽에서는 무엇이 최종인지 알 수 없다.

이번 세션은 `goal` 이 1건이라 안 드러났을 뿐 **기전은 그대로다.**

`GOAL.md` 가 IP-3 을 두고 적은 것은 *"새 세션이 그 파일을 읽으면 이전 대화를 복원하지 않고도 같은 뜻 위에 선다"* 인데(`GOAL.md:28` 머리말. IP-3 표 자체에는 이 문장이 없다),
서로 어긋나는 다섯 판을 받은 세션은 같은 뜻 위에 못 선다. **시드가 오염된다.**

> **착수 전 검토에서 이 항목이 뒤집혔다.** 초안은 이것을 `show` 의 표시 문제로 보고 ⓐ(마지막 판만
> 출력)를 추천했다. **표시 문제가 아니다** — 그리고 "판본인가 항목인가"는 사람에게 물을 것이
> 아니라 **코드가 이미 답하고 있었다.**

**코드는 `goals` 를 판본이 아니라 항목 목록으로 다룬다.** 결정적 자리는 `show` 가 아니라 `check` 다.

- `src/check.ts:110-129` 가 `goals.texts` 의 **모든 인덱스에 증거를 요구**하고, 하나라도 없으면
  `fabricate check <id>` 가 실패한다
- `check record --goal <n>` 이 인덱스로 증거를 붙인다(`src/check.ts:63`·`:263` `Goal index mismatch`)
- `goal_hash` 는 전부를 `join("\n")` 해 해싱한다(`src/interview-state.ts:158-159`)

그래서 판본 쓰임(`cc9ed860`)에서는 **`fabricate check` 가 죽은 초안 네 개에 증거를 요구한다.**
IP-4(완료를 통치한다)가 도달 불가가 된다. 이것이 진짜 고장이고, 초안은 그것을 못 봤다.

**ⓐ 는 그 고장을 화면에서 가릴 뿐이다.** `check` 는 그대로 다섯을 요구하고, 출력의 `goal-hash` 는
**화면에 없는 네 판까지 덮으므로 읽는 쪽이 대조할 수 없다** — IP-1ⓕ("저장된 레코드가 사용자에게
보여준 문안의 해시와 묶여 있다")가 무너진 채 남는다. 그리고 항목 쓰임(`b63faf03`)에서는 술어
여섯 중 다섯을 시드에서 지운다. **초안이 스스로 "ⓐ 는 항목별 쓰임을 깨뜨린다"고 적어 놓고 ⓐ 를
추천했다.**

**갈래를 다시 놓는다.**

| | 무엇 | 판정 |
| --- | --- | --- |
| ⓐ | `show` 가 마지막 판만 낸다 | **반대.** 고장을 가린다 (위) |
| ⓑ | `goal` 을 갈아치우기로 바꾼다 | 개정 이력이 사라진다 |
| **ⓓ** | **`goal` 에 `--id` 를 준다.** `goals: string[]` 을 `Map<id, {text, covers}>` 로 바꾸고, **같은 id 재기록은 그 자리를 갈아치운다** (`review` 가 같은 `--question` 으로 덮이는 것과 같은 꼴, `src/ledger.ts:216-230`) | **추천** |

ⓓ 하나로 닫히는 것이 다섯이다.

- `show` 가 살아 있는 판만 낸다 — **F-2 해결**
- `goal_hash` 가 보여준 것과 일치한다 — **IP-1ⓕ 회복**
- `check --goal <index>` 의 인덱스가 개정에도 안 흔들린다 — **IP-4 회복**
- `goalCovers` 를 살아 있는 항목의 것만 모을 수 있다 — **§2 가 "안 고친다"로 미뤄 둔 합집합 잔여까지
  닫힌다**(`src/interview-state.ts:382-391`, `src/intent.ts:234-236`)
- `settled` 의 "판본마다 한 줄"이 사라진다 — **F-7 의 3번 절반**

손대는 자리: `src/interview-state.ts:382-391` · `src/ledger.ts:639-661`·`:711` · `src/intent.ts:363` ·
`src/status.ts:67-69` · `SKILL.md:326`·`:351`. **id 는 `requiredId` 가 아니라
`requiredString`+`idPattern` 으로 받는다** — 중복 허용이 요점이라 기존 중복 금지 검사에 걸리면 안 된다.

ⓐ 보다 크다. 그러나 ⓐ 는 고장을 안 고치므로 "가장 간단하되 **검증된**"의 후보가 아니다.

> **덧붙은 결함**: `SKILL.md:351` 이 개정을 *"record `goal` again"* 으로 시사하는데 **그 길에 개정
> 경로가 아예 없다.** 다시 적으면 쌓일 뿐이다. ⓓ 가 이것도 같이 판다.

**크기**: 중

---

### F-3. `request_written` 이 실제 쓰기를 뜻하지 않는다

`src/hooks.ts:68` 이 재는 것은 **"인자가 비어 있지 않았다"** 뿐이다. 실제 쓰기는
`writeFileIfAbsent`(`src/files.ts:28`)라 파일이 있으면 안 덮고, 반환값이 `void` 라 덮었는지 여부가
호출부로 오지도 않는다. 실측: 3건 전부 `true` 인데 `request.txt` 는 82바이트 원 요청 그대로였다.

관찰 기록이 거짓말을 하면 그것으로 판정한 모든 것이 흔들린다. **`writeFileIfAbsent` 가 실제로 썼는지를
돌려주고 그 값을 적는다.**

> **그것만 적으면 거짓말의 방향이 뒤집힐 뿐이다**(착수 전 검토). 같은 세션의 2·3번째 UPE 가
> `false` 가 되어 이번엔 **"원문이 없다"로 오독된다** — F-4 와 §3 이 요구하는 "관찰 기록만으로
> 셀 수 있음"을 도로 깬다. **둘 다 적는다** — `request_written`(이번 호출이 썼다)과
> `request_present`(호출 뒤 파일이 있다). `close` 가 거부하는 조건은 파일 부재
> (`src/intent.ts:167`)이므로 **IP-2ⓜ 와 짝이 맞는 값은 뒤엣것**이다.

**크기**: 소

---

### F-4. Stop 훅이 몇 번 막았는지 관찰 기록만으로는 못 센다

`turn-violation` 은 **재호출에서 장부가 늘면 안 남는다**(`src/hooks.ts:142` 가 그 분기로 빠진다).
이번 세션은 원장 `turn-violation` 0건인데 실제로는 한 번 막혔다.

**`hooks.jsonl` 로도 직접은 못 센다.** 훅이 차단을 반환했다는 기록이 없고, 우리가 세는 것은 그 다음
재호출의 `stop_hook_active:true` 흔적이다. 캡(기본 8회)에 걸리거나 모델이 아무것도 안 내면 그
흔적조차 안 남는다.

**다만 "어디에서도 못 센다"는 아니다.** 대화 원문에는 차단 문구가 그대로 남는다 —
`"Stop hook feedback:"` 이 이번 세션에 정확히 1건이고, 그것이 위 "한 번 막혔다"의 근거다.
그래도 고칠 값어치가 있다: 대화 원문은 저장소 산출물이 아니라 호스트가 쥔 파일이고, 다른 기계나
다른 호스트에서 이어받으면 그 근거가 사라진다.

**차단을 반환할 때 `hooks.jsonl` 에 그 사실을 적는다.** 원장이 아니라 관찰 기록 쪽이다 — 차단은
인터뷰 내용이 아니라 훅의 행동이다.

> **더 싼 구현이 있다**(착수 전 검토). `appendHookObservation` 이 지금 **결정 전에**
> 불린다(`src/hooks.ts:119`). 줄을 하나 더 붙이지 말고 **append 를 결정 뒤로 옮겨
> `decision: "block"|"pass"|"violation"|"inactive"` 한 필드를 실어라.** Stop 당 기록 1건이
> 유지되고, 캡 초과로 조용히 버려지는 경우까지 "우리 쪽에서는 차단을 냈다"가 남는다.
>
> **주의**: `promptId === null`(`:125`)과 `!active`(`:131`) 조기 반환 경로에서도 반드시 한 건이
> 남아야 한다 — `verify/checks/S-5.ts:18-19`·`S-6.ts` 가 `hook_event_name` 별 건수만 세므로
> 어느 경로에서든 기록이 빠지면 그 둘이 깨진다.

**크기**: 소

---

### F-5. 드라이버가 상태 블록을 옮기면서 다시 쓴다 — **그런데 프롬프트가 그걸 허가하고 있다**

D-1 결정(CLI 는 영어, 사용자에게 쓰는 문장은 드라이버가 사용자 언어로)이 의도대로 돌았다 —
블록 27건 중 **영어 원문 그대로 붙인 것은 0건**이다. 그런데 옮기면서 **재구성한다.** 셋을 셌다.

| 무엇 | 몇 번 |
| --- | --- |
| 세 칸을 `이번 턴에 바뀐 것 (없음 — …)` 한 줄로 접었다 | **13 / 27** |
| 없는 칸을 지어냈다 (`이번 턴에 다시 열린 것`) | 1 |
| 요약 줄 끝의 "전체 보기 …status" 안내를 통째로 뺐다 | 1 |

셋째가 특히 나쁘다. 그 안내는 D-2 가 블록을 줄이면서 대신 판 **출구**인데, 하필 마지막 블록
(`정리됨 22 · 열림 0`)에서 사라졌다.

**원인은 드라이버가 아니라 지시문이다.** `skills/deep-interview/SKILL.md:156-157` 이 블록을
*"one summary line on top, and **of the three sections only the ones that actually moved**"* 라고
설명한다. 그런데 실제 출력을 만드는 `statusDiff`(`src/status.ts:29-39`)는 **언제나 세 칸을 다 내고
빈 칸을 `(none)` 으로 채운다.** 설명과 구현이 어긋나 있고, 드라이버는 설명을 따랐다.
**지시문은 자기 예시와도 어긋난다** — 바로 아래 `:159-168` 의 본보기 블록이 세 칸을 다 내고
`(none)` 을 채운다(착수 전 검토). 근거가 하나 더 있는 셈이다.
`INTERVIEW-DEFECTS.md` D-1 절이 미리 우려했던 그것이고, **이제 추정이 아니라 관측이다.**

**고칠 자리는 프롬프트 두 곳이다**: ① `:156-157` 의 설명을 실제 출력대로 고친다(세 칸은 늘 나오고
빈 칸은 `(none)` 이다) ② 옮기되 **칸을 없애거나 항목을 접거나 요약 줄을 잘라내지 마라**를 명시한다.
①을 안 하고 ②만 넣으면 지시문이 자기모순이 된다.

**크기**: 소 (`skills/deep-interview/SKILL.md`)

---

### F-6. `remark` 의 기록 기준이 도구 명령까지 삼킨다

`SKILL.md:194` 는 `remark` 를 *"사용자가 먼저 꺼낸 반박 · 방향 수정 · 되물음"* 으로 정의한다.
`"status"` 는 셋 중 아무것도 아닌데 기록됐다. 원인은 F-1 의 4번 걸음(훅을 만족시키려고)이라
**F-1 을 고치면 대부분 사라진다.** 다만 정의에 *"도구에 내리는 명령은 `remark` 가 아니다"* 를
한 줄 더하는 것이 남는다.

**크기**: 소 · **F-1 하위**

---

### F-7. 상시 블록의 숫자가 화면의 어떤 것과도 안 맞는다

최종 확인 턴에 사용자가 실제로 본 것은 이렇다(대화 원문, `"맞아"` 직전 응답).

```
─ 지금까지: 정리됨 8 · 열림 13 · 전체 보기 … ─
이번 턴에 정리된 것
  d7 …                      ← 한 줄
```

그 아래에 드라이버가 **확인표**를 붙였다 — 답 열 개를 다루는데 표의 행은 아홉이다(9·10번 답이
`9→10` 한 행으로 합쳐졌다). 한 화면에 `8`·`13`·`1`·`9`(다루는 답으로는 `10`)가 있고 넷이 서로
다른 것을 센다. 원인이 셋이다.

1. **요약 줄은 누적, 본문은 이번 턴 증분.** `statusDiff`(`src/status.ts:30`)의 `settledAfter.length`
   는 누적 전체이고, 바로 아래 목록은 `newLines(before, after)` 로 이번 턴에 새로 생긴 줄만이다
   (`:32`). 같은 낱말 "정리됨"이 한 화면에서 두 뜻으로 쓰인다
2. **`settled` 가 이질적인 것을 한 통에 센다.** 차원 9 + 확인된 답 10 + 완료 기준 2 + 목표 판본 1
   = **22**(`:47-72`, 마지막 턴 실측). 그런데 **차원 아홉이 전부 답으로 닫혔고**(`d1`←`a1` … `d9`←`a6`)
   그 답은 여섯이다 — `a5` 혼자 네 차원을 닫았다. 즉 22줄 중 여섯 줄이 이미 차원 줄로 세어진
   것을 한 번 더 센다. 확정된 사실은 아홉인데 화면은 22라고 말한다
3. **완료 기준은 쓰이는 순간 무조건 정리됨이다.** `settled` 가 `criteria` 를 조건 없이 다 넣는다
   (`:62-65`). 그래서 **`close` 가 아직 거부할 `hard` 기준**(예시도 규칙도 없는 것,
   `src/intent.ts:288-308`)까지 정리됨에 앉는다. `goal` 도 판본마다 한 줄
   (`:67-69`)이라 **F-2 의 판본 누적이 그대로 정리됨 개수 부풀리기가 된다**

> **초안이 여기 든 예시가 틀렸다**(착수 전 검토). `k2` 는 `--type soft` 이고, **soft 는 규칙을
> 못 갖는다** — `src/ledger.ts:614` 가 *"A soft criterion gets no machine rule. It stays a human
> judgement."* 로 거부한다. `k2` 에 규칙이 없는 것은 결함이 아니라 설계다. 주장을 세우는 예시는
> **규칙도 예시도 아직 없는 `hard` 기준**이어야 한다.

**넷째 원인이 하나 더 있다 — `open` 쪽이고, D-3 이 만든 것이다**(착수 전 검토). `open()` 은
**미확인 답을 전부 낸다**(`src/status.ts:90-97`). 그런데 D-3 이 승인을 끝으로 몰았으므로, 인터뷰
내내 **답 하나당 열림 한 줄이 확정적으로 쌓인다.** 실측 `열림 13` 의 대부분이 답 10건이다.
**요약 줄의 두 숫자가 진척과 반대로 움직인다** — 답을 많이 받을수록 열림이 는다. 셈 단위를 다시
잡을 때 이것을 같이 봐야 한다.

**깨질 시험**: `verify/fixtures/D-2.test.ts:43-44` 가 요약 줄에 `"settled 0"`·`"open 3"` 을
정확히 요구한다.

**고칠 방향**: 요약 줄과 본문의 기준을 하나로 맞추고(요약 줄이 `이번 턴 +N · 누적 M` 을 같이 낸다),
정리됨을 **사실 단위**로 센다 — 차원 하나가 하나이고, 그것을 닫은 답은 그 차원 밑에 붙는다.

**크기**: 소~중

---

### F-8. 뒤집힌 답이 뒤집혔다는 표시 없이 정리됨에 앉아 있다 — 표시가 아니라 사실이 틀렸다

`a7 (가)` 는 `a8` 이 뒤집었고(`--overturns d4`), `a9 3` 은 `a10` 이 뒤집었다(`--overturns d7`).
그런데 둘 다 나중에 의도 확인(`r11`·`r13`)을 받았고 그것이 `accepted` 로 확인되면서
`confirmed=true` 가 됐다(원장 실측). `settled` 는 `answer.confirmed` 만 본다(`src/status.ts:56-60`).
결과: 화면이 `a7 (가)` 와 `a8 (나)로 할게`, `a9 3` 과 `a10 아니. 1로 할게` 를 **나란히 정리됨으로
낸다.** 어느 쪽이 살아 있는지 화면에 없다.

`confirmed` 가 뜻하는 것은 *"이 답에 대한 내 읽기를 사용자가 받아들였다"* 이지 *"이 답이 살아
있다"* 가 아니다. `r11` 의 본문이 바로 *"처음 낸 판단이고 곧 뒤집혔다"* 인데, 그 문장은
`current reading` 에서 60자로 잘려 나가고 `settled` 에는 원문 `(가)` 만 남는다. **의도 확인이 정반대로
접힌다.**

**드라이버는 이걸 손으로 메웠다** — 확인표 7번 줄에 *"처음 낸 판단이고 곧 뒤집힘. 살아 있는 결정은
(나)"*, 9→10 줄에 `3 → 아니. 1로 할게`. 도구가 못 그리는 것을 **모델 재량이 매번 새로 그리고
있다.** 그리는 방식은 턴마다 달라질 수 있고, 안 그려도 아무것도 안 막는다.

**고칠 방향**: 답을 질문별로 묶고 뒤집힌 판은 살아 있는 판 밑에 접어 화살표 한 줄로 낸다.
"의도의 변화가 눈에 보여야 한다"의 최소 형태가 이것이다.

> **어느 값으로 그리는지가 초안에서 틀렸다**(착수 전 검토). 초안은 *"`overturns` 는 이미 상태에
> 있다(`src/interview-state.ts:59`)"* 라고 적었는데, `AnswerState.overturns` 가 담는 것은
> **차원 id** 다 — `src/ledger.ts:304-308` 이 `requireDimension` 으로 검사한다. 실측도 그렇다:
> `a8.overturns = "d4"`, `a10.overturns = "d7"`. 그것으로는 *"a8 이 a7 을 뒤집었다"* 를 못 그린다.
>
> **살아 있는 판을 고르는 키는 `answer.question` + 기록 순서**다. 실측으로 `a7`·`a8` 이 둘 다
> `q8`, `a9`·`a10` 이 둘 다 `q9` 다. `AnswerState` 에 `question` 과 `index` 가 이미 있으므로
> (`src/interview-state.ts:274-281`) **새 기록은 필요 없다.** 인용만 고치면 그대로 간다.

**크기**: 소 — **이 문서에서 가장 값싸다**

---

### F-9. 상태가 인터뷰의 다섯 가지 일 중 둘 반만 그린다 — 그리고 상세 보기랄 것이 없다

**먼저 상세 보기.** `showStatus`(`src/ledger.ts:41`)는 `statusBlock` 을, 상시 블록은 `statusDiff`
를 부른다(`:88`). 둘은 **같은 세 축**(`settled`·`open`·`current reading`)을 누적으로 내느냐
증분으로 내느냐만 다르다. 상세 보기가 부실한 게 아니라 **상세랄 것이 없다** — 줄 수만 늘어난다.

**다음이 더 무겁다.** `status.ts` 가 읽는 상태 필드는 24개 중 11개다. 안 읽는 것 중 내용을 담은
것이 아홉이다 — `fragments`·`questions`·`reviews`·`goalReviews`·`interpretations`·`challenges`·
`examples`·`contradictionPassIndexes`·`readiness`.

**여기서 세는 기준을 못박는다** — *"그 레코드가 적은 글자가 화면에 실리는가"* 다. 이 기준으로
`e8e7e3e7` 원장의 kind 21종 중 **12종이 한 글자도 안 나온다**(`start`·`closed` 메타 둘을 빼면
**10종**): `fragment`·`question`·`review`·`goal-review`·`challenge`·`example`·
`contradiction-pass`·`contradiction-resolved`·`set-aside`·`confirm`. 뒤의 셋은 **다른 줄을
움직이기만 한다** — `confirm` 은 답을 `settled` 로 옮기고, `set-aside` 는 `remark` 를 `open` 에서
빼고, `contradiction-resolved` 는 모순 줄을 지운다. **자기가 무엇을 했는지는 어디에도 안 적힌다.**
(초안은 이 셋 중 `confirm`·`set-aside` 만 "간접적으로 나온 것"으로 쳐서 10종/8종이라고 적었는데,
같은 기준을 `contradiction-resolved` 에는 안 썼다. 기준이 일관되지 않아 엄격한 쪽으로 통일했다.)

인터뷰가 하는 일에 대 보면 이렇다.

| 인터뷰가 하는 일 | 원장의 자리 | 지금 화면 |
| --- | --- | --- |
| ① 한 말의 의도를 확정 | fragment → dimension → question → answer → restate → confirm | **절반.** 답 원문과 60자로 잘린 의도 확인 |
| ② 모호성 해소 | ambiguity → interpretation → materiality | **미해소만.** 해소되면 사라진다 |
| ③ 잠재된 의미 발굴 | fragment · dimension | **없음.** fragment 는 아예 안 나온다 |
| ④ 진위 여부 파악 | challenge · contradiction → resolved · review · goal-review | **없음.** 넷 다 안 나온다 |
| ⑤ 놓친 것 보충 | remark → goalCovers / set-aside | **부정형만** — "목표 술어에 없다" |

실측으로 보면 ③④의 공백이 이렇다. 사용자 원 요청의 다섯 조각 `f1 시스템 프롬프트` ·
`f2 프로젝트 프롬프트` · `f3 hooks` · `f4 등을` · `f5 줄이려고 해` 가 상태 어디에도 안 나온다.
코드 근거로 사용자 답을 반박한 `ch1`·`ch2` 도, 모순 `c1`·`c2`·`c3` 도 마찬가지다 — **셋 다
해소돼서 `open` 에서 빠졌고, 해소됐다는 사실을 적는 자리가 없다.** 블라인드 검토 9건도 안 보인다.
`readinessLine`(`src/interview-state.ts:527`)은 만들어져 있는데 `intent.ts` 에서만 쓰이고 상태
출력에는 안 들어간다 — **진척의 유일한 기계 판정이 정작 상태에 없다.**

**갈래 셋.**

| | 무엇 | 값 |
| --- | --- | --- |
| ⓐ | 세 축을 두고 빠진 kind 를 각 축에 끼워 넣는다 | 작다. 그러나 ③④가 여전히 "정리됨/열림" 중 어디에도 안 맞는다 |
| **ⓑ** | **축을 위 다섯으로 다시 잡는다.** 상시 블록은 축별 한 줄 진척 + 이번 턴에 움직인 축만, 상세 보기는 축별 전문(잘림 없음) | 상시/상세가 비로소 다른 것이 된다 |
| ⓒ | 상세 보기를 원 요청 조각(fragment) 기준 트리로 낸다 — "내 말의 이 조각이 어느 차원·질문·답으로 갔나" | ③이 그림으로 선다 |

**ⓑ 를 추천하고 ⓒ 를 그 위에 얹는다.** 둘은 겹치지 않는다 — ⓑ 는 무엇을 세느냐, ⓒ 는 어떻게
배열하느냐다.

> **비용이 초안에 안 적혀 있었다**(착수 전 검토). `verify/fixtures/G-3.test.ts` 둘과
> `D-2.test.ts:43`·`:158` 이 **칸 이름**(`settled this turn`·`opened this turn`·
> `reading updated this turn`·`still open`)과 요약 줄 문안에 **직접 걸려 있다.** 다섯 축으로 갈면
> 넷을 다시 써야 한다. 정당한 비용이지만, **F-9 는 이 문서에서 유일하게 "고칠 것"이 아니라
> "다시 설계할 것"이다** — F-1~F-6 과 같은 배치에 넣지 않는다.
>
> **ⓒ 는 지금 장부로는 끝까지 못 그린다.** 조각→질문 연결은 `question --covers` 하나뿐이고
> (`src/ledger.ts:265-270`), **조각→해소를 잇는 것이 없다.** ⓒ 로 그릴 수 있는 것은 "이 조각이
> 이 질문에 걸렸다"까지다. 그 이상은 새 기록이 필요하고 이 문서 범위 밖이다.

**크기**: 중~대 · **이 문서에서 가장 크다**

---

### F-10. 최종 확인이 인터뷰 덤프다 — 그런데 도구는 그 자리에 아무것도 안 준다

`SKILL.md:247-252` 가 지시하는 것은 *"모든 답의 최신 의도 확인을 한 응답에 다 보이고 틀린 게 있냐고
물어라"* 이고, 그 재료로 `deep-interview status` 의 `current reading` 을 가리킨다. 그런데 그 줄은
`a1 ← r1 <60자>` 다. **이번 세션 의도 확인 13건은 평균 553자·최대 678자이고 13건 전부가 60자에서
잘린다 — 잘려 나가는 글자가 6,416자, 판정 대상의 89%다.** 맞는지 틀린지 판정해야 할 내용의
대부분이 화면에 없다.

그래서 드라이버가 원장을 직접 읽어 **손으로 표를 만들었다**. 그 표는 지금 도구가 내는 어떤 것보다
낫다 — `하신 말` / `제가 읽은 것` 두 칸이고, 뒤집힘을 `3 → 아니. 1로 할게` 로 그렸다. 그리고
**사용자는 답 열 개(표로는 아홉 행)에 `"맞아"` 한 낱말로 답했다.**

두 가지가 동시에 사실이다. ① 확인표의 모양이 도구가 아니라 모델 재량이라 매번 새로 만들어진다
② 줄마다 짚을 손잡이가 없어 통째로 승인된다. 그 위의 요약 줄 `정리됨 8 · 열림 13` 은 표 열 줄과
아무 관계가 없다(F-7).

> **먼저 F-12 를 고쳐야 이 항목에 착수할 수 있다**(착수 전 검토). 지금 장부의 의도 확인 13건은
> **사용자가 본 적 없는 영어 산문**이다. 그대로 확인표를 도구가 내게 하면, 사용자는 자기가 본 적
> 없는 글을 승인하게 된다. **F-12 를 고치면 확인표의 내용은 저절로 사용자 언어가 되고**, 영어로
> 남는 것은 열 제목과 화살표뿐이다 — §4-7 의 갈림도 거기서 거의 사라진다.

**고칠 방향**: 확인표를 **도구가 낸다.** 잘림 없이, 질문별로 묶고, 뒤집힌 판은 화살표로 접고,
줄마다 번호와 의도 확인 id 를 달아 `"3번 틀렸다"` 로 개별 반박이 되게 한다. `status` 의 한 절이냐
별도 하위 명령이냐는 F-9 ⓑ 를 정하고 나서 갈린다.

**크기**: 중 · **F-9 하위**

---

### F-11. 의도 확인이 묻는 형식이라 응답에 물음표가 둘이 된다

`SKILL.md:215` 의 본보기가 **물음으로 끝난다** — *"So for instance, if \<concrete situation\>, then
\<this\> happens — **is that the idea?**"*. 의도 확인은 사용자 답을 에이전트가 **이해한 대로 펴서
보여 주는 것**인데, 본보기가 그것을 확인 질문으로 만들어 놓았다.

실제 출력이 그래서 갈렸다. 이번 세션에서 사용자에게 나간 의도 확인 **9건 중 2건이
`이렇게 보는 게 맞나요?` 로 끝났고 나머지 7건은 평서문**이다(대화 원문). 형식이 턴마다 다르다.

**혼란의 기전은 물음표가 둘이라는 것이다.** `SKILL.md:174-177` 은 그 턴의 진짜 질문이 응답 **맨
아래**에 오게 못박는다. 의도 확인이 `맞나요?` 로 끝나면 그 위에 질문이 하나 더 생겨서, 사용자는
**무엇에 답해야 하는지 고르는 일부터** 해야 한다.

**지시문 안에서 이미 어긋나 있다.** 셋이 서로 맞지 않는다.

| 자리 | 무엇을 말하나 |
| --- | --- |
| `:215` | 의도 확인을 `is that the idea?` 로 끝내라 |
| `:217-219` | *"여기서 '네'를 기다리지 마라."* 의도 확인은 다음 질문과 같은 응답에 실어 보내고 진행한다 — 답마다 승인을 받으면 사용자 턴이 내용 대신 승인에 쓰인다(앞 세션에서 왕복 11회) |
| `:380` | *"답을 유도하지 마라. `So you want X, right?` 는 질문이 아니라 제안이다"* |

`:215` 는 `:380` 이 금지한 꼬리를 그대로 붙이라 하고, `:217` 이 안 받겠다는 "네"를 되묻는다.
**D-2·D-3 이 줄인 왕복을 의도 확인 쪽에서 도로 부른다.**

**고칠 방향**: `:215` 의 본보기를 평서문으로 바꾸고, **의도 확인은 물음표로 끝내지 않는다**를 한 줄로
못박는다. 반박은 물음이 아니라 **조건문으로 연다** — 같은 세션에 이미 좋은 본보기가 있다:
*"유지 쪽이 C가 아니라 B였다면 지금 알려 주세요."* 형식은 `설명 → 구체적 사례 → 틀렸을 때의
조건문` 이고, 응답 안의 물음표는 맨 아래 질문 하나뿐이 된다.

**F-5 와 같은 파일이다**(`skills/deep-interview/SKILL.md`). 한 번에 고친다. **다만 손댈 때 걸리는
것이 하나 있다** — `verify/fixtures/D-3-confirm.test.ts:25` 가 `:217-218` 을 **줄바꿈 위치까지**
잡는다(`skill.includes("in the same response as the next\nquestion")`). `:215` 를 고치다 문단이
다시 흐르면 그 시험이 깨진다. 문단을 건드릴 거면 시험도 같이 고친다.

**낱말도 함께 고친다.** `restate` 를 사용자에게 옮길 때 쓰던 **`되말하기` 를 버리고 `의도 확인` 을
쓴다**(사용자 지시, 2026-07-31). 뜻이 틀린 말은 아니지만 이질적이다 — 그리고 이건 **두 번째로
지목된 것**이다. `INTERVIEW-DEFECTS.md:454` 가 이미 `되말(이)` 를 *"과도한 한글화 … 셋 중 가장
실제 병이 짙은 항목"* 으로 적어 뒀는데 그대로 살아남았다. 이 문서는 다 바꿨고, 한 자리만 남겼다 —
`§0` 의 `q12` 판정 사유 인용(`:45`)은 **원장에 그 낱말로 적힌 문장의 직접 인용**이라 손대면 인용이
아니게 된다.

**같이 정한 것**: `confirm` 은 `승인` 으로 적는다. `의도 확인`(에이전트가 이해한 것을 펴 보이는
것)과 `확인`(사용자가 그 읽기를 받아들이는 것)이 한 화면에서 같은 낱말이 되면 F-11 이 고치려는
혼란이 낱말 쪽에서 되살아난다.

**`INTERVIEW-DEFECTS.md` 도 소급해 고쳤다**(사용자 지시). 용어로 쓰인 15자리를 바꾸고, 인용문과
**낱말 자체가 셈의 대상인 열 자리**는 남겼다 — 그 문서 머리말에 무엇을 남겼고 왜인지 적어 뒀다.
`contract/`·`gate-a/` 는 `verbatim` 계약 문안이라 손대지 않는다.

> **낱말을 지시문에 못박을 때 걸리는 것**: `SKILL.md` 는 영어라 `되말하기` 가 아예 안 나온다 —
> 그 말은 드라이버가 그때그때 지어낸 것이다. 그래서 고칠 자리는 **한국어 호칭을 지시문에 정해
> 주는 것** 하나뿐인데, *"`되말` 을 쓰지 마라"* 라고는 못 쓴다.
> `verify/fixtures/G-5.test.ts:16-29` 가 **배송물(`SKILL.md` 포함) 전체에서 `되말` 이라는 글자를
> 금지**하기 때문이다. 금지어를 적으려면 금지어를 써야 하는 자리라, **긍정형으로만** 쓸 수 있다 —
> *"Korean readers: call this 의도 확인"*. 문안을 미리 정하고 들어간다.

**크기**: 소

---

### F-12. 장부에 남은 의도 확인이 **사용자가 본 문장이 아니다** — 새로 드러났고, 가장 무겁다

착수 전 설계 검토에서 나왔다. 원본으로 확인했다.

| 세션 | `question` 중 한국어 | `restate` 중 한국어 |
| --- | --- | --- |
| `b63faf03` | 9 / 9 | **12 / 12** |
| `cc9ed860` | 14 / 14 | **22 / 22** |
| **`e8e7e3e7`** (이번 판) | 9 / 9 | **3 / 13** |

질문은 한국어 그대로인데 **의도 확인만 영어로 돌아섰다.** D-1(CLI 는 영어, 사용자에게 쓰는 문장은
드라이버가 옮긴다) 이후에 생긴 회귀다.

**무슨 일이 벌어지나.** 드라이버는 사용자에게 한국어로 냈다 — *"제가 이렇게 읽었습니다 (r1): 이건
한 곳이 아니라 두 곳을 동시에 손대는 일입니다…"*. 장부에 적힌 `r1` 은 *"So the work spans two
places at once, not one…"* 이다. **사용자가 본 그 한국어 문장은 어디에도 저장돼 있지 않다.**

**왜 이것이 다른 것보다 무거운가.**

1. **`question` 에는 바이트 대조 게이트가 있는데**(`src/ledger.ts:280` 의 `sameBytes` — 보여준
   문안과 저장된 문안이 다르면 거부) **`restate` 에는 없다.** 그래서 사용자가 `accepted` 로 승인한
   문장과 잠긴 레코드에 남는 문장이 **다른 언어인 채로** 통과한다. IP-1ⓕ 의 취지가 이 경로에서만
   비어 있다
2. **`F-10` 이 이 위에 서 있었다.** 확인표를 도구가 내게 하면, 지금 장부로는 **사용자가 본 적 없는
   영어 산문 13건**을 확인표로 내놓게 된다. F-10 의 "60자로 잘려 판정 대상의 89%가 안 보인다"는
   그대로 참이지만, **잘리는 그 글부터가 사용자가 본 적 없는 글이다**
3. `F-9`·`F-11` 도 같은 뿌리를 공유한다 — 상태에 담긴 "현재 이해"가 사용자 언어가 아니다
4. `INTERVIEW-DEFECTS.md:41` 이 D-1 착지 때 *"모델을 한 번 거쳐 원문 대조가 약해진다는 것은
   상태 블록·거부 사유에만 해당하고, 그 둘은 원문이 아니라 파생물이다"* 로 범위를 그었다.
   **`restate` 는 파생물이 아니다.** 범위가 잘못 그어졌고 실측이 그것을 보여준다

**고칠 방향**: `SKILL.md` 에 *"`restate --text` 는 사용자에게 보낸 그 문장 그대로"* 를 못박는다.
**코드 0줄.** 더 단단하게 가려면 `question` 과 같은 바이트 대조를 `restate` 에도 다는 것인데,
그건 별건이다 — 먼저 지시문으로 막고 3회차에서 재 본다.

**크기**: 소 (지시문 한 줄) · **F-9·F-10·F-11 이 전부 여기에 걸려 있다**

---

## 2. 안 고치는 것 (이번 범위 밖으로 명시)

- **`goalCovers` 가 모든 `goal` 기록의 합집합인 것.** 나중 판이 앞선 판의 내용을 빼도 기계가 못 본다
  (`src/interview-state.ts:382-391` 이 `goal` 마다 `covers` 를 같은 `Set` 에 더한다).
  `a609e3b` 에서 알면서 남긴 것이고 `GOAL.md` §8 이 긋는 선과 같다. **F-2 와 함께 봐야 한다** —
  `show` 를 마지막 판만 내는 쪽으로 고치면 커버리지도 마지막 판 기준으로 좁힐 수 있다
- **`S-12`.** 러너에 남는 유일한 비통과 항목이다. **초안의 서술이 낡았다**(착수 전 검토) —
  `59/60` 이라는 숫자는 `5337e34` 가 점수판을 걷어내면서 없어졌고, 근거로 든 `STATE.md` 도
  `c77bc85` 에서 지워졌다. 러너는 이것을 `FAIL` 이 아니라 **`n/a`** 로 낸다
  (`verify/checks/S-12.ts:8` 의 `targets: []` → `verify/run.ts:93`). 이번 실증과 무관하다
- **의도 확인 · 배경 설명의 블라인드 검토.** `1ac3424` 에서 의도적으로 뺐다(호출이 두 배가 되고,
  둘 다 같은 턴에 사용자에게 그대로 나가 바로 반박할 수 있다)

---

## 3. 못 잰 것을 재기 위한 3회차 설계

수정과 별개로, **어휘 관문 B 와 D-5 는 이번 판으로 못 쟀다.** 이유는 같다 — 이번 인터뷰가 안 다퉜다.
여기에 이번 재검에서 새로 드러난 못 잰 것 둘을 더한다.

- **일부러 다투는 요청으로 돌린다.** 사용자가 반박·방향 수정을 자주 내면 `remark` 가 쌓여 D-5 에
  하중이 걸리고, 드라이버가 낯선 말을 들여올 여지도 생긴다
- **훅 셋 중 하나는 한 번도 안 불렸다.** `PreToolUse`(매처 `Skill`) 관찰 기록이 **세 세션 전부
  0건**이다. `GOAL.md` IP-2ⓚ 는 활성 표식이 *"모델 재량 밖의 두 자리"* 에서 서야 한다고 못박는데
  실증된 것은 `UserPromptExpansion` 한쪽뿐이고, IP-2ⓜ(원문 없이 시작된 세션의 `close` 거부)도
  같은 이유로 한 번도 안 걸렸다. **3회차 중 한 판은 슬래시 명령이 아니라 Skill 도구로 열어야 한다**
- **거부 사유가 사실인지도 잰다.** 지금 D-6 은 판정 사유에 어휘가 나왔는지만 세고 사유가 맞는지는
  안 본다. 이번 판에서 첫 `goal-review` 거부의 부차 사유 *"UserPromptExpansion 훅이 프롬프트를
  확장한다"* 는 **틀렸다** — 그 처리기는 `ok()`(기본값 `""`)를 돌려주고 아무 글도 안 낸다
  (`src/hooks.ts:71`, `src/result.ts:7`). 주 사유(자기모순)가 옳아서 거부 자체는 정당했지만,
  사유가 틀린 거부는 다음번엔 엉뚱한 걸 고치게 만든다
- **미리 정할 셈**: `remark` 개수 · `close` 거부 횟수와 사유 · 관문 A/B 거부 각각의 건수 ·
  **거부 사유 중 사실이 틀린 것의 수** · 사람이 친 발화 수 · `PreToolUse` 관찰 건수.
  **앞 두 판을 같은 방법으로 다시 세어 표를 셋으로 만든다**
- **주의 셋**: ① 대화 원문 없이는 왕복을 못 센다. 원장의 턴 구조만 보면 서브에이전트 알림이 사용자
  발화로 보인다 — 이번에 실제로 그렇게 잘못 읽을 뻔했다 ② **발화를 셀 때 구간을 먼저 못박는다.**
  세션 파일 전체와 인터뷰 구간은 다르고, 초안이 그 둘을 섞어서 개선폭을 부풀렸다 ③ **무엇을 발화로
  치는지도 먼저 적는다.** 슬래시 명령 턴을 넣느냐 빼느냐로 개선폭이 1.7배와 1.4배로 갈린다(§0).
  이번 문서는 규칙을 안 적고 세서 `F-1` 과 어긋났다

---

## 4. 미해결 질문

착수 전 검토가 **셋을 지웠다**(2·3·8). 남은 것은 사람이 정해야 하는 것들이다.

1. **F-1 을 ⓒ 로 가도 되는가 — 질문을 다시 세운다.** 갈림은 *"장부에 조회 기록이 섞이는가"* 가
   아니다. **"Stop 훅을 만족시키는 비용 0 · 흔적 0 의 경로를 열 것인가"** 다. 다른 우회는 전부 다른
   게이트에 걸리는데 `status-view` 만 안 걸린다(F-1 의 검토 상자). 열되 **보이게** 할 것인가,
   아니면 ⓐ·ⓑ 로 돌 것인가.
2. ~~판본인가 항목인가.~~ **코드가 답했다 — 항목이다.** `src/check.ts:110`·`:63`·`:263` 이
   인덱스로 증거를 요구한다. 판본 쓰임은 IP-4 를 깬다. → **F-2ⓓ**
3. ~~순서.~~ **정했다 — §5.** 1~4 를 하고 3회차를 돌린다.
4. ~~사용자가 더할 항목.~~ **들어왔다 — F-7~F-11.**
5. **F-9 의 축을 다섯으로 다시 잡을 것인가(ⓑ), 세 축에 끼워 넣을 것인가(ⓐ).** 나머지가 여기에
   달렸다 — F-7 의 셈 단위도 F-10 의 확인표 모양도 이걸 정하고 나서 갈린다. **고를 때 알아야 할
   사실 하나**: ⓒ 는 지금 장부로 "조각→질문"까지만 그릴 수 있다. 조각→해소를 잇는 기록이 없다.
6. **상시 블록의 예산.** D-2 가 30줄짜리 누적 블록을 잘라 내서 지금 모양이 됐다. 다섯 축을 그리면
   다시 늘어난다. 상시 블록은 몇 줄까지 허용하는가 — 아니면 축별로 한 줄씩 다섯 줄인가.
   **먼저 정리할 것**: `open()` 이 답 하나당 최소 한 줄을 내므로(F-7 의 넷째 원인) 답이 늘면 예산을
   어떻게 잡아도 넘친다.
7. **확인표를 도구가 내면 그 출력도 영어인가** — **F-12 에 딸린 질문이다.** 지금은 고를 처지가
   아니다: 장부의 의도 확인이 영어이고 사용자가 본 한국어는 **아무 데도 저장돼 있지 않다.**
   F-12 를 고치면 확인표의 **내용은 전부 사용자 언어로 자동 해결되고** 영어로 남는 것은 열 제목과
   화살표뿐이다.
8. ~~`되말하기` 를 지난 회차 기록에도 소급해 고칠 것인가.~~ **고쳤다** — `INTERVIEW-DEFECTS.md`
   용어 15자리. 인용문과 낱말 자체를 센 열 자리는 남겼다(그 문서 머리말에 근거).
9. **이제 계약은 어디에 있는가 — 착수 전에 답해야 한다.** 아래 §5 를 보라.

---

## 5. 착수 전 검토가 남긴 것

### 5-1. 이 문서가 근거로 드는 불변식이 트리에 없다 — **착수를 막는다**

이 문서는 `IP-1ⓕ`·`IP-2ⓙⓚⓛⓜ`·`IP-3`·`§8`·`§9` 를 판정 근거로 든다. **`GOAL.md` 는
`c77bc85` 에서 지워졌다.** 그래서 F-1ⓑ 의 *"IP-2ⓙ 를 약화시킨다 → §9 를 밟는다"* 는 **밟을 절차가
없는 조건**이고, F-1 각주의 *"착수 전에 IP-2ⓙ·ⓛ 원문을 다시 대본다"* 는 트리에 없는 파일을 대보겠다는
말이다. (이 문서를 쓰면서 실제로 `git show c77bc85^:GOAL.md` 로 꺼내 읽었다. 그건 이력 고고학이지
계약이 아니다.)

**이미 같은 이유로 코드가 한 번 바뀌었다.** `5337e34` 커밋 메시지 원문 —
*"59/60 이라는 숫자는 GOAL.md 가 '60개를 다 채워야 끝'이라고 정했기 때문에 뜻이 있었다.
그 문서가 사라진 지금 잴 근거가 없다."*

**지금 살아 있는 불변식 인코딩은 `verify/fixtures/IP-*.test.ts` 와 `verify/checks/*` 뿐이다.**
`contract/`·`gate-a/` 는 **어떤 코드도 읽지 않는다** — `src`·`verify`·`bin`·`hooks` 전체에서 참조
0건(직접 확인). 게다가 `contract/criteria.json` 은 `goal_state`·`firedTurnCount` 처럼 **지금
`src/` 에 없는 설계**를 서술한다. 즉 이 저장소에서 `contract/` 를 "고정된 계약 문안"이라 부르며
비켜 온 것은 **보존이 아니라 방치**에 가깝다.

**착수 전에 하나를 정한다**: ① *"이제 계약은 `verify/` 다"* 를 명시하고 `contract/`·`gate-a/` 의
지위를 기록으로 내린다 · ② `GOAL.md` 를 되살린다. **그 전에는 이 문서의 "§9 필요/불필요" 칸이
판정할 근거가 없다.**

> 앞 문서가 세 번 "§9 가 필요할 것"이라 적고 세 번 다 틀렸다고 이 문서가 경고했다. 이제는 **틀릴
> 근거조차 없다** — 그것이 더 나쁘다.

### 5-2. 착수 순서

프롬프트만 고치는 것과 코드를 고치는 것을 섞지 않는다.

| # | 무엇 | 왜 이 자리 |
| --- | --- | --- |
| 1 | **F-5 + F-11 + F-6 + F-12** | `SKILL.md` 한 파일, 코드 0줄. `D-3-confirm.test.ts:25` 의 줄바꿈 의존과 `G-5` 의 낱말 금지를 같이 처리 |
| 2 | **F-3 + F-4** | 관찰 기록을 먼저 정직하게 만든다. 이게 먼저여야 뒤 항목의 효과를 3회차에서 **셀 수 있다** |
| 3 | **F-1ⓒ** (+ 잔여 가시화 + `S-3` 목록에 `status` 추가) | F-4 가 먼저 들어가야 "정말로 안 막혔다"를 관찰 기록으로 증명한다 |
| 4 | **3회차 실증** | §4-3 의 답. 1~3 은 인터뷰의 **모양을 안 바꾸면서** §3 이 재겠다는 여섯 항목을 실제로 셀 수 있게 만든다 |
| 5 | **F-2ⓓ** | 코드 층, 3회차와 독립 |
| 6 | **F-8** | 가장 값싸고 F-7 의 셈 단위를 안 건드린다 |
| 7 | **F-9ⓑ → F-7 → F-10** | 한 덩어리. 축이 정해지기 전엔 나머지 둘을 못 정한다 |

**3회차를 4번에 두는 근거**: F-7~F-10 을 먼저 하면 **화면이 바뀌어 앞 두 판과 비교가 불가능해진다.**
그리고 지시문이 자기모순인 채로 돌리면(F-5·F-11) 그 교란이 그대로 반복된다.

### 5-3. 계획서가 못 본 얽힘 넷

- **F-2 ↔ F-7-3 ↔ §2(`goalCovers`)** — 셋이 같은 원인(`goal` 누적)이다. **F-2ⓓ 면 나머지 둘이
  따라 닫힌다.** 지금 문서는 셋을 따로 센다
- **F-1 ↔ F-4** — F-4 없이 F-1 을 고치면 "안 막혔다"를 대화 원문 말고는 못 확인한다.
  F-4 가 스스로 적은 이유가 그것이다
- **F-8 ↔ F-10** — 뒤집힘 접기는 확인표의 부분집합이다. F-8 을 상태에 넣어 두면 F-10 은 그것을
  재배열하는 일이 된다
- **F-12 ↔ F-9·F-10·F-11** — 사용자가 본 문장이 장부에 없으면 셋 다 못 푼다
