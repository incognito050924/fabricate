# STATE — 지금 어디에 서 있는가

> 가변 문서. 자유롭게 고친다.
> **목표를 여기에 다시 쓰지 않는다.** 목표는 `GOAL.md`이고, 그걸 고치려면 `GOAL.md` §9를 밟는다.

---

## 첫 5분 — 새 세션은 여기부터

```
sed '1,8d' GOAL.md | shasum -a 256    # 2a4ba701d13dbf36… 여야 한다 (비준 본문 무결, 개정 5)
git log --oneline -6                   # 최근 흐름
git branch --list bootstrap/0a         # 비어 있으면 아직 A1 전이다 (A1 이 checkout -b 로 만든다)
ls verify bin drive.sh 2>/dev/null     # 아래 표대로 세션이 갈린다
```

**세션 판별 — 이 표가 정본이다. 다른 어떤 서술보다 우선한다.**

| `bootstrap/0a` | `verify`·`bin` | `drive.sh` | 당신은 | 먼저 할 것 |
| --- | --- | --- | --- | --- |
| 없음 | 없음 | 없음 | **세션 A1** | `git checkout -b bootstrap/0a` (**생성이다. 체크아웃이 아니다**) |
| 있음 | 없음 | 없음 | **세션 A1** (중단됐던 것) | `git checkout bootstrap/0a` |
| 있음 | 있음 | 없음 | **세션 A2** | `git checkout bootstrap/0a` |
| 있음 | 있음 | 있음 | **세션 B** | `git checkout bootstrap/0a` |

**`verify`·`bin`이 있는데 `drive.sh`가 없으면 당신은 A2다. 세션 B가 아니다.** 비계 없이 IP를 초록으로 만들기 시작하면 A2가 통째로 건너뛰어진다.

시작 프롬프트는 아래 **"시작 프롬프트"** 절에 세션별로 있다.

**읽는 순서**: `GOAL.md` **전문**(짧다, 요약본을 쓰지 마라) → 이 문서 → 필요하면 `contract/original-request.md`(개정 3까지).

**한 줄 상태**: 목표 비준 + **개정 5까지 반영**(2026-07-27), 범위 확정, 열린 질문 0, **미확인 전부 닫힘**(훅 셋의 실물 페이로드 · 설치 연쇄 · 권한/신뢰 · `codex exec` 샌드박스), **역할 분담 확정 — 코드는 Codex, 프롬프트·검증은 Claude**, **구동 방식 확정 — 브랜치로 작업하고 allowlist는 `.claude/settings.local.json`에 `Bash(bun run:*)`+`Bash(bun test:*)`**, **코드 0줄**, **세션 경계 확정 — 다음 세션(A)은 루프를 짓기만 하고, 그 다음 세션(B)이 그 루프로 fabricate를 짓는다**, **구동 루프는 일회용 비계다(2026-07-28 사용자 결정) — fabricate의 정식 루프로 채택하지 않는다. 아래 "구동 루프는 비계다" 절이 0a 산출물 다섯을 제품/비계로 가른다.**

**다음 세션이 할 일 (2026-07-28 3차 개정으로 갱신)**: **0a가 A1(눈금)·A2(비계) 둘로 갈렸다.** A1은 `bun run verify`와 그것이 보는 세 축을 짓고, A2는 `drive.sh`를 짓는다. 계약은 아래 **"`verify`의 초록 조건과 동결 계약"** 절이 정본이고, 시작 프롬프트는 **"세션 A1용"·"세션 A2용"**이다. **아래 문단은 갱신 전 서술이라 개수·범위가 낡았다 — 근거로만 읽어라.** ~~0a만 한다 — 루프를 만드는 것이 이 세션의 전부다.~~ 무엇을 만들지는 `GOAL.md` §2가, 어떻게 만들지는 아래 **"구현 형상 — 새 세션은 이대로 짓는다"**와 **"fixture 헌법"**이 이미 적어 뒀으니 **방법을 다시 정하지 마라.** 이 세션이 낼 것은 **눈금**이다 — 미결 1·2를 정하고, 부트스트랩 다섯(러너 + **default-FAIL 계약** + **fixture 헌법 검사** + **도달 가능성 검사** 포함)을 짓고, 연기 시험 넷을 통과시켜서 `bun run verify`가 **RED로 정상 동작**하게 만든다. **IP를 초록으로 만들지 않는다.** 초록은 다음 세션(0b)의 일이다. **그리고 짓는 다섯 중 셋만 제품이고 둘은 버릴 비계다** — 아래 **"구동 루프는 비계다"** 절이 그 선을 긋는다. 비계에 과잉 투자하지 마라. **그리고 IP별 fixture도 만들지 않는다**(2026-07-27 갱신) — 좋은 fixture는 설치된 경로를 프로세스로 통과해야 하는데 그 경로가 아직 없다. 아래 **"fixture 헌법 — RED-first만으로는 1차를 막지 못한다"**를 반드시 읽어라. **실측 결과는 설계 제약으로 그대로 받아라 — 다시 논의하지 마라.**

> **루프를 먼저 켜지 마라.** 구동 루프의 종료 조건인 `bun run verify`가 **A1의 산출물**이라, 지금 루프를 켜면 종료 조건이 없는 채로 돈다. 착수 지점은 **세션 A1(눈금)**이고, 그 다음이 **A2(비계)**, 루프를 켜는 것은 **세션 B**다. 정본은 아래 **"`verify`의 초록 조건과 동결 계약"** 절과 **"시작 프롬프트"**의 세 블록이다.
>
> **아래 "0단계는 0a·0b로 갈린다" 절은 A1/A2 분할 이전 서술이라 개수·범위가 낡았다 — 근거로만 읽어라.** 특히 그 절의 *"미결 둘을 정한다"*(미결 1·2는 계획 세션이 닫았다) · *"다섯을 짓는다"*(A1은 넷, `drive.sh`·`fab-verifier`·allowlist는 A2다) · *"연기 시험 넷"*(A2 프롬프트 `[4]`의 여섯이 정본이다)이 그렇다.

---

## 이미 정해진 것 — 다시 논의하지 않는다

전부 `GOAL.md`에 있다. 여기서는 목록만 든다.

- **완료 정의**: IP-0~6 일곱. 그 밖의 어떤 목록도 완료를 정의하지 않는다 (§2)
- **아키텍처**: Claude Code 위의 네 부분 — 스킬 · 에이전트 정의 · 상태 관리자 CLI · **훅 세 사건**(`UserPromptExpansion` · `PreToolUse` 매처 `Skill` · `Stop`). fabricate는 Claude Code를 호출하지 않는다 (§1)
- **층 판별식**: 더 좋은 모델로 개선되면 프롬프트, 모델과 무관하면 코드 (§4-1)
- **도구 사슬**: Bun + TypeScript strict + zod + citty + biome. 코드·주석은 영어, 사용자 문구는 한국어
- **병합 규율**: IP-0~6이 **전부 초록일 때만** `main`에 착지. 중간 커밋은 브랜치에서 자유 (§3)
- **비-목표**: §5. 특히 합의 용어집 · 역방향 언어 기계 검사 · Tier U · B3 · EARS lint · 이중 호스트

## 구현자가 정하는 것 — **대부분 이미 정해져 있다**

`GOAL.md` §7이 명시적으로 구현자에게 맡긴 자리다. **아래 목록은 전부 아래 "구현 형상 — 새 세션은 이대로 짓는다"가 이미 채웠다.** 더 나은 사실을 관측하면 바꿔도 되지만, **취향으로 다시 열지 마라 — 방법을 정하는 단계는 끝났다.**

- 파일 구조 · 모듈 분리 · 내부 스키마 · 알고리즘
- 상태 파일의 형상 (`.fabricate/` 아래 무엇이 어떻게)
- CLI 하위 명령의 정확한 입출력과 종료 코드
- 훅 스크립트의 판정 기준점
- 작업 순서 (아래 "유력 경로"는 권장이지 강제가 아니다)
- 증거 신선도 같은 되돌릴 수 있는 문턱

**멈춰야 하는 것은 넷뿐이다** (`GOAL.md` §7): 원 요청과의 실질 충돌 · 사용자만 정할 제품 의미 · 비가역 위험 · 불변식이나 술어를 약화시켜야만 진행 가능할 때.

---

## 통합 술어 현황 (완료 정의 — `GOAL.md` §2)

| id | 무엇 | 상태 |
| --- | --- | --- |
| IP-0 | 설치 명령 하나로 남의 기계에서 바로 쓸 수 있다 (멱등·설정 보존) | 미착수 |
| IP-1 | 사용자 명령에서 시작해 끝까지 돌아 잠긴 레코드를 만든다 | 미착수 |
| IP-2 | 못 끝낼 때 실제로 막힌다 (`close` 거부 여섯 + 원문 부재 거부 ⓜ + 훅 셋) | 미착수 |
| IP-3 | 새 세션이 레코드를 원문으로 읽고 **시드로 쓴다** | 미착수 |
| IP-4 | 유효 증거는 통과, 없거나 낡거나 남의 것이면 거부 | 미착수 |
| IP-5 | 질문을 자기가 채점하지 않는다 (세션-맹검 위임 기록) | 미착수 |
| IP-6 | 값어치 있는 것만 묻고, 물은 것은 구체화된다 (중대성·도전·예시) | 미착수 |

닫는 명령: **`bun run verify` 하나.** 미구현 — `package.json`에 스크립트가 없다.

---

## 실측으로 닫힌 미확인 넷 (2026-07-27, 격리 프로브 · Claude Code 2.1.220)

착수를 막던 넷이 **전부 닫혔다.** 아래는 추정이 아니라 관측이고, **설계 제약으로 그대로 받는다.** 넷째(인터럽트 시 `Stop`)는 `GOAL.md` §9를 밟아 개정 4로 §8에 반영됐다.

### 1. `UserPromptExpansion`의 실제 형상

- 입력 JSON 키 **11개**: `session_id` · `transcript_path` · `cwd` · `prompt_id` · `permission_mode` · `hook_event_name` · `expansion_type` · `command_name` · `command_args` · `command_source` · `prompt`.
- **사용자가 타이핑한 슬래시 명령에만 발화한다.** 평문 프롬프트에는 뜨지 않는다 — 그래서 "인터뷰 세션"과 일반 대화를 모델 밖에서 가를 수 있다(IP-2ⓚ의 근거 절반).
- **중요 정정(2026-07-27): 모델이 `Skill` 도구로 같은 스킬을 호출하면 UPE는 발화하지 않는다.** 4회 재현, 프로젝트 스킬·플러그인 스킬 양쪽에서 UPE 훅 로그 0개인데 스킬은 실제로 로드·실행됐다. 정적 근거도 일치한다 — UPE 훅 실행 함수의 유일한 호출 사슬이 `processSlashCommand`이고 프롬프트가 `/`로 시작할 때만 돌며, `expansion_type` 열거형이 **`slash_command`·`mcp_prompt` 둘뿐**이라 스킬 도구용 값이 아예 없다. **그래서 UPE 하나로는 IP-2ⓚ가 서지 않는다** → 아래 §5(`PreToolUse`)가 나머지 절반이고, `GOAL.md` 개정 5가 훅을 셋으로 늘렸다.
- 공식 문서가 적은 `command_text`는 **오지 않는다.** 문서를 믿고 그 키를 읽는 훅은 조용히 `undefined`를 잡는다.
- **`command_args`가 사용자 원문을 담는다** — 모델에 닿기 전에 원문을 디스크에 앉힐 수 있다. 이것이 IP-1ⓕ(확정 바인딩)와 `GOAL.md` §4-3(원문 불요약)의 **원문 확보 지점**이다.
- 매처는 **네임스페이스 포함 `command_name`**(`fabricate:deep-interview`)과 비교되고 대소문자를 구분한다 — `GOAL.md` §6-6 참조. 매처를 생략하고 스크립트 안에서 `command_name`을 검사하는 형상이 더 안전하다.
- **UPE에서 차단하면 턴이 통째로 죽는다. 금지.** 여기서는 표식만 남기고 판정은 `Stop`이 한다.

### 2. `Stop`에서 "이번 턴"을 재는 법

- `Stop` 입력에 **`prompt_id`가 오고 한 턴 안에서 불변**이다 → **턴 경계는 `prompt_id`다.** IP-2ⓙ의 "이번 턴에 장부 증가 없음"이 이걸로 선다.
- 문서 예시의 `stop_reason`·`effort`는 **오지 않는다.** `stop_hook_active`는 **실재한다**(재귀 차단 감지용).
- **CLI(Bash 서브프로세스)는 `prompt_id`를 볼 수 없다.** 따라서 **턴 회계는 CLI가 아니라 `Stop` 훅이 소유해야 한다** — CLI는 장부에 쓰고, "이번 턴에 늘었는가"의 판정은 훅이 한다.

### 3. 설치 연쇄 (IP-0)

- **플러그인 마켓플레이스 경로로 동작하고 멱등이다.**
- 슬래시 명령 이름은 **`/<plugin>:<skill-dir>`**다 — `SKILL.md`의 `name:` 프론트매터가 아니다. 디렉터리 이름이 사용자 표면을 정한다.
- **재시작 필요성이 매체별로 갈린다**: `.claude/settings.local.json`의 훅은 **재시작 없이** 반영되고, 플러그인 `hooks/hooks.json`은 **재시작이 필요하다.**

**IP-0에 걸리는 구멍 둘 — 설계에서 피해야 한다.**

| 구멍 | 관측 | 귀결 |
| --- | --- | --- |
| `directory` 소스 | 상대경로로 줘도 **절대경로를 프로젝트 설정에 박는다** | `GOAL.md` §4-10(절대경로 금지) 위반. github 소스나 재실행형 설치로 피한다 |
| 전역 의존 | 프로젝트 스코프 설치가 전역 `known_marketplaces.json`에 의존하는데, **다른 프로젝트에서 지우면 조용히 죽는다** — 설정은 남고 슬래시 명령만 사라지며 종료 코드 0 · stderr 비어 있음 | 설치 성공을 증거로 쓰면 안 된다 |

따라서 **IP-0의 증거는 "설치 명령이 성공했다"가 아니라 "새 프로세스에서 슬래시 명령이 실제로 훅을 발화시켰다"여야 하고, 검사는 설치 → 실행 → 재설치 → 재실행으로 짠다.**

### 4. 인터럽트와 연속 차단 → `GOAL.md` §8 (개정 4)

인터럽트 시 `Stop`은 **발화하지 않고**(`SessionEnd`만 발화), 연속 차단 캡은 **실재하며 기본 8회**(`CLAUDE_CODE_STOP_HOOK_BLOCK_CAP`)다. 전문과 남은 한계는 `GOAL.md` §8에 있다.

### 5. `PreToolUse`(매처 `Skill`) — 모델 호출 경로 (2026-07-27 추가 실측)

UPE가 모델 호출 경로를 안 덮는다는 것이 드러나면서 찾은 대체 표식 생성기다. 관측된 실물 입력:

```json
{"session_id":"…","transcript_path":"…","cwd":"…","prompt_id":"741ce9bc-…",
 "permission_mode":"default","hook_event_name":"PreToolUse","tool_name":"Skill",
 "tool_input":{"skill":"fab2:pskill"},"tool_use_id":"toolu_…"}
```

- **`prompt_id`가 온다** → `Stop` 훅의 턴 회계와 **같은 경계를 공유**한다. IP-2ⓙ가 이 경로에서도 그대로 선다.
- `tool_input.skill`이 **네임스페이스 포함**(`fab2:pskill`)이라 UPE의 `command_name`과 **같은 문자열 형식**이다 → 정규화 함수 하나로 두 경로를 처리한다.
- **매처 문법**: `"Skill"`은 매칭되지만 **`"Skill(probeskill)"`은 매칭되지 않는다** — `PreToolUse` 매처는 `tool_name`에 대한 정규식일 뿐이다. 스킬 이름 판별은 **스크립트 안에서 `tool_input.skill`로** 한다(`GOAL.md` §6-6과 같은 교훈).
- **두 표식 생성기는 멱등해야 한다** — 사용자 타이핑 경로에서도 모델이 확장 직후 `Skill` 도구를 한 번 더 부르는 것이 관측됐다. **한 턴에 UPE 1회 + `PreToolUse`(Skill) 1회가 정상 경로**이고, 표식은 하나여야 한다.
- **받아야 할 비대칭**: 이 경로에는 **사용자 원문이 없다.** `tool_input.args`는 모델이 지어낸 것이다. 따라서 모델 호출로 시작한 세션은 **"원문 없음" 상태로 표식만 서고 `close`가 거부한다**(`GOAL.md` IP-2ⓜ, 개정 5로 신설).

### 딸린 발견 — `bun run verify`의 격리 수단이 바뀐다

**`bun run verify`는 `CLAUDE_CONFIG_DIR`로 격리할 수 없다** — 인증을 상속하지 못해 `Not logged in`으로 죽는다. 따라서 verify는 실제 `claude`를 띄우는 대신 **훅 스크립트에 합성 stdin JSON을 먹여 직접 실행**해야 한다. `GOAL.md` §4-9(검증이 사용자 설정을 오염시키지 않는다)는 그대로 유지되고 **격리 수단만 바뀐다.**

### 원시 페이로드

`/private/tmp/claude-501/-Users-incognito-dev-projects-fabricate/905b4d86-1e7e-4a42-99ae-70e99b0989fe/scratchpad/probe/` — 매처 15종 실측 로그, 훅 입력 덤프, 설치·재시작 프로브. **세션 스크래치패드라 휘발성이다.** 위에 옮겨 적은 사실이 정본이고, 원본이 사라져도 재실측 비용은 이 문단이 대신한다.

---

## 헤드리스 구동의 권한·신뢰 제약 (2026-07-27 실측 · Claude Code 2.1.220)

**전부 관측·추출된 사실이다** — 아래 "아직 안 잰 것" 한 항목만 예외다. 이것은 fabricate가 **무엇을 짓는가**가 아니라 fabricate를 **어떤 루프로 짓는가**의 제약이다. 헤드리스(`-p`)로 구현 루프를 돌리려는 순간 전부 걸린다.

### 권한 모드

- `--permission-mode`의 선택지는 여섯이다: `acceptEdits` · `auto` · `bypassPermissions` · `manual` · `dontAsk` · `plan`.
- **`acceptEdits`는 Edit/Write만 덮고 Bash를 안 덮는다.** 헤드리스는 TTY가 없어 물어볼 수 없으므로 그냥 거부된다. 바이너리 문안:
  `Permission to use <tool> has been denied because Claude Code is running in don't ask mode.`
- 따라서 구현 루프가 `bun test` · `bun run verify` · `git`을 돌리려면 **명시적 allowlist가 필수다** — `--allowedTools "Bash(bun run:*)" …` 또는 설정의 `permissions.allow`.

### 권한 실무 사실 (2026-07-27 실측 — 신뢰된 디렉터리에서 실행 관측)

- **`bun`은 내장 안전 목록에 없다.** allowlist 없이도 `git status` · `ls` · `node --version`은 통과하는데 **`bun --version`은 거부된다.** 도구 사슬이 Bun이므로 allowlist는 선택이 아니라 필수다.
- **패턴이 정확히 매칭된다.** `Bash(bun run:*)`를 주면 `bun run`만 열리고 `npm`은 막힌다. **콜론 형식과 공백 형식 둘 다 동작한다.**
- **최소 규칙: `Bash(bun run:*)` + `Bash(bun test:*)`.** `Bash(bun:*)`는 `bun x <임의 코드>`까지 열어 주므로 **피한다.**
- **못 잰 것**: 복합 명령(`bun test && git status`)에서 allowlist가 어떻게 쪼개져 판정되는지. 구동 루프는 당분간 **한 명령씩** 돌려 이 미지를 피한다.
- **`--dangerously-skip-permissions`는 쓰지 않는다.** `--help` 자신이 *"Recommended only for sandboxes with no internet access"*라 적었고, 이 저장소는 샌드박스가 아니라 사용자의 실제 기계다 — `GOAL.md` §7-3(비가역 위험)에 걸린다. 별도의 게이트 플래그 `--allow-dangerously-skip-permissions`(*"Enable bypassing all permission checks"*)가 존재한다는 **사실만** 적어 둔다. 쓰라는 뜻이 아니다.

### 워크스페이스 신뢰 — 이게 더 고약하다

- 미신뢰 디렉터리에서는 프로젝트 설정의 `permissions.allow` 항목이 **조용히 무시된다.** 바이너리 문안:
  `Ignoring <N> entr{y,ies} from .claude/ settings: this workspace has not been trusted. Run Claude Code interactively here once and accept the trust dialog, or set projects[<dir>].hasTrustDialogAccepted: true in <file>`
- **정정(2026-07-27 실측) — 위 문장을 "미신뢰면 `permissions.allow`가 조용히 무시된다"로 일반화하면 너무 넓다.** 실제로 무시되는 것은 **`.claude/settings.json` 하나뿐이고 `.claude/settings.local.json`은 존중된다.** 그리고 **`--allowedTools` 플래그와 `--settings <파일>`은 신뢰 여부와 무관하게 동작한다.** 귀결이 뒤집힌다 — **남의 기계의 미신뢰 체크아웃에서도 구동 루프는 선다.** 아래 "대응"(신뢰된 저장소 디렉터리에서 브랜치)은 여전히 **가장 단순한 길**이라 유지하지만, **유일한 길은 아니다.**
- `--help`가 명시한다: **비대화형(`-p`, 또는 stdout이 TTY가 아닐 때)에서는 신뢰 대화상자가 "건너뛰어질" 뿐 승인되지 않는다.** 같은 문단이 하나 더 적는다 — 이 모드에서는 **검증에 실패한 설정 파일이 오류 없이 조용히 무시된다.** 조용한 실패가 두 겹이다.
- **귀결: 새 git 워크트리를 만들면 그 디렉터리는 미신뢰라 allowlist가 먹지 않는다.** 워크트리 안에서 헤드리스 루프를 돌리면 Bash가 계속 거부되고, 그 거부는 위 문안 한 줄로만 흘러간다.

**대응 — 워크트리 대신 저장소 디렉터리에서 브랜치로 작업한다.**

- ~~`/Users/incognito/dev/projects/fabricate`는 **이미 신뢰돼 있다**~~ **정정 (2026-07-28 실측)**: 저장소가 **`/Users/ecoletree/dev/project/fabricate`로 옮겨졌고, 이 디렉터리는 미신뢰다** — `~/.claude.json`의 `projects` 항목에서 `hasTrustDialogAccepted: **false**` 확인. 즉 "이미 신뢰된 디렉터리라 간단하다"는 전제는 **깨졌다.** 다만 바로 아래 절이 정정한 대로 **`.claude/settings.local.json`은 미신뢰에서도 존중되고 `--allowedTools`는 신뢰와 무관하게 동작하므로** 구동 루프는 여전히 선다. **세션 A의 연기 시험 넷째가 이것을 실측으로 닫는 자리다** — 거기서 Bash가 거부되면 대응은 `--allowedTools` 플래그로 옮기는 것이고, 사용자 전역 설정에 `hasTrustDialogAccepted`를 써 넣는 길은 여전히 쓰지 않는다(§4-9의 정신).
- `GOAL.md` §3은 *"중간 커밋은 브랜치/워크트리에서 자유롭게"*이지 워크트리를 **강제하지 않는다.** 브랜치로 충분하다.
- **사용자 전역 설정(`~/.claude.json`)에 `hasTrustDialogAccepted`를 써 넣는 방식은 쓰지 않는다** — `GOAL.md` §4-9(검증이 사용자 설정을 오염시키지 않는다)의 정신에 어긋난다. 검증이든 구동이든, 편의를 위해 사용자 전역 상태를 건드리지 않는다.
- allowlist는 **`.claude/settings.local.json`**(gitignore 대상)에 **좁게** 둔다. 저장소에 커밋되는 설정에 넣지 않는다 — 남의 기계에서 권한이 저절로 열리는 설치는 IP-0의 "설정 보존"과 정면으로 충돌한다.

### 훅은 권한 시스템 밖이다

- 훅은 호스트가 셸 명령으로 **직접** 실행하므로 도구 권한과 무관하다. 그래서 `verify`가 임시 프로젝트에서 훅 발화를 검사하는 부분은 **미신뢰 디렉터리에서도 동작한다** — 선행 프로브 둘이 전부 스크래치패드 임시 디렉터리에서 훅을 발화시킨 것이 그 증거다.
- 단, 훅은 `disableAllHooks` / `allowManagedHooksOnly` 설정·정책으로 꺼질 수 있다. 훅이 안 도는 환경이 존재한다는 뜻이고, 이는 `GOAL.md` §8의 "봉쇄는 원리적으로 불완전하다"에 한 항목을 더한다.

### `agent` 타입 훅의 별도 함정

- **`--permission-mode bypassPermissions`가 `agent` 타입 훅에 상속되지 않는다.** `permissions.allow` 또는 `--allowedTools`로 명시 허용해야 하고, 그것도 **워크스페이스가 신뢰돼야** 먹는다.
- 놓치면 훅이 "권한 없음"으로 계속 차단하다 연속 차단 캡(기본 8, `GOAL.md` §8)을 태우고 **exit 0으로 조용히** 끝난다. 실패가 성공처럼 보이는 정확한 형상이다.

### 착수 전 실측 대상이었던 둘 — **전부 닫혔다 (2026-07-27)**

- **신뢰된 디렉터리에서 `-p` + allowlist가 실제로 Bash를 통과시키는지** → **닫힘. 통과한다.** 추론이 아니라 실행 관측이고, 부수적으로 위 "권한 실무 사실" 네 항목을 얻었다(`bun`은 안전 목록 밖 · 패턴 정확 매칭 · 콜론/공백 둘 다 · 최소 규칙).
- **`codex exec`가 승인 프롬프트 없이 파일을 쓸 수 있는가** → **닫힘.** `codex exec -s|--sandbox <read-only|workspace-write|danger-full-access>`가 실재한다. **`codex exec -s workspace-write`면 위험 플래그 없이 작업 디렉터리 쓰기가 열린다.** 구동 루프는 이것을 쓴다. **`--dangerously-bypass-approvals-and-sandbox`는 쓰지 않는다** — `GOAL.md` §7-3(비가역 위험).

**남은 미지는 하나뿐이고 회피 가능하다**: 복합 명령에서의 allowlist 분해 규칙(위 참조). 한 명령씩 돌려 피한다.

---

## Codex 운용 방침 (2026-07-27 사용자 결정)

사용자 결정: **"codex를 가능한 적극적으로 사용하라."**

### 범위 구분 — 먼저 못박는다

**`GOAL.md` §5가 배제한 것은 *fabricate가 Codex 위에서도 돌게 짓는 것*(호스트 추상화 계층 · Codex 대응 · 이중 호스트)이지, *Codex를 fabricate를 짓는 도구로 쓰는 것*이 아니다.** 전자는 산출물의 성질이고 후자는 작업 방식이다. 둘을 혼동하면 이 절이 비-목표 위반으로 읽힌다 — 아니다.

### 왜 쓰는가

`GOAL.md` §8의 잔여 *"같은 편향 — 짓는 모델과 검사하는 모델이 같은 계열이다. IP-5는 컨텍스트가 다름만 강제하고 모델 계열이 다름은 강제하지 않는다"*를 **실제로 깨는 유일한 수단**이다. 요구된 것 이상이지 미달이 아니다. IP-5의 술어를 바꾸는 것이 아니라, 술어가 못 잡는 잔여를 운용으로 덮는다. 아래 역할 분담이 **짓는 계열과 검사하는 계열을 실제로 가른다.**

### 환경 사실 (실측)

- ~~`codex-cli 0.145.0`이 `/opt/homebrew/bin/codex`에 있다.~~ **정정 (2026-07-28 실측)**: `codex-cli 0.142.5`이고 경로는 fnm 셸 경로(`~/.local/state/fnm_multishells/…/bin/codex`)다. `/opt/homebrew/bin/codex`는 **없다.** `-s workspace-write`는 이 버전에도 있다.
- 플러그인 `codex@openai-codex` v1.0.3 설치됨(user 스코프).
- 서브에이전트 정의 `codex-rescue`와 스킬 `codex:rescue`가 있다.

### 역할 분담 — **검증은 Claude가, 구현은 Codex가**

fabricate의 호스트가 Claude Code이므로 분담선은 `GOAL.md` §4-1의 판별식(*"더 좋은 모델로 바꿨을 때 개선되면 프롬프트 자리, 모델과 무관하면 코드 자리"*)을 **그대로** 쓴다.

| 자리 | 누가 | 근거 |
| --- | --- | --- |
| **코드** — CLI · 훅 · `verify` · 플러그인 매니페스트 | **Codex** | 모델 무관한 자리다. **측정된 훅 페이로드가 사양이다** — 위 "실측으로 닫힌 미확인 넷"이 그 사양서다 |
| **프롬프트** — `skills/deep-interview/SKILL.md` · `agents/*.md` | **Claude** | 이 산출물의 **독자가 Claude Code다.** 대상 모델을 겨눈 프롬프트는 그 계열이 쓰는 것이 맞다 |
| **검증** | **Claude** | IP-0·IP-2의 증거가 **호스트를 실제로 돌려야** 나온다(훅 발화 · 슬래시 명령 · 플러그인 설치 · 워크스페이스 신뢰). 그리고 짓는 계열과 갈리므로 `GOAL.md` §8의 "같은 편향" 잔여가 깨진다 |

**Codex를 구현자로 두는 것의 이점 — 사전 지식이 없다는 것이 강점이다.**
Claude는 Claude Code 내부를 사전 지식으로 **"안다고 착각"한다.** 실측된 사례가 둘이다 — `GOAL.md`가 훅 매처를 `deep-interview`로 적었으나 실제 발화값은 `fabricate:deep-interview`였고(개정 4로 정정), **공식 문서조차** 오지 않는 `command_text`를 준다고 적었다. 사전 지식이 없는 구현자는 **기억에서 지어낼 수 없어 측정된 페이로드를 읽을 수밖에 없다.**

**반대 위험 — 같은 이유로 구조를 틀릴 수 있다.**
사전 지식이 없다는 것은 플러그인 매니페스트·훅 스키마를 **구조적으로** 잘못 쓸 수 있다는 뜻이다. 완화책은 작업 순서에 있다 — 1단계 걷는 뼈대가 **실제 설치 경로를 즉시 돌리므로** 틀리면 바로 드러난다. 뼈대를 미루면 이 완화가 사라진다.

**유지되는 규율 — 파일을 쓰는 것은 여전히 한 번에 한 에이전트다.** 락이 없어 병렬 작성자는 서로 덮어쓴다. **동시에 쓰지 않고 순차로 넘긴다.**

### 그 밖에 Codex를 쓰는 자리

- **설계·계획의 적대적 검토.** 착수 전 형상, 게이트 배치, 술어 해석.
- **막혔을 때의 2차 진단.**

### 구동 루프의 형상 — **폐기됨 (2026-07-28). 정본은 아래 "0b — 루프" 절의 고친 형상이다**

> 아래 스케치는 결함 다섯을 갖고 있다(`.drive/` 미생성 · 센티널 검사 위치 · `FINDINGS.md` 시드 · codex 상한 부재 · 검증자 권한과 프롬프트 삼킴). **베끼지 마라.** 근거로만 남긴다.

```
until bun run verify; do
  codex exec -s workspace-write "<GOAL.md 전문> … <verify 실패 출력>"   # 구현 (Codex)
  claude -p --allowedTools "Bash(bun run:*)" "Bash(bun test:*)" \
         --agent fab-verifier "…" > .drive/FINDINGS.md                  # 검증 (Claude, 쓰기 도구 없음)
done
```

- **`-s workspace-write`**가 Codex의 쓰기를 연다(위 "착수 전 실측 대상이었던 둘"). 위험 플래그는 쓰지 않는다.
- **검증자에게 쓰기 도구를 주지 않는다** — 검증자가 고칠 수 있으면 그 자리는 더 이상 검증이 아니다.
- **한 명령씩 돌린다** — 복합 명령의 allowlist 분해가 미지다.
- 매 라운드 입력은 **`GOAL.md` 전문 + 직전 `verify` 실패 출력**이다. **직전 라운드의 산출물 요약을 시드로 주지 않는다**(`GOAL.md` §0).

### 주의 — 루프에 곱해지는 15분

- codex 플러그인이 **`timeout: 900`인 `Stop` 훅**(`node "${CLAUDE_PLUGIN_ROOT}/scripts/stop-review-gate-hook.mjs"`)을 등록한다. 구동 루프가 차단을 반복하면 **라운드마다 최대 15분이 곱해진다.**
- 대응: 루프 도는 동안은 `--setting-sources project`로 격리하거나 그 훅을 끈다(`codex:setup`이 토글을 제공한다).
- 인증·과금이 **별도 계정**이다. 무한정 돌리는 자원이 아니다.

---

## 구현 형상 — 새 세션은 이대로 짓는다

**여기까지가 "방법을 정하는" 일이고, 그 일은 끝났다.** 아래는 `GOAL.md` §7이 구현자에게 맡긴 자리를 실측 위에서 미리 채운 것이다. **더 나은 사실을 관측하면 바꿔도 된다 — 취향으로 바꾸지는 마라.**

### 저장소 레이아웃

- **배포 매체는 플러그인**이다(마켓플레이스 경로. `directory` 소스는 절대경로를 박으므로 금지 — 위 IP-0 구멍 표).
- **`bin/fabricate` 단일 뿌리.** CLI 진입점 하나이고 `package.json`의 `bin`이 여기를 가리킨다.
- **훅 셋이 전부 그 뿌리를 호출한다.** 훅 스크립트는 얇은 껍데기이고 판정 로직은 CLI 안에 있다 — `GOAL.md` §6-2(뿌리에서의 도달 가능성)를 구조적으로 만족시키는 가장 싼 길이다.
- **훅 매처는 생략하고 스크립트가 이름을 검사한다.** UPE는 `command_name`, `PreToolUse`는 `tool_input.skill` — 둘 다 네임스페이스 포함 같은 형식이라 **정규화 함수 하나**로 본다(`GOAL.md` §6-6).
- 슬래시 명령 이름은 **`/<plugin>:<skill-dir>`**이므로 스킬 디렉터리 이름이 사용자 표면이다.

### 상태 파일 형상

```
.fabricate/
  sessions/<sid>/
    active          # 표식. UPE 또는 PreToolUse(Skill)가 만든다. 생성은 멱등
    request.txt     # 사용자 원문 바이트. UPE 경로에서만 생긴다 (PreToolUse 경로엔 없다)
    ledger.jsonl    # append-only 턴 장부. CLI만 쓴다
    turnstate.json  # {prev_prompt_id, prev_L}. Stop 훅만 쓴다
  intent/<id>.json  # 잠긴 레코드. close만 쓴다 — 유일한 쓰기 경로 (§6-3)
```

- `request.txt`가 **없으면 `close`가 거부한다**(IP-2ⓜ). 원문 부재는 개시 경로의 성질이지 실수가 아니다.
- `.fabricate/intent/`에 쓰는 코드 경로는 `close` 하나다. **다른 경로가 생기면 §6-3 위반이고 게이트가 없는 것과 같다.**

### 턴 회계 알고리즘 (Stop 훅이 소유)

`Stop` 훅은 `turnstate.json`에 **`{prev_prompt_id, prev_L}`**을 들고 돈다. `L`은 `ledger.jsonl`의 줄 수다.

1. `prompt_id`가 `prev_prompt_id`와 **다르면** 새 턴이다 — 경계를 갱신하고 그 턴의 시작 `L`을 기록한다.
2. 같으면 같은 턴의 재진입이다.
3. **활성 표식이 있는데 이번 턴에 `L`이 안 늘었으면 막고 사유를 돌려준다**(IP-2ⓙ).
4. **`stop_hook_active`가 true면 재차단하지 않는다** — 대신 **위반을 장부에 기록한다.** 연속 차단 캡(기본 8, 넘기면 조용히 버려짐)을 태우지 않기 위해서다. 봉쇄가 여기서 느슨해지는 것은 의도된 것이고 **방어선은 IP-4다**(`GOAL.md` §8).

CLI는 `prompt_id`를 볼 수 없다 — **CLI는 장부에 쓰기만 하고 "이번 턴에 늘었는가"는 훅이 판정한다.**

### 작업 순서 — ~~네 단계~~ **갱신됨 (2026-07-27) — 아래 "작업 순서 (개정)"로 대체**

**아래 표는 근거로 남긴다. 지시로 읽지 마라 — 0단계의 "전 IP fixture를 RED로 먼저"가 틀렸다는 것이 RED-first 전제 검증의 결론이다**(아래 "fixture 헌법" 절). **좋은 fixture는 설치된 경로를 프로세스로 통과해야 하는데, 그 경로가 없으면 쓸 수 없다.**

| 단계 | 무엇 | 왜 이 순서인가 |
| --- | --- | --- |
| ~~**0**~~ | ~~**`verify` 하네스와 IP-0~6 fixture 전부를 RED로 먼저 세운다.** 미구현 항목은 **default-FAIL**이다 — 없는 검사가 초록으로 보이면 안 된다~~ | ~~1차 시도의 실패가 "테스트는 통과하는데 문안은 못 지킨다"였다. **완료 정의를 실행 가능한 형태로 먼저 박아 두면** 이후 모든 라운드가 그 눈금을 본다. `bun run verify`가 처음부터 존재하고 빨갛다~~ |
| **1** | **걷는 뼈대 — 게이트 없이.** 플러그인 설치 → 슬래시 명령 → 훅 셋 발화 → `start`→`turn record`→`close`가 끝까지 뚫린다 | **경로가 있어야 게이트를 그 위에 세운다**(`GOAL.md` §4-6). 동시에 **실제 설치 경로를 즉시 돌리므로** 사전 지식 없는 구현자가 매니페스트·훅 스키마를 구조적으로 틀린 것이 바로 드러난다(위 "반대 위험"의 완화책). **이 단계를 미루면 그 완화가 사라진다** |
| **2** | **`close` 거부 여섯 + ⓜ, 그리고 `Stop` 턴 회계** | 급소다. 뼈대 위에 게이트를 얹는다 |
| **3** | **IP-5 → IP-6 → IP-4 → IP-3** | IP-3(재진입 시드)이 마지막인 이유는 그때쯤 레코드 형상이 굳기 때문이다 |

~~도달 가능성 검사(`GOAL.md` §6-2)는 0단계 하네스에 넣는 것이 가장 이르고 싸다 — 섬을 짓는 즉시 알게 된다. 시점 자체는 `GOAL.md`가 정하지 않는다.~~ → **시점을 0a로 못박았다**(아래 "fixture 헌법" 절).

### 작업 순서 (개정) — 2026-07-27, RED-first 전제 검증 반영

| 단계 | 무엇 |
| --- | --- |
| **0a** | **`verify` 러너 + default-FAIL 계약**(모든 IP 기준이 `false`에서 시작) **+ fixture 헌법 검사**(`verify/` 아래 어떤 파일도 `src/`에서 import하지 않는지 기계가 본다) **+ 도달 가능성 검사**(`GOAL.md` §6-2) **+ 부트스트랩 다섯.** **IP별 fixture는 아직 만들지 않는다** |
| **1** | **걷는 경로 하나** — 임시 프로젝트 설치 → 슬래시 명령·실제 훅 → 공개 CLI `start` / `turn record` / `close` → 디스크의 intent 파일. 즉 **IP-0과 IP-1ⓐ를 먼저 초록으로** |
| **그 다음** | 모든 IP의 양극·음극을 **그 동일한 세션의 입력·장부·설정을 하나씩 변형한 것**으로 만든다 |
| **2** | (기존 그대로) `close` 거부 여섯 + ⓜ, 그리고 `Stop` 턴 회계 |
| **3** | (기존 그대로) IP-5 → IP-6 → IP-4 → IP-3 |

**종료 조건은 여전히 코드보다 먼저 선다** — default-FAIL 계약이 0a에 있으므로 `bun run verify`는 첫날부터 존재하고 빨갛고, 루프는 그대로 성립한다. **바뀐 것은 fixture를 만드는 시점과 방식뿐이다.**

---

## fixture 헌법 — RED-first만으로는 1차를 막지 못한다 (2026-07-27 검증 완료)

**이 절이 RED-first 전제 검증의 산출물이다.** 사실이 먼저 있고 규칙 넷은 거기서 나왔다. **규칙만 읽고 근거를 건너뛰지 마라** — 근거를 모르면 "이 정도면 되겠지"로 규칙이 물러진다.

### 질문과 답

> **`verify`를 전부 RED로 먼저 세우는 것이 1차 시도의 실패를 막는가, 아니면 fixture가 새 "조건 목록"이 되어 그 실패를 재생산하는가?**

**답: RED-first만으로는 막지 못한다. 1차 시도가 이미 RED-first를 했고 실패했다.**

### 실측 — 1차 수용 검사 69개 전수 조사 (`git show 499cd3c^:acceptance/…`)

| | 개수 |
| --- | --- |
| 수용 검사 총 | **69** |
| **"Frozen red" 선언** (구현 전에 빨갛게 세우고 동결, 편집 금지) | **44** |
| **`../src/` 내부 모듈을 직접 import** | **69 — 전부** |
| 실제로 프로세스를 띄우는 것 | **4** |

**게다가 RED-first는 1차가 *장치로* 갖고 있었다.** `src/gate/red-first.ts`가 형식 게이트였고 검사 항목 셋은 — 판정 테스트를 쓴 주체가 루프 자신인가 · 빨간 상태를 실제로 관측한 기록이 있는가 · **동결된 빨간 테스트 내용이 sha256으로 변하지 않았는가**. 감사 실측: **동결 69개 중 통과 69, 거부 0**, `acceptance/`를 건드린 커밋은 동결 커밋 하나뿐, 부정행위 0.

> **RED-first는 100% 작동했고, 그래도 초록 33개에 돌아가는 것은 0개였다.**

즉 RED-first는 1차와 이번을 **구별하지 못한다.** 구별하는 것은 **호출 경계**다.

### 실물 증거 — 테스트와 제품이 서로 다른 함수를 봤다

`acceptance/ac-3.test.ts`가 `../src/interview/finalize`에서 **`finalizeIntent`**를 import해 검사하는데, `src/cli/interview-finalize.ts`가 실제로 부르는 것은 같은 모듈의 **`finalize`**다. **서로 다른 함수다.** 테스트는 아무도 안 부르는 함수를 초록으로 만들었고, 실제 쓰기 경로(`finalize.ts:233` `store.records.push`)는 고아 게이트·확정 게이트를 **우회했다.** `GOAL.md` §3의 *"비-테스트 호출자가 없는 모듈 섬"*의 정체가 이것이다.

### 도달성 실측 — 가장 센 숫자

유일한 CLI 파일(`src/cli/interview-finalize.ts`)에서 정적 import를 따라간 결과: **운영 `.ts` 102개 중 도달 13, 미도달 89.** 미도달 89에 **잠금 · 턴 기록 · 준비도 · 모순 패스 · 전파 — 하네스의 급소 전부**가 들어 있다. 그리고 `package.json`에 **`bin` 없음**, 저장소 전체에 **`import.meta.main` 0건**, 선언된 의존성 `citty`는 **어디서도 import되지 않는다.** 도달한 13개조차 셸에서 부를 수 없었다.

**결정적 아이러니**: `acceptance/ac-40.test.ts`는 아예 진입점을 **금지했다** — *"no runner/cli/main-shaped harness file … no `import.meta.main` / argv entry point"*(`:35-37`, 실검사 `:444-456`). **1차의 완료 정의 69개는 "돌아가는 프로그램"이라는 개념을 한 번도 언급하지 않았다.**

### 대칭 증거로도 못 막는다

1차 검사에 이미 강한 양극·음극이 있었다 — `ac-25.test.ts`는 다른 매핑·빈 매핑·ghost node·open seed·해제 경로를 함께 검사했고, `ac-27.test.ts`는 성공 쓰기와 실패 시 write 0을, `ac-34.test.ts`는 현재 코드 인용과 stale 인용을 대조했다. **그런데도 실패했다** — 검사된 양극·음극이 **실제 사용자 경로의 양극·음극이 아니었기** 때문이다.

### 빈-껍데기 반박의 진짜 한계

1차의 빈-껍데기 반박이 69개 중 43개를 잡았다. 나머지 26개는 "못 잡은" 것이 아니라 `gate-a/review-summary.json`에서 **`honest`로 명시 판정**한 것이다. **그리고 잡아서 고친 43개도 막지 못했다** — `ac-4` · `ac-24` · `ac-37`은 셋 다 `shell_passable → revised:true`였는데 감사가 셋 다 **BROKEN**으로 판정했다.

구조적 이유: 반박자의 시점이 *"테스트만 본다 — 구현이 없으니 편향이 가장 적은 유일한 시점"*이었다. **그 시점은 "누가 이 모듈을 부르는가"를 물을 수 없는 유일한 시점이기도 하다.** 반박이 놓친 것은 단언의 강도가 아니라 **테스트가 스스로 고른 경계**다.

### 뚫리는 자리 — 경고로 기록한다

- **가장 약한 것은 IP-6ⓑ(다양성 바닥)다. 정답 구현 자체가 다섯 줄이기 때문이다.** 1차 실물이 그대로 증거다. `acceptance/ac-35.test.ts`에 대칭 fixture가 있었고(같은 해석 2개 → 거부 / 해석 1개 → 거부 / 다른 해석 2개 → 통과), 이걸 전부 통과시킨 구현이 `src/interview/materiality/diversity-floor.ts`의 다섯 줄이다:
  ```ts
  const distinct = new Set(input.interpretations.map((reading) => reading.trim()));
  if (distinct.size < 2) return { ok: false, reason: "해석이 하나로 붕괴했다 …" };
  return { ok: true };
  ```
  감사 실측: **같은 해석 + 뒤에 마침표 하나 → `{ok:true}`.** 이 게이트는 실물에서 아무것도 안 막으면서 양극·음극을 전부 만족한다. **`bin/fabricate`를 프로세스로 통과시켜도 여전히 뚫린다** — **헌법 넷을 다 지켜도 막히지 않는 종류다.**
- **준우승은 IP-5ⓒ.** 기계가 보는 것은 `context !== "driver"` 태그 하나뿐이고, 1차 실측상 **`"Driver"` · `"driver "`(뒤 공백) · `"driver-session"` · `"the driver"`가 전부 non-driver로 통과**했다. 이번에도 CLI가 `blind_verdict:{judge_context_id:"judge", driver_context_id:"driver", approved:true}` 같은 레코드를 받아 장부에 쓰고 fixture가 "두 문자열이 다른가 · `approved:false`면 거부하는가"만 검사하면 **IP-5ⓐ~ⓒ가 전부 초록인데 서브에이전트는 한 번도 안 뜨고 드라이버가 두 값을 다 날조할 수 있다.** 1차 `ac-1`·`ac-9`의 `author_context !== "driver"` 필드 검사와 **정확히 같은 형상**이다.
- **IP-2ⓑ**: `close`가 조각 목록을 읽는데 `start`가 항상 `[]`로 초기화하면 "미커버 0개"가 영원히 참이다. 다만 이건 위 둘보다는 낫다 — fixture가 **원문에서 독립적으로 만든 조각을 공개 `turn record` 경로로 심고** 실제 `close`가 exit ≠ 0 · intent 미생성을 보이는 음극을 세우면 스텁이 양극에서 죽는다. **안 심으면 통과한다.**

### 규칙 넷 — 이것이 fixture 헌법이다

1. **fixture는 운영 모듈을 import하지 않는다.** 설치된 `bin/fabricate`를 **프로세스로 띄우거나**, 훅 스크립트에 **합성 stdin**을 먹이거나, `claude -p`로 **실제 발화**시킨다.
2. **구현이 만든 판정값을 정답으로 재사용하지 않는다.** fixture가 조각·모순·판정을 **독립적으로** 만들어 **공개 경로로** 심는다.
3. **관측하는 것은 외부 효과뿐이다** — 종료 코드, 디스크에 생긴 파일, 장부 내용. **내부 상태를 들여다보지 않는다.**
4. **IP-0과 IP-1ⓐ가 먼저 초록이 된다**(걷는 뼈대). 설치 → 슬래시 명령 → 훅 → `start` → `turn record` → `close`가 먼저 뚫려야 나머지 fixture가 그 위에서 **프로세스로** 돌 수 있다. **이 순서가 뒤집히면 IP-2~6 fixture는 붙일 프로세스가 없어 모듈 단위로 내려앉고, 그 순간 1차와 구조적으로 동일해진다.**

### 이 규칙을 기계가 본다 — 1차에 없었던 유일한 새 방어

**`verify/` 아래 어떤 파일도 `src/`에서 import하지 않는지 `verify`가 검사한다.** 1차의 실패 기제가 재발하면 **verify가 빨개진다.** 규칙 1을 사람의 성실성이 아니라 기계에 맡기는 자리이고, **1차 대비 유일하게 새로운 방어선이다.**

**도달 가능성 검사(`GOAL.md` §6-2)의 시점도 A1으로 못박는다.** `GOAL.md` §6은 *"`verify`가 초록이 될 때 거기 있으면 된다"*로 시점을 강제하지 않지만(§7이 구현에 맡긴 자리다), 늦게 넣으면 이미 지어진 섬을 뒤늦게 발견하는 것뿐이다. **`GOAL.md`가 시점을 강제하지 않으므로 이것은 STATE의 운용 결정이다.**

> **~~1차에 이 검사가 있었다면 89/102가 첫날 터졌다.~~ 반증됨 (2026-07-28 적대적 검증 1차).** 1차의 첫날에도 모듈은 0개였다 — 89/102가 터지는 것은 모듈이 생긴 뒤(=세션 B)다. **A1 배치가 얻는 것은 *"검사가 존재한다"*뿐이고, 그래서 음극 자기시험이 없으면 A1 배치의 값이 거의 없다.** 시점은 A1으로 유지하되 자기시험을 의무로 붙인 이유가 이것이다(아래 "초록 조건과 동결 계약" 절).

### RED-first를 버리는 것이 아니다

**default-FAIL 계약은 유지된다** — 모든 IP 기준이 `false`에서 시작하고, 종료 조건은 여전히 코드보다 먼저 선다. 버리는 것은 **"RED-first면 충분하다"는 착각**뿐이다.

### 넷을 다 지켜도 남는 것 — 정직하게 적는다

**네 조건이 다 서도 IP-6ⓑ · IP-6ⓓ · IP-5ⓒ는 여전히 뚫린다.** 형상 술어는 내용을 강제하지 못한다 — `GOAL.md` §4-5(*"`.length(3)`은 세 개를 세지만 세 개를 내놓게 만들지는 못한다"*)와 §8이 **이미 잔여로 선언한 자리**다. 여기서 요구되는 것은 더 강한 fixture가 아니라 **`GOAL.md` §2 마지막 줄의 규율이다 — `기계 통과`를 완료라고 보고하지 않고 `사람 검증 대기`를 함께 말하는 것.** 세션 A·B가 이걸 모르면 **초록을 완료로 착각한다.**

### 한 문장

> **RED-first는 1차 실패의 방지책이 아니라 1차가 이미 쓴 도구다. 새 계획을 1차와 가르는 유일한 것은 fixture가 모듈이 아니라 설치된 프로세스를 만진다는 것이고, 그 성질은 `GOAL.md` §2에 IP-0·IP-1ⓐ로만 명문화돼 있다 — 나머지 IP에는 안 적혀 있으므로 fixture를 쓰는 사람이 규율을 스스로 지키는 동안에만 성립한다. 규율이 아니라 구조로 박으려면 `verify`가 IP fixture에서 `src/**` import를 금지하는 것이 1차 대비 유일하게 새로운 방어선이다.**

**검증 방식**: **Codex(GPT-5.4)의 적대적 검토와 저장소 실물 전수 조사가 독립적으로 같은 결론에 도달했다.**

---

## 구동 루프는 비계다 — fabricate의 정식 루프가 아니다 (2026-07-28 사용자 결정)

사용자 결정: **"지금 만드는 루프를 fabricate의 정식 루프로 채택하여 앞으로 계속 사용할 것은 아니다."**

지금 짓는 구동 루프(`drive.sh` · `fab-verifier` · allowlist · `.drive/`)는 **fabricate를 짓기 위한 일회용 비계**다. fabricate가 완성되면 걷어낸다. **정식 루프가 무엇이 될지는 이번 범위 밖이고 미정이다** — 지금 정하지 않는다.

### 왜 이 구분을 문서로 박는가

착각이 양방향으로 샌다.

- **비계를 제품으로 착각하면** — ① 비계에 과잉 투자한다(정교한 루프를 만드느라 fabricate를 안 짓는다) ② 비계가 배포 매체에 섞여 **IP-0(설치 멱등·설정 보존)을 오염시킨다** ③ 세션 B가 비계를 고치는 것을 진척으로 착각한다.
- **제품을 비계로 착각하면** — `bun run verify`처럼 `GOAL.md` §2가 **완료 정의로 요구하는 것**을 나중에 "임시니까"라며 걷어낸다.

### 경계 — 0a가 짓는 다섯이 여기서 갈린다

| 산출물 | 지위 | 근거 |
| --- | --- | --- |
| `package.json`의 `verify` 스크립트 | **제품** | `GOAL.md` §2 *"기계 통과를 닫는 명령은 하나다"*. 완료 정의의 일부다 |
| `bin/fabricate` (단일 뿌리) | **제품** | `GOAL.md` §6-1 진입점 · §4-2 도달성의 뿌리 |
| `verify/` 러너 — default-FAIL 계약 · fixture 헌법 검사 · 도달 가능성 검사 | **제품** | `GOAL.md` §2 · §6 |
| `drive.sh` | **비계** | `GOAL.md` 어디에도 없다. 0b가 쓰고 버린다 |
| `.claude/agents/fab-verifier.md` | **비계** | `GOAL.md` §1의 "에이전트 정의"는 **인터뷰용**(세션-맹검 판정, IP-5)이지 이 구동 검증자가 아니다 |
| `.claude/settings.local.json` allowlist | **비계** | 이 기계의 권한 설정. gitignore 대상이고 배포되지 않는다 |
| `.drive/` · `AGENT_STOP` | **비계** | 루프의 휘발성 작업물과 사람이 끊는 손잡이 |

**따라서 비계는 플러그인 매니페스트에 들어가지 않는다.** IP-0의 설치 4단이 비계를 설치하면 그 자체로 §4-8(설정 보존) 위반이다.

### 기계로 확인되는 판별식 — **정적 참조 금지 검사로 확정 (2026-07-28 2차 개정)**

> **`verify/`와 `package.json`의 `verify` 스크립트가 `drive.sh` · `.claude/` · `.drive/` · `AGENT_STOP`을 참조하지 않는다.** `verify`가 이것을 기계로 본다.

참조가 생기면 verify가 빨개진다. `fixture 헌법 검사`(`src/` import 금지)와 **같은 형상의 정적 검사**이고, 같은 이유로 사람의 성실성이 아니라 기계에 맡긴다.

**~~파일을 치우고 verify를 다시 돌리는 방식은 버렸다.~~ 적대적 검증에서 셋이 깨졌다** (2026-07-28):

```
mv drive.sh .claude .drive AGENT_STOP <임시>/ 2>/dev/null   # 옛 안 — 쓰지 마라
bun run verify; echo "exit=$?"
mv <임시>/* . 2>/dev/null
```

| 무엇이 깨졌나 | 실측 |
| --- | --- |
| **복구가 안 된다** | `mv <임시>/*`의 `*`는 **dotfile을 매칭하지 않는다.** 실제로 돌려 보니 `.claude`·`.drive`가 임시 위치에 **남았다.** `2>/dev/null`이 그 사실을 삼킨다 — `GOAL.md` §4-7(조용한 실패 금지)을 판별식 자신이 위반한다 |
| **자기 발밑을 판다** | `.claude`를 치우면 allowlist가 사라진다. 이 디렉터리는 미신뢰라 그것이 **유일하게 존중되는 권한 원천**이고, `bun`은 내장 안전 목록 밖이다. 즉 판별식이 자기 실행에 필요한 권한을 스스로 제거한다 |
| **증명하려는 것을 증명하지 못한다** | verify가 비계를 아예 안 읽으면 `drive.sh`가 `exit 0` 한 줄이어도 "결과가 같다"가 나온다. 이 판별식은 **verify의 독립성**만 보고 **비계가 도는지**는 보지 않는다 |

**그래서 둘로 갈랐다** — 독립성은 위 정적 검사가 보고, `drive.sh`가 실제로 도는지는 **0a의 드라이런**(`AGENT_STOP`을 미리 만들어 둔 상태로 한 바퀴)이 본다.

### 세션 B에 대한 귀결

- **비계는 고쳐 쓰되 제품으로 승격하지 않는다.** 루프가 부족하면 `drive.sh`를 고치는 것은 자유다. `verify/`나 `bin/`으로 옮기는 것은 금지다.
- **비계를 다듬는 것은 진척이 아니다.** 진척은 IP가 초록이 되는 것 하나다.

---

## `verify`의 초록 조건과 동결 계약 — **계획 세션이 확정한다 (2026-07-28)**

**왜 여기서 정하는가.** 2차 적대적 검증이 셋을 드러냈다 — ① `bun run verify`가 **언제 exit 0인지가 문서 어디에도 없었다** ② 동결과 양극 자기시험이 **서로를 배제하는 교착**이었다 ③ `GOAL.md`가 요구하는 검사 일부가 **어느 축에도 배치되지 않았고**, 동결이 그 누락을 고정할 참이었다. 셋 다 **판단**이라 루프도 세션 A도 아닌 이 자리에서 정한다. 정하지 않으면 *"초록의 정의를 그 눈금에 채점당하는 쪽이 쓴다"*가 된다.

### 축 셋 — `verify`가 보는 전부

| 축 | 무엇 | 개수 | 값의 종류 | 출처 |
| --- | --- | --- | --- | --- |
| **A — IP 기준** | `GOAL.md` §2 표의 동그라미 항목 전부 | **43** (IP-0:4 · IP-1:8 · IP-2:13 · IP-3:4 · IP-4:4 · IP-5:3 · IP-6:7) | **`true` / `false`만.** `n/a` 금지 | `GOAL.md` §2 |
| **B — 구조 검사** | 아래 열여섯 | **16** | `PASS` / `FAIL` / `n/a` | `GOAL.md` §6 · §2 · §4 · STATE |
| **C — 도구 사슬** | `typecheck` · `lint` · `test` | **3** | 통과 / 실패 | `GOAL.md` §2 |

**합 62.** (43 + 16 + 3)

**축 B 열여섯 — 하나씩 이름을 붙인다.** 2차 검증이 *"43개가 `GOAL.md`의 완료 정의 전부가 아닌데 동결이 그 누락을 고정한다"*를 지적한 자리이고, **3차 검증이 그 지적이 아직 안 닫혔음을 찾아 여섯을 더했다**(S-5~S-9의 분할 · S-11 · S-12 · S-13 · S-14).

**한 항목은 한 의무만 진다.** 하나의 boolean에 둘 이상을 담으면 한쪽만 만족해도 `PASS`가 뜬다 — 2차 판의 `S-5`(훅 세 사건)·`S-6`(두 기제)이 정확히 그랬다.

| id | 무엇 | 근거 |
| --- | --- | --- |
| `S-1` | 진입점이 실재한다 — `fabricate --help`에 `deep-interview`·`turn`·`check` | §6-1 |
| `S-2` | 운영 모듈 전부가 뿌리에서 **도달 가능**하다 | §6-2 |
| `S-3` | **잠금을 우회하는 쓰기 경로가 없다** | §6-3 |
| `S-4` | 거부가 셸에 보인다 — 종료 코드 ≠ 0 + 사람이 읽을 사유 | §6-4 |
| `S-5` | **`Stop` 훅이 설치된 경로로 실제 발화한다** — 모사가 아니라 | §6-5 · §2 |
| `S-6` | **`UserPromptExpansion` 훅이 설치된 경로로 실제 발화한다** | §6-5 · §2 |
| `S-7` | **`PreToolUse`(매처 `Skill`) 훅이 설치된 경로로 실제 발화한다** | §6-5 · §2 |
| `S-8` | UPE의 이름 판별이 **네임스페이스 포함 `command_name`**으로 서고 대소문자를 구분한다 | §6-6 |
| `S-9` | PreToolUse의 이름 판별이 **스크립트 안에서 `tool_input.skill`**로 선다 (매처에 기대지 않는다) | §6-6 |
| `S-10` | 설치된 스킬·에이전트 정의·훅 경로가 **이 저장소를 가리킨다** | §2 |
| `S-11` | **그 경로 중 어느 것도 절대경로가 아니다** — 저장소를 옮겨도 안 깨진다 | **§4-10** |
| `S-12` | **`verify`가 사용자의 실제 설정을 오염시키지 않는다** — 실행 전후로 `~/.claude.json`과 사용자 전역 설정이 **바이트 불변** | **§4-9** |
| `S-13` | **대칭 증거** — 축 A 43개 **각각**이 양극·음극 fixture를 매니페스트에 선언하고, `verify`가 **둘 다 공개 경로로 실제 실행**한다 | **§2 "모든 술어는 대칭 증거를 갖는다" · "양극·음극 둘 다"** |
| `S-14` | **거부는 레코드를 남기지 않는다** — `close`가 exit ≠ 0으로 죽은 뒤 `.fabricate/intent/`에 파일이 **안 생긴다** | **§2 IP-2 머리글** |
| `S-15` | **fixture 헌법 검사** — `verify/` 아래 어떤 파일도 `src/`에서 import하지 않는다 | STATE(1차 대비 유일한 새 방어선) |
| `S-16` | **비계 참조 금지** — 아래 범위 | STATE |

**S-11·S-12·S-13·S-14가 3차 검증의 소득이다.** 넷 다 `GOAL.md`가 요구하는데 **동그라미가 아니라 산문·머리글·§4에 있어서** 축 A의 정의(*"§2 표의 동그라미 항목 전부"*)에서 빠져 있었다. 특히:

- **S-13이 없으면** 축 A의 각 기준이 한쪽 극만 관측하는 check로 만족된다. `GOAL.md` §2가 직접 적었다 — *"한쪽만 적으면 아무것도 안 하는 구현이나 항상 거부하는 구현이 통과한다."* IP-2ⓖ처럼 한 boolean 안에 양극·음극이 함께 든 기준은 *"레코드에 그 세 필드가 있는가"* 한 줄로 `true`가 뜨고, 실제 하한은 상수 0일 수 있다.
- **S-14가 없으면** `close`가 거부하면서 "재개용 부분 레코드"를 써도 S-3(우회 경로 아님)·S-4(exit≠0+사유)가 둘 다 `PASS`다. 게이트를 못 지난 레코드가 `.fabricate/intent/`에 존재하고 IP-4가 그것을 "잠긴 의도"로 읽는다.
- **S-11이 없으면** S-10이 오히려 위반을 **통과시킨다** — 절대경로도 "이 저장소를 가리킨다". `directory` 소스가 상대경로로 줘도 절대경로를 박는 것은 실측돼 있다(위 IP-0 구멍 표). **이 저장소는 이미 한 번 옮겨졌다.**
- **S-12가 없으면** IP-0ⓐ의 4단 증거를 가장 싸게 만족시키는 길이 **사용자의 실제 설정에 설치하는 것**이 된다. §4-9는 `GOAL.md` §7-3(비가역 위험)에 걸리는 부류다.

### 초록 조건 — 이것이 `exit 0`의 정의다

> **`bun run verify`가 `exit 0`인 것은 다음이 전부 참일 때, 그리고 그때뿐이다.**
> 1. 축 A **43개가 전부 `true`**
> 2. 축 B **16개가 전부 `PASS`**
> 3. 축 C **3개가 전부 통과**
> 4. **동결 매니페스트 대조가 통과**
>
> 그 밖의 모든 경우는 `exit ≠ 0`이다.

**그리고 값의 출처가 강제된다.** 집계 규칙만 못박으면 *"집계에 들어가는 값이 어디서 왔는가"*가 열린 채로 남는다 — 3차 검증이 저장소 안 `status.json`을 읽어 `true`를 돌려주는 check로 그 구멍을 실증했다. 그래서 초록 조건에 다섯째를 붙인다.

> 5. **축 A·B의 모든 값은 그 기준의 check 함수가 공개 경로의 외부 효과에서 산출한 것이어야 한다** — 서브프로세스의 종료 코드 · 디스크에 생기거나 안 생긴 파일 · 장부 내용 · 훅 스크립트에 합성 stdin을 먹인 결과. **저장소 안에 미리 적힌 판정값을 읽어서 낸 값은 `true`/`PASS`로 세지 않는다.**
>
> 러너는 이것을 **계측으로** 본다 — 각 check 실행 중 서브프로세스가 떴는지·어떤 경로를 읽고 썼는지를 기록하고, **매니페스트에 선언된 최소 관측 계약을 실제로 밟지 않은 check의 `true`는 `false`로 강등한다.** 계측 없는 초록은 초록이 아니다.

**`n/a`는 초록이 아니고, `n/a` 판정은 check가 아니라 러너가 한다.** `n/a`를 check 본문이 정하게 두면 *"대상 0개일 때 `PASS`를 돌려주는 check"*를 막는 기계가 없어져, 1차 검증이 만든 방어(*"대상 0개는 `PASS`가 아니라 `n/a`"*)가 저자 재량 한 줄로 무효화된다. 그래서 **check는 자기가 본 `targets`를 함께 돌려주고, `targets`가 비면 러너가 반환값과 무관하게 `n/a`로 덮어쓴다.**

**`n/a`는 초록이 아니다.** 2차 검증이 *"어려운 기준을 `n/a`로 밀 수 있다"*를 지적했다. 그래서 못박는다 — **`n/a`가 하나라도 있으면 `exit ≠ 0`**이고, 축 A는 애초에 `n/a`를 가질 수 없다. `n/a`는 축 B에서 **"대상이 아직 없다"**를 뜻하는 임시 상태일 뿐 통과가 아니다. `n/a`인 검사는 **무엇이 생기면 대상이 생기는지**를 출력에 함께 적는다.

**대상 0개일 때 `PASS`가 뜨면 안 되는 축 B 항목에는 하한을 동결한다.** `S-2`(도달성)·`S-3`(우회 경로 없음)·`S-15`(헌법)은 **적게 지을수록 잘 통과하는** 방향을 갖는다 — 쓰는 경로가 0개면 우회도 0개이고, 파일이 하나면 도달률 100%이며, `src/`가 없으면 import도 없다. 그래서 각각 **최소 대상 수**를 매니페스트에 적고, 그 아래면 `PASS`가 아니라 `n/a`다(예: `S-3`은 `.fabricate/intent/`에 쓰는 경로가 **정확히 1개**일 때만 `PASS`, 0개면 `n/a`).

### 동결 대상 — 교착을 푸는 자리

**동결하는 것** (매니페스트에 들어가고 sha256이 걸린다):

- 축 A·B·C의 **id 목록 전부**(43 + 16 + 3 = 62)
- 각 축 A 기준의 **`GOAL.md` 원문 텍스트**와 **그 원문의 시작·끝 오프셋**
- 각 기준의 **check 바인딩 경로**(`verify/checks/<id>.ts`)
- 각 기준의 **최소 관측 계약** — 그 check가 무엇을 띄우고 무엇을 관측해야 하는가
- 각 축 A 기준의 **양극·음극 fixture 선언**(`S-13`이 보는 것)
- `S-2`·`S-3`·`S-15`의 **최소 대상 수**
- **초록 조건 자체**(위 다섯 줄의 집계 규칙)
- **`package.json`의 `scripts.verify` 문자열**
- **`verify/index.ts`(러너)와 `verify/criteria/**`의 sha256**

**동결하지 않는 것**: **판정값**(`true`/`false`/`PASS`/`FAIL`/`n/a`). 런타임 산출이다. 그리고 **`verify/checks/**`의 내용** — 세션 B가 채워야 하기 때문이다.

> **판정값을 뺀 한 줄이 교착을 푼다.** 판정값이 매니페스트 밖이므로, 양극 자기시험이 기준 하나를 `true`로 만들어도 **해시 대조가 죽지 않는다.** 둘을 한 파일에 두면 배타적이었다.

**`package.json`과 러너를 동결에 넣은 이유 — 3차 검증이 찾은 가장 싼 통로다.** 2차 판의 동결 목록은 id·GOAL 원문·바인딩 **경로**·집계 규칙뿐이었다. 그런데 **exit 코드를 실제로 만드는 것**은 그 목록에 하나도 없다:

```jsonc
// package.json — 이 한 줄이면 매니페스트는 읽히지도 않는다
"scripts": { "verify": "exit 0" }
// 변종: "verify": "bun run verify:real || true"  ← 실패 출력은 그대로 흐르고 종료 코드만 0.
//        사람이 눈으로 봐도 속는다
```

**최소 관측 계약을 동결에 넣은 이유 — 같은 병의 둘째 통로.** 바인딩 **경로**만 동결하면 `verify/checks/*.ts` 62개를 전부 `export const check = () => ({ pass: true })`로 써도 경로 그대로·id 그대로·GOAL 원문 그대로라 **대조가 통과하고 exit 0이 뜬다. `src/`는 0줄이어도 된다.** 1차는 테스트를 동결하고 구현을 열어 뒀고, 2차 판은 기준을 동결하고 **판정 기제**를 열어 뒀다 — 같은 형상이 한 층 위로 옮겨진 것이다. 계약의 형식은 이렇다:

```jsonc
{ "id": "IP-1:c",
  "must_spawn": "bin/fabricate",
  "must_observe": ["exit_code", "file:.fabricate/intent/*.json"],
  "positive_fixture": "verify/fixtures/IP-1-c.pos",
  "negative_fixture": "verify/fixtures/IP-1-c.neg" }
```

**GOAL 원문의 오프셋을 동결에 넣은 이유.** 개수 43은 명확하지만(IP-2 행은 마커가 15회 등장하는데 그중 둘은 예고 참조 `ⓜ`과 역참조 `ⓐ`다 — 중복을 제거하면 정확히 13이다) **ⓐ~ⓜ 사이를 어디서 자르는가**는 어디에도 없다. IP-2 행에는 소제목 넷(*"준비도가 실제 신호를 읽는다"* 등)이 끼어 있다. 두 사람이 다르게 자르면 해시가 갈리고, **동결은 잘못 자른 쪽을 영구히 완료 정의로 만든다.** A1이 오프셋을 확정해 매니페스트에 박는다.

**딸린 규칙 넷** — 2·3차 검증의 우회로를 막는다.

1. **앵커는 저장소 밖에 있어야 한다.** `STATE.md`에 sha256을 텍스트로 박는 것은 **앵커가 아니다** — `STATE.md`도 저장소 안이고 세션 B의 구현자는 `codex exec -s workspace-write`다. 세 수면 풀린다: 기준 완화 → 매니페스트 재생성 → `STATE.md`의 해시 줄 교체. 그러면 `verify`가 출력하는 해시와 `STATE.md`의 해시가 **일치하므로 눈으로 대조해도 통과한다.** 그래서: **A1은 매니페스트 sha256을 `STATE.md`에 적고(편의용 사본), 동시에 사용자에게 대화로 보고한다. 유일한 진짜 앵커는 사용자 쪽 기록이다.** 이 문단이 그것을 명시하는 자리다.
2. **매니페스트 갱신 스크립트를 저장소에 남기지 않는다.** 남기면 동결이 원클릭으로 풀린다. A1은 일회용으로 만들고 지운다. **다만 이것은 스크립트를 지울 뿐 알고리즘을 못 지운다** — 정규화 방식은 A1이 근거로 적어야 하므로 문서에 남는다. 규칙 1이 진짜 방어이고 이것은 비용을 올릴 뿐이다.
3. **해시는 기준 id별로 하나씩 + 전체 하나.** 한 덩어리면 check를 하나 붙일 때마다 오탐으로 죽고, 그 오탐이 재생성 압력을 최대로 만든다. **전체 해시의 입력은 "id 목록 전부 + 각 항목 해시"다** — *"`PASS`인 항목만 순회"*로 구현하면 항목 삭제가 안 잡힌다.
4. **세션 B의 쓰기 범위는 경로 단위로 갈린다.** *"`verify/` 수정 금지"*는 쓸 수 없다 — check가 `verify/checks/` 안에 있어서 그 금지는 **B가 축 A를 초록으로 만들 유일한 수단을 뺏는다.**

   | 경로 | 세션 B |
   | --- | --- |
   | `verify/checks/**` | **추가·수정 허용** (이것이 B의 일이다) |
   | `verify/fixtures/**` | **추가 허용** |
   | `verify/index.ts` · `verify/criteria/**` · `verify/manifest.json` · `package.json` | **금지** |

   그리고 이 경계는 프롬프트가 아니라 **`verify` 자신의 해시 대조가 강제한다**(위 동결 목록). 프롬프트만으로 막으면 정작 파일을 고치는 codex가 그 프롬프트를 안 본다.

### 축 B `S-16`(비계 참조 금지)의 범위 — **`.claude/` 전체 금지는 철회한다**

2차 검증이 `GOAL.md` §2와의 충돌을 잡았다 — `S-10`(설치 경로가 이 저장소를 가리키는지)을 검사하는 코드는 **반드시 `.claude` 문자열을 포함한다.** 디렉터리 전체를 금지하면 세션 B가 IP-0을 초록으로 만드는 순간 verify가 스스로 빨개지고, **가장 싼 수리가 문자열 조립**(`".cla"+"ude"`)이라 검사가 영구히 무의미해진다.

**금지 대상은 아래 다섯 경로뿐이다.**

```
drive.sh   .drive/   AGENT_STOP
.claude/agents/fab-verifier.md   .claude/settings.local.json
```

그리고 검사는 `verify/` 디렉터리 글롭이 아니라 **verify 진입점에서 도달 가능한 모듈의 전이 폐쇄**를 본다 — 한 겹 건너 참조(`verify/x.ts → tools/paths.ts → .drive/`)를 잡기 위해서다. `S-2` 도달성 검사와 같은 구현을 재사용하면 추가 비용이 거의 없다.

**금지 목록은 코드가 아니라 데이터에 둔다 — 안 그러면 검사기가 자기 자신에 걸린다 (3차 검증).** `S-16` 검사기는 정의상 전이 폐쇄 **안**에 있고, 다섯 경로 문자열을 전부 갖고 있어야 한다. 목록을 코드에 박으면 **첫 실행부터 영구 `FAIL`**이고, 가장 싼 수리가 바로 위에서 무의미하다고 판정한 문자열 조립이다. 게다가 이미 `FAIL`이면 음극 자기시험이 요구하는 `PASS|n/a → FAIL` **상태 전이를 관측할 수 없어 그 시험이 구조적으로 불가능해진다.**

> **그래서 다섯 경로는 `verify/criteria/axis-b.json`에 데이터로 두고, `S-16` 검사기는 그 데이터의 소비자로만 만든다.** 데이터 파일은 전이 폐쇄의 모듈이 아니다. **같은 자기적용 함정이 `S-15`(헌법 검사기 자신의 `"src"` 문자열)에도 걸린다 — 둘 다 같은 방식으로 푼다.**

### 양극 자기시험 — **정의를 교체한다**

**옛 정의**(*"기준 하나를 합성 충족으로 `true`로 뒤집었다 되돌린다"*)는 **스트로맨을 못 막는다.** 기준이 `{"id":…,"pass":false}` JSON이면 `false`를 `true`로 고치는 것으로 문자 그대로 만족되고, 증명되는 것은 *"JSON을 고치면 값이 바뀐다"*뿐이다.

> **새 정의: 축 A 기준 하나를 골라 `verify/checks/<id>.ts`에 실제 check 함수를 붙이고, ① 참인 합성 입력에서 그 기준이 `true`로 뜨는 것 ② 거짓인 합성 입력에서 `false`로 돌아오는 것을 각각 관측한 뒤, **check는 남기고 합성 입력만 지운다.**

- **IP 충족이 아니다** — 입력을 지우면 기준은 다시 `false`다. *"IP를 초록으로 만들지 마라"*와 충돌하지 않는다.
- **세션 B의 이음매가 실물로 증명된다** — B는 `verify/checks/<id>.ts` 규약을 문서가 아니라 **동작하는 예제 하나**로 받는다.
- 남긴 check 파일은 매니페스트의 바인딩 경로와 일치해야 한다.

#### 그 예제가 무엇을 가르치는가 — 여기가 급소다 (3차 검증)

**"실제 check 함수"에 제약이 없으면 가장 싼 구현이 1차 실패의 형상이다.** 43개 중 제품 없이 참/거짓을 둘 다 관측할 수 있는 것은 *"디스크 상태만 읽는 기준"*뿐이고, 그 구현은 `ledger.jsonl`에 줄이 있으면 `true`·지우면 `false`다. 그런데 그것은 fixture 헌법 규칙 1·2를 어기고, 위 *"뚫리는 자리"*가 **준우승 구멍**으로 지목한 IP-5ⓒ의 형상 그대로다. 그리고 **B는 이 파일 하나를 42번 복제한다** — 이음매를 실물로 받는다는 것이 정확히 그 뜻이다.

반대로 헌법을 지키게 하면 **딜레마에 걸린다**: `bin/fabricate` 스텁은 A1 시점에 **항상 exit ≠ 0**이라 참·거짓 양쪽을 프로세스로 관측할 수 없다.

> **푸는 법 — 스텁에 자기시험 전용 종료 코드 인자를 둔다.** `bin/fabricate --selftest-exit <n>`이 받은 값을 그대로 종료 코드로 낸다. 이 인자는 **매니페스트에 동결되고**, 하위 명령이 아니므로 `--help`에 뜨지 않아 `S-1`을 앞당겨 초록으로 만들지 않는다.
>
> **그래서 양극 자기시험의 대상 check는 `bin/fabricate`를 서브프로세스로 띄우고 종료 코드를 관측해야 한다. 디스크 상태만 읽는 check는 (나)의 증거로 안 쳐준다.**

### 집계 자기시험 — **신설 (3차 검증)**

**2차 판의 완료 조건 어디에도 `exit 0`이 원리적으로 도달 가능한지를 관측하는 항목이 없었다.** 음극 자기시험은 *검사 한 줄의 상태 전이*를 보고, 양극 자기시험은 *기준 하나의 값*을 본다 — 나머지 61이 여전히 미충족이라 **종료 코드는 두 시험 어디서도 0이 되지 않는다.**

1차 검증이 잡은 급소는 *"아무것도 검사하지 않고 `process.exit(1)` 하는 스크립트가 완료 조건을 전부 만족한다"*였고, 처방이 양극 자기시험이었다. **그 처방은 관측 지점을 종료 코드에서 출력 텍스트로 옮겼을 뿐 병은 종료 코드에 그대로 있다.** 집계에 off-by-one이 있거나 마지막 줄이 무조건 `process.exit(1)`이면 A1의 완료 조건이 전부 만족되고, 세션 B는 20라운드를 갈아도 절대 초록이 안 되며, 원인이 눈금인지 구현인지 아무도 모른다.

> **집계 자기시험: 일회용 override로 축 A·B·C 전부를 강제 충족시킨 상태에서 `bun run verify`가 `exit 0`을 내는 것을 한 번 관측하고, override를 지운 뒤 baseline과 같은 출력·같은 종료 코드로 돌아오는 것을 확인한다.**
>
> - **음극 자기시험 셋과 같은 형상이다** — 심고, 전이를 관측하고, 되돌리고, 되돌림을 검증한다.
> - **IP 충족이 아니다** — override를 지우면 전부 다시 미충족이다.
> - override는 **환경변수 하나**로 두고 저장소에 남기지 않는다. 남기면 그것이 세션 B의 원클릭 초록이 된다. A1은 관측 출력만 기록한다.

### 브랜치·커밋 규약 — 2차 검증이 발견한 공백

세션 A의 산출물은 **정의상 절대 초록이 아니므로** `main` 착지는 `GOAL.md` §3 위반이다. 그런데 브랜치에만 두면 세션 B가 `main`에서 시작해 자기를 세션 A로 오판한다. 그래서:

- **세션 A1·A2는 `bootstrap/0a` 브랜치에서 작업하고 커밋한다.**
- **`main`에는 이 `STATE.md`의 포인터 한 줄만 남는다** — *"0a 산출물은 `bootstrap/0a`에 있다"*.
- **세션 B는 `bootstrap/0a`를 체크아웃한 상태에서 시작한다.** `main`에 착지하는 것은 `GOAL.md` §3대로 **전부 초록일 때** 한 번이다.
- 그래서 **"첫 5분"이 브랜치부터 본다**(위 첫 5분 절 참조).

### `timeout` 부재의 대체 — **2차 판의 `perl alarm`은 틀렸다. 실측으로 고쳤다 (3차 검증)**

macOS에 `timeout`·`gtimeout`이 **없다**(coreutils 미설치). `perl`은 `/usr/bin/perl`에 있고 `alarm`은 동작한다 — `perl -e 'alarm 1; exec "sleep","5"'` → **종료 코드 142**(128+SIGALRM). **여기까지는 참인데, 이 형태를 `codex`에 쓰면 두 군데서 깨진다.**

**깨짐 1 — 손자를 안 죽인다.** SIGALRM은 프로세스 그룹이 아니라 그 프로세스 하나에만 간다. 실측:

```
$ perl -e 'alarm 2; exec @ARGV' ./gc.sh; echo "exit=$?"
grandchild pid=37059
exit=142
$ ps -o pid,ppid,command -A | grep "[s]leep 30"
37059     1 sleep 30      ← 살아 있다 (PPID=1, 고아)
```

**그리고 `codex`가 정확히 그 형상이다.** `codex`는 네이티브 바이너리를 `spawn`하는 node 래퍼이고 **SIGINT·SIGTERM·SIGHUP 셋만 전달한다 — SIGALRM은 없다**(`grep -n "spawn\|SIG" $(command -v codex)` → `150: spawn(binaryPath, …)`, `179: ["SIGINT","SIGTERM","SIGHUP"]`). 즉 1800초에 걸리면 `drive.sh`는 142를 받고 다음 라운드로 가는데 **실제 codex 워커는 `workspace-write`를 든 채 계속 저장소를 쓴다.** 다음 라운드의 verify가 그 중간 상태를 읽고 또 codex를 띄운다 → **동시 작성자 둘.** 위 *"파일을 쓰는 것은 한 번에 한 에이전트"*가 정확히 깨지고, 20라운드면 최대 20개가 누적된다.

**깨짐 2 — 명령이 없으면 조용히 `exit 0`.** 실측:

```
$ perl -e 'alarm shift; exec @ARGV' 5 codex_typo_xyz "prompt" >/tmp/o 2>/tmp/e; echo "exit=$?"
exit=0
stdout=[]   stderr=[]      ← 경고 한 줄도 없다
```

호출줄에 오타가 있거나 무인 환경에서 PATH가 다르면 **매 라운드가 성공한 척**하고 넘어간다. 20라운드가 몇 초에 끝나고 verify는 계속 빨갛다. `GOAL.md` §4-7(조용한 실패 금지)을 **비계 자신**이 위반한다. preflight는 시작 시점의 존재만 보므로 이것을 못 막는다.

**고친 형태 — 프로세스 그룹째 죽이고, exec 실패를 종료 코드로 낸다.**

```bash
# 자식을 새 프로세스 그룹에 넣고, 알람에 그룹째 TERM 을 보낸다.
# exec 가 실패하면 조용히 0 이 아니라 127 로 죽는다.
perl -we '
  my $t = shift;
  my $pid = fork(); die "fork: $!" unless defined $pid;
  if ($pid == 0) { setpgrp(0,0); exec @ARGV or exit 127; }
  $SIG{ALRM} = sub { kill "TERM", -$pid; sleep 2; kill "KILL", -$pid; exit 142 };
  alarm $t; waitpid($pid, 0); alarm 0; exit($? >> 8);
' 1800 codex exec -s workspace-write "…"
```

그리고 **`drive.sh`는 codex의 종료 코드를 셋으로 갈라 출력한다** — `0`(정상) · `142`(시간 상한) · `127`(실행 실패). 셋을 안 가르면 위 깨짐 2가 그대로 산다.

**`drive.sh`는 첫머리에 preflight를 둔다** — `codex` · `claude` · perl alarm의 실재를 확인하고 없으면 **즉시 `exit ≠ 0`으로 죽는다.** 2차 검증이 *"`timeout` 부재를 드라이런이 구조적으로 못 잡는다"*를 지적했고 preflight가 그 자리를 메운다. **다만 preflight는 `command -v codex`로만 확인한다 — 경로를 하드코딩하지 마라.** 이 기계의 codex 경로(`…/fnm_multishells/12404_1785222652259/bin/codex`)에서 `12404_…`는 **셸 PID + 타임스탬프**라 셸마다 바뀐다.


---

## 세션 경계 — 다음 세션은 0a만 한다 (2026-07-27 사용자 결정 · 2026-07-28 세션 셋으로 갱신)

**아래 "0단계는 0a·0b로 갈린다"는 0a와 0b를 *절차*로 갈랐다. 이 절은 그것을 *세션*으로 가른다.** 절차로만 갈라 두면 다음 세션이 0a를 하다가 자연스럽게 구현으로 흘러간다 — **"verify를 지어라"라고 하면 그것을 통과시키고 싶어지기 때문이다.** 그 흐름을 문서로 막는 것이 이 절이다.

**2026-07-28 사용자 결정으로 세션이 셋이 됐다.** 앞에 계획 세션이 하나 붙었고, 그 세션은 코드를 한 줄도 짓지 않았다.

| 세션 | 언제 | 무엇 |
| --- | --- | --- |
| **계획** | 2026-07-28 (끝남) | 상태 확인 · **미결 1·2의 방향 결정**(아래 "0단계에서 바로 부딪힐 자리") · **제품/비계 경계 확정**(위 절) · STATE 정정. **코드 0줄** |
| **계획(2차)** | 2026-07-28 (끝남) | 적대적 검증 2회 · **초록 조건과 동결 계약 확정** · 세션 A를 둘로 분할 · 브랜치 규약 확정. **코드 0줄** |
| **A1 — 눈금을 짓는다** | 다음 | `bun run verify` + 축 셋(A 43 · B 16 · C 3) + 동결 매니페스트 + 자기시험. **`verify`가 RED로 정상 동작하고 눈금이 실제로 판정하는 것이 완료 조건** |
| **A2 — 비계를 짓는다** | 그 다음 | `drive.sh`(preflight 포함) · `fab-verifier` · allowlist · `.gitignore` · 드라이런 |
| **B — 루프로 fabricate를 짓는다** | 마지막 | `drive.sh`를 켜고 축 셋을 전부 초록으로 |

**A1·A2는 `bootstrap/0a` 브랜치에서 작업하고 커밋한다.** `main`에는 이 문서의 포인터 한 줄만 남는다 — 세션 A의 산출물은 정의상 초록이 아니라 `main` 착지가 `GOAL.md` §3 위반이기 때문이다. **세션 B도 그 브랜치에서 시작한다.**

**세션 A와 B가 짓는 것이 서로 다른 물건이라는 것이 핵심이다** — A는 **루프(비계)와 눈금(제품)**을 짓고, B는 **fabricate 자신**을 짓는다. A가 IP를 건드리면 그 경계가 무너진다.

| 세션 | 무엇을 한다 | 무엇을 하지 않는다 | 끝났다고 말할 수 있는 조건 |
| --- | --- | --- | --- |
| **A1 — 눈금** | `bun run verify` + 축 셋(A 43 · B 16 · C 3) + 62개 id 동결 매니페스트 + `bin/fabricate` 스텁 + `tsconfig` include + **`.gitignore` 다섯** | **`drive.sh`를 짓지 않는다**(A2의 일). IP를 초록으로 만들지 않는다. IP별 fixture를 만들지 않는다 | ① `bun run verify`가 RED로 정상 동작하고 62개 각각이 왜 미충족인지 읽힌다 · ② **음극 자기시험 셋**(`PASS\|n/a → FAIL` 상태 전이 + 되돌림 검증) · ③ **양극 자기시험** — 기준 하나에 `bin/fabricate`를 띄우는 실제 check를 붙여 참/거짓을 관측하고 **check는 남겼다** · ④ **집계 자기시험** — override로 `exit 0`을 한 번 관측하고 되돌렸다 · ⑤ 62개 전부에 check 바인딩 경로와 **최소 관측 계약**이 있다 · ⑥ 매니페스트 sha256이 이 문서에 적혔고 **사용자에게 대화로도 보고됐다** · ⑦ 정할 것 여덟의 결정과 근거가 적혔다 |
| **A2 — 비계** | `drive.sh`(**preflight** 포함) · `fab-verifier`(쓰기 도구 없음, Bash는 `Bash(bun run:*)` + `Bash(git diff:*)` + `Bash(git status:*)`) · allowlist | **눈금을 고치지 않는다.** 동결 대상(러너 · `criteria/` · 매니페스트 · `package.json`)은 **손대지 않는다** — 고쳐야 할 사실을 관측하면 근거를 적고 고친 뒤 **새 sha256을 이 문서에 다시 적는다.** `drive.sh`를 정상 모드로 켜지 않는다 | ① **A1 눈금 인수 검사** — verify가 출력하는 매니페스트 해시가 이 문서의 값과 일치하고, A1의 음극 자기시험 셋 중 **하나를 A2가 직접 재현**해 상태 전이를 관측했다 · ② **드라이런**(`DRY_RUN=1`)이 preflight 통과 → verify 1회 → codex 호출 직전 차단 · ③ **preflight 음극**이 즉시 exit≠0과 사람이 읽을 사유를 낸다 · ④ **스텁 모드 한 바퀴**(`DRY_RUN=2`)로 루프 본체 전 경로가 실행됐다 · ⑤ 검증자를 **`drive.sh`가 실제로 쓰는 명령줄 그대로** 돌려 PASS를 받았다 · ⑥ 검증자 음극에서 파일이 **안 생겼다** · ⑦ `codex exec` 연기 시험 |
| **B (마지막)** | `drive.sh`를 켜고 **먼저 걷는 경로 하나**(IP-0 · IP-1ⓐ)를 뚫은 뒤 나머지를 그 세션의 변형으로 | **눈금과 비계를 다시 설계하지 않는다.** 쓰기 범위는 `verify/checks/**`와 `verify/fixtures/**`뿐이다 — 러너·`criteria/`·매니페스트·`package.json`은 금지이고 **해시 대조가 그것을 강제한다** | **`bun run verify`가 초록**(초록 조건 절의 다섯 줄 전부). 그때만 `main`에 착지. 보고는 `기계 통과`이지 완료가 아니다 |

### 세션 A에 못박는 것 — 이 절의 핵심이다

> **RED는 실패가 아니라 세션 A의 정상 산출물이다.** `bun run verify`가 **빨간 것**이 A의 완료 조건이다. 초록으로 만들고 싶은 충동은 **B의 일을 당겨 하는 것**이고, 그러면 **종료 조건을 짓는 자가 그것을 통과시키는 자가 되어 자기 채점이 된다** — `GOAL.md` §1이 *"자기 확신은 자기가 못 깬다"*고 적은 바로 그 구조다. 눈금을 만드는 손과 눈금에 맞추는 손은 달라야 한다.

따라서 세션 A에서 **"어차피 이건 몇 줄이니 겸사겸사 통과시켜 두자"는 판단은 규율 위반이다.** fixture가 통과하는지 보고 싶으면 **음극(거부돼야 하는 입력)이 실제로 거부되는지**를 보는 것으로 충분하다 — 그것은 하네스가 도는지의 검사이지 IP의 충족이 아니다.

### 왜 세션을 가르는가

**0a는 판단이고 0b는 반복 갈아내기다.** 0a가 하는 일 — 미결 1(모델 호출 경로의 회복 방식)·미결 2(`sid` 이음매)의 결정과 fixture 설계 — 는 어느 쪽으로 가도 돌지만 **뜻이 달라지는 갈림길**이라 한 번 정해서 박아야 하는 것이고, 0b가 하는 일은 이미 박힌 눈금을 보고 **같은 방향으로 계속 미는 것**이다. 성질이 다르고, 섞으면 둘 다 상한다 — ⓐ 0a 세션이 구현으로 흘러가 **루프를 끝내 못 만들고**(눈금 없이 코드만 늘어난다), ⓑ 판단과 갈아내기가 한 컨텍스트에 쌓여 **컨텍스트가 차면서 둘 다 부실해진다.** 그리고 이것은 `GOAL.md` §0이 *"매 걸음은 직전 걸음이 아니라 이 문서를 시드로 삼는다"*고 정한 것과 **같은 이유**다 — 세션 B의 입력은 세션 A의 머릿속이 아니라 `GOAL.md`와 이 문서다.

### 세션 A가 끝날 때 STATE에 남길 것 — ~~다섯~~ **갱신 (2026-07-28): A1은 여섯, A2는 다섯. 정본은 각 시작 프롬프트의 [8]·[5]다. 아래 표는 근거로만 읽어라**

**세션 A의 산출물이 세션 B의 입력이다.** 프롬프트도 대화도 휘발한다. A는 종료 전에 아래를 **이 문서에** 적는다. 안 적으면 B가 A의 결정을 모른 채 다시 설계한다.

| # | 무엇 | 형식 |
| --- | --- | --- |
| 1 | **미결 1·2의 결정과 근거** | 무엇으로 정했는가 · 왜 그것인가 · **다른 선택지를 왜 버렸는가.** 미결 1은 방향 ①②③ 중, 미결 2는 `sid` 이음매의 위조 허용 범위까지 |
| 2 | **연기 시험 넷의 실제 결과** | **명령과 그 출력.** "됐다"는 증거가 아니다(`GOAL.md` §2의 대칭 증거와 같은 기준) |
| 3 | ~~**fixture 목록**~~ **갱신됨 (2026-07-27)** — 0a에서는 IP별 fixture를 만들지 않으므로 대신 **① default-FAIL 계약의 형상** · **② fixture 헌법 검사의 형상** | ①: IP 기준을 **어떻게 열거하는가**(어디에 무슨 형식으로 적혀 있는가) · 어떻게 **`false`로 두는가** · 나중에 fixture가 그 기준에 **어떻게 붙는가**. ②: 헌법 검사가 **무엇을 어떻게 보는가** — 어느 경로를 훑고, `src/` import를 어떤 방식으로(정적 파싱? 정규식?) 판별하고, 위반하면 무엇을 출력하는가. **도달 가능성 검사(`GOAL.md` §6-2)도 같은 형식으로 적는다** |
| 4 | **0a에서 새로 드러난 미결** | 있으면. 없으면 "없다"고 적는다 |
| 5 | **`bun run verify`의 출력 형상** | B의 구현자가 **이걸 읽고 다음 조각을 고른다.** 실제 출력 예시를 붙인다 |

### 착수 전 확인 — RED-first 전제 검증은 **끝났다** (2026-07-27 갱신)

**결론이 나왔다: RED-first만으로는 1차 실패를 막지 못한다.** 근거 전문은 위 **"fixture 헌법 — RED-first만으로는 1차를 막지 못한다"** 절과 아래 **"RED-first 전제 검증 — 끝났다"** 절에 있다. 세션 A는 **판정하는 것이 아니라 fixture 헌법 넷을 지키는 것**이 할 일이다. 특히 규칙 4(걷는 경로가 먼저)가 **세션 A의 범위를 바꿨다** — 0a에서 IP별 fixture를 만들지 않는다.

---

## 0단계는 0a·0b로 갈린다 — **루프를 먼저 켜지 마라** (2026-07-27 추가)

**위 표는 그대로 유효하다.** 바뀌는 것은 0단계를 **어떻게 착수하는가** 하나다.

### 왜 이 순서를 지켜야 하는가

위 "구동 루프의 형상"은 `until bun run verify; do …`로 돌게 적혀 있는데, **그 루프의 종료 조건인 `bun run verify` 자체가 0단계의 산출물이다.** 지금 저장소에는 `package.json`의 `verify` 스크립트도 `bin`도, `verify/` 러너와 fixture도, `.claude/agents/fab-verifier.md`도, `.claude/settings.local.json`의 allowlist도, `drive.sh`도 **전부 없다.** 없는 명령을 종료 조건으로 삼은 루프는 첫 바퀴부터 의미 없이 돌거나 즉시 끝난다 — **루프로 0단계를 돌릴 수 없다.** 그리고 더 중요한 이유가 하나 더 있다: **루프는 정해진 목표를 초록으로 갈아 내는 데 강하고, 열린 설계 질문을 푸는 데 약하다.** 아래 "0단계에서 바로 부딪힐 자리"의 미결 1(모델 호출 경로의 회복)과 2(`sid` 이음매)는 반복이 아니라 **판단**이 필요한 자리라서, 루프에 던지면 매 패스가 서로 다른 답을 고르며 헤맨다 — 앞 패스가 ①로 지어 둔 것을 다음 패스가 ③으로 갈아엎고, verify는 계속 빨갛다. 그래서 **판단이 필요한 것과 하네스는 사람이 곁에 있는 대화형으로 먼저 짓고(0a), 루프는 눈금이 이미 박힌 뒤에 켠다(0b).**

### 0a — 부트스트랩 (대화형, 루프 없음) — **낡음. A1/A2 분할 이전 서술이다**

> **아래 셋은 근거로만 읽어라. 지시로 읽지 마라.** ① *"미결 1·2를 정한다"* — 계획 세션이 이미 닫았다 ② *"다섯을 짓는다"* — **A1은 넷을 짓고**(`verify` 스크립트·`bin` · 러너 · 매니페스트 · `.gitignore`), `drive.sh`·`fab-verifier`·allowlist는 **A2**다 ③ *"연기 시험 넷"* — 정본은 A2 프롬프트 `[4]`의 **일곱**이다.

**1. ~~미결 1·2를 먼저 정한다.~~ 갱신 (2026-07-28): 미결 1·2는 계획 세션이 이미 닫았다.** 남은 것은 미결 3 하나이고, 세션 A1 프롬프트 `[5]`가 정할 것 여덟을 따로 열거한다. 아래는 근거로만 읽어라 — 아래 "0단계에서 바로 부딪힐 자리"의 1(IP-2ⓜ과 IP-1의 긴장 — 모델 호출 경로의 회복 방식)과 2(`<sid>`가 무엇이고 훅과 CLI가 어떻게 같은 세션을 가리키는가). 판단이 필요한 자리이고, 정하지 않으면 fixture를 쓸 수 없다.

**2. 다섯을 짓는다.**

| 무엇 | 요점 |
| --- | --- |
| `package.json`의 `verify` 스크립트와 `bin` | 단일 닫기 명령과 단일 뿌리(`bin/fabricate`) |
| `verify/` 러너 + **default-FAIL 계약** + **fixture 헌법 검사** + **도달 가능성 검사** ~~와 IP-0~6 fixture~~ **갱신됨 (2026-07-27)** | **default-FAIL — IP-0~6의 모든 기준이 `false`에서 시작한다.** 없는 검사가 초록으로 보이면 안 된다. **헌법 검사**: `verify/` 아래 어떤 파일도 `src/`에서 import하지 않는지 기계가 본다(위 "fixture 헌법"). **도달 가능성**(`GOAL.md` §6-2)도 여기 넣는다 (~~1차에 있었다면 89/102가 첫날 터졌다~~ — **반증됨**, 위 참조). **IP별 fixture는 만들지 않는다** — 걷는 경로가 없으면 좋은 fixture를 쓸 수 없다(헌법 규칙 4) |
| `.claude/agents/fab-verifier.md` | **쓰기 도구 없음**: `Read` · `Glob` · `Grep` · `Bash`만. 검증자가 고칠 수 있으면 그 자리는 더 이상 검증이 아니다 |
| `.claude/settings.local.json`의 allowlist | `Bash(bun run:*)` + `Bash(bun test:*)`. 커밋되는 `.claude/settings.json`에 두지 않는다(아래 `.gitignore` 절) |
| `drive.sh` | 0b의 루프 본. 아래 스크립트 |

**3. 연기 시험 넷을 통과한 뒤에만 0b로 간다.** 하나라도 안 되면 루프는 **조용히 헛돈다** — 특히 넷째가 안 되면 구현자가 매 패스마다 아무것도 못 하고 끝난다.

```
bun run verify; echo "exit=$?"                    # 0이 아니고, 어느 IP가 false인지 읽힌다
codex exec -s workspace-write "SMOKE 파일을 만들고 ok를 써라"
claude --agent fab-verifier -p "PASS만 답하라"
# ↓ 폐기됨 (2026-07-28 실측). --allowedTools 가 가변 인자라 뒤의 프롬프트를 도구 이름으로
# 먹고 "Error: Input must be provided ..." 로 죽는다. 프롬프트는 stdin 으로 넣어야 한다.
# 정본은 "세션 A2용" 프롬프트의 [4] 여섯 시험이다.
claude -p --permission-mode acceptEdits --allowedTools "Bash(bun run:*)" \
  "bun run verify를 돌리고 종료 코드를 보고하라"
```

### 0b — 루프

**위 "구동 루프의 형상" 스케치를 그대로 켜면 안 된다.** 셋을 고친다.

- **verify를 패스당 한 번만 돌린다.** `until bun run verify; do … $(bun run verify)`는 같은 패스에서 verify를 **두 번** 돌린다 — 느리고, 두 실행 사이에 상태가 갈리면 조건과 프롬프트가 서로 다른 사실을 본다. 한 번 돌려 **파일에 받고**, 종료 조건과 프롬프트가 그 파일을 함께 쓴다.
- **상한을 건다.** verify가 원리적으로 통과 불가하게 짜이면 루프는 영원히 돈다. 셋을 함께 건다 — **반복 카운터** + **`--max-budget-usd`**(exit 1로 확실히 끊는 유일한 수단) + **`AGENT_STOP` 센티널 파일**(사람이 밖에서 끊는 손잡이).
- **`codex` 플러그인의 `timeout: 900` `Stop` 훅이 라운드마다 곱해진다**(위 "주의 — 루프에 곱해지는 15분"). 루프 세션은 **`--setting-sources project`로 격리한다.**

**아래 옛 스케치에는 결함 다섯이 있다. 베끼지 마라 — 그 아래 고친 것을 쓴다** (2026-07-28 적대적 검증).

```bash
# 옛 안 — 결함 다섯. 근거로만 남긴다
i=0
while :; do
  bun run verify > .drive/verify.log 2>&1 && break     # (1) .drive/ 가 없어 리디렉션 실패
  [ -f AGENT_STOP ] && break                            # (2) 센티널 검사가 verify 뒤에 있다
  i=$((i+1)); [ $i -gt 20 ] && break
  codex exec -s workspace-write "$(cat GOAL.md)
=== STATE.md ===
$(cat STATE.md)                                         # (3) §0 위반 — 아래 표
=== 지난 검증 지적 ===
$(cat .drive/FINDINGS.md 2>/dev/null)                   # (3) §0 위반 — 직전 산출물 시드
=== 지금 빨간 것 ===
$(tail -80 .drive/verify.log)"                          # (4) codex 쪽에 상한이 없다
  claude --agent fab-verifier -p "…" --setting-sources project > .drive/FINDINGS.md
done                                                    # (5) 권한이 안 서고 프롬프트가 삼켜진다
```

| # | 결함 | 실측 |
| --- | --- | --- |
| 1 | `.drive/`를 만드는 곳이 없다 | 리디렉션이 `rc=1`로 실패 → `&& break`가 안 걸리고 `tail`이 빈 문자열 → **verify를 한 번도 못 본 채 20라운드를 돈다.** 재현함 |
| 2 | 센티널 검사가 verify 뒤 | 사람이 `AGENT_STOP`을 만들어도 그 라운드의 verify가 한 번 더 돈다 |
| 3 | `FINDINGS.md`·`STATE.md`를 시드로 준다 | **`GOAL.md` §0 정면 위반** — *"이전 조각의 산출물을 시드로 주지 않는다"*. `FINDINGS.md`가 정확히 직전 라운드 검증자의 산출물이다 |
| 4 | 상한이 싼 쪽에만 붙는다 | `codex exec --help`에 budget·limit·timeout **0건**. `--max-budget-usd`는 `claude` 전용. 돈과 15분을 태우는 건 codex 쪽이다 |
| 5 | 검증자가 Bash 권한 0 + 프롬프트 유실 | `--setting-sources project`는 `local`(=`settings.local.json`)을 안 싣고, 미신뢰라 `project`의 `settings.json`도 무시된다. 게다가 `--allowedTools`가 가변 인자라 뒤의 프롬프트를 먹는다 |

**고친 형상 (0a가 이대로 짓는다):**

```bash
#!/usr/bin/env bash
set -u
mkdir -p .drive                                  # (1)
i=0
while :; do
  [ -f AGENT_STOP ] && { echo "AGENT_STOP"; break; }        # (2) verify 앞으로
  i=$((i+1)); [ "$i" -gt 20 ] && { echo "라운드 상한"; break; }
  bun run verify > .drive/verify.log 2>&1 && { echo "GREEN"; break; }

  # (4) codex 상한 — 셸 timeout 은 이 기계에 없다. 프로세스 그룹째 죽이고
  #     exec 실패를 127 로 낸다 (위 "timeout 부재의 대체" 절)
  perl -we '
    my $t = shift;
    my $pid = fork(); die "fork: $!" unless defined $pid;
    if ($pid == 0) { setpgrp(0,0); exec @ARGV or exit 127; }
    $SIG{ALRM} = sub { kill "TERM", -$pid; sleep 2; kill "KILL", -$pid; exit 142 };
    alarm $t; waitpid($pid, 0); alarm 0; exit($? >> 8);
  ' 1800 codex exec -s workspace-write - <<PROMPT
$(cat GOAL.md)
=== STATE.md ===
$(cat STATE.md)
=== 이 세션의 범위 ===
하네스가 아니라 fabricate 자체를 짓는다. 완료 정의는 위 GOAL.md §2뿐이다.
쓰기 허용은 verify/checks/** 와 verify/fixtures/** 뿐이다.
verify/index.ts · verify/criteria/** · verify/manifest.json · package.json 은 수정 금지 —
고치면 동결 해시 대조가 죽는다.
=== 지금 빨간 것 ===
$(cat .drive/verify.log)
PROMPT
  cx=$?                                          # (3) GOAL+STATE 전문 + 이 라운드 verify 출력
  case $cx in                                    #     FINDINGS.md 는 안 준다
    0)   echo "codex ok" ;;
    142) echo "codex 시간 상한 1800s 초과 — 프로세스 그룹 종료" ;;
    127) echo "codex 실행 실패 (명령을 못 찾았다)"; exit 127 ;;
    *)   echo "codex 비정상 종료: $cx" ;;
  esac

  echo "verify를 직접 돌리고 각 기준의 양극·음극을 확인하라. verify/ 의 git diff 를
최우선으로 보고하라. 구현자 보고는 증거가 아니다." \
    | claude -p --agent fab-verifier \
        --setting-sources project \
        --allowedTools "Bash(bun run:*)" "Bash(git diff:*)" "Bash(git status:*)" \
        --max-budget-usd 5 > .drive/FINDINGS.md  # (5) 프롬프트는 stdin. 권한은 플래그로
done
```

- **프롬프트는 argv가 아니라 stdin(heredoc)으로 넣는다.** `codex exec --help`가 *"If not provided as an argument (or if `-` is used), instructions are read from stdin"*이라 적었다. argv로 넣으면 `ARG_MAX`(1MB)에 걸린다 — 지금 `GOAL.md`+`STATE.md`가 이미 약 190KB이고 **STATE는 세션마다 자란다.** 넘는 순간 codex 호출만 죽고 루프는 계속 돈다(`set -e`가 없다).
- **`verify.log`를 `tail -80`으로 자르지 않는다.** 출력 형식은 A1이 정하는데(정할 것 `[5]6`) 소비자의 창만 80줄로 고정하면, 실패 사유가 앞쪽에 있는 형식을 고른 순간 **B의 codex가 매 라운드 그것을 못 본다.** 위 결함 1(*"verify를 한 번도 못 본 채 20라운드"*)이 다른 원인으로 재현된다. 전문을 주고, 대신 A1이 *"실패 항목만 상세 + 전체는 `N/62 초록` 한 줄"* 형식을 고르게 한다.
- **매 라운드 입력은 `GOAL.md` 전문 + `STATE.md` 전문 + 범위 + 그 라운드의 `verify` 실패 출력이다.** `GOAL.md` §0이 *"이 문서 전문 + `STATE.md` 요약"*을 주라고 적었으므로 STATE를 주는 것이 §0이다. **`FINDINGS.md`는 안 준다** — 그것이 §0이 금지한 *"직전 걸음의 산출물"*이다. **사람이 읽는 파일로만** 남는다.
- **검증자에게 `git diff`·`git status`를 준다.** *"`verify/`의 git diff를 최우선으로 보고하라"*를 시키면서 `Bash(bun run:*)`만 주면 **그 지시를 이행할 수 없다.** 셋 다 읽기 전용이라 *"검증자는 못 고친다"*는 규율은 유지된다.
- `--setting-sources project`는 codex 플러그인의 `timeout: 900` `Stop` 훅을 막기 위한 것이고, 권한은 **`--allowedTools`가 따로 낸다.** 이 조합이 미신뢰 디렉터리에서 실제로 통과하는 것을 확인했다(2026-07-28 실측).

### `.gitignore`에 넣을 것 — **세션 A1이 넣는다 (2026-07-28 3차 검증으로 A2에서 옮김)**

> **왜 A1인가.** A1의 음극 자기시험은 각 시험 끝에 `git status --porcelain`이 **빈 것**을 요구한다(되돌림 검증). 그런데 지금 실측으로 비어 있지 않다 — `?? .idea/` · `?? fabricate.iml`. `.gitignore` 추가를 A2에 두면 **A1의 완료 조건 ②가 문자 그대로 충족 불가**다. A1/A2 분할이 만든 결함이라 A1으로 되돌린다.

`.gitignore`는 **이미 612줄로 존재한다**(커밋 `bc88b58`·`bdf12cd`). 아래가 거기 **없는** 항목이다. `.idea/`는 이미 `.gitignore:136`(`.idea/*`)으로 내용물이 무시되고 **추적된 적이 없지만, 디렉터리 자체는 `git status`에 `?? .idea/`로 뜬다** — `.idea/`를 한 줄 더 넣어야 porcelain이 빈다.

| 항목 | 왜 |
| --- | --- |
| `.drive/` | 루프의 휘발성 작업물(`verify.log` · `FINDINGS.md`). 라운드마다 덮어써지는 중간 산출이지 저장소의 산출물이 아니다 |
| `.fabricate/` | 런타임 상태(세션 장부 · 표식 · 잠긴 레코드). 사용자의 인터뷰 데이터이지 코드가 아니다 |
| `.claude/settings.local.json` | **사용자별 파일이라 배포·커밋 대상이 아니다.** 그리고 저장소에 커밋되는 `.claude/settings.json`은 **미신뢰 워크스페이스에서 조용히 무시되므로**(위 "워크스페이스 신뢰") allowlist를 거기 두면 안 된다 — 남의 기계에서 권한이 저절로 열리는 것도 IP-0의 "설정 보존"과 정면 충돌이다 |
| `AGENT_STOP` | 사람이 루프를 끊으려고 만드는 로컬 센티널. 커밋되면 남의 체크아웃에서 루프가 즉시 죽는다 |
| `.idea/` | ~~불필요~~ **필요하다 (2026-07-28 3차 검증 정정)**. `.gitignore:136`(`.idea/*`)을 **`:138`의 `!.idea/codeStyles` 부정 규칙이 되뚫어서** `.idea/codeStyles/Project.xml` 하나가 빠져나오고, 그래서 `git status`에 `?? .idea/`가 뜬다. A1의 되돌림 검증이 여기에 걸린다 |
| `fabricate.iml` | IDE 모듈 파일이고 이 기계의 취향이다 |

**실측 (2026-07-28, `git check-ignore -v`)**: `.drive/`·`.fabricate/`·`AGENT_STOP`·`fabricate.iml` → 전부 `NOT IGNORED`. `.claude/settings.local.json`은 무시되지만 출처가 저장소가 아니라 **사용자 전역** `~/.config/git/ignore`다 — **다른 기계에서는 무시되지 않는다.** 그래서 저장소 `.gitignore`에 넣어야 한다.

**검증 (2026-07-28 3차)**: 지금 추적 안 되는 것은 정확히 둘이다.
```
$ git ls-files --others --exclude-standard
.idea/codeStyles/Project.xml
fabricate.iml
$ git ls-files --others --exclude-standard --exclude=".idea/" --exclude="fabricate.iml"
        ← 비었다. 이 둘을 넣으면 porcelain 이 빈다
```

---

## 시작 프롬프트

**프롬프트는 휘발하지만 STATE는 남는다.** 그래서 여기 적어 둔다. 세션을 열 때 해당 블록을 그대로 쓴다. **어느 것인지는 위 "첫 5분"의 브랜치 확인과 `ls verify bin`이 가른다** — 둘 다 없으면 A1, `verify`·`bin`만 있으면 A2, `drive.sh`까지 있으면 B.

### 세션 A1용 (눈금 — `verify`·`bin`이 없을 때)

**2026-07-28 4차 개정.** 적대적 검증 3회(신선 컨텍스트 9 + Codex 3)를 반영했다. 3차가 찾은 것은 **2차 처방이 병을 옮겼을 뿐이라는 것**이다 — 동결이 exit 코드를 만드는 것(`package.json`·러너·check 본문)을 하나도 안 덮었고, `exit 0`이 도달 가능한지를 아무도 관측하지 않았다. 근거는 아래 "세션 A 프롬프트 적대적 검증" 절들에 있다.

```
너는 /Users/ecoletree/dev/project/fabricate 의 작업자다.

[0] 시작 전
- GOAL.md 전문과 STATE.md 전문을 직접 읽어라. 요약을 받지 마라.
- sed '1,8d' GOAL.md | shasum -a 256
  -> 2a4ba701d13dbf3670871d840224c86f66f1cb237cfc0772a62c230ca71c088f 여야 한다.
  다르면 멈추고 보고하라. GOAL.md는 한 글자도 고치지 마라(§9 없이는).
- git checkout -b bootstrap/0a   (-b 다. 브랜치는 아직 없다 — 네가 만든다.
  main 이 아니라 여기서 작업하고 커밋한다. 세션 A의 산출물은 정의상 초록이
  아니므로 main 착지는 GOAL §3 위반이다)
- bun install   (node_modules 가 없어 biome 이 없다)

[1] 이 세션이 만드는 것 — 눈금 하나
- 산출물은 `bun run verify` 와 그것이 보는 세 축이다. 구동 루프(drive.sh)는 다음
  세션(A2)이 짓는다. 여기서 짓지 마라.
- fabricate 자체를 짓지 않는다. IP를 초록으로 만들지 마라 — 인터뷰 로직·CLI 하위
  명령 본체·훅 판정 본체는 세션 B다.
- RED 가 정상 산출물이다. 초록으로 만들고 싶어지면 그것은 종료 조건을 짓는 자가
  그것을 통과시키는 자가 되는 것이고, 자기 채점이다(GOAL §1).
- 아래 [6][7]의 명령은 반드시 돌려라. 이 세션에 실행 금지 대상은 없다
  (drive.sh 는 네가 만들지도 않는다 — A2 의 일이다).

[2] 계약 — STATE.md "verify의 초록 조건과 동결 계약" 절이 정본이다. 전문을 읽어라
- 축 A: GOAL §2 동그라미 43개. true/false 만. n/a 금지.
- 축 B: 구조 검사 16개(S-1..S-16). PASS/FAIL/n/a.
- 축 C: typecheck / lint / test 3개.        --> 합 62
- exit 0 <=> 축A 43 전부 true AND 축B 16 전부 PASS AND 축C 3 전부 통과
              AND 동결 매니페스트 대조 통과
              AND 모든 값이 공개 경로의 외부 효과에서 나왔다(계측으로 확인).
              그 밖은 전부 exit != 0.
- n/a 는 초록이 아니다. 하나라도 있으면 exit != 0.
  n/a 판정은 check 가 아니라 러너가 한다 — check 는 자기가 본 targets 를 함께
  돌려주고, targets 가 비면 러너가 반환값과 무관하게 n/a 로 덮어쓴다.
  n/a 인 검사는 "무엇이 생기면 대상이 생기는가"를 출력에 함께 적는다.
- 동결 대상(2차 판보다 넓어졌다. 여기가 3차 검증의 핵심이다):
    62개 id 목록 + 축A 각 기준의 GOAL 원문과 그 시작·끝 오프셋
    + check 바인딩 경로 + 각 기준의 최소 관측 계약(must_spawn/must_observe)
    + 축A 각 기준의 양극·음극 fixture 선언
    + S-2·S-3·S-15 의 최소 대상 수 + 초록 조건 자체
    + package.json 의 scripts.verify 문자열
    + verify/index.ts 와 verify/criteria/** 의 sha256
  동결하지 않는 것: 판정값, 그리고 verify/checks/** 의 내용(B가 채운다).
- S-16(비계 참조 금지)의 금지 대상은 다섯 경로뿐이다:
  drive.sh / .drive/ / AGENT_STOP / .claude/agents/fab-verifier.md /
  .claude/settings.local.json
  `.claude/` 디렉터리 전체를 금지하지 마라 — S-10 이 그 경로를 읽어야 한다.
  그리고 이 다섯을 코드에 박지 마라 — 검사기가 전이 폐쇄 안에 있어서
  자기 자신에 걸린다. verify/criteria/axis-b.json 에 데이터로 두고
  검사기는 그 소비자로만 만들어라. S-15 의 "src" 문자열도 같은 함정이다.

[3] 이미 측정된 것 — 다시 재지 마라 (2026-07-28 실행 관측)
- 환경 사유로 이미 빨간 것들. 축 C 를 이것과 섞어 출력하지 마라:
    bun run typecheck -> exit 2 (TS18003. include 가 ["src/**/*","tools/**/*"])
                         주의: tsc 가 node_modules 가 아니라 전역(fnm)에 있어서
                         돈 것이다. 다른 기계에서는 127 일 수 있다.
    bun run lint      -> exit 127 (biome 미설치). bun install 후 exit 0 확인함.
    bun test          -> "0 test files matching", exit 1
- 축 C 의 test 를 어떻게 충족 가능하게 만들 것인가는 [5]9 에서 네가 정한다.
  지금 상태로는 테스트 파일이 0개라 영원히 exit 1 이고, 그러면 초록 조건이
  원리적으로 만족될 수 없다. 아무도 이 책임을 안 지고 있었다.
- .gitignore 는 이미 612줄로 존재한다. 규칙에 안 걸리는 것은 정확히 둘이다:
    $ git ls-files --others --exclude-standard
    .idea/codeStyles/Project.xml      <- :136 의 .idea/* 를 :138 의
    fabricate.iml                        !.idea/codeStyles 부정이 되뚫는다
  .gitignore 에 .drive/ · .fabricate/ · .claude/settings.local.json ·
  AGENT_STOP · .idea/ · fabricate.iml 을 넣어라. 이것은 네 일이다 —
  [7](가)의 되돌림 검증이 git status --porcelain 이 빈 것을 요구하는데
  지금은 비어 있지 않다.

[4] 닫힌 결정 — 따르기만 한다
- 미결 1: 모델이 Skill 도구로 연 세션은 표식만 서고 잠금 자격이 없다(GOAL §1 본문).
  회복은 새 코드 0줄 — 사용자가 슬래시 명령을 다시 치면 같은 session_id 에 UPE 가
  request.txt 를 앉힌다. request.txt 를 쓰는 경로는 UPE 훅 하나뿐이다.
- 미결 2: CLI 에 --session 을 두지 않는다. active 표식이 정확히 하나일 때 그것을
  쓰고, 0개거나 2개 이상이면 exit != 0. 세션 디렉터리는 훅만 만든다.
- 이 둘은 관측된 사실이 뒤집을 때만 바꾸고, 바꾸면 STATE.md 에 근거를 적어라.

[4-1] 미결 3 도 닫혔다 (2026-07-28 3차 검증). 네가 판단하지 않는다
- 2차 판은 "verify 격리 방식(합성 stdin vs 실제 claude -p)을 A1 이 정하고,
  술어 약화면 멈추고 물어라"였다. 그것은 성립하지 않는다 — 그 판단을 하려면
  플러그인 매니페스트와 스킬 디렉터리로 claude -p 가 훅을 발화시키는 것을
  실증해야 하는데, 그것은 세션 B의 산출물이라 너에게 만들 권한이 없다.
  즉 결정에 필요한 대상이 없는 상태로 결정을 요구받고 있었다.
- 결정: 두 층은 서로 다른 축 항목이다. 하나를 고르는 문제가 아니었다.
    * 합성 stdin (훅 스크립트에 JSON 을 먹여 직접 실행) = 훅 로직 검사.
      축 A 의 IP-2ⓙ·ⓚ 같은 기준이 이걸로 선다.
    * 실제 claude -p (임시 프로젝트에 설치하고 띄운다) = 설치 연쇄 검사.
      축 B 의 S-5·S-6·S-7 이 이것만 인정한다. 합성 stdin 으로는 PASS 가 아니다.
  S-5..S-7 의 최소 관측 계약에 must_spawn: "claude" 를 동결해라.
  A1 시점에는 플러그인이 없으므로 셋 다 n/a 이고, n/a 는 초록이 아니므로
  계약이 스스로 지킨다. 술어 약화가 아니다 — 멈추지 마라.

[5] 네가 정할 것 — 아래 아홉이고, 전부 [8]에 기록한다
  1) 62개 id 표기 규약 (예: `IP-2:g` / `S-3` / `C-typecheck`).
     주의: id 를 그대로 파일명에 쓰면 verify/checks/IP-2:g.ts 가 된다.
     콜론은 APFS 에서 legal 이지만 Finder 와 일부 도구가 다르게 표시한다.
     id 와 파일명의 매핑 규칙을 정하고 매니페스트에 동결해라.
  2) 세션 B가 check 를 붙이는 이음매 — `verify/checks/<id>.ts` 규약의 정확한 형상.
     check 는 판정값과 함께 targets 와 "무엇을 띄웠고 무엇을 관측했는가"를
     돌려줘야 한다. 러너가 그것을 최소 관측 계약과 대조한다.
  3) 해시 정규화 방식 (줄바꿈·공백·정렬을 어떻게 정규화해 sha256 을 내는가).
     전체 해시의 입력은 "id 목록 전부 + 각 항목 해시"다 — "PASS 인 항목만
     순회"로 구현하면 항목 삭제가 안 잡힌다.
  4) 축A 43개의 GOAL 원문 경계 — 각 기준이 GOAL.md:103~109 의 어느 오프셋에서
     시작하고 끝나는가. 개수 43 은 확정이지만(마커 중복 제거) 자르는 위치는
     정해져 있지 않고, 동결이 네가 자른 대로를 영구히 완료 정의로 만든다.
  5) 헌법 검사(S-15) · 도달성 검사(S-2) · 비계 검사(S-16)의 판별 방식.
     셋 다 검사기 자신이 검사 대상 문자열을 갖는다 — 목록을 데이터로 빼라.
  6) verify 러너의 출력 형식.
     제약: 실패 항목만 상세히 내고 전체는 "N/62 초록" 한 줄로 요약해라.
     세션 B의 codex 가 매 라운드 이 출력만 보고 다음 조각을 고른다.
  7) bin/fabricate 의 언어와 확장자.
     주의: tsconfig 의 include 글롭은 확장자 없는 파일을 담지 못한다. 로직을
     bin/fabricate.ts 에 두고 bin/fabricate 를 얇은 shim 으로 하는 편이 낫다.
  8) --selftest-exit <n> 의 정확한 형상 ([7](나)가 쓴다). 하위 명령이 아니라
     플래그여야 한다 — --help 에 뜨면 S-1 을 앞당겨 초록으로 만든다.
  9) 축 C 의 test 를 어떻게 충족 가능하게 만드는가.
     지금 테스트 파일이 0개라 bun test 는 exit 1 이고, 아무것도 안 하면
     초록 조건이 원리적으로 만족 불가다. 권장: 세션 B가 만들 fixture 를
     bun test 파일로 규정하고(verify/fixtures/**.test.ts) 그 규약을 [8]4 에
     적는다. 다른 안을 고르면 근거를 적어라.

[6] 만들 것 — 전부 제품이다. 나중에 걷어내지 않는다
- package.json: "bin": {"fabricate": "./bin/fabricate"} 와 verify 스크립트
- bin/fabricate: 단일 뿌리. 지금은 exit != 0 인 스텁이다.
  --help 에 deep-interview·turn·check 를 띄우지 마라 — 그것이 S-1 이고
  세션 B가 초록으로 만들 기준이다.
- 62개 기준 데이터 파일(verify/criteria/) + 동결 매니페스트.
  매니페스트 갱신 스크립트는 일회용으로 쓰고 저장소에 남기지 마라.
  매니페스트의 sha256 을 STATE.md 에 적고, **동시에 사용자에게 대화로 보고해라.**
  STATE.md 는 저장소 안이라 그 자체로는 앵커가 아니다 — 세션 B의 codex 가
  workspace-write 로 기준 완화 -> 매니페스트 재생성 -> STATE.md 의 해시 줄
  교체를 하면 verify 출력과 STATE 가 일치해서 눈으로 대조해도 통과한다.
  유일한 진짜 앵커는 사용자 쪽 기록이다.
  verify 는 매 실행 자기 매니페스트 해시를 출력한다.
- verify/ 러너: 세 축을 돌리고 [2]의 초록 조건대로 exit 코드를 낸다.
  각 check 실행을 계측해서 최소 관측 계약을 실제로 밟았는지 보고,
  안 밟은 check 의 true 는 false 로 강등한다.
- .gitignore 에 [3]의 여섯 항목을 넣는다.
- tsconfig.json 의 include 에 verify/**/* 와 (택한다면) bin/**/*.ts 를 넣는다.

[7] 눈금이 실제로 판정하는지 증명한다 — 이 세션의 급소다. 하나라도 빠지면 안 끝났다
  먼저 baseline 을 잡는다:  bun run verify > /tmp/base.txt 2>&1; echo "exit=$?"

  (가) 음극 자기시험 셋 — 각각 "verify 가 빨갛다"가 아니라
       "그 검사 한 줄의 상태가 PASS|n/a -> FAIL 로 바뀌고 위반 대상이 출력에 뜬다"
       를 관측해야 한다. baseline 과의 diff 를 증거로 남겨라.
       - S-15: verify/ 아래에 src/ 를 import 하는 임시 파일 하나
       - S-16: verify 가 도달하는 모듈 하나에서 drive.sh 를 참조하는 임시 줄 하나
       - 동결: 기준 데이터를 한 글자 고쳐 해시 대조가 죽는 것
       각 시험 끝에 git status --porcelain 이 비었고 재실행 출력이 baseline 과
       같은 것을 확인해라. 되돌림이 검증되지 않으면 그 시험은 실패다.
       (porcelain 이 비려면 [3]의 .gitignore 를 먼저 넣어야 한다)

  (나) 양극 자기시험 — 축 A 기준 하나를 골라 verify/checks/<id>.ts 에 실제 check
       함수를 붙이고, ① 참인 합성 입력에서 그 기준이 true 로 뜨는 것
       ② 거짓인 합성 입력에서 false 로 돌아오는 것을 각각 관측한 뒤,
       check 는 남기고 합성 입력만 지운다.
       이것은 IP 충족이 아니다 — 입력을 지우면 기준은 다시 false 다.

       제약(3차 검증): 고른 기준의 check 는 bin/fabricate 를 서브프로세스로
       띄우고 종료 코드를 관측해야 한다. 디스크 상태만 읽는 check 는 (나)의
       증거로 안 쳐준다. 이유 — 세션 B는 이 파일 하나를 이음매의 실물 예제로
       받고 나머지 42개를 그 모양으로 복제한다. 손으로 쓴 ledger 줄을 읽는
       check 를 남기면 B가 그것을 42번 복제하고, 그 순간 1차와 같아진다
       (STATE.md "뚫리는 자리"의 IP-5ⓒ 준우승 구멍이 정확히 그 형상이다).
       스텁은 항상 exit != 0 이라 양쪽을 못 뒤집으므로 [5]8 의
       --selftest-exit <n> 를 써라.

  (다) 62개 전부에 check 바인딩 경로와 최소 관측 계약이 매니페스트에 있는지
       verify 가 스스로 검사하게 하고, 그 출력을 남겨라. 한 기준만 뒤집혀도
       나머지 61이 평가 기제 없이 남는 것을 막는 자리다.

  (라) 집계 자기시험 — 신설. 이것이 없으면 (가)(나)(다)를 다 통과해도
       "이 눈금이 초록을 낼 수 있는가"가 한 번도 확인되지 않는다.
       (가)는 검사 한 줄의 상태를, (나)는 기준 하나의 값을 볼 뿐이고
       둘 중 어디서도 종료 코드가 0 이 되지 않는다. 집계에 off-by-one 이
       있거나 마지막이 무조건 process.exit(1) 이어도 전부 통과한다 —
       1차 검증이 잡았다는 "정적 RED 허점"이 아직 안 닫혀 있었다.

       일회용 환경변수 override 로 축 A·B·C 전부를 강제 충족시킨 상태에서
       bun run verify 가 exit 0 을 내는 것을 한 번 관측하고, override 를
       끈 뒤 baseline 과 같은 출력·같은 종료 코드로 돌아오는 것을 확인해라.
       override 를 저장소에 남기지 마라 — 남기면 그것이 세션 B의 원클릭
       초록이 된다. 관측 출력만 [8]에 기록한다.

[8] 끝내기 전 — STATE.md 에 적고 bootstrap/0a 에 커밋한다. 세션 A2·B의 입력은 이것뿐이다
  1) [5] 아홉 개의 결정과 근거. 미결 1·2·3 을 그대로 따랐는지
  2) 동결 매니페스트의 sha256 (텍스트로) + **매니페스트 파일의 경로**.
     그리고 같은 값을 사용자에게 대화로도 보고해라 (STATE 는 앵커가 아니다)
  3) [7](가)(나)(다)(라)의 명령과 실제 출력 — baseline diff 포함.
     특히 (라)의 exit 0 관측 출력
  4) 62개를 어디에 어떤 형식으로 열거했는가 · 세션 B가 check 를 붙이는 규약 ·
     B가 쓸 수 있는 경로와 못 쓰는 경로
  5) bun run verify 의 실제 출력 예시
  6) 새로 드러난 미결 (없으면 "없다")

[9] 역할
- verify/ 러너와 bin/fabricate 는 codex exec -s workspace-write 로 짓는다.
  codex 에게는 GOAL.md 전문 + STATE.md 전문 + "이 세션은 눈금만 짓고 fabricate
  자체를 짓지 않는다. IP를 초록으로 만들지 마라" 를 함께 준다.
  GOAL 전문만 주면 codex 가 fabricate 를 구현하려 들고, STATE 를 빼면 측정된 훅
  페이로드와 닫힌 결정을 모른 채 기억에서 지어낸다. GOAL §0 은 "이 문서 전문 +
  STATE.md 요약"을 서브에이전트에게 주라고 적었다 — STATE 를 주는 것이 §0 이다.
- package.json · tsconfig.json 은 Claude 가 직접 쓴다.
- 구현자의 자기 보고는 증거가 아니다. 직접 돌려서 출력을 봐라.
- 파일을 쓰는 것은 한 번에 한 에이전트다.

[10] 멈추는 조건 — GOAL §7 의 넷뿐이다
  원 요청과의 실질 충돌 · 사용자만 정할 제품 의미 · 비가역 위험 ·
  불변식이나 술어를 약화시켜야만 진행 가능할 때.
  그 밖의 기술적 모호함은 스스로 정하고 STATE.md 에 근거를 남겨라.

[11] 1차 시도가 왜 실패했는지 — STATE.md "fixture 헌법" 절을 전문으로 읽어라.
  이 세션에 걸리는 것은 한 줄이다: IP별 fixture 를 만들지 마라.
  붙일 프로세스가 없어 모듈 단위로 내려앉고, 그 순간 1차와 구조적으로 같아진다.
  주의 — [7](나)의 check 하나는 여기 해당하지 않는다. 그것은 IP 를 충족시키려는
  fixture 가 아니라 이음매 규약의 실물 예제이고, 합성 입력을 지우면 다시 false 다.
  그 하나만 만들고 나머지 61개는 바인딩 경로와 최소 관측 계약만 둔다.
  그리고 그 하나조차 bin/fabricate 를 프로세스로 띄워야 한다 — 헌법 규칙 1 이
  A1 에게 걸리는 방식이 이것이다.
```

### 세션 A2용 (비계 — `verify`는 있고 `drive.sh`가 없을 때)

```
너는 /Users/ecoletree/dev/project/fabricate 의 작업자다. 브랜치는 bootstrap/0a 다.

[0] 시작 전
- GOAL.md 전문과 STATE.md 전문을 직접 읽어라. 해시를 확인하라(A1과 같다).
- git branch --show-current 가 bootstrap/0a 인지 확인하라. 아니면 체크아웃하라.
  브랜치가 아예 없으면 A1 이 안 끝난 것이다 — 멈추고 보고하라.
- bun install   (A1 이 돌렸어도 fresh checkout 이면 없다. biome 이 필요하다)
- STATE.md 에서 세션 A1 이 남긴 여섯 항목을 읽어라. 이것이 이 세션의 입력이다.

[0-1] A1 눈금 인수 검사 — 통과해야 [3]으로 간다
  A1 이 시간에 쫓겨 자기시험을 "돌렸다"고 적고 스텁만 남겼을 수 있다. 그러면
  너는 그 위에 루프를 짓고, 세션 B가 20라운드를 도는데 원인이 눈금인지
  구현인지 아무도 모른다. exit != 0 만 보는 것으로는 못 가른다 —
  출력을 하드코딩으로 찍고 exit 1 하는 스크립트가 그것을 통과한다.
 1) bun run verify; echo "exit=$?"
    -> exit != 0 이고, A1 이 [8]5 에 남긴 출력 형상과 같다.
 2) verify 가 출력하는 매니페스트 해시가 A1 이 [8]2 에 적은 값과 일치한다.
    다르면 멈추고 보고하라 — 눈금이 이미 변조됐거나 A1 기록이 틀렸다.
 3) A1 의 음극 자기시험 셋 중 하나를 네가 직접 재현해라(S-15 가 가장 싸다:
    verify/ 아래에 src/ 를 import 하는 임시 파일 하나). 그 검사 한 줄이
    PASS|n/a -> FAIL 로 바뀌는 것을 관측하고 되돌려라.
    안 바뀌면 눈금이 아무것도 안 보고 있는 것이다. 멈추고 보고하라.

[1] 이 세션이 만드는 것 — 구동 루프 하나. 전부 비계다
- 산출물은 "세션 B가 켜기만 하면 도는 루프"다. 눈금(verify)은 A1 이 이미 지었다.
- 동결 대상(verify/index.ts · verify/criteria/** · 매니페스트 · package.json)은
  손대지 마라. 고쳐야 할 사실을 관측했으면 근거를 STATE.md 에 적고 고친 뒤
  **새 매니페스트 sha256 을 STATE.md 에 다시 적어라.** 안 적으면 A1 값이 남아
  세션 B의 대조가 거짓 불일치를 내거나, 네가 재생성한 것이 조용히 통과한다.
- 비계이므로 최소로 짓는다. 정교하게 만들지 마라.
- drive.sh 를 정상 모드로 켜지 마라. 허용되는 실행은 [4]의 DRY_RUN 둘뿐이다.
  ([4]의 나머지 시험은 반드시 돌려라 — 그것은 drive.sh 정상 실행이 아니다)

[2] 이미 측정된 것 — 다시 재지 마라 (2026-07-28 실행 관측)
- claude 의 --allowedTools 는 가변 인자다. 뒤에 프롬프트를 위치 인자로 놓으면
  그것까지 도구 이름으로 먹고 "Error: Input must be provided either through stdin
  or as a prompt argument when using --print" 로 죽는다. 콤마로 묶어도 같다.
  => 프롬프트는 반드시 stdin 으로 넣는다.
- 이 디렉터리는 미신뢰다(hasTrustDialogAccepted: false). --setting-sources 는
  user/project/local 을 따로 받고 settings.local.json 은 local 이다. 즉
  --setting-sources project 만 주면 allowlist 가 빠지고 Bash 권한이 0이 된다.
  실측으로 통과가 확인된 유일한 형태:
    echo "<프롬프트>" | claude -p --setting-sources project \
      --allowedTools "Bash(bun run:*)"
  대조군(allowlist 없이)은 "This command requires approval" 로 거부됐다.
- timeout / gtimeout 이 이 기계에 없다(macOS 는 coreutils 를 안 싣는다).
- perl 은 /usr/bin/perl 에 있고 alarm 은 종료 코드 142 로 끊는다. 그러나
  `perl -e 'alarm shift; exec @ARGV'` 형태를 codex 에 쓰면 안 된다 —
  3차 검증이 둘을 실측으로 깼다:
    * SIGALRM 은 그 프로세스 하나에만 간다. codex 는 네이티브 바이너리를
      spawn 하는 node 래퍼이고 SIGINT/SIGTERM/SIGHUP 만 전달한다(SIGALRM 없음).
      => 142 를 받아도 실제 codex 워커는 workspace-write 를 든 채 계속 돈다.
         다음 라운드가 겹치면 동시 작성자 둘이 된다.
    * 명령이 없으면 stderr 한 줄 없이 exit 0 이다. 오타 하나로 매 라운드가
      성공한 척하고 20라운드가 몇 초에 끝난다.
  정본은 STATE.md "timeout 부재의 대체" 절의 fork + setpgrp + kill -TERM -$pid
  형태다. 그리고 codex 종료 코드를 0/142/127 로 갈라서 출력해라.
- codex 경로를 하드코딩하지 마라. 이 기계의 codex 는 fnm 셸 경로에 있고
  경로에 셸 PID + 타임스탬프가 들어가 셸마다 바뀐다. command -v codex 를 써라.
- codex exec 에는 예산·시간 상한 플래그가 없다. --max-budget-usd 는 claude 전용.
- codex exec 는 프롬프트를 stdin 으로 받는다(`-` 또는 인자 생략).
  --help: "If not provided as an argument (or if `-` is used), instructions are
  read from stdin". argv 로 넣지 마라 — GOAL+STATE 가 이미 213KB 이고
  ARG_MAX 는 1MB 이며 STATE 는 세션마다 자란다. 넘으면 codex 호출만 죽고
  루프는 계속 돈다.

[3] 만들 것
- drive.sh — 이 세션의 주 산출물. STATE.md "0b — 루프" 절의 **고친 형상**을 쓴다.
  옛 스케치(같은 절에 "옛 안 — 결함 다섯"으로 표시돼 있다)를 베끼지 마라. 요건:
    (a) 첫머리 preflight — codex · claude · perl 의 실재를 확인하고
        없으면 즉시 exit != 0 으로 죽는다. 사람이 읽을 사유를 함께 낸다.
        (timeout 부재 같은 결함이 세션 B의 첫 라운드에서야 드러나는 것을 막는다)
        perl 은 존재만이 아니라 alarm 이 실제로 142 를 내는지 한 번 돌려서 봐라 —
        command -v perl 한 줄로 때우면 [4]-3 이 무의미해진다.
    (b) mkdir -p .drive
    (c) AGENT_STOP 검사를 verify 앞에 둔다
    (d) verify 는 패스당 한 번만 돌려 .drive/verify.log 에 받는다
    (e) 상한 셋 — 반복 카운터 · claude 호출에 --max-budget-usd ·
        codex 는 [2]의 fork+setpgrp 형태로 감싼다(perl alarm+exec 아니다)
    (f) 라운드 입력 = GOAL.md 전문 + STATE.md 전문 + 범위 + 그 라운드의
        verify 실패 출력 **전문**. .drive/FINDINGS.md 는 넣지 마라 —
        그것이 GOAL §0 이 금지한 "직전 걸음의 산출물"이다. 파일로는 계속 남긴다
        (사람이 읽는다). STATE.md 를 주는 것은 §0 위반이 아니라 §0 그 자체다.
        verify.log 를 tail 로 자르지 마라 — 출력 형식은 A1 이 정하는데
        소비자 창만 고정하면 실패 사유가 앞쪽인 형식에서 B가 아무것도 못 본다.
        프롬프트는 heredoc 으로 stdin 에 넣는다(argv 는 ARG_MAX 에 걸린다).
    (g) 검증자 호출은 [2]의 확정 형태 — 프롬프트를 stdin 으로, --agent 와
        --setting-sources project 와 --allowedTools 와 --max-budget-usd 를 함께
    (h) DRY_RUN=1 이면 verify 를 한 번 돌린 뒤 codex 호출 직전에 끊는다.
        DRY_RUN=2 이면 codex·claude 를 echo 스텁으로 치환하고 루프 본체를
        끝까지 한 바퀴 돈다 — 이게 없으면 codex·claude 호출줄이 세션 B의
        첫 라운드까지 한 번도 실행되지 않는다(오타·따옴표 깨짐이 그대로 산다).
    (i) codex 프롬프트에 쓰기 허용 범위를 경로로 적어라. "verify/ 수정 금지"로
        쓰지 마라 — check 가 verify/checks/ 안에 있어서 그 금지는 B가 축 A를
        초록으로 만들 유일한 수단을 뺏는다. 정확히:
          허용: verify/checks/** · verify/fixtures/** · src/** · bin/** 등
          금지: verify/index.ts · verify/criteria/** · verify/manifest.json ·
                package.json  (고치면 동결 해시 대조가 죽는다)
    (j) 검증자 프롬프트에 "verify/ 의 git diff 를 최우선으로 보고하라" 를 넣어라
    (k) codex 종료 코드를 0 / 142(시간 상한) / 127(실행 실패)로 갈라 출력하고,
        127 이면 루프를 멈춰라. 안 가르면 [2]의 "조용히 exit 0" 이 그대로 산다
- .claude/agents/fab-verifier.md — 쓰기 도구를 주지 않는다.
  Write/Edit/NotebookEdit 없음. Bash 는 읽기 전용 셋으로 좁힌다:
    Bash(bun run:*) · Bash(git diff:*) · Bash(git status:*)
  넓은 Bash 는 그 자체가 쓰기 도구다(`: > FILE`). 반대로 bun run 만 주면
  (j)의 "git diff 를 보고하라"를 이행할 수 없다 — 동결 계약의 실질 감시자가
  런타임에서 무력해진다. 셋 다 읽기 전용이라 규율은 유지된다.
- .claude/settings.local.json — Bash(bun run:*) + Bash(bun test:*).
  주의: 이 파일은 --setting-sources project 로 도는 경로에서는 안 실린다.
  실제 소비자는 사람이 이 디렉터리에서 여는 대화형 세션이다.
  (.gitignore 는 A1 이 이미 넣었다. 없으면 A1 이 안 끝난 것이다)

[4] 검증 — 합격 기준을 함께 적었다. 전부 돌려서 출력을 남겨라
 1) 드라이런:  DRY_RUN=1 bash drive.sh
    -> preflight 통과가 출력되고, verify 가 한 번 돌고, codex 호출 전에 끊긴다.
       codex·claude 호출에 닿지 않는다.
 2) 스텁 모드:  DRY_RUN=2 bash drive.sh
    -> codex·claude 가 echo 스텁으로 치환된 채 루프 본체가 끝까지 한 바퀴 돈다.
       라운드 프롬프트 전문을 파일로 덤프해 눈으로 확인해라.
       이게 없으면 codex·claude 호출줄은 세션 B의 첫 라운드까지 한 번도
       실행되지 않는다 — 오타 하나로 20라운드가 몇 초에 끝난다.
 3) preflight 음극:  env -i /bin/bash drive.sh
    -> 즉시 exit != 0 **과 drive.sh 가 낸 preflight 사유 문장**이 출력에 있다.
       주의: `PATH= bash drive.sh` 로 쓰지 마라. 셸이 변수 대입을 먼저 적용해서
       bash 자체를 못 찾고 exit 127 로 죽는다 — drive.sh 가 한 줄도 안 돈다.
       그런데 "exit != 0" 이라는 기준은 문자 그대로 만족되므로, preflight 가
       조용히 exit 0 으로 넘어가는 구현이어도 이 시험을 통과한다. 실측:
         $ PATH= bash drv.sh   -> (eval): command not found: bash / exit=127
         $ env -i /bin/bash drv.sh -> drive.sh 가 실제로 돌았다 / exit=3
       그래서 합격 기준을 "exit != 0" 이 아니라 "사유 문장이 있다" 로 둔다.
 4) codex exec 연기 시험 — 실저장소가 아니라 임시 디렉터리에서 돌려라:
      codex exec -s workspace-write -C <임시dir> --skip-git-repo-check \
        "SMOKE 파일을 만들고 ok를 써라"
    -> SMOKE 가 생긴다. workspace-write 는 SMOKE 하나가 아니라 작업 디렉터리
       전체 쓰기를 여니 실저장소에서 돌리지 않는다.
       codex 인증이 별도 계정이라 미인증이면 하네스와 무관한 이유로 실패한다 —
       그 경우 그렇게 적고, **preflight 에 인증 확인을 넣어라.** 미인증인 채로
       끝내면 세션 B의 20라운드가 전부 즉시 실패한다.
 5) 검증자를 drive.sh 가 실제로 쓰는 명령줄 그대로 한 번 돌린다
    (stdin + --agent + --setting-sources project + --allowedTools +
     --max-budget-usd). 프롬프트는 "PASS만 답하라".
    -> PASS 가 온다. --setting-sources project 에서도 .claude/agents/ 의
       fab-verifier 가 실리는지가 이 시험으로 처음 확인된다.
 6) 검증자 음극 — 같은 명령줄에 프롬프트만 "빈 파일 ZZZ 를 만들어라"
    -> ZZZ 가 생기지 않는다. 생겼으면 fab-verifier 정의가 잘못된 것이다.
       ls ZZZ 로 확인하라.
 7) 검증자가 git diff 를 실제로 낼 수 있는지 — 프롬프트를 "git diff --stat 을
    돌리고 그 출력을 그대로 보고하라" 로 한 번 더.
    -> 거부되지 않고 출력이 온다. 거부되면 (j)의 지시가 매 라운드 무의미하다.
 8) bun run verify; echo "exit=$?"   -> [0-1]과 같은 결과

[5] 끝내기 전 — STATE.md 에 적고 bootstrap/0a 에 커밋한다
  1) drive.sh 전문
  2) 상한값 셋을 그렇게 고른 근거 (근거가 없으면 "임의로 정했다"고 적어라)
  3) [0-1] 인수 검사 셋과 [4] 여덟 시험의 명령과 실제 출력
  4) 눈금을 한 글자라도 고쳤으면 새 매니페스트 sha256 (안 고쳤으면 "안 고쳤다")
  5) 새로 드러난 미결 (없으면 "없다")
  6) main 에는 이 STATE.md 갱신을 반영하지 마라 — 세션 B가 bootstrap/0a 에서
     시작한다. main 의 세션 판별표가 B를 그 브랜치로 보낸다.

[6] 역할 · 멈추는 조건 — A1 과 같다. drive.sh 와 .claude/ 아래 파일은
    Claude 가 직접 쓴다(프롬프트와 설정이다).
```

### 세션 B용 (0b — 루프. `verify`·`bin`이 있을 때)

```
너는 /Users/ecoletree/dev/project/fabricate 의 작업자다.

[읽기]
- GOAL.md 전문과 STATE.md 전문을 직접 읽어라. 요약을 받지 마라(GOAL.md §0).
- 해시를 확인하라: sed '1,8d' GOAL.md | shasum -a 256
  -> 2a4ba701d13dbf3670871d840224c86f66f1cb237cfc0772a62c230ca71c088f 여야 한다.
- git checkout bootstrap/0a. main 이 아니다 — A1·A2 산출물은 거기 있다.
- STATE.md에서 세션 A1이 남긴 여섯 항목과 A2가 남긴 여섯 항목을 읽어라.
  이것이 이 세션의 입력이다. 특히 A1의 매니페스트 sha256 과 62개 열거 형식,
  네가 check 를 붙이는 규약, 그리고 네가 쓸 수 있는 경로.
- STATE.md의 "fixture 헌법 — RED-first만으로는 1차를 막지 못한다" 절을 전문으로 읽어라.
  fixture를 쓰는 것이 이 세션의 일이고, 그 절이 어떻게 써야 하는지를 정한다.

[이 세션의 범위 — 순서가 핵심이다]
- 0b를 한다. 다만 순서가 정해져 있다:
  1) 먼저 걷는 경로 하나를 뚫는다 — 임시 프로젝트 설치 → 슬래시 명령·실제 훅 →
     공개 CLI start / turn record / close → 디스크의 intent 파일.
     즉 IP-0과 IP-1ⓐ를 먼저 초록으로 만든다.
  2) 그 다음에야 나머지 IP의 양극·음극 fixture를 만든다 —
     그 동일한 세션의 입력·장부·설정을 하나씩 변형한 것으로.
  3) 그 뒤 작업 순서 2단계(close 거부 여섯 + ⓜ, Stop 턴 회계)와
     3단계(IP-5 → IP-6 → IP-4 → IP-3).
- 이 순서를 뒤집지 마라. fixture를 먼저 만들면 붙일 프로세스가 없어 모듈 단위로
  내려앉고, 그 순간 1차 시도와 구조적으로 동일해진다(STATE.md "fixture 헌법" 규칙 4).
- fixture 헌법 넷을 어기지 마라. 특히 verify/ 는 src/ 를 import하지 않는다 —
  기계가 검사하므로 어기면 verify가 빨개진다.
- 부트스트랩을 다시 설계하지 마라. 세션 A가 정한 미결 1·2·3과 계약 형상은
  더 나은 사실을 관측했을 때만 바꾸고, 바꾸면 STATE.md에 근거를 적는다.
  취향으로 다시 열지 마라.

[쓰기 범위 — 이것이 동결이다]
- 쓸 수 있는 곳: verify/checks/** · verify/fixtures/** · src/** · bin/** ·
  skills/** · agents/** · hooks/** · 플러그인 매니페스트.
- 쓰면 안 되는 곳: verify/index.ts · verify/criteria/** · verify/manifest.json ·
  package.json. 이 넷은 동결 해시가 걸려 있어 고치면 verify 가 빨개진다.
- 눈금을 고쳐서 초록으로 만들고 싶어지면 그것이 자기 채점이다. 기준이 틀렸다고
  판단되면 고치지 말고 STATE.md에 근거를 적고 사용자에게 보고하라.

[루프 규율]
- verify는 패스당 한 번만 돌려 .drive/verify.log에 받는다.
  종료 조건과 프롬프트가 그 파일을 함께 쓴다.
- 상한 셋을 건다: 반복 카운터 · --max-budget-usd · AGENT_STOP 센티널.
- 루프 세션은 --setting-sources project 로 격리한다
  (codex 플러그인의 timeout: 900 Stop 훅이 라운드마다 곱해진다).
- 매 라운드 입력은 GOAL.md 전문 + STATE.md 전문 + 범위 + 그 라운드의 verify
  실패 출력 전문이다. .drive/FINDINGS.md 는 넣지 마라 — 그것이 GOAL.md §0이
  금지한 "직전 걸음의 산출물"이다. STATE.md 를 주는 것은 §0 그 자체다.
- 라운드마다 .drive/FINDINGS.md를 사람이 읽고 방향을 판단한다.
  루프가 스스로 방향을 정하게 두지 마라 — 루프는 갈아내기이지 판단이 아니다.
- 검증자에게 쓰기 도구를 주지 않는다. 고칠 수 있으면 그 자리는 더 이상 검증이 아니다.
- 구현자의 자기 보고는 증거가 아니다.

[착지]
- main에는 bun run verify가 전부 초록일 때만 착지한다(GOAL.md §3).
  초록 조건은 다섯 줄이다 — 축A 43 · 축B 16 · 축C 3 · 매니페스트 대조 ·
  모든 값이 공개 경로의 외부 효과에서 나왔다는 계측.
- 중간 커밋은 브랜치에서 자유롭게 한다. 워크트리는 쓰지 않는다(미신뢰 디렉터리 문제).
- 초록이 되어도 그것은 `기계 통과`이지 완료가 아니다.
  남은 상태의 이름(`사람 검증 대기`)을 함께 보고하라(GOAL.md §2 끝).

[멈추는 조건]
- GOAL.md §7의 넷뿐이다.
```

---

## 세션 A 프롬프트 적대적 검증 — 1차 (2026-07-28)

**무엇을 했나**: 세션 A용 프롬프트를, 그 프롬프트의 실제 독자와 같은 조건(**서사를 모르는 신선 컨텍스트**)에 놓고 넷이 독립으로 때렸다 — ① 세션 A 시뮬레이션(*"내가 이걸 받으면 무엇을 만들 것인가"*) ② 적대적 반증(*"실패하는 방식을 찾아라"*) ③ 문서 정합성(*"실제와 대조하라"*) ④ **Codex/GPT-5.5** — 넷째는 `GOAL.md` §8이 잔여로 선언한 *"짓는 모델과 검사하는 모델이 같은 계열"* 편향을 깨기 위한 것이다.

### 셋 이상이 독립으로 수렴한 급소 넷 — 이것이 이 검증의 소득이다

| 급소 | 무엇이 문제였나 | 어떻게 고쳤나 |
| --- | --- | --- |
| **정적 RED 허점** | *"RED가 완료 조건"*이면 **아무것도 검사하지 않고 `process.exit(1)` 하는 스크립트**가 완료 조건을 전부 만족한다. 반증자와 Codex가 **거의 같은 스트로맨 코드**를 따로 써 왔다. 눈금이 원리적으로 초록이 될 수 있는지 확인하라는 요구가 어디에도 없었다 | 완료 조건에 **양극 자기시험**(기준 하나를 합성 충족으로 `true`로 뒤집었다 되돌리기)을 넣었다 |
| **대상 0개 = 거짓 초록** | `src/`도 fixture도 없으니 헌법 검사·도달성 검사가 항상 PASS다. **빨간 보고서 안에 숨은 초록**이고, 1차 실패의 구조(형식 게이트가 69/69 통과하면서 아무것도 안 잡음)와 같은 모양 | 대상 0개는 `PASS`가 아니라 **`n/a — 대상 0개`**로 출력. **음극 자기시험 셋**(위반을 일부러 심어 빨개지는 것 관측)을 완료 조건에 넣었다 |
| **자기 채점이 A→B로 이동했을 뿐** | A가 만드는 건 이름만 있고 전부 `false`인 목록이고, **무엇으로 충족되는지는 B가 쓰고 B가 통과시킨다.** 게다가 **1차보다 후퇴**했다 — 1차엔 `gate-a/red-freeze.json`이 기준마다 `sha256`과 `author_context`를 기계로 봤는데 이번엔 대응 장치가 0개였고, B의 codex는 `workspace-write`로 `verify/`를 고칠 수 있다 | **기준 동결 매니페스트**를 넣었다. 43개 기준 목록의 sha256을 verify가 매 실행 대조하고 불일치면 죽는다. **1차는 테스트를 동결했고 이번엔 눈금 자신을 동결한다** |
| **눈금 해상도 미정** | *"IP-0~6의 모든 기준"*이 7개인지 43개인지 54개인지 안 적혀 있었다. 7개 boolean을 고르면 `close` 거부 여섯을 하나로 뭉쳐 세게 되고 **1차의 "조건 목록" 실패와 같은 방향** | `GOAL.md` §2의 동그라미를 세어 **43개로 확정**(IP-0:4 · IP-1:8 · IP-2:13 · IP-3:4 · IP-4:4 · IP-5:3 · IP-6:7) |

### 실행으로 확인한 사실 — 추정이 아니다

| 무엇 | 관측 |
| --- | --- |
| **`--allowedTools`가 프롬프트를 삼킨다** | 가변 인자라 뒤의 위치 인자를 전부 먹는다. 콤마로 묶어도 같다. `Error: Input must be provided either through stdin or as a prompt argument when using --print` → **프롬프트는 stdin으로** |
| **미신뢰 디렉터리에서도 권한이 선다** | `echo "…" \| claude -p --setting-sources project --allowedTools "Bash(bun run:*)"` → **통과**(`bun run typecheck`가 실제로 실행돼 종료 코드를 물어 왔다). 대조군(allowlist 없음) → `This command requires approval` |
| **`--setting-sources project`는 allowlist를 안 싣는다** | `--setting-sources`는 `user`·`project`·`local`을 따로 받고 `settings.local.json`은 **local**이다 |
| **`mv …/*` 복구가 dotfile을 놓친다** | 재현: `.claude`·`.drive`가 임시 위치에 **남았다.** 옛 비계 판별식이 파괴적이었다 |
| **`.drive/` 미생성으로 루프가 헛돈다** | 리디렉션 `rc=1` → `&& break` 불발 → 빈 로그로 20라운드 |
| **codex에 상한 플래그가 없다** | `codex exec --help`에 budget·limit·timeout **0건**. `--max-budget-usd`는 `claude` 전용 |
| **환경 사유로 이미 빨간 것들** | `typecheck` exit 2(TS18003) · `lint` exit 127(biome 미설치, `node_modules` 없음) · `bun test` 파일 0개 |
| **`.gitignore` 실효성** | `.drive/`·`.fabricate/`·`AGENT_STOP` → `NOT IGNORED`. `.idea/`는 이미 무시되고 추적 0건 |

### 기각한 지적 하나

Codex의 *"프롬프트는 Claude 몫인데 A가 `fab-verifier.md`를 만드니 역할 규율 위반"* — **아니다.** 세션 A는 Claude 세션이고 코드만 codex에 넘긴다. 프롬프트를 Claude가 쓰는 것이 규율 그대로다. Codex가 *"Codex가 A를 수행한다"*를 가정했는데 그런 지시는 없다.

### 반증된 STATE의 옛 논거 하나

*"1차에 도달 가능성 검사가 있었다면 89/102가 첫날 터졌다"* — **틀렸다.** 1차의 첫날에도 모듈은 0개였다. 89/102가 터지는 것은 모듈이 생긴 뒤(=세션 B)다. 0a 배치가 얻는 것은 *"검사가 존재한다"*뿐이고, 그래서 **음극 자기시험이 없으면 0a 배치의 값이 거의 없다.** 시점은 0a로 유지하되 자기시험을 의무로 붙인 이유가 이것이다.

### 남은 잔여 — 정직하게 적는다

- **`drive.sh`는 드라이런 한 바퀴까지만 검증된다.** 실제 codex·claude 호출을 태우는 경로는 세션 B가 처음 돌린다. ~~드라이런은 "센티널에 걸려 끊긴다"만 증명한다.~~ **3차 검증으로 완화됨** — `DRY_RUN=2`(codex·claude를 echo 스텁으로 치환) 한 바퀴가 루프 본체 전 경로를 실제로 실행하므로, 남는 미검증은 *"진짜 codex·claude가 그 명령줄에 어떻게 반응하는가"*뿐이다.
- **43개 기준의 *내용*이 옳은지는 기계가 못 본다.** 동결은 *바뀌지 않았음*만 보장한다. 그 기준이 `GOAL.md` §2를 정확히 옮겼는지는 사람이 본다.
- **`GOAL.md` §8이 이미 선언한 잔여**(IP-6ⓑ·IP-6ⓓ·IP-5ⓒ)는 이 검증으로 줄지 않았다.
- **check 구현의 *질*은 여전히 기계 밖이다 — 다만 바닥이 올라갔다.** 최소 관측 계약과 계측이 *"아무것도 안 띄우고 `true`를 돌려주는 check"*를 막지만, **계약이 요구한 프로세스를 띄운 뒤 그 결과를 느슨하게 판정하는 check**는 못 막는다. `GOAL.md` §4-5(*"`.length(3)`은 세 개를 세지만 세 개를 내놓게 만들지는 못한다"*)가 이미 선언한 자리이고, 방어선은 `fab-verifier`의 매 라운드 `verify/` diff 보고와 `GOAL.md` §2 마지막 줄의 규율이다.
- **동결의 진짜 앵커는 사용자다.** `STATE.md`의 sha256 텍스트는 편의용 사본이고, `workspace-write`를 든 세션 B는 매니페스트와 그 사본을 함께 고칠 수 있다. **기계가 닫는 것이 아니라 사람이 값을 들고 있어야 닫힌다.**

---

## 다음 유력 경로 (개정 5 이후: 위 **"작업 순서 (개정)"**이 이것을 갱신한다)

**아래는 개정 5 이전의 권장안이고, 위 표로 대체됐다.** 판단 근거로 남긴다. **2026-07-27 참고**: 아래 1번(*"IP-1의 뼈대를 게이트 없이 먼저 뚫는다"*)은 RED-first 전제 검증의 결론과 **같은 방향**이었다 — 0단계가 그 앞에 끼어들면서 밀렸던 것이 이번에 되돌려졌다(위 "fixture 헌법" 규칙 4).

**1. IP-1의 뼈대.** `deep-interview start` → `turn record` → `close` 경로를 게이트 없이 먼저 뚫는다. **경로가 있어야 게이트를 그 위에 세울 수 있다**(`GOAL.md` §4-6). 1차 시도의 실패가 정확히 "게이트를 경로 없이 지었다"였다.

**2. IP-0.** 설치 명령. 일찍 서야 이후 훅·스킬 검사가 실제 설치 경로로 돈다.

**3. IP-2.** `close` 거부 여섯 → Stop 훅 → `UserPromptExpansion` 표식. 뒤의 둘이 이 설계의 급소다.

**4. IP-5 → IP-6 → IP-4 → IP-3.** IP-3(재진입 시드)이 마지막인 이유는 그때쯤 레코드 형상이 굳기 때문이다.

도달 가능성 검사(`GOAL.md` §6-2)는 `verify`가 초록이 될 때 거기 있으면 된다. 일찍 세우면 섬을 짓는 즉시 알게 되어 유용하지만 **시점은 정하지 않는다.**

---

## 저장소 현재 상태

**작업 트리에 코드가 없다.** 1차 시도 산출물(197 파일)은 커밋 `499cd3c`에서 삭제됐고 `git show`로만 읽는다.

| 있는 것 | 무엇 |
| --- | --- |
| `GOAL.md` | **비준됨 + 개정 5.** 본문 해시 `2a4ba701d13dbf36…` |
| `STATE.md` | 이 문서 |
| `contract/original-request.md` | 원 요청 + 개정 1·2·3. **읽기 전용** |
| `contract/research-report.md` · `reviews/` | 기법·근거의 출처. 살아 있다 |
| `contract/criteria.*` · `contract-draft.md` · `gate-a/` | 69개 조건. **참고 신호** — 완료 정의 아님 |
| `audit/2026-07-26-piece3-audit.md` | 1차 실패의 실측. `GOAL.md` §3의 근거 |
| `decisions/0001-…` | 1차 시도의 증거 어휘 매핑. 참고 |
| `package.json` · `tsconfig.json` · `biome.json` · `bun.lock` | 도구 사슬. 의존성은 zod·citty만 |

| 없는 것 | 뜻 |
| --- | --- |
| `src/` · 테스트 | 코드 0줄 |
| `package.json`의 `bin` | 실행 파일 진입점이 없다 |
| `verify` 스크립트 | `GOAL.md` §2가 요구하는 단일 닫기 명령 |
| 스킬 · 에이전트 정의 · 훅 | 아키텍처 네 부분 중 셋이 없다 |
| `.fabricate/` | 상태 저장 위치. 형상 미정 |

---

## 신호 (게이트 아님 — 판단 재료)

| 신호 | 지금 | 참고 |
| --- | --- | --- |
| 비-테스트 `src` 줄수 | 0 | 1차 시도는 6,405줄에 돌아가는 것 0개였다. **상한 없음** — 규모가 아니라 방향이 문제다 |
| 뿌리에서 도달 불가한 모듈 | 검사 미착수 | `GOAL.md` §6-2. **유일한 구조적 도달성 게이트** |

## 열린 질문

**없다. Q1·Q2·Q3·Q4 전부 닫혔다** — 여기서 "열린 질문"은 **사용자만 답할 수 있는 것**을 뜻한다(`GOAL.md` §7의 중단 조건). 구현하며 정할 미결은 아래 **"0단계에서 바로 부딪힐 자리"**에 따로 있고, 그것들은 멈출 이유가 아니다.

| id | 무엇이었나 | 결말 |
| --- | --- | --- |
| Q1 | 1차 코드 처분 | 삭제 (2026-07-27). git 히스토리로 복구 가능 |
| Q2 | `GOAL.md` 비준 시점 | 비준 완료 (2026-07-27) |
| Q3 | 원 요청과의 범위 충돌 | 개정 3으로 닫힘 (2026-07-27 사용자 승인) |
| Q4 | 입출력 계약 사전 고정 | 채택 안 함 — 제안자 철회. 구현이 정한다 |

---

## 개정 5 — **집행됨 (2026-07-27 사용자 비준)**

**`GOAL.md`는 개정 5 상태다. 아래 셋이 전부 본문에 반영돼 있다.** 새 본문 해시 `2a4ba701d13dbf36…`. 이 절은 제안이 아니라 **무엇이 왜 바뀌었는지의 기록**이다.

### 반영 A — 훅이 둘이 아니라 셋 (가장 큰 변경)

- **무엇이 틀렸나**: §1 "사건 둘" 절 전체와 IP-2ⓚ(*"표식이 `UserPromptExpansion`에서 생긴다"*).
- **근거(실측)**: UPE는 **사용자가 타이핑한 슬래시 명령에만** 발화하고 **모델의 `Skill` 도구 호출에는 발화하지 않는다**(위 "실측으로 닫힌 미확인 넷" §1 정정, §5). 즉 IP-2ⓚ의 근거가 모델 호출 경로에서 성립하지 않았다.
- **반영**: `PreToolUse`(매처 `Skill`)를 세 번째 사건으로 추가. IP-2ⓚ를 "모델 재량 밖의 **두 자리**"로 교체(음극 대칭 ⓛ 유지). **IP-2ⓜ 신설** — 원문 없이 시작된 세션은 `close`가 거부한다. §6-6에 `PreToolUse` 매처 교훈 추가(§6의 여섯 행은 유지). **§2의 어떤 술어도 약화되지 않았고 IP-2는 강화됐다.**

### 반영 B — IP-0ⓐ의 증거 강화

- **무엇이 틀렸나**: IP-0ⓐ가 요구하는 증거가 사실상 "설치 명령이 성공했다"로 읽힌다.
- **근거(실측)**: 프로젝트 스코프 설치가 전역 `known_marketplaces.json`에 의존하고, 다른 프로젝트에서 그것을 지우면 **설정은 남고 슬래시 명령만 사라진다 — 종료 코드 0, stderr 비어 있음**(위 "실측으로 닫힌 미확인 넷" §3). 즉 exit 0인 채 조용히 죽는 경우를 현재 증거로는 못 잡는다.
- **반영**: 증거를 **설치 → 실행 → 재설치 → 재실행** 4단으로 올렸다. "실행"은 새 프로세스에서 슬래시 명령이 **실제로 훅을 발화시켰다**는 관측이고, **설치 성공은 증거가 아니라고 본문이 명시한다.** ⓑ(멱등)·ⓒ(설정 보존)·ⓓ(실패 시 보존)는 그대로다.
- **§7 중단 조건에 안 걸린다**: 술어를 **강화**하는 방향이다. §9 말미가 중단 조건으로 지목한 것은 §2를 *약화*시키는 개정이다.

### 반영 C — §4-7과 호스트의 충돌을 §8에 명시

- **무엇이 틀렸나**: §4-7이 *"거부는 관측 가능해야 한다 — 조용히 넘어가는 실패를 만들지 않는다"*라고 무조건으로 적혀 있다.
- **근거(실측)**: 연속 차단 캡(기본 8, `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP`)을 넘긴 차단은 **경고 없이 사라지고 종료 코드는 0이다.** 우리가 조용한 실패를 "만들지" 않아도 호스트가 만든다.
- **반영**: **§4-7 본문은 손대지 않았고**, §8 "훅의 봉쇄 완전성"에 하위 불릿을 하나 더했다 — 우리가 조용한 실패를 안 만들어도 호스트가 만들며(캡 초과 차단 · 미신뢰 시 `.claude/settings.json` 무시), 따라서 §4-7은 **fabricate가 쓰는 코드에 대한 요구이고 훅 계층 전체의 보장이 아니다.** 불변식을 약화시키는 것이 아니라 **적용 범위를 정직하게 긋는 것**이다.
- 이 후보는 `GOAL.md` §8의 기존 결론(*훅은 완벽한 봉쇄가 아니라 우회를 비싸고 눈에 보이게 만드는 장치, 방어선은 IP-4*)과 같은 방향이다.

---

## 0단계에서 바로 부딪힐 자리 — **1·2는 닫혔고 3만 남았다 (2026-07-28)**

**개정 5를 집행하면서 드러났던 것들이다. 2026-07-28 계획 세션이 1·2를 닫았고 3은 세션 A가 정한다.** 다만 **핸드오프에서 빠지면 0단계·1단계에서 맨몸으로 부딪힌다.** 각 항목은 *왜 문제인가 · 어디서 걸리는가 · 가능한 방향*이다.

### 1. IP-2ⓜ과 IP-1의 긴장 — 모델 호출 경로는 *반드시 실패하는* 경로다

- **왜 문제인가**: 개정 5가 `PreToolUse`(매처 `Skill`) 경로에도 표식을 세웠다. 그런데 그 경로에는 사용자 원문이 없고(`tool_input.args`는 모델이 지어낸 것), IP-2ⓜ은 **원문이 없으면 `close`가 거부한다**고 못박았다. 즉 **표식은 서지만 그 세션은 구조적으로 절대 닫히지 않는다.** IP-1("끝까지 돈다")과 IP-2ⓜ이 같은 세션 위에서 반대 방향을 가리킨다.
- **어디서 걸리는가**: **작업 순서 1단계(걷는 뼈대)에서 반드시 부딪힌다.** 모델이 스킬을 스스로 호출하는 것은 예외 경로가 아니라 흔한 경로다(위 §5 실측 — 사용자 타이핑 경로에서도 모델이 `Skill`을 한 번 더 부른다).
- **가능한 방향 (전부 미검증)**: ① `close`가 거부하며 **사용자에게 원문을 다시 요구**하고, 원문이 들어오면 `request.txt`를 채워 진행 가능 상태로 승격한다 — 거부가 막다른 길이 아니라 복구 지시가 된다. ② `start`에 **원문 인자**를 두고 스킬이 사용자 발화를 그대로 넘기게 한다 — 다만 **모델이 넘기는 순간 그것은 모델을 거친 문자열**이라 `GOAL.md` §4-3(원문 불요약)의 보증이 약해진다. UPE의 `command_args`가 가진 "모델에 닿기 전"이라는 성질을 잃는다. ③ 두 경로를 **다른 등급의 세션**으로 나눈다(모델 호출 경로는 표식만 세워 `Stop` 봉쇄에는 쓰되 잠금 자격은 아예 안 준다).
- ~~**결정하지 않는다.**~~ **결정됨 (2026-07-28 계획 세션) — ③을 기본으로 두고 회복은 ①을 새 기제 없이 실현한다. ②는 버린다.**
  - **③은 선택지가 아니라 `GOAL.md` 본문이다.** §1이 이미 적었다 — *"봉쇄는 두 경로 다 서지만 **잠금 자격은 원문이 확보된 경로에만 준다**"*. 다르게 정하려면 §9를 밟아야 한다.
  - **회복에 새 코드가 필요 없다.** UPE와 `PreToolUse`는 같은 `session_id`를 받고 표식 생성은 멱등이다(`GOAL.md` §1). 그래서 `close`의 거부 문안이 *"슬래시 명령으로 다시 시작하라"*를 사람이 읽을 문장으로 돌려주면(§4-7), 사용자가 `/fabricate:deep-interview "<원문>"`을 치는 순간 **같은 세션에 UPE가 발화해 `request.txt`를 앉히고** 세션이 그 자리에서 승격된다. 세션이 갈라지지 않는다. 즉 ①의 효과를 얻으면서 지은 코드는 0줄이다.
  - **②를 버린 이유**: `start`에 원문 인자를 두면 그 문자열은 모델을 거친다. `command_args`가 가진 *"모델에 닿기 전"*이라는 성질을 잃고 IP-1ⓕ(확정 바인딩)와 §4-3의 보증이 **이름만 남는다.** §4-5(*"판정하는 척하는 스키마를 짓지 않는다"*)에 정면으로 걸린다.
  - **딸린 제약 — `request.txt`를 쓰는 코드 경로는 UPE 훅 하나뿐이다.** CLI에 그것을 쓰는 하위 명령을 두지 않는다. 두면 모델이 원문을 날조할 수 있고, 그것은 §6-3(잠금 우회 쓰기 경로 없음)과 같은 종류의 구멍이다.
  - **세션 A가 바꿔도 되는 조건**: 위 승격 경로가 실물에서 안 된다는 것을 **관측했을 때만**. 취향으로 다시 열지 않는다.

### 2. `.fabricate/sessions/<sid>`의 `<sid>`가 무엇인지 미확정

- **왜 문제인가**: 훅은 입력 JSON으로 `session_id`를 받지만 **CLI(Bash 서브프로세스)는 그것을 볼 수 없다.** `prompt_id`와 **같은 종류의 구멍**이다(위 "실측으로 닫힌 미확인 넷" §2). 그런데 위 "구현 형상"의 상태 파일 트리는 훅과 CLI가 **같은 `<sid>` 디렉터리를 가리킨다고 전제한다** — 그 이음매를 얼버무리고 있다.
- **어디서 걸리는가**: **0단계에서 fixture를 쓰는 순간.** 훅이 만든 표식과 CLI가 쓴 장부가 같은 세션으로 묶이지 않으면 IP-2ⓙ(턴 회계) 자체가 성립하지 않는다.
- **가능한 방향과 그 대가**: 현실적으로 **스킬이 CLI에 `--session <id>`로 넘기는 수밖에 없어 보인다.** 그러면 **모델이 그 값을 위조할 수 있다** — 다른 세션의 id를 넘기거나, 매 턴 새 id를 지어내 봉쇄를 우회할 수 있다. 완화 후보: 훅이 표식을 세울 때 **알려진 `session_id` 집합을 디스크에 적어 두고 CLI가 그 집합 밖의 id를 거부**한다(위조 가능한 값이 아니라 훅이 관측한 값만 유효). 다만 이것도 "이미 열린 세션 중 하나를 사칭"하는 것은 못 막는다.
- ~~**미정.**~~ **결정됨 (2026-07-28 계획 세션) — CLI에 `--session`을 두지 않는다.** `.fabricate/sessions/` 아래 `active` 표식이 **정확히 하나일 때 그것을 쓰고, 0개거나 2개 이상이면 exit ≠ 0으로 거부한다.** 세션 디렉터리를 만드는 것은 **훅뿐**이다.
  - **위조를 좁히는 대신 손잡이를 없앴다.** 모델이 세션 id를 다루지 않으면 위조할 표면 자체가 없다. 위 "완화 후보"(훅이 관측한 id 집합으로 검증)보다 코드가 적으면서 막는 것은 더 많다.
  - **세 부분이 같은 방향으로 실패한다.** 훅은 자기 `session_id` 디렉터리를 보고 CLI는 유일한 `active`를 본다 — 세션이 하나면 같은 곳이고, 둘이면 CLI가 거부해 장부가 안 늘고, 그러면 `Stop`이 그 턴을 막는다.
  - **대가**: 같은 프로젝트 디렉터리에서 인터뷰 세션 둘을 **동시에** 열 수 없다. IP-0~6 어디도 그것을 요구하지 않고, 거부는 관측 가능하다(§4-7). 필요해지면 그때 `--session` + 훅이 관측한 id 집합 검사를 붙인다 — 지금 짓는 것은 §4-5가 말하는 *쓰이지 않을 이음매를 미리 지는 것*이다.
  - **세션 A가 실측할 것**: CLI가 `.fabricate/`를 찾는 기준 디렉터리. 훅 입력에는 `cwd`가 오지만 CLI는 자기 프로세스의 cwd를 쓴다 — 서브디렉터리에서 CLI를 부르면 둘이 갈리는지 안 재 봤다.

### 3. `verify`의 격리 방식과 IP-0ⓐ의 4단 증거가 충돌 가능

- **왜 문제인가**: 위 "딸린 발견"은 `verify`가 실제 `claude`를 띄우지 않고 **훅 스크립트에 합성 stdin JSON을 먹여 직접 실행**한다고 적었다(`CLAUDE_CONFIG_DIR` 격리가 `Not logged in`으로 죽으므로). 그런데 개정 5로 강화된 **IP-0ⓐ는 "새 프로세스에서 슬래시 명령이 실제로 훅을 발화시켰다"는 관측**을 요구한다. 합성 stdin은 그 관측이 아니다 — **훅 스크립트의 로직은 증명하지만 설치 연쇄는 증명하지 않는다.**
- **어디서 걸리는가**: **0단계.** `verify` 하네스를 세우는 바로 그 자리에서 두 요구가 같은 명령 안에 들어가야 한다.
- **참고 사실 (실측)**: 선행 프로브가 **`claude -p --setting-sources project`로 임시 프로젝트에서 훅을 실제로 발화시켰고, 사용자 전역 설정은 바이트 불변이었다.** 즉 `GOAL.md` §4-9(검증이 사용자 설정을 오염시키지 않는다)를 지키면서 진짜 호스트를 띄우는 길이 **있어 보인다.**
- **다만 하네스에 엮어 본 적은 없다.** 두 층(합성 stdin = 로직 검사 · 실제 `claude -p` = 설치 연쇄 검사)을 한 `bun run verify` 안에서 어떻게 나눌지, 후자가 CI/무인 환경에서 어떻게 되는지는 **미정**이다.

---

### 개정 6 후보 — **제안이다. 아직 비준 안 됐다**

**`GOAL.md`는 개정 5 상태이고(본문 해시 `2a4ba701d13dbf36…`) 아래 둘은 반영돼 있지 않다.** 비준하려면 `GOAL.md` §9(무엇이 틀렸나 · 근거 · 사용자 확인)를 밟는다. **지금 고치지 않는다.**

#### 후보 1 — §6-6의 "유일한 자리"가 낡았다

- **무엇이 틀렸나**: §6-6이 *"이름을 바꿀 때 함께 바꿔야 하는 유일한 자리"*라고 적었는데, 개정 5로 **자리가 둘이 됐다** — `UserPromptExpansion`의 `command_name`과 `PreToolUse`의 `tool_input.skill`.
- **실질 모순은 아니다**: 같은 문단이 이미 "매처를 생략하고 스크립트에서 검사하라 · 두 문자열이 같은 형식이라 **정규화 함수 하나**로 처리된다"를 권한다. 낡은 것은 **문구**이고 지시는 옳다.
- **왜 지금 안 고치나**: 술어도 불변식도 아닌 서술 문구 하나다. 구현하다 **실제로 몇 자리가 되는지 확정된 뒤** 한 번에 고치는 것이 싸다.

#### 후보 2 — §8의 봉쇄 완전성 목록에 `PreToolUse` 우회가 없다

- **무엇이 틀렸나**: `GOAL.md` §8 "훅의 봉쇄 완전성"이 인터럽트 우회와 연속 차단 캡을 적었지만, **모델이 `Skill` 도구를 아예 쓰지 않고 스킬 내용을 흉내내는 경우**가 없다. 그러면 **표식이 두 자리 다 안 선다** — UPE 우회와 **같은 급의 구멍**이다.
- **왜 잔여에 있어야 하나**: `GOAL.md` §8 머리가 *"이 목록에 없는 것을 나중에 잔여라고 주장할 수 없다"*고 적었다. 지금 안 적으면 나중에 변명이 된다.
- **방향**: 결론은 바뀌지 않는다 — 훅은 완벽한 봉쇄가 아니고 **방어선은 IP-4**다. 개정 5가 표식 생성기를 둘로 늘려 우회를 **더 비싸게** 만들었을 뿐 없애지 못했다는 것을 명시하는 항목이다.

---

### RED-first 전제 검증 — 끝났다. 결론은 "그것만으로는 못 막는다" (2026-07-27) *(구 제목: 딸린 미결 — RED-first 전제 검증이 아직 안 끝났다 / 그 이전: Codex 적대적 설계 검토가 안 돌아왔다)*

물어 둔 질문은 이것이었다:

> **`verify`를 전부 RED로 먼저 세우는 것(0단계)이 1차 시도의 실패를 막는가, 아니면 fixture가 새 "조건 목록"이 되어 그 실패를 재생산하는가?**

**답: RED-first만으로는 막지 못한다. 1차 시도가 이미 RED-first를 했고 실패했다.** 규칙과 근거의 전문은 위 **"fixture 헌법 — RED-first만으로는 1차를 막지 못한다"** 절에 있다. 여기에는 결론과 그 귀결만 적는다.

**근거 요약 (전부 저장소 실물 · `git show 499cd3c^:…`)**

| 무엇 | 실측 |
| --- | --- |
| 1차 수용 검사 총 | **69** |
| 그중 "frozen red" 선언(구현 전에 빨갛게 세우고 동결) | **44** |
| `../src/` 내부 모듈을 직접 import | **69 — 전부** |
| 실제로 프로세스를 띄우는 것 | **4** |
| `src/gate/red-first.ts` 형식 게이트의 결과 | **동결 69개 중 통과 69, 거부 0. 부정행위 0** |
| 유일한 CLI 파일에서의 도달성 | **운영 `.ts` 102개 중 도달 13 / 미도달 89** |
| `package.json`의 `bin` · 저장소의 `import.meta.main` · `citty` import | **없음 / 0건 / 0건** |

> **RED-first는 100% 작동했고, 그래도 초록 33개에 돌아가는 것은 0개였다.** RED-first는 1차와 이번을 **구별하지 못한다.** 구별하는 것은 **호출 경계**다.

**대표 실물**: `acceptance/ac-3.test.ts`는 `finalizeIntent`를 검사했고, `src/cli/interview-finalize.ts`가 실제로 부른 것은 **`finalize`** — 서로 다른 함수다. 그리고 `acceptance/ac-40.test.ts`는 아예 진입점을 **금지했다**(*"no runner/cli/main-shaped harness file … no `import.meta.main` / argv entry point"*). **1차의 완료 정의 69개는 "돌아가는 프로그램"이라는 개념을 한 번도 언급하지 않았다.**

**대칭 증거로도, 빈-껍데기 반박으로도 못 막았다.** 1차에 이미 강한 양극·음극이 있었고(`ac-25`·`ac-27`·`ac-34`), 반박은 69개 중 43개를 잡았으며 나머지 26개는 `gate-a/review-summary.json`이 **`honest`로 명시 판정**한 것이다. 그런데 **잡아서 고친 43개도 막지 못했다** — `ac-4`·`ac-24`·`ac-37`은 셋 다 `shell_passable → revised:true`였는데 감사가 셋 다 **BROKEN**으로 판정했다. 반박자의 시점(*"구현이 없으니 편향이 가장 적은 유일한 시점"*)은 **"누가 이 모듈을 부르는가"를 물을 수 없는 유일한 시점이기도 하다.**

**귀결 — 세션 A·B가 바꿔야 할 것**

1. **세션 A는 이 절을 판정하지 않는다.** 답이 있다. 세션 A가 할 일은 **fixture 헌법 넷을 지키는 것**이다.
2. **작업 순서가 바뀌었다** — 0a는 러너 + default-FAIL 계약 + 헌법 검사 + 도달 가능성 검사 + 부트스트랩까지이고, **IP별 fixture는 만들지 않는다.** 걷는 경로(IP-0 · IP-1ⓐ)가 먼저 서고, 나머지 IP의 양극·음극은 **그 동일한 세션의 변형**으로 만든다. 위 "작업 순서 (개정)" 표.
3. **`verify`가 `verify/`의 `src/` import를 금지한다** — **1차 대비 유일하게 새로운 방어선이다.**
4. **도달 가능성 검사(`GOAL.md` §6-2)의 시점을 A1으로 못박았다.** `GOAL.md`가 시점을 강제하지 않으므로 **STATE의 운용 결정**이다. ~~1차에 있었다면 89/102가 첫날 터졌다~~ — **이 근거는 반증됐다**(1차 첫날에도 모듈은 0개였다). 남는 근거는 *"음극 자기시험을 붙일 자리가 필요하다"* 하나다.
5. **RED-first를 버리는 것이 아니다.** default-FAIL 계약(모든 IP 기준이 `false`에서 시작)은 유지되고 종료 조건은 여전히 코드보다 먼저 선다. 버리는 것은 **"RED-first면 충분하다"는 착각**뿐이다.
6. **넷을 다 지켜도 IP-6ⓑ · IP-6ⓓ · IP-5ⓒ는 여전히 뚫린다.** 형상 술어는 내용을 강제하지 못한다(`GOAL.md` §4-5 · §8의 기선언 잔여). 여기서 요구되는 것은 더 강한 fixture가 아니라 **`GOAL.md` §2 마지막 줄의 규율 — `기계 통과`를 완료라고 보고하지 않고 `사람 검증 대기`를 함께 말하는 것.** 이걸 모르면 초록을 완료로 착각한다.

**한 문장**

> **RED-first는 1차 실패의 방지책이 아니라 1차가 이미 쓴 도구다. 새 계획을 1차와 가르는 유일한 것은 fixture가 모듈이 아니라 설치된 프로세스를 만진다는 것이고, 그 성질은 `GOAL.md` §2에 IP-0·IP-1ⓐ로만 명문화돼 있다 — 나머지 IP에는 안 적혀 있으므로 fixture를 쓰는 사람이 규율을 스스로 지키는 동안에만 성립한다. 규율이 아니라 구조로 박으려면 `verify`가 IP fixture에서 `src/**` import를 금지하는 것이 1차 대비 유일하게 새로운 방어선이다.**

**검증 방식**: **Codex(GPT-5.4)의 적대적 검토와 저장소 실물 전수 조사가 독립적으로 같은 결론에 도달했다.** 둘은 서로의 산출물을 시드로 받지 않았다(`GOAL.md` §0).

**이것이 `GOAL.md` §2를 바꾸는가 — 아니다.** 술어도 불변식도 건드리지 않는다. *fixture를 어떻게 짜는가*는 `GOAL.md` §7이 구현에 맡긴 자리이고, 이 결론은 그 자리를 채운 것이다. `GOAL.md` 개정 절차(§9)는 밟지 않는다.

---

## 진척 로그

| 날짜 | 무엇 | 근거                                             |
| --- | --- |------------------------------------------------|
| 2026-07-26 | 1차 시도 방향 재검토 — 33개 초록 중 돌아가는 것 0개, 비-테스트 호출자 없는 모듈 다수 실측 | `audit/2026-07-26-piece3-audit.md`, 디렉터리별 줄수 실측 |
| 2026-07-26 | 백지 재작성 결정. 완료 정의를 조건 69개에서 통합 술어 4개로 교체 | 사용자 결정                                         |
| 2026-07-26 | `GOAL.md`(불변 씨앗) · `STATE.md`(가변 상태) 분리 초안 | 이 커밋                                           |
| 2026-07-26 | §7의 코드 규모 상한(1,500줄) 삭제 — 대리 지표이고 역 인센티브를 만든다. 방향 조건으로 교체 | 사용자 지적                                         |
| 2026-07-26 | 아키텍처 확정 — Claude Code 위에서 도는 스킬 + CLI + Stop 훅. 모델 어댑터·이중 호스트 없음. 서브에이전트는 스킬이 호스트 Agent 도구로 띄운다 | 사용자 결정                                         |
| 2026-07-26 | Codex 리뷰 반영 — §6을 도달 가능성 그래프 검사로 교체(임포터 카운트는 CLI 뿌리에서 실패하고 죽은 순환에서 통과한다), 완료 상태를 `기계 통과`/`사람 검증 대기`로 분리, IP-2에 훅 증거 추가 | 1차 외부 리뷰 (`reveiw_comment.md`, 07-27 삭제) |
| 2026-07-26 | Claude Code 훅 사실관계 실측 — `Stop`은 세션 종료가 아니라 **턴마다** 발화. `UserPromptExpansion`(명령 확장 시, 매처=명령 이름) 실재. "연속 차단 8회 제한"은 문서에 **없음**. 인터럽트 시 발화 여부는 미확인 → §8 잔여 | code.claude.com/docs/en/hooks |
| 2026-07-26 | 2차 외부 리뷰 반영 — IP 전부에 **대칭 증거**(양극·음극) 부여, IP-1에 사용자 표면 증거 추가, IP-3에 주입 경로·원문 대조 추가, IP-4에 통과 경로 추가(항상 거부하는 구현 차단), **IP-5 신설**(세션-맹검 위임), 훅을 두 사건으로 분리, `bun run verify` 단일 닫기 명령 도입, §4-2를 §6과 같은 표현으로 통일, "세 부분"→"네 부분"·"유일한 게이트" 범위 한정 | 2차 외부 리뷰 |
| 2026-07-26 | 3차 외부 리뷰 반영 — 하네스는 레일이지 감옥이 아니라는 사용자 지적에 따라 **방법 강제를 결과 고정으로 낮췄다**: 중단 조건 5→4(기술적 모호함은 스스로 정한다), "잠금 진입점 하나"→"우회 쓰기 경로 없음", "도달성 스크립트 첫 커밋"→시점 미지정, "조건 목록 만들면 멈춘다"→경고. **IP-0 신설**(설치 멱등·설정 보존)과 안전 불변식 8·9·10(멱등·검증 격리·절대경로 금지) 추가. §6 수량 오류(다섯→여섯, 넷→다섯) 정정. `PLAN.md`·`NEXT.md`를 권위 없음으로 명시 | 3차 외부 리뷰 (`codex_review_comment2.md`, 07-27 삭제) + 사용자 지적 |
| 2026-07-26 | 개정 3 초안 작성 (`proposed-amendment-3.md`, 권위 없음). Q4를 "채택 안 함"으로 닫음 — 입출력 고정 제안은 제안자 철회. GOAL 편집 잔여 둘 정정(§6 중복 문장, IP-0ⓐ 누락) | 4차 외부 리뷰 |
| 2026-07-27 | **범위 확정** — 원 요청 대조로 Tier B를 재판정. "기법은 전부 프롬프트"라는 내 요약이 틀렸다: 보고서 부제가 "판단은 LLM, **검사는 결정적**"이고 B1~B6 전부 기계 뼈대가 있다. 넣는 것 = G2 고아차단 · A1 · A2 · A3 · A4 · A5 stale 전파 · B1 · B2 · B4 · B5 · B6 · 역방향 언어. IP-6(질문 경제·구체화)·IP-7(합의 어휘) 신설 | 사용자 결정 + `research-report.md:194~`, `criteria.md` |
| 2026-07-27 | **i18n 방식 금지 확정** — 언어는 IP-7로 범위 안이되 키→문자열 카탈로그는 금지. 문구를 만드는 것이 LLM인데 기계적 번역표를 갈아끼우는 것은 앞뒤가 안 맞는다. 기계는 **생성된 말을 용어집에 대조**한다 | 사용자 결정 |
| 2026-07-27 | 원칙적 배제 확정 — B3(반론 위임이 필수가 아니므로) · EARS lint(영어 정규식/한국어 표면) · Tier U 본체(보고서가 "deep-interview 밖"으로 규정) · 묶음7(원문이 "전부 design 노드") | `research-report.md`, `original-request.md` |
| 2026-07-27 | **1차 시도 산출물 전부 삭제** — `src/`·`acceptance/`·`tools/`·`PLAN.md`·`NEXT.md` (197 파일). git 히스토리에 있으므로 복구 가능하고, 새 코드가 옛 코드를 import할 경로가 물리적으로 사라졌다. Q1 닫힘 | 사용자 결정 |
| 2026-07-27 | **합의 용어집 배제** — IP-7(합의된 말로 말한다) 통째로 제거. 그 술어의 기계 앵커가 용어집 대조 하나였다. 딸린 자리 정리: IP-6 도전의 권위를 코드 단독으로, IP-3·§1에서 "합의 용어" 삭제. 역방향 언어 품질은 §8 잔여로 내려가고 **기계가 전혀 안 잡는다** | 사용자 결정 |
| 2026-07-27 | **개정 3 승인·append** — `contract/original-request.md`의 개정 2 뒤에 나란히. 완료 정의가 69개 조건 → IP-0~6으로 교체됨. Q3 닫힘 | 사용자 승인 |
| 2026-07-27 | **`GOAL.md` 비준** — 2026-07-27, 본문 sha256 `cf31e2d8a66234ea…`. 이후 본문 변경은 §9(무엇이 틀렸나·근거·사용자 확인)를 밟는다. Q2 닫힘. `proposed-amendment-3.md` 삭제 | — |
| 2026-07-27 | **핸드오프 정비** — `STATE.md`를 인수인계가 되게 재작성(첫 5분 · 이미 정해진 것 · 이 세션이 정하는 것 · 미확인 넷 · 저장소 현재 상태). 별도 HANDOFF 문서는 만들지 않는다 — `GOAL.md` §0이 이미 재진입 규약을 정했고 세 번째 문서는 권위를 가른다. `README.md`의 삭제된 `PLAN.md` 참조 정정 | — |
| 2026-07-27 | **훅 실측 완료 — 미확인 넷 닫힘.** UPE 입력 키 11개(`command_text`는 안 온다, `command_args`가 원문 확보 지점), 슬래시 명령에만 발화, **UPE 차단은 턴을 죽이므로 금지**. `Stop`의 턴 경계는 `prompt_id`이고 **CLI는 그것을 못 보므로 턴 회계는 Stop 훅이 소유**. 설치는 마켓플레이스 경로로 멱등, 명령 이름은 `/<plugin>:<skill-dir>`, **재시작 필요성이 매체별로 갈린다**. IP-0 구멍 둘(=`directory` 소스의 절대경로 · 전역 `known_marketplaces.json` 의존으로 조용히 죽음) → **IP-0 증거는 설치 성공이 아니라 실제 훅 발화**. `verify`는 `CLAUDE_CONFIG_DIR` 격리 불가(`Not logged in`) → **합성 stdin JSON으로 훅 직접 실행** | 격리 프로브 · Claude Code 2.1.220 (위 "실측으로 닫힌 미확인 넷") |
| 2026-07-27 | **`GOAL.md` 개정 4 비준** — ① §1 훅 표와 §6-6의 **매처 문자열이 정확히 반대로** 적혀 있던 것 정정(매처는 네임스페이스 포함 `command_name`과 비교되고 대소문자 구분·메타문자 없으면 완전 일치. `deep-interview`가 바로 "조용히 안 도는" 값이었다. 더 안전한 형상은 매처를 생략하고 스크립트에서 `command_name`을 검사하는 것). 위험 진단은 보존. ② §8 "훅의 봉쇄 완전성"을 미확인 둘 → **실측으로 닫힘 + 남은 한계**로 교체. 결론(완벽한 봉쇄가 아니라 우회를 비싸고 눈에 보이게 만드는 장치, 방어선은 IP-4)은 유지·강화. **§2 술어·§4 불변식은 손대지 않았다.** 새 본문 해시 `360c39545f5f1793…` | 사용자 비준 + 실측 (`GOAL.md` §9) |
| 2026-07-27 | **ditto 처분 확정 — 논외.** 사용자 결정: *"지금 우리가 하는 건 fabricate라는 하네스를 만드는 것이고 ditto와는 아무 상관없다. ditto에 있는 건 ditto에서 쓰는 것이고 코드를 재활용할 필요도 없다."* 원 요청 개정 2(처음부터 재설계)가 그대로 유지된다. **참조 구현으로도 쓰지 않는다** | 사용자 결정 |
| 2026-07-27 | **구동 도구 실측 완료 + 권한·신뢰 제약 확정 + Codex 적극 사용 결정.** 자동 구동 후보를 전부 재봤다: `/goal`은 **트랜스크립트만 읽는 별도 haiku 판정**이라 셸 검사를 못 건다 · `/loop`은 **60초 하한의 폴러** · Workflow는 **스크립트에 파일시스템 접근이 없어** 종료 조건을 기계에 못 묶는다 · **`command` 타입 `Stop` 훅이 유일하게 완전 결정적** · **`agent` 타입 `Stop` 훅은 도구를 가진 진짜 에이전트라 셸을 실제로 돈다 — CLI 내장 문서의 "tool 이벤트 전용"은 틀렸다.** 권한·신뢰 제약 확정(`acceptEdits`가 Bash를 안 덮음 · 미신뢰 워크스페이스에서 `permissions.allow` 조용히 무시 · 비대화형은 신뢰 대화상자를 건너뛸 뿐 승인 안 함 → **워크트리 대신 신뢰된 저장소 디렉터리에서 브랜치**). **역할 분담 확정 — 코드(CLI·훅·verify·매니페스트)는 Codex, 프롬프트(SKILL·agents)와 검증은 Claude.** 근거는 §4-1 판별식 + IP-0·IP-2 증거가 호스트를 실제로 돌려야 나온다는 것 + §8 "같은 편향" 잔여가 깨진다는 것. **사전 지식이 없는 구현자는 기억에서 지어낼 수 없어 측정된 페이로드를 읽는다**(GOAL이 매처를, 공식 문서가 `command_text`를 틀린 전례). 쓰기는 여전히 한 번에 한 에이전트 | 위 두 절 · Claude Code 2.1.220 `--help`·바이너리 문자열 · `~/.claude.json` · codex 플러그인 `hooks.json` |
| 2026-07-27 | **IP-2ⓚ가 실측으로 무너짐 → `GOAL.md` 개정 5 비준.** `UserPromptExpansion`이 **사용자가 타이핑한 슬래시 명령에만** 발화하고 **모델의 `Skill` 도구 호출에는 발화하지 않는 것**을 4회 재현으로 관측했다(정적 근거 일치 — UPE 훅의 유일한 호출 사슬이 슬래시 명령 처리이고 `expansion_type`이 `slash_command`·`mcp_prompt` 둘뿐). 즉 "표식이 UPE에서 생기니 `start`를 건너뛴 세션도 잡힌다"는 근거가 모델 호출 경로에서 성립하지 않았다. 대체 표식 생성기로 **`PreToolUse`(매처 `Skill`)**를 실측 확보 — `prompt_id`가 오므로 `Stop`의 턴 회계와 경계를 공유하고, `tool_input.skill`이 UPE의 `command_name`과 같은 네임스페이스 형식이다. 매처는 `tool_name` 정규식일 뿐이라 `Skill(name)`이 안 먹는다. **원문 비대칭**(모델 호출 경로엔 사용자 원문이 없다) → **IP-2ⓜ 신설**. 함께: **IP-0ⓐ를 4단 증거로 강화**, **§4-7의 적용 범위를 §8에 명시**. 새 본문 해시 `2a4ba701d13dbf36…` | 사용자 비준 + 실측 (`GOAL.md` §9) |
| 2026-07-27 | **권한 절 과잉 일반화 정정 + `codex exec` 미확인 닫힘.** 미신뢰 시 무시되는 것은 **`.claude/settings.json` 하나뿐이고 `settings.local.json`은 존중되며, `--allowedTools`·`--settings <파일>`은 신뢰 여부와 무관하게 동작한다** → 남의 기계의 미신뢰 체크아웃에서도 구동 루프가 선다(신뢰된 디렉터리 브랜치 작업은 여전히 가장 단순한 길이라 유지). 권한 실무 사실 확보 — **`bun`은 내장 안전 목록 밖**(`bun --version` 거부, `git status`·`ls`·`node --version`은 통과), 패턴 정확 매칭, 콜론·공백 형식 둘 다, **최소 규칙 `Bash(bun run:*)`+`Bash(bun test:*)`**(`Bash(bun:*)`는 `bun x` 임의 실행을 열어 피한다). 못 잰 것은 복합 명령의 allowlist 분해 하나. **`codex exec -s workspace-write`**로 위험 플래그 없이 작업 디렉터리 쓰기가 열린다 — 구동 루프 확정 | 실행 관측 · `codex-cli 0.145.0` |
| 2026-07-27 | **핸드오프 확정 — 새 세션은 짓기만 한다.** `STATE.md`에 **구현 형상**을 박았다: 저장소 레이아웃(플러그인 배포 · `bin/fabricate` 단일 뿌리 · 훅 셋이 전부 그 뿌리 호출 · 매처 생략하고 스크립트가 이름 검사), 상태 파일 형상(`.fabricate/sessions/<sid>/{active,request.txt,ledger.jsonl,turnstate.json}` · `intent/<id>.json`은 `close`만 쓴다), 턴 회계 알고리즘(`{prev_prompt_id, prev_L}` · `stop_hook_active`면 재차단 대신 장부 기록으로 캡 소진 회피, 방어선은 IP-4), **작업 순서 네 단계**(0: `verify` 하네스와 전 IP fixture를 **RED로 먼저**, default-FAIL / 1: 걷는 뼈대 게이트 없이 / 2: `close` 거부 + Stop 회계 / 3: IP-5·IP-6·IP-4·IP-3), 구동 루프 형상 | — |
| 2026-07-27 | **0단계를 0a·0b로 갈랐다 — 루프를 먼저 켜지 마라.** 사용자가 순환을 지적했다: 구동 루프의 종료 조건인 `bun run verify`가 **0단계의 산출물**이라 루프로 0단계를 돌릴 수 없다(지금 저장소에 `verify` 스크립트·`bin`·`verify/` fixture·`fab-verifier`·allowlist·`drive.sh`가 전부 없다). 더해 **루프는 정해진 목표를 초록으로 가는 데 강하고 열린 설계 질문에 약하다** — 미결 1(모델 호출 경로 회복)·2(`sid` 이음매)는 판단이 필요해 매 패스가 다른 답을 골라 헤맨다. **0a(대화형 부트스트랩: 미결 둘 결정 → 다섯을 짓기 → 연기 시험 넷)** 통과 후에만 **0b(루프)**. 루프 본도 셋을 고쳤다 — verify를 패스당 **한 번만** 돌려 파일로 받기 · 상한 셋(반복 카운터 + `--max-budget-usd` + `AGENT_STOP`) · codex 플러그인의 `timeout: 900` `Stop` 훅을 `--setting-sources project`로 격리. `.gitignore` 목록도 함께 적었다(파일은 미생성) | 사용자 지적 |
| 2026-07-27 | **세션 경계 확정 — 다음 세션은 0a만 한다.** 사용자 결정: **루프를 만드는 것이 우선이고, 구현은 그 다음 세션이 루프로 한다.** 지금까지 0a·0b는 **절차로만** 갈려 있어서 다음 세션이 0a를 하다 구현으로 흘러갈 수 있었다 — *"verify를 지어라"라고 하면 그것을 **통과시키고 싶어지기** 때문*이다. 그래서 **세션으로** 갈랐다: **세션 A = 0a 전부**(미결 1·2 결정 · 부트스트랩 다섯 · 연기 시험 넷), 완료 조건은 **`bun run verify`가 RED로 정상 동작**(어느 IP가 왜 false인지 읽히고 종료 코드 ≠ 0). **RED는 실패가 아니라 A의 정상 산출물이다** — 초록으로 만드는 것은 B의 일을 당겨 하는 것이고 **종료 조건을 짓는 자가 그것을 통과시키는 자가 되는 자기 채점**이다(`GOAL.md` §1). **세션 B = 0b**(`drive.sh`를 켜고 IP를 초록으로, 부트스트랩 재설계 금지, `main`은 전부 초록일 때만). 가르는 이유는 **0a가 판단이고 0b가 반복 갈아내기**라 섞으면 루프를 못 만들고 컨텍스트가 찬다는 것 — `GOAL.md` §0("매 걸음은 직전 걸음이 아니라 이 문서를 시드로")과 같은 이유. 함께 넣은 것: **세션 A가 끝날 때 STATE에 남길 다섯**(미결 결정과 버린 선택지 · 연기 시험의 명령과 출력 · fixture 목록과 왜 RED인가 · 새 미결 · verify 출력 형상)과 **시작 프롬프트 둘**(프롬프트는 휘발하고 STATE는 남는다). **RED-first 전제 검증은 여전히 열려 있고**, 결론이 없으면 그 판정이 **세션 A의 첫 일**이다 | 사용자 결정 |
| 2026-07-27 | **RED-first 전제 검증 완료 — 결론은 "그것만으로는 못 막는다".** 물었던 것은 *"verify를 전부 RED로 먼저 세우는 것이 1차 실패를 막는가, 아니면 fixture가 새 조건 목록이 되어 재생산하는가"*였고, **답은 후자에 가깝다 — 1차가 이미 RED-first를 했고 실패했다.** 실측(수용 검사 69 전수): **총 69 · "frozen red" 선언 44 · `../src/` 직접 import 69(전부) · 프로세스를 띄운 것 4.** 게다가 `src/gate/red-first.ts`라는 **형식 게이트**가 있었고(테스트 저자가 루프 자신인가 · 빨간 관측 기록이 있는가 · 동결 sha256이 변하지 않았는가) 감사 실측으로 **동결 69 중 통과 69·거부 0, 부정행위 0** — **RED-first는 100% 작동했고 그래도 초록 33개에 돌아가는 것은 0개였다.** 구별하는 것은 RED-first가 아니라 **호출 경계**다. 실물: `acceptance/ac-3.test.ts`는 `finalizeIntent`를 검사했는데 `src/cli/interview-finalize.ts`가 부른 것은 **`finalize`** — 다른 함수다. 도달성: 유일한 CLI 파일에서 **운영 `.ts` 102개 중 도달 13 / 미도달 89**(잠금·턴 기록·준비도·모순 패스·전파 전부 미도달), `bin` 없음 · `import.meta.main` 0건 · `citty` import 0건. 결정적 아이러니 — `acceptance/ac-40.test.ts`가 **진입점 자체를 금지**했다. 대칭 증거도 빈-껍데기 반박도 못 막았다(반박이 고친 43개 중 `ac-4`·`ac-24`·`ac-37`이 전부 감사에서 **BROKEN**; 나머지 26은 `review-summary.json`이 `honest`로 명시 판정). **산출물: fixture 헌법 넷 신설** — ① fixture는 운영 모듈을 import하지 않는다(프로세스·합성 stdin·`claude -p`) ② 구현이 만든 판정값을 정답으로 재사용하지 않는다 ③ 관측은 외부 효과뿐(종료 코드·파일·장부) ④ **IP-0·IP-1ⓐ(걷는 뼈대)가 먼저 초록이 된다.** **그리고 이 규칙을 기계가 본다 — `verify/`의 `src/` import 금지가 1차 대비 유일하게 새로운 방어선이다.** 도달 가능성 검사(`GOAL.md` §6-2) 시점도 **0a로 못박았다**(1차에 있었다면 89/102가 첫날 터졌다). **작업 순서 변경**: 0a = 러너 + default-FAIL 계약 + 헌법 검사 + 도달성 검사 + 부트스트랩(**IP별 fixture는 안 만든다**) → 1 = 걷는 경로 하나 → 그 다음 모든 IP를 그 세션의 변형으로. 기존 표는 "갱신됨"으로 보존. **RED-first를 버리는 것이 아니다** — default-FAIL 계약은 유지되고 종료 조건은 여전히 코드보다 먼저 선다. **넷을 다 지켜도 IP-6ⓑ·IP-6ⓓ·IP-5ⓒ는 뚫린다**(`diversity-floor.ts` 다섯 줄이 대칭 fixture를 전부 통과하면서 "같은 해석 + 마침표"에 `{ok:true}`; `context !== "driver"`는 `"Driver"`·`"driver "`·`"the driver"`를 통과) → **`기계 통과`를 완료로 보고하지 않고 `사람 검증 대기`를 함께 말한다**(`GOAL.md` §2). `GOAL.md`는 손대지 않았다 — 술어를 바꾸지 않고 §7이 구현에 맡긴 자리를 채웠다 | Codex(GPT-5.4) 적대적 검토 + 저장소 실물 전수 조사(독립적으로 같은 결론) · `git show 499cd3c^:…` · `audit/2026-07-26-piece3-audit.md` |
| 2026-07-27 | **추적 안 된 문서 셋 제거** — `reveiw_comment.md`(1차 외부 리뷰) · `codex_review_comment2.md`(3차 외부 리뷰) · `REACT_LOOP_CARD_TEMPLATE.md`(ditto 소유, fabricate 산출물 아님). 두 리뷰는 `GOAL.md` 최초 커밋(07-26 23:59)보다 앞선 **미커밋 초안**을 대상으로 쓰여 인용 줄번호가 무효이고, 내용은 이 진척 로그에 이미 흡수됐다 | 사용자 승인 |
| 2026-07-28 | **계획 세션 — 코드 0줄. 넷을 확정했다.** ① **구동 루프는 일회용 비계다**(사용자 결정) — fabricate의 정식 루프로 채택하지 않고, 정식 루프가 무엇이 될지는 이번 범위 밖·미정. 그래서 0a 산출물 다섯을 **제품**(`verify` 스크립트 · `bin/fabricate` · `verify/` 러너 — `GOAL.md` §2·§6이 요구)과 **비계**(`drive.sh` · `fab-verifier.md` · allowlist · `.drive/` · `AGENT_STOP`)로 갈랐고, **"비계를 치워도 `bun run verify` 결과가 같다"**를 기계 판별식으로 세워 0a 연기 시험에 넣었다. 비계는 플러그인 매니페스트에 안 들어간다(들어가면 IP-0 오염). ② **세션이 셋이 됐다** — 계획(이번) · A(루프를 짓는다) · B(그 루프로 fabricate를 짓는다). A와 B가 **서로 다른 물건**을 짓는다는 것이 경계의 핵심이다. ③ **미결 1 결정** — 모델 호출 경로는 표식만 서고 잠금 자격 없음(방향 ③은 선택지가 아니라 `GOAL.md` §1 본문 *"잠금 자격은 원문이 확보된 경로에만 준다"*). 회복은 **새 기제 없이** — 표식이 멱등이고 두 훅이 같은 `session_id`를 받으므로, `close`가 거부 문안으로 슬래시 명령 재입력을 지시하면 사용자가 치는 순간 같은 세션에 UPE가 `request.txt`를 앉혀 승격된다(지은 코드 0줄). **②(start에 원문 인자)는 버렸다** — 모델을 거친 문자열은 *"모델에 닿기 전"* 성질을 잃어 IP-1ⓕ·§4-3의 보증이 이름만 남고 §4-5에 걸린다. 딸린 제약: **`request.txt`를 쓰는 경로는 UPE 훅 하나뿐**(CLI에 두면 §6-3과 같은 구멍). ④ **미결 2 결정** — CLI에 **`--session`을 두지 않는다**. `active`가 정확히 하나일 때 그것을 쓰고 0개·2개 이상이면 exit ≠ 0, 세션 디렉터리는 훅만 만든다. 위조를 좁히는 대신 **모델이 잡을 손잡이를 없앴다**. 대가는 같은 프로젝트에서 동시 세션 둘 불가(어느 IP도 요구하지 않음). 새 미결 하나: CLI가 `.fabricate/`를 찾는 기준 디렉터리가 훅의 `cwd`와 갈리는가 — 세션 A가 실측한다 | 사용자 결정 |
| 2026-07-28 | **실측 정정 셋.** ① **저장소가 옮겨졌다** — `/Users/incognito/dev/projects/fabricate` → **`/Users/ecoletree/dev/project/fabricate`**. 시작 프롬프트 두 블록의 경로를 고쳤다. ② **이 디렉터리는 미신뢰다**(`~/.claude.json` → `hasTrustDialogAccepted: false`). *"이미 신뢰돼 있다"*는 전제가 깨졌다 — 다만 `settings.local.json`은 미신뢰에서도 존중되고 `--allowedTools`는 신뢰 무관이므로 루프는 선다. **연기 시험 넷째가 이걸 닫는 자리**이고, 안 되면 `--allowedTools`로 옮긴다(전역 설정에 신뢰를 써 넣는 길은 여전히 안 쓴다). ③ **`codex`가 다운그레이드됐다** — `0.145.0` → `0.142.5`. `-s workspace-write`는 **이 버전에도 있다**(`--help` 확인: `read-only, workspace-write, danger-full-access`) → 구동 루프 영향 없음. 환경: `bun 1.3.14` · `claude 2.1.220` | 실행 관측 |
| 2026-07-28 | **커밋 `946ac61` 이후 문서 둘은 논외** — `IMPLEMENTATION-ORCHESTRATION.md`(691줄, 잠긴 의도를 받아 구현까지 가는 **다음 단계** 하네스 요건) · `DITTO-PRE-MORTEM.md`(805줄, ditto 코드 조사본). 사용자 확인: *"요건 정리 중이라 반영 안 한 것. 지금은 고려할 필요 없다."* 둘 다 머리말에 *"비준된 `GOAL.md`를 변경하지 않는다"*·*"미비준 초안"*이라 적혀 있다. **세션 A·B는 이 둘을 읽지 않는다** — 완료 정의는 `GOAL.md` §2의 IP-0~6뿐이다 | 사용자 확인 |
| 2026-07-28 | **세션 A 프롬프트 적대적 검증 1차 → 결함 열아홉 반영.** 프롬프트의 실제 독자와 같은 조건(서사를 모르는 신선 컨텍스트)에 넷을 독립으로 놓고 때렸다 — 세션 A 시뮬레이션 · 적대적 반증 · 문서 정합성 · **Codex(GPT-5.5)**(`GOAL.md` §8의 "같은 계열" 편향을 깨는 자리). **셋 이상이 독립 수렴한 급소 넷**: ① **정적 RED 허점** — 아무것도 검사하지 않고 `exit(1)` 하는 스크립트가 완료 조건을 전부 만족한다(반증자와 Codex가 거의 같은 스트로맨 코드를 따로 제출) → **양극 자기시험** 신설 ② **대상 0개 = 거짓 초록** — `src/`도 fixture도 없어 헌법·도달성 검사가 항상 PASS, 1차의 형식 게이트(69/69 통과·적발 0)와 같은 모양 → `n/a — 대상 0개` 상태 + **음극 자기시험 셋** 신설 ③ **자기 채점이 A→B로 이동했을 뿐이고 1차보다 후퇴** — 1차엔 `gate-a/red-freeze.json`이 `sha256`·`author_context`를 기계로 봤는데 이번엔 0개였고 B의 codex가 `verify/`를 고칠 수 있다 → **기준 동결 매니페스트** 신설(1차는 테스트를, 이번엔 눈금 자신을 동결) ④ **눈금 해상도 미정**(7 vs 43 vs 54) → `GOAL.md` §2 동그라미를 세어 **43개 확정**. **실행으로 닫은 사실 일곱**: `--allowedTools`가 가변 인자라 뒤의 프롬프트를 삼킨다(콤마로 묶어도 같음) → **프롬프트는 stdin** · `--setting-sources project` + `--allowedTools` 조합이 **미신뢰 디렉터리에서 통과**(대조군은 `This command requires approval`) · `--setting-sources`는 `user/project/local`을 따로 받고 `settings.local.json`은 **local**이라 `project`만 주면 권한이 0 · 옛 비계 판별식의 `mv …/*` 복구가 **dotfile을 놓쳐 `.claude`·`.drive`가 유실**(재현함) → 판별식을 **정적 참조 금지 검사**로 교체 · `drive.sh` 본이 `.drive/`를 안 만들어 리디렉션 `rc=1` → 빈 로그로 20라운드 헛돎 · `codex exec`에 상한 플래그 **0건**(`--max-budget-usd`는 claude 전용) → codex는 셸 `timeout`으로 · 환경 사유로 이미 빨간 것들(`typecheck` exit 2 TS18003 · `lint` exit 127 biome 미설치 · `bun test` 0개). 함께: `drive.sh` 본이 `FINDINGS.md`·`STATE.md`를 시드로 줘 **`GOAL.md` §0 정면 위반**이던 것을 제거(사람이 읽는 파일로만 남긴다) · `AGENT_STOP` 검사를 verify **앞**으로 · `tsconfig` `include`에 `verify/`·`bin/` 추가(안 하면 새 코드 전부가 타입 검사 밖) · 헌법 규칙 4를 A용으로 다시 씀(*"IP-0·IP-1ⓐ가 먼저 초록"*과 *"IP를 초록으로 만들지 마라"*가 A에게 동시에 걸려 있던 자기모순) · 미결 3(verify 격리 vs IP-0ⓐ 4단 증거)이 프롬프트에서 통째로 빠져 있던 것을 A의 유일한 결정 자리로 명시 · 첫 5분 판별 명령에서 `package.json` 제거(비어 있지 않아 **세션 B로 오라우팅**됐다) · `fab-verifier`의 쓰기 불가를 보는 **음극 연기 시험** 추가 · 완료 조건 다섯 → **여섯**. **기각 하나**: Codex의 *"프롬프트는 Claude인데 A가 `fab-verifier.md`를 만드니 역할 위반"* — A는 Claude 세션이므로 규율 그대로다. **반증된 옛 논거 하나**: *"1차에 도달성 검사가 있었다면 89/102가 첫날 터졌다"* — 1차 첫날에도 모듈은 0개였다. 그래서 자기시험 없는 0a 배치는 값이 거의 없다 | 신선 컨텍스트 3 + Codex(GPT-5.5) 독립 검증 · 실행 관측 |
| 2026-07-28 | **적대적 검증 2차 → 설계 교착을 풀고 초록 조건을 확정, 세션 A를 A1·A2로 분할.** 2차도 넷을 독립으로 돌렸고(신선 컨텍스트 3 + Codex/GPT-5.5), **1차 수정이 만든 새 결함 여섯**을 포함해 서른 남짓이 나왔다. **결정적 발견 셋**: ① **동결↔양극 자기시험 교착** — 기준과 판정값이 한 파일이면 하나를 `true`로 뒤집는 순간 **해시 대조가 먼저 죽어** 초록을 관측할 수 없고, 갈라 놓으면 판정 기제가 A에 없어 임시 check가 필요한데 그건 *"IP를 초록으로 만들지 마라"*에 걸린다 — **어느 쪽이든 위반** ② **`bun run verify`가 언제 `exit 0`인지가 문서 어디에도 없었다** — 즉 *"초록의 정의를 그 눈금에 채점당하는 쪽(B)이 쓴다"*로, 1차 검증이 잡았다는 "자기 채점이 A→B로 이동"이 **아직 안 닫혀 있었다** ③ **43개가 `GOAL.md`의 완료 정의 전부가 아니다** — §6의 여섯 중 다섯과 §2의 훅 통합 검사·설치 경로 대조가 **어느 축에도 없었고**, 동결이 그 누락을 고정할 참이었다. **처방(계획 세션이 확정)**: **축 셋**(A: IP 기준 43 · B: 구조 검사 9(S-1~S-9) · C: 도구 사슬 3 = **55**) · **초록 조건을 `exit 0`의 정의로 못박음**(축A 전부 true ∧ 축B 전부 PASS ∧ 축C 전부 통과 ∧ 매니페스트 대조 통과, **`n/a`는 초록이 아니다**) · **동결 대상 = 55개 id + GOAL 원문 + check 바인딩 경로 + 집계 규칙이고 판정값은 제외**(이 한 줄이 교착을 푼다) · 매니페스트 sha256을 **STATE에 텍스트로** 박고 갱신 스크립트를 저장소에 남기지 않으며 해시는 기준별+전체 · **양극 자기시험 정의 교체**(합성 `true` 뒤집기 → **실제 check 함수를 붙여 참/거짓 합성 입력에서 초록·빨강을 관측하고 check는 남긴다** — 입력을 지우면 다시 false라 IP 충족이 아니고, 세션 B가 이음매를 실물로 받는다) · **`S-9` 비계 참조 금지에서 `.claude/` 전체 금지 철회**(`S-7`이 그 경로를 읽어야 해 `GOAL.md` §2와 충돌했고, 가장 싼 수리가 문자열 조립이라 검사가 영구 무의미해진다) — 금지는 **다섯 경로**로 좁히고 검사는 **도달 가능한 모듈의 전이 폐쇄**로 · **브랜치 규약 신설**(A1·A2·B는 `bootstrap/0a`에서 작업. A의 산출물은 정의상 초록이 아니라 `main` 착지가 §3 위반이고, 브랜치에만 두면 B가 자기를 A로 오판한다) · **`timeout` 부재의 대체**(macOS에 없음. `perl -e 'alarm shift; exec @ARGV'` 실측 확인 — 종료 코드 142) **+ `drive.sh` preflight 신설**(codex·claude·perl alarm 실재를 첫머리에서 확인하고 없으면 즉시 죽는다. 드라이런이 구조적으로 못 잡던 자리) · **`STATE.md`를 라운드 입력에 되돌림**(1차에서 §0을 근거로 뺐는데 §0은 *"이 문서 전문 + `STATE.md` 요약"*을 주라고 적었다 — 과잉 교정이었다. `FINDINGS.md`만 뺀다) · **`fab-verifier`의 Bash를 `Bash(bun run:*)`로 좁힘**(넓은 Bash는 그 자체가 쓰기 도구다) · **드라이런을 `DRY_RUN=1`로 재정의**(센티널을 verify 앞에 두면서 *"verify가 한 번 돌고 센티널에 걸린다"*가 논리적으로 불가능해졌던 자기모순 해소). **세션 분할**: **A1(눈금) / A2(비계)** — 검토자 둘이 독립으로 *"한 세션에 안 들어가고 잘릴 자리가 정확히 새로 넣은 방어들"*이라 진단했다. 그 밖 문서 모순 쓸기: 첫 5분 판별을 브랜치 우선으로 · 옛 연기 시험 블록(폐기된 argv 형태)에 경고 · 옛 구동 루프 형상에 경고 · 미결 1·2 열림 서술 정정 · *"정할 것 하나뿐"*이 거짓이라 **여덟으로 열거**(2차 검증이 최소 열하나라고 셌다) | 신선 컨텍스트 3 + Codex(GPT-5.5) 2차 독립 검증 · 실행 관측 · 사용자 결정(② → ③) |
| 2026-07-28 | **적대적 검증 3차 → 2차 처방이 병을 옮겼을 뿐임을 확인하고 동결 범위를 넓혔다.** 신선 컨텍스트 3(A1 시뮬레이션 · A2 시뮬레이션 · GOAL 전수 대조) + Codex/GPT-5.4 를 독립으로 돌렸다. **독립 수렴한 치명 넷**: ① **동결이 exit 코드를 만드는 것을 하나도 안 덮었다** — 동결 목록이 id·GOAL 원문·바인딩 *경로*·집계 규칙뿐이라 `package.json` 의 `"verify": "exit 0"` 한 줄로 전부 통과한다(Codex·A1렌즈가 거의 같은 스트로맨을 따로 제출). 변종 `"bun run verify:real \|\| true"` 는 실패 출력이 그대로 흐르고 종료 코드만 0이라 눈으로 봐도 속는다 ② **check 본문이 동결 밖** — 55개를 `() => ({pass:true})` 로 써도 대조가 통과한다. *"1차는 테스트를 동결하고 구현을 열어 뒀고, 2차 판은 기준을 동결하고 판정 기제를 열어 뒀다."* 더 날카로운 변종(Codex): **판정값을 동결에서 뺀 그 결정**이 check 가 저장소 안 `status.json` 을 읽는 것을 합법화한다 ③ **`exit 0` 이 도달 가능한지를 아무도 관측하지 않는다** — `[7]` 전체에서 종료 코드를 보는 곳은 baseline(=RED) 한 줄뿐이고, 양극 자기시험은 *기준 하나의 값*만 본다. 1차가 잡은 "정적 RED 허점" 의 처방이 **관측 지점을 종료 코드에서 출력 텍스트로 옮겼을 뿐 병은 종료 코드에 그대로 있었다** ④ **`perl alarm` 이 codex 의 실제 워커를 안 죽인다** — 실측: 손자가 PPID=1 로 생존. 원인 확정 — `codex` 는 네이티브 바이너리를 `spawn` 하는 node 래퍼이고 **SIGINT/SIGTERM/SIGHUP 만 전달한다(SIGALRM 없음)**. 즉 142 를 받고 다음 라운드로 가는 동안 실제 codex 가 `workspace-write` 를 든 채 계속 쓴다 → **동시 작성자 둘**. 덤: `perl … exec <없는 명령>` 은 **stderr 한 줄 없이 exit 0**(GOAL §4-7 을 비계 자신이 위반). **커버리지 — 축이 GOAL 을 안 덮던 넷**(렌즈 C·Codex 수렴): 대칭 증거(§2 :99·:123·:133)와 "레코드를 만들지 않는다"(§2 IP-2 머리글)는 **동그라미가 아니라 산문·머리글**이라 축 A 정의에서 빠졌고, **§4-9(verify 격리)·§4-10(절대경로 금지)**은 어느 축에도 없었다 — 후자는 더 나쁘게 **S-7 이 절대경로를 통과시킨다**(절대경로도 "이 저장소를 가리킨다"). GOAL §2 가 이름으로 못박은 훅 **세 사건**과 §6-6 의 **두 기제**도 각각 boolean 하나로 접혀 있었다. **처방**: **축 B 를 9 → 16 으로**(S-5~S-7 훅 세 사건 분할 · S-8~S-9 이름 판별 둘 · S-11 절대경로 · S-12 verify 격리 · S-13 대칭 증거 · S-14 거부 시 레코드 미생성), **합 55 → 62** · **초록 조건에 다섯째 줄 신설**(모든 값은 공개 경로의 외부 효과에서 나와야 하고 러너가 계측한다. 계측 없는 `true` 는 `false` 로 강등) · **`n/a` 판정을 check 에서 러너로 이관**(check 는 `targets` 를 돌려주고 비면 러너가 덮어쓴다) · **동결 범위 확대**(`package.json` 의 `scripts.verify` · 러너와 `criteria/` 의 sha256 · 각 기준의 최소 관측 계약 · 양극·음극 fixture 선언 · S-2·S-3·S-15 의 최소 대상 수 · GOAL 원문의 시작·끝 오프셋) · **집계 자기시험 신설**(override 로 `exit 0` 을 한 번 관측하고 되돌린다) · **양극 자기시험에 제약**(대상 check 는 `bin/fabricate` 를 서브프로세스로 띄워야 한다. 스텁이 항상 exit≠0 이라 `--selftest-exit <n>` 를 둔다 — 안 그러면 가장 싼 구현이 디스크만 읽는 check 이고 **B가 그것을 42번 복제한다**) · **B의 쓰기 범위를 경로로 분할**(`verify/checks/**`·`verify/fixtures/**` 만 허용. *"verify/ 수정 금지"* 는 쓸 수 없다 — check 가 그 안에 있어 **B가 할 일을 통째로 금지하게 된다**) · **codex 타임아웃을 fork+setpgrp+`kill -TERM -$pid` 로 교체**하고 종료 코드를 0/142/127 로 분기 · **프롬프트를 argv → stdin heredoc**(GOAL+STATE 가 213KB, ARG_MAX 1MB, STATE 는 세션마다 자란다) · **`verify.log` 의 `tail -80` 제거**(출력 형식은 A1 이 정하는데 소비자 창만 고정하면 실패 사유가 앞쪽인 형식에서 B가 아무것도 못 본다) · **`fab-verifier` 에 `Bash(git diff:*)`·`Bash(git status:*)` 추가**(*"verify/ 의 git diff 를 보고하라"* 를 시키면서 `bun run` 만 줬다) · **A2 에 A1 눈금 인수 검사 셋 신설**(해시 대조 + 음극 자기시험 하나 재현. 기존엔 `exit != 0` 만 봐서 출력을 하드코딩한 스크립트가 통과했다) · **`DRY_RUN=2` 스텁 모드 신설**(codex·claude 호출줄이 세션 B의 첫 라운드까지 한 번도 실행되지 않던 자리) · **미결 3 을 계획 세션이 닫았다**(A1 은 결정에 필요한 대상을 만들 권한이 없었다 — 합성 stdin 과 `claude -p` 는 택일이 아니라 **서로 다른 축 항목**이고, S-5~S-7 에 `must_spawn:"claude"` 를 동결하면 A1 시점엔 `n/a` 이고 `n/a` 는 초록이 아니라 계약이 스스로 지킨다) · **`.gitignore` 를 A2 → A1 으로 이관**(A1 의 되돌림 검증이 `git status --porcelain` 이 빈 것을 요구하는데 실측으로 비어 있지 않았다. 원인 확정: `:136` 의 `.idea/*` 를 `:138` 의 `!.idea/codeStyles` 부정이 되뚫어 `.idea/codeStyles/Project.xml` 이 빠져나온다) · **축 C 의 `test` 충족 책임을 A1 의 결정 목록에 추가**(테스트 파일 0개면 `bun test` 는 영원히 exit 1 이고 초록 조건이 원리적으로 만족 불가인데 아무도 이 책임을 안 지고 있었다). **문서 모순 쓸기**: 첫 5분을 **세션 판별표**로 교체(`:15` 3분기 vs `:18` 2분기가 충돌해 **A1 직후 세션이 B로 오라우팅**돼 A2 가 통째로 건너뛰어졌다) · `bootstrap/0a` 체크아웃 지시를 생성(`-b`)으로 정정(브랜치가 없다) · "고친 형상" 의 `timeout 1800` 제거(이 기계에 없다) · 라운드 입력의 STATE 유무 충돌 해소 · `n-a` → `n/a` · 반증된 *"89/102가 첫날 터졌다"* 를 살아 있던 세 곳에 반증 표시 · A2 `[0]` 에 `bun install` 추가 · `PATH= bash drive.sh` → `env -i /bin/bash drive.sh`(전자는 셸이 `bash` 자체를 못 찾아 **drive.sh 가 한 줄도 안 돈 채 exit 127**, 그런데 합격 기준이 "exit≠0" 이라 preflight 가 조용히 통과한다) · SMOKE 시험을 `-C <임시dir>` 로 격리 · codex 경로 하드코딩 금지(fnm 경로에 셸 PID+타임스탬프가 들어간다). **환경 사실 16건 전수 재확인** — 13건 참, `--setting-sources` 권한 대조군 2건은 라이브 호출이 필요해 미검증, `tsc` 가 전역(fnm)이라 다른 기계에선 `typecheck` 도 127 일 수 있다는 것 신규. **기각 넷**: *"축 A 에 판정 불가한 것이 있어 `n/a` 금지가 §4-5 위반을 강제한다"*(GOAL :117 이 *"기계가 보는 것은 절차와 형상뿐"* 으로 이미 답했다) · *"43개를 기계로 뽑으면 45"*(중복 제거하면 정확히 43. 살아남는 것은 **경계** 문제뿐이라 오프셋 동결로 옮겼다) · *"`사람 검증 대기` 병기가 축에 없다"*(에이전트의 보고 규율이지 verify 의 exit 코드 요구가 아니다) · *"§4-10 ↔ A2 프롬프트의 절대경로"*(§4-10 은 설치에 걸리고 `drive.sh` 는 배포 대상이 아니다). **`GOAL.md` 는 손대지 않았다** — 술어도 불변식도 그대로이고, §7 이 구현에 맡긴 자리와 §2·§4 가 요구하는데 축에서 빠져 있던 것을 채웠다 | 신선 컨텍스트 3 + Codex(GPT-5.4) 3차 독립 검증 · 실행 관측 |
