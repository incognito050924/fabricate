# 다중 에이전트 심의는 왜 합의에 도달하지 않는가
> 기존 실행 로그 측정 · 두 축의 진단 · 검증 설계

이 문서는 [DISCUSSION.md](DISCUSSION.md) §8~§9의 실행 계약이 전제하는 것 — 심의가 언젠가 끝나고, 끝난 결과가 유지된다 — 을 실제 실행 기록으로 확인한 것이다.

확인 결과 둘 다 성립하지 않았다.

---

# 1. 무엇을 묻는가

관찰은 둘이고, 서로 다른 것이다. 섞으면 진단이 흐려진다.

**관찰 1 — 끝나지 않는다.** "더 이상 이슈가 안 나옴" 또는 "모든 이슈에 합의 완료"에 도달하는 경우를 보지 못했다.

**관찰 2 — 도달해도 유지되지 않는다.** 한 번 합의에 도달했더라도 다시 돌리면 또 새로운 것이 나오고, 진폭이 줄지 않는다.

관찰 2가 더 강한 주장이다. 관찰 1은 "목표선에 못 닿는다"이고, 관찰 2는 "목표선이 지나가도 머물지 않는다"이기 때문이다. 둘은 각각 다른 고장이며, 뒤에서 보듯 **하나만 고치면 나머지가 그 효과를 없앤다.**

## 1.1 범위 — 합의는 여러 목적 중 하나다

측정 대상인 ditto의 dialectic은 **합의 모델**이다. 이 문서의 진단과 대응도 합의 모델에 한정된다.

다중 에이전트를 쓰는 목적은 그것만이 아니고, 목적이 다르면 종료 조건도 실패 모드도 다르다.

| 목적 | 묻는 것 | 종료 조건 | 수렴이 좋은가 |
|---|---|---|---|
| **합의** | 이 안으로 가도 되는가 | 잔여가 감당 가능 | 예 |
| **발산** | 다른 길은 없는가 | 후보 k개 확보 | **아니오 — 수렴이 곧 실패다** |
| **탐색** | 무엇을 모르는가 | 커버리지 목록 소진 | 부분적 |
| **판정** | 상충하는 증거 중 무엇이 이기는가 | 결정 1개 산출 | 불필요 — 불일치해도 결정은 난다 |
| **검증** | 이 명제가 참인가 | 명제 전부 판정 | 예 (거의 결정적) |

합의 모델의 종료 조건을 발산 과제에 걸면 다양성을 죽이고, 탐색 과제에 만장일치를 요구하면 끝나지 않는다. DISCUSSION.md §8.3의 Planner(후보 2개 이상)는 발산 단계인데 같은 계약 아래 놓여 있다 — 이 문서는 그 문제를 다루지 않는다.

---

# 2. 무엇을 쟀는가

실행 기록이 남아 있는 코퍼스 둘을 썼다.

| | 데이터셋 A | 데이터셋 B |
|---|---|---|
| 위치 | `ditto/reports/design/agentic-governance/reviews/` | `boxwood-workspace-63-e2e/.claude/state/reviews/tmp/` |
| 형태 | Producer / Opponent / Synthesizer 3역할 dialectic | architect / redteam / security / codex 병렬 리뷰 |
| 규모 | 9라운드 | 4개 주제 × 2~5회 반복 × 3~4역할 = 15계열 |
| 스키마 | `ditto.dialectic.v1` 외 2종 | 역할별·회차별 상이 |

데이터셋 A는 worktree 두 곳에 복제되어 있으나 md5가 동일해 재실행이 아니라 사본이다.

> **코퍼스 전체에 같은 입력으로 두 번 돌린 기록이 하나도 없다.**

그래서 관찰 2(재실행 시 재현되는가)는 이 데이터로 **직접 측정할 수 없다.** 이것이 §7의 실험이 필요한 이유다. 다만 §3.3에서 보듯 재실행 없이도 확인되는 불안정성이 따로 나왔다.

---

# 3. 측정 결과

## 3.1 데이터셋 A — dialectic 9라운드

| 라운드 | R1 | R2 | R3 | R4 | R5 | R6 | R7 | R8 | R9 |
|---|---|---|---|---|---|---|---|---|---|
| 반박 수 | 14 | 10 | 6 | **20** | 9 | 7 | 8 | 13 | 11 |
| 인정(admissible) | 10 | 6 | 2 | 15 | 7 | — | — | 5 | 5 |
| Producer 자평 | accept | accept | accept | accept | accept | — | — | accept | revise |
| 최종 판정 | revise | revise | revise | revise | revise | revise | revise | revise | revise |

**9라운드 전부 `revise`. accept에 한 번도 도달하지 않았다.**

반박 수는 단조 감소하지 않는다. R3에서 6까지 내려갔다가 R4에서 20으로 3.3배 튄다.

Producer의 자기 평가는 기록된 7라운드 중 6회가 `accept`다. 자기 안이 통과할 만하다는 판단이 여섯 번 연속 틀렸다.

그리고 원인으로 보이는 것들이 같이 나왔다.

### 검토 대상이 라운드마다 이동했다

```
R1~R6  설계문서 6개 (00-framework ~ 60-practice-ingestion-map)
R7     "코드정합 편집(00/10/20/60/v0-implementation-plan)"
R8     80-acg-cleanup-deslop-plan.md   ← 새로 쓴 문서
R9     80-acg-cleanup-deslop-plan.md (v3) + ADR-0017
```

같은 것을 9번 검토한 것이 아니다. 수렴할 대상이 고정된 적이 없으므로 수렴 여부를 물을 수 없다.

### 반박 ID 네임스페이스가 충돌한다

R1~R5는 `OBJ-01`~`OBJ-59`로 전역 일련번호를 썼는데, R8·R9는 다시 `OBJ-01`부터 시작한다. `OBJ-01`이 서로 다른 세 가지를 가리킨다. 라운드를 넘어 반박을 추적하는 것이 원리적으로 불가능하다.

### 분류 체계가 매 라운드 새로 만들어졌다

카테고리 40종 중 두 라운드 이상에 등장하는 것은 5종뿐이다. R4는 반박 20개에 고유 카테고리 20개(`implementation_readiness.envelope`, `open_question.q3` 같은 점 표기)를 붙였다. 라운드끼리 대조할 좌표계가 없다.

### 오라클은 강해졌는데 종료율은 그대로다

```
R1~R3   00 §6 ↔ 20 §0                      문서 절 번호 — 문서가 편집되면 무효
R4~R5   20:169-175, 30:57-59                라인 번호
R8~R9   src/acg/fitness/fitness-runner.ts:56-59
        grep src/ (ts-morph·LanguageService) 0건    실행 결과
```

오라클 품질이 3단계로 올라갔다. 종료율은 0으로 동일하다.

> **오라클 규칙만으로는 종료가 되지 않는다.** DISCUSSION.md §8.2 (1)이 판정의 질을 올리는 장치이지 종료 장치가 아니라는 뜻이다.

## 3.2 데이터셋 B — 병렬 다역할 리뷰

발견 수 / 판정. `*`는 리스트가 아니라 severity 스칼라 개수로 기록된 회차, `·`는 기록 없음.

```
s1  architect   8/req   10/req   ·/req    5*/req   ·/req
    redteam    12/req   13/req   ·/req    9*/req   ·/req
    security    9/req    6/req   ·/req    7*/req   ·/req

s2  architect   3/req    4/req    1/req
    codex       6/req    5/req    1/req
    redteam     5/req    8/req    6/req
    security    5/req    2/req    4/req

s3  architect   5/app    4/req
    codex       1/req    3/req
    redteam     7/req    6/req
    security    3/req    1/req

s4  architect   2/app    4/app    0/app    3/req    3/req
    codex       4/req    1/unk    ·        5/req    2/req
    redteam     8/req    8/req    9/req    7/req   10/req
    security    2/req    3/req    2/req    5/req    0/app
```

**15계열 중 발견 수가 단조 감소한 것은 4계열뿐이다** (s2-codex, s3-architect, s3-redteam, s3-security).

**s4 redteam은 8 → 8 → 9 → 7 → 10.** 5회 반복 후 시작보다 지적이 많다. 감쇠 진동이 아니라 평균 주위의 요동이다.

### 전원 동시 approve는 0회다

개별 역할의 `approve`는 5회 나왔다 — s3 architect it1, s4 architect it1·it2·it3, s4 security it5. 그런데 **모든 역할이 같은 회차에 approve한 경우는 15회 반복 중 한 번도 없다.**

s4 it5가 전형이다. 같은 시점에 security는 `approve`(0건), architect는 critical 3건, redteam은 10건이다.

이건 산술적으로 예상되는 결과다. 역할 하나가 approve할 확률이 p이고 회차마다 독립이면 역할 k개의 동시 approve 확률은 p^k다. p=0.6이어도 k=4면 0.13이고, 회차를 늘려도 개선되지 않는다 — 각 회차가 독립 표본이라 누적되지 않기 때문이다.

> **"모두가 동의할 때 끝낸다"는 종료 조건은 역할을 나눌수록 지수적으로 멀어진다.**

역할 분리가 검증을 강하게 만든 만큼 종료를 어렵게 만들었고, 지금 구조에는 이 둘을 분리하는 장치가 없다.

### 합의가 뒤집힌 사례

- **s3 architect**: it1 `approve` → it2 `request_changes`
- **s4 architect**: it1·it2·it3 연속 `approve`(it3은 발견 0건) → it4 `request_changes` critical 2건 → it5 `request_changes` critical 3건

s4 architect가 관찰 2의 직접 사례다. 3회 연속 승인 후 재개하자 critical이 나왔고, 이후 승인으로 돌아오지 않았다.

발견 위치도 회차 간에 겹치지 않는다.

```
it4   L75-82,  L112,     L255
it5   L101-110, L181-184, L186
```

매 회차가 같은 문서의 다른 영역을 본다. 누적 커버 영역이 계속 넓어지고 포화되지 않는다.

## 3.3 단일 판정 안에서 이미 무너져 있다

`iter3-security.json` 한 레코드 안에:

```
resolved_from_iter2_critical:   ["C5", "C6", "H-hash-trunc"]
still_unresolved_critical:      [ C5(...), C6(...) ]
```

**C5와 C6이 해결됨과 미해결에 동시 등재됐다.** 같은 에이전트가 같은 판정을 쓰면서 같은 항목을 양쪽에 넣었다.

합의 붕괴가 회차 사이의 확률적 요동이 아니라 **하나의 판정 안에서** 일어났다. 재실행 실험 없이 확인되는 불안정성이며, "해결됨"이라는 판정이 애초에 안정된 상태가 아니었음을 뜻한다.

## 3.4 원장은 있었으나 작동하지 않았다

이 코퍼스에는 원장(닫힌 항목 기록)이 실제로 있었다. `resolved_from_iter1`, `still_unresolved`, `unresolved_from_iter3` 같은 필드가 14개 레코드에 붙어 있다.

그런데 `iter4-architect.json`은 이렇다.

```json
{"reviewer":"architect","iteration":4,"verdict":"request_changes",
 "critical":0,"high":1,"medium":2,"low":2,
 "unresolved_from_iter3":[]}
```

이월된 미해결은 0건인데 신규 발견이 5건이고 판정은 `request_changes`다.

> **원장이 이월 추적에만 쓰였고 탐색 범위 제한에는 쓰이지 않았다.**

닫힌 것을 기록은 했지만 다음 회차는 여전히 전체를 다시 봤다. 그래서 감쇠가 없었다. 원장의 가치는 기억이 아니라 **범위 축소**에 있다는 것이 여기서 드러난다.

## 3.5 계측 자체가 거짓 수렴을 읽었다

이 분석 과정에서 자동 판독이 두 번 틀렸다.

| 읽은 값 | 실제 | 원인 |
|---|---|---|
| R7 반박 0건 | 8건 | 반박이 `objections`가 아니라 `synthesizer.accepted_objections`에 저장 |
| s1 it4 발견 0건 | 5건 | 리스트가 아니라 `high:1, medium:2, low:2` 스칼라 필드 |
| s1 it5 발견 0건 | 미상 | 기록 자체가 없음. verdict만 `request_changes` |

데이터셋 A는 심의 도중 레코드 스키마가 세 번 바뀌었고(`ditto.dialectic.v1` → `0.1.0` → `v1`+`round`/`max_rounds`), 반박 저장 위치가 세 곳으로 갈렸다.

> **형식이 고정되지 않으면 기계적 판독기가 침묵을 수렴으로 읽는다.**

형식 고정은 위생 문제가 아니라 **측정 가능성의 전제**다. 형식이 흔들리면 수렴 여부를 물을 수단 자체가 없어진다.

---

## 3.6 항목은 어떻게 사라지는가 — 소멸률과 이관율

**소멸 = 앞 회차에 기록된 항목 중, 다음 회차 원장에서 해소·이월 어느 쪽으로도 명시되지 않은 것.**

### 먼저, 항목에 식별자가 없다

| | 데이터셋 A | 데이터셋 B |
|---|---|---|
| 반박·이슈 건수 | 83 | 216 |
| 식별자 필드를 가진 것 | 83 (100%) | **0 (0%)** |

데이터셋 B는 식별자가 없는데 원장은 그 이슈들을 참조한다. 참조 83건의 형태는 이렇다.

| 참조 형태 | 건수 | 해소 가능? |
|---|---|---|
| 위치 인덱스 (`[5,6,7,8]`) | 48 (58%) | 앞 회차 목록의 1-기반 위치라고 **가정하면** 가능 |
| 라벨 (`"C1"`, `"CT-001"`, `"nested_json"`) | 35 (42%) | **불가** — 이슈 레코드에 그런 이름이 없다 |

원장 참조의 42%가 존재하지 않는 이름을 가리킨다. 소멸을 세기 전에, 무엇이 닫혔다는 기록이 무엇을 가리키는지부터 확인되지 않는다.

### 소멸률

| 코퍼스 | 소멸률 | 범위 |
|---|---|---|
| 데이터셋 B | **18%** (18/99) | 15개 회차 전이. 해소 불가한 라벨 참조 35건도 이월로 인정해 준 관대한 계산 |
| 데이터셋 A | **87%** (13/15) | 전역 일련번호 구간 R1~R5. R8·R9는 ID 재시작으로 측정 불가 |

데이터셋 A에서 살아남은 2건은 둘 다 **직전 라운드(R4)** 것이다. R1과 R3에서 미결로 남은 5건은 이후 네 라운드 어디에도 나타나지 않는다. **이월은 한 라운드를 넘기지 못한다.**

R5에 명시적 원장(`closed_in_this_round`, `already_closed_prior`)이 있으나 거기 적힌 번호는 전부 R4·R5 자기 항목이다. R1~R3의 미결 항목은 어느 원장에도 등장하지 않는다.

### 이관이 소멸의 경로다

데이터셋 A 반박 83건의 처리 분포:

```
defer(후속 이관)          13건
open_question(열린 질문)   12건
v0-scope(범위 밖)          5건
surfaced(드러냄)           3건
partial(부분)              2건
─────────────────────────────
합계                      35건 = 전체의 42%
```

**42%가 미래나 범위 밖으로 넘어갔다.** 그리고 그중 "언제·무엇이 되면 닫히는가"가 적힌 것은 **4건(11%)**뿐이다.

R4에서 `defer-*`로 넘긴 6건 중 **5건이 이후 라운드에 다시 나타나지 않았다.** 닫힘 조건 없이 이관하면 사라진다 — 이관율과 소멸률이 같은 현상의 앞뒤다.

### 방법의 한계

- 데이터셋 B의 위치 인덱스를 "1-기반 전체 목록 위치"로 해석했다. 이 가정은 검증할 수 없다.
- `defer-*`를 미닫힘으로 분류했다. 의도적 이관은 소멸과 다르다고 볼 수도 있으나, **추적되지 않으면 둘을 구별할 수 없다**는 것이 요점이다.
- "이후 언급"을 레코드 전문 정규식 검색으로 판정했다. 이름만 스쳐도 이월로 쳤으므로 관대한 쪽이다.

---

## 3.7 닫힘은 검사되지 않는다

반박을 제기하는 쪽과 반박을 없애는 쪽에 걸린 관문이 다르다.

### 적합성 심사가 한쪽에만 있다

| | 반박 | 닫힘 |
|---|---|---|
| 건수 (데이터셋 A) | 83 | 32 |
| 적합성 판정 필드(`admissible`) | **100%** | **0% — 스키마에 없다** |

반박 쪽 심사는 형식만이 아니라 실제로 작동했다. **33건이 `admissible=False`로 걸렸고, 오라클이 정확히 붙어 있는데도 기각된 것들이 있다.** R3 OBJ-25는 `20 §6 line561`이라는 라인 단위 앵커를 갖고도 기각됐다.

닫힘 쪽에는 그 심사가 존재하지 않는다.

### 닫힘의 근거는 검증이 아니라 편집 서술이다

닫힘 32건의 근거 서술을 분류했다.

```
편집 행위를 서술한 것       29건 (91%)
재실행·재검증을 인용한 것    1건 ( 3%)
```

표본:

```
R1 OBJ-01: "00 §6 표에 3행 추가 + 결론을 '...'로 정정, 20 §0과 일치"
R1 OBJ-10: "10 §4 리팩토링 행을 1,2(얇게),5,6,8로 수정 + 단계5 입력에 ... 명시"
```

**이 서술들은 참이다.** 정말 3행을 추가했다. 그러나 이는 "내가 무엇을 고쳤다"이지 "그래서 그 반박이 해소됐다"가 아니다.

### 근거 없는 닫힘 선언

R5가 `already_closed_prior`로 "이미 닫혔다"고 선언한 항목의 실제 기록:

| 항목 | R5의 주장 | R4의 실제 disposition |
|---|---|---|
| OBJ-33 | 이미 닫힘 | `fix-needed` |
| OBJ-34 | 이미 닫힘 | `fix-needed` |
| OBJ-42 | 이미 닫힘 (Opponent도 인정) | `surfaced` |

**3건 중 3건이 닫힌 기록 없이 닫힘으로 계상됐다.** OBJ-42는 "Opponent도 인정"이라는 단서까지 붙었는데, 그 합의가 가리키는 원기록은 `surfaced`(드러남)다.

데이터셋 B에서는 `iter3-security.json` 한 레코드 안에서 C5·C6이 해소·미해소 양쪽에 동시 등재됐다(§3.3).

### 근거의 범위가 반박의 범위를 못 덮는다

닫힘 32건에 대해, 반박의 오라클이 지목한 위치를 닫힘 서술이 덮는지 봤다.

```
오라클 자리를 전부 덮음    2건 ( 6%)
일부만 덮음              10건 (31%)
한 자리도 안 덮음        20건 (62%)
```

`fix`는 산문이라 고친 자리를 다 열거하지 않았을 수 있다. 그래서 확정적 결론은 "안 고쳤다"가 아니라 **"기록으로 확인할 수 없다"**이며, 그 비율이 **94%(30/32)**다.

전형적인 모양은 이렇다.

```
반박:   Change Map이 '파싱 가능 정본'이라 주장하는데 grammar가 없다
오라클: 50 §0·§2·§5   (세 곳)
닫힘:   50 §2.1b에 최소 EBNF 추가 → 거버넌스 산출물로 격상
```

반박은 일반적 주장이고 닫힘은 한 점이다. "최소"라는 낱말이 범위가 좁다는 자백에 가깝다.

대비되는 경우도 있다. OBJ-24는 닫힘이 "5곳은 journey_id 참조로 통합"이라며 보편성을 **주장한다.** 그러나 그 5곳을 근거로 대지 않는다. **주장된 보편성과 입증된 보편성은 다르다.**

### 잔여를 등록할 칸이 없다

기존 필드의 충실도는 높다 — 데이터셋 A는 식별자·심각도·오라클·적합성·처리가 모두 100%, 처리 서술만 86%다. 데이터셋 B도 심각도·분류·위치·근거·제안이 전부 100%다.

문제는 다른 데 있다.

| §7.3 (d)가 요구하는 칸 | 데이터셋 A | 데이터셋 B |
|---|---|---|
| 닫힘 조건 | 필드 없음 | 필드 없음 |
| 감수 사유 | 필드 없음 | 필드 없음 |
| 영향 | 필드 없음 | 필드 없음 |
| 승인자 | 필드 없음 | 필드 없음 |

빈칸률을 물을 수 없다. **칸이 없다.** 반박을 기록하는 형식은 정교한데, 무엇을 안고 갈지 등록하는 형식은 설계되지 않았다.

### 하향률은 이 기록으로 측정할 수 없다

같은 자리를 다시 지적한 반박 쌍 45건 중 심각도가 낮아진 것이 22건(49%)으로 나왔지만, **이 수치는 쓸 수 없다.**

앵커가 절 단위(`20 §6`)라 "같은 자리"가 같은 문제를 뜻하지 않는다. 그리고 심각도 하락은 **진짜 개선**으로도 설명된다 — 심각한 것이 고쳐지고 사소한 것만 남으면 당연히 내려간다. 둘을 분리하려면 같은 항목이 재평가돼야 하는데, 그러려면 식별자가 필요하다. 없다.

**하향률은 빈칸률과 함께 §7.6의 지표 중 이 코퍼스로 측정 불가능한 둘이다.** 둘 다 원인이 같다 — 식별자와 잔여 등록 칸이 없다.

---

# 4. 진단

## 4.1 축 1 — 종료 불가능성

`새 반박 0건`을 종료 조건으로 삼은 것이 설계 오류다.

"결함이 없다"는 **부정 존재 명제**다. 증명하려면 탐색 공간 전체를 봐야 한다. 타입 체커나 유한 상태 모델 검사처럼 공간이 유한하고 기계적으로 열거되면 가능하다. 자연어 설계 문서에 대한 "충분한가?"는 그 공간이 열거 불가능하다.

따라서 도달 불가는 에이전트의 결함이 아니라 **문제의 성질**이다. 무한히 표본을 뽑을 수 있는 생성기 앞에서 "생성이 멈출 때까지" 기다리는 것과 같다.

## 4.2 축 2 — 안정성 부재

합의가 **상태의 성질**이면 재실행해도 유지돼야 한다. 심의를 연산자 f로 보면 합의 상태 x\*는 f(x\*) = x\* 를 만족해야 한다.

관찰된 것은 f(x\*) ≠ x\* 다. 지금까지의 `approve`는 **상태가 아니라 이벤트**였다 — "이 문서는 승인 가능한 상태다"가 아니라 "이번 실행에서 이 리뷰어가 승인했다"는 기록이다. 이벤트는 재현되지 않는다.

원인은 하나다. **재실행이 "검증"이 아니라 "재탐색"이다.**

| | 검증 | 탐색 |
|---|---|---|
| 질문 | 명제 N개가 각각 참인가 | 뭐가 문제인가 |
| 공간 | 고정, 유한 | 열림, 무한 |
| 분산 | 낮음 | 높음 |
| 고정점 | 있음 | **정의상 없음** |

탐색 연산자는 매번 새 것을 찾도록 만들어진 것이다. 거기에 고정점을 기대할 수 없다. §3.2에서 본 회차 간 발견 위치 불일치가 이것의 직접 증거다.

## 4.3 두 축은 곱해진다

|  | 안정성 있음 | 안정성 없음 |
|---|---|---|
| **종료 가능** | 작동한다 | 끝나긴 하는데 그 종료가 우연이다 |
| **종료 불가** | 진행은 믿을 수 있으나 안 끝난다 | **현재 상태** |

하나만 고치면:

- **종료 조건만 고치면** — 심각도 임계로 끝낼 수 있게 되지만 그 판정이 재현되지 않으므로 "끝났다"가 한 번의 표본이 된다. s4 architect가 정확히 이 상태였다. 종료는 됐는데 믿을 근거가 없다.
- **안정성만 고치면** — 합의를 명제로 동결하면 재확인은 안정되지만, *언제* 동결할지 결정하는 순간이 오지 않는다. 안정적인 대상이 만들어지지 않는다.

종료 조건이 있어야 동결 시점이 생기고, 안정성이 있어야 그 동결이 의미를 갖는다.

## 4.4 공통 뿌리

두 고장이 같은 결핍에서 나온다.

> **심의 결과가 상태로 표현되지 않고 이벤트로만 기록된다.**

- 상태가 없으니 무엇이 남았는지 셀 수 없다 → 종료를 판정할 수 없다 (축 1)
- 상태가 없으니 재진입할 때 돌아갈 기준점이 없다 → 매번 처음부터 재탐색한다 (축 2)

DISCUSSION.md §8은 역할·판정 규칙·종료 조건을 정의했지만 **라운드를 넘어 살아남는 자료구조를 정의하지 않았다.** `overturn` 필드가 그 씨앗이나 보고서 하나에만 붙고 지속되지 않는다.

---

# 5. 원리적 한계와 실행 대응의 분리

각 축에는 넘을 수 없는 부분과 만들면 되는 부분이 따로 있다. 섞으면 못 고치는 것을 고치려 하거나, 고칠 수 있는 것을 포기하게 된다.

| | 원리적 한계 | 실행 가능한 대응 |
|---|---|---|
| **종료** | 소진은 도달 불가 — 열거 불가능한 공간의 부정 존재 명제 | 목표를 **잔여 등록 + 심각도 임계**로 교체 |
| **안정** | 완전 재현 불가 — 모델이 확률적 | 합의를 **오라클 명제로 동결**, 재진입을 검증 모드로 |
| **공통** | — | 결과를 이벤트가 아니라 **상태**로 기록 |

안정성에 대해 한 가지 덧붙인다. 분산은 **질문의 폭에 비례**한다.

- "이 설계에 문제가 있는가?" → 분산 큼
- "20번 문서 169-175행이 505-524행과 모순되는가?" → 분산 작음
- "`grep src/ ts-morph`가 0건인가?" → **분산 0.** 모델이 아니라 실행이 판정한다

> **안정성은 합의를 얼마나 기계 검증 가능한 명제로 환원했는가에 비례한다.** 모델의 성질이 아니라 설계 선택이다.

---

# 6. 합의의 대체 정의 — 감수된 합의

"결함이 없다"를 증명하려 하지 말고 **"남은 결함이 감당 가능하다"를 판정한다.** 후자는 유한하게 판정된다.

```
합의   = 명제 N개(각각 오라클 부착) + 등록된 잔여 M개(각각 감수 사유)
재진입 = N개를 다시 확인한다. 새로 찾지 않는다.
새 탐색 = 별도 이벤트. 결과는 "합의 위반"이 아니라 "합의 범위 확장 제안"
```

이 형태라야 f(x\*) = x\* 가 성립할 수 있다. 재확인은 오라클에 묶인 만큼 결정적이고, 새 발견이 나와도 합의가 무너지는 것이 아니라 **범위가 넓어질 뿐**이다.

필요한 것 셋.

**1. 잔여를 없애지 말고 등록한다.** 닫히지 않은 항목을 목록으로 남기고 각각에 "왜 지금 감수하는가"를 적는다. 항공의 최소 장비 목록, 의료기기의 잔여 위험 문서, 소프트웨어의 known issues가 같은 구조다. 닫히지 않은 것이 사라지는 것이 아니라 **이름을 갖고 남는다.**

**2. 정지 규칙을 발견 수가 아닌 양에 건다.**

| 규칙 | 종료 조건 | 도달 가능? |
|---|---|---|
| 소진 | 새 반박 0건 | **아니오** — §4.1 |
| 심각도 임계 | critical 0건. 그 아래는 등록만 하고 통과 | 예 — s4 architect가 3회 달성 |
| 한계 수익 | 2회 연속 새 critical 0 | 예 |
| 커버리지 | 사전 고정된 검사 목록 N개 전부 수행 | 예 — 정의상 유한 |
| 예산 | 라운드 상한 | 도달이 아니라 포기. 다만 포기했다고 적으면 정직하다 |

핵심은 critical과 그 이하를 **다르게** 다루는 것이다. 지금은 medium 지적 하나가 critical과 같은 무게로 종료를 막는다. DISCUSSION.md §8.4의 3번 관문이 이 역할을 하려던 것인데, 판정 기준인 acceptance criteria가 그 계약에서 생산되지 않아 작동하지 않았다.

**3. 감수 판정은 사람이 한다.** "이 위험을 안고 간다"는 판단은 위험을 지는 주체만 할 수 있다. 에이전트가 할 일은 잔여 목록을 정확히 만드는 것까지다. 여기서 만장일치를 요구하지 않는 것이 중요하다 — 역할들의 불일치 자체가 사람에게 넘길 정보다.

---

# 7. 감수된 합의의 공격면 — 정상처럼 포장하기

## 7.1 도달 가능성과 조작 가능성은 함께 온다

§6은 종료를 가능하게 만들면서 **새 공격면을 열었다.** 정직하게 적어 둔다.

소진형 종료(`새 반박 0건`)는 도달 불가능했지만 조작할 유인도 없었다. 반박이 나오면 나온 것이고, 낮춰 부를 자리가 없다.

임계형 종료는 도달 가능하다. 그런데 종료가 **심각도 라벨**에 걸린다. 라벨은 서술이고, 서술은 서술하는 쪽이 정한다.

> **종료를 통제하는 양은 조작 대상이 된다.** 도달 가능성과 조작 가능성이 함께 온다.

그래서 §6은 그 자체로는 미완성이다. 종료 조건을 바꾸는 것과 **그 종료 조건을 지키는 것**이 한 묶음이어야 한다.

## 7.2 조작의 형태

"빨리 끝내려고 정상처럼 포장하는" 방식은 여러 가지고, 각각 다른 방어가 필요하다.

| 형태 | 하는 일 | 겉보기 | 이 코퍼스에서 |
|---|---|---|---|
| **축소** | critical을 medium으로 부른다 | 등급이 낮으니 통과 | 측정 불가 (§3.7) |
| **누락** | 아예 목록에 올리지 않는다 | 그런 문제는 없었던 것이 된다 | 소멸률 18~87% |
| **얼버무림** | "일부 개선 여지 있음"처럼 판정 불가능하게 쓴다 | 적기는 적었다 | 해당 칸 자체가 없음 |
| **범위 밖 선언** | "이번 범위가 아니다"로 옮긴다 | 잔여가 아니라 무관 항목 | 이관 42% 중 일부 |
| **분할** | critical 하나를 medium 셋으로 쪼갠다 | critical 0건 | 미측정 |
| **미래 이관** | "후속 작업에서"로 넘긴다 | 계획이 있으니 해결된 셈 | **42%**, 닫힘 조건 있는 것 11% |
| **조건부 무해화** | "정상 사용 시에는 문제없음"을 붙인다 | 조건부라 심각하지 않다 | 미측정 |
| **허위 닫힘** | 닫히지 않은 것을 닫혔다고 선언한다 | 원장에 닫힘으로 남는다 | **3건 중 3건 근거 없음** |
| **오라클 훼손** | 근거가 참이 되도록 측정 대상을 바꾼다 | 근거가 실제로 참이다 | 미측정 |
| **범위 미달** | 한 점만 만족시키고 전체를 주장한다 | 근거가 실제로 참이다 | **확인 불가 94%** |

앞의 일곱은 서술을 조작한다. 뒤의 셋은 다르다 — **근거가 참인 채로 통과한다.** 그래서 근거의 존재를 요구하는 것만으로는 걸리지 않는다. §7.4가 이 셋을 다룬다.

특히 **오라클 훼손**은 형태를 정확히 볼 필요가 있다. 테스트를 주석 처리한 뒤의 `all-green`은 거짓말이 아니다. 명령을 실행했고 정말 통과했다. 거짓인 것은 관찰이 아니라 **그 관찰로부터의 추론**이다. **범위 미달**도 같다 — `f(2)==4`를 하드코딩으로 통과시켰을 때, 테스트 결과는 참이고 다만 참인 범위가 주장의 범위를 못 덮는다.

## 7.3 방어

원리는 하나다. **서술과 판정을 분리하고, 등급을 낮추는 방향에 비용을 붙인다.**

**(a) 등급은 주장이 아니라 규칙의 출력이다.**
심각도를 에이전트가 판단해서 붙이면 조작된다. 사전에 고정한 분류 규칙의 적용 결과여야 한다 — "acceptance criterion을 못 맞추면 critical", "데이터 손실 가능하면 critical" 같은 식이다. 그러면 등급을 낮추려면 **사실을 왜곡**해야 하고, 사실은 오라클로 검증된다. 조작을 등급 층에서 사실 층으로 밀어내는 것이 목적이다.

**(b) 하향에만 비용을 붙인다 — 비대칭.**
등급 상향은 자유, 하향은 오라클 필수 + 원장에 이력을 남긴다. 하향 이력이 남으면 사후 감사가 가능하다. 조작은 흔적이 남을 때 비용이 생긴다.

**(c) 애매하면 높은 쪽 — 기본값이 종료를 막는다.**
등급이 갈리면 높은 쪽을 취한다. "판정 불가"는 통과가 아니라 blocked다. 기본값이 통과 쪽이면 얼버무림이 곧 통과가 된다.

**(d) 빈칸을 허용하지 않는 등록 형식.**
잔여 항목은 다음을 모두 채워야 등록된다. 하나라도 비면 **등록 불가이고, 등록 안 된 잔여가 있으면 종료 불가**다.

```
닫힘 조건:  무엇이 참이 되면 이 항목이 닫히는가 (판정 가능한 문장)
감수 사유:  왜 지금 이것을 안고 가는가
영향:       실현되면 무슨 일이 일어나는가
승인자:     누가 감수했는가
```

얼버무림은 형식에 빈칸이 허용될 때만 가능하다. "일부 개선 여지 있음"은 닫힘 조건 칸을 채울 수 없어서 자동으로 걸린다.

**(e0) 항목에 식별자를 준다 — 나머지 전부의 전제.**
§3.6에서 본 대로 데이터셋 B의 이슈 216건 중 식별자를 가진 것이 0건이고, 원장 참조의 42%가 존재하지 않는 이름을 가리킨다. 식별자가 없으면 "사라졌다"를 판정할 대상 자체가 없고, 하향률도 잴 수 없다. 아래 (e)와 §7.6의 절반이 이 하나에 걸려 있다.

**(e) 조용한 소멸을 금지한다 — 닫힘은 명시적 사건이다.**
이전 라운드에 있던 항목이 이번 목록에 없으면 자동으로 표시한다. 항목은 닫힘 근거 없이 사라질 수 없다. 누락은 목록에 **없는 것**을 찾는 문제라 원리적으로 어려운데, 이전 상태와의 차집합으로 보면 기계적으로 검출된다.

이것이 원장의 두 번째 역할이다. §3.4에서 본 원장은 이월 추적만 했고 범위 축소에도 소멸 검출에도 쓰이지 않았다. §3.3의 C5·C6 동시 등재도 닫힘이 사건으로 기록됐다면 그 자리에서 충돌로 걸렸을 것이다.

**(f) 서술자와 종료 판정자를 분리한다.**
잔여 목록을 만드는 주체와 "이 잔여로 끝낼 수 있는가"를 판정하는 주체가 달라야 한다. 같으면 자기가 쓴 라벨로 자기를 통과시킨다. §3.1에서 Producer가 7라운드 중 6회 자기 안을 `accept`로 자평한 것이 이 구조의 결과다.

## 7.4 닫힘 관문 — 적합성의 네 층

§7.3은 서술을 다룬다. 그런데 §7.2 뒤 세 형태는 **근거가 참인 채로** 통과한다. 근거를 요구하는 것만으로는 걸리지 않는다.

근거가 닫힘을 정당화하려면 네 가지를 만족해야 하고, 층마다 다른 실패가 있다.

| 층 | 묻는 것 | 실패했을 때 | 하드코딩이 통과하는가 |
|---|---|---|---|
| **① 존재** | 근거가 있는가 | 인상 비평 | 통과 |
| **② 관련성** | 그 근거가 이 반박을 겨냥하는가 | 참인 사실로 다른 것을 증명 | 통과 |
| **③ 무결성** | 근거가 반박보다 먼저 확정됐고 그 뒤로 안 변했는가 | 측정 대상을 바꿔 근거를 만족 | 통과 |
| **④ 보편성** | 근거가 참인 범위가 반박의 범위를 덮는가 | 한 점만 만족시키고 전체를 주장 | **여기서 걸린다** |

§3.7에서 본 것은 이 넷이 닫힘 쪽에 **하나도 설치되지 않았다**는 것이다. 반박 쪽에는 ①과 ②가 있고(오라클 규칙, `admissible` 판정), DISCUSSION.md §8.2 (3)의 `overturn`이 ③에 해당한다. 닫힘 쪽은 비어 있다.

### ③ 무결성 — 저항력은 존재가 아니라 선행성에서 온다

사후에 고르는 근거는 언제나 주장에 맞춰진다. 그래서 **반박이 제기되는 그 시점에 닫힘 조건을 확정하고 동결**해야 한다. 나중에 조건을 고쳐 만족시킬 수 없게.

TDD의 red-first 규칙이 정확히 이것이고, 그것만으로 부족해서 "테스트를 비활성화·삭제·완화·skip하지 말 것"이 별도 조항으로 따로 있다. 심의 계약에는 그에 해당하는 조항이 없다.

기계적 보조 둘.

- **차분 검사** — 근거 자체가 라운드 사이에 약해졌는지 센다. 코드라면 테스트 수·assertion 수·skip 수, 심의라면 닫힘 조건 문구가 제기 시점과 같은지.
- **음성 대조** — "이 근거가 거짓이려면 무엇이 참이어야 하는가"에 답하게 한다. 답할 수 없으면 반증 불가능한 근거이고, **반증 불가능한 근거는 아무것도 닫지 못한다.** 절대 빨개지지 않는 테스트가 아무것도 검사하지 않는 것과 같다.

### ④ 보편성 — 한 점은 전체를 못 덮는다

- **속성으로 쓴다.** 닫힘 조건을 편집이 아니라 불변식으로 적는다. "§6에 3행 추가"가 아니라 "모든 신설 스키마는 20번 문서에 등재되어 있다". 전자는 만족시키고 끝이고, 후자는 새 사례가 생겨도 계속 검사된다. 속성 기반 테스트가 하드코딩을 막는 원리 그대로다.
- **되돌림 검사.** 닫힘 근거가 된 편집을 되돌렸을 때 원래 반박이 되살아나는가. 되살아나지 않으면 그 편집은 그 반박을 닫은 것이 아니다. 필요조건 검사이고 기계적으로 가능하다.
- **홀드아웃.** 닫힘을 검증할 케이스 일부를 고치는 쪽에 보여주지 않는다. 무엇을 맞춰야 할지 모르면 맞춰 쓸 수 없다.
- **범위 진술 강제.** "이 근거가 지목된 자리 전부에 대해 유효한 이유"를 한 문장으로 요구한다. §3.7의 OBJ-24는 이걸 했고 OBJ-14는 안 했다. 요구하지 않으면 안 쓴다.

### 관문의 형태

```
닫힘 주장이 원장에 들어가려면:
  ① 근거가 붙어 있는가
  ② 그 근거가 이 반박을 겨냥하는가                    ← 반박과 동일한 적합성 심사
  ③ 근거가 반박 제기 시점에 확정됐고 그 뒤 안 변했는가   ← 동결 + 차분 검사
  ④ 근거의 범위가 반박의 오라클 자리를 전부 덮는가       ← 속성화 · 되돌림 검사
넷 중 하나라도 아니면 닫힘이 아니라 '부분 닫힘'으로 등록된다.
```

## 7.5 방어는 조작을 없애지 못한다

비용을 올릴 뿐이다. 그리고 **모든 방어는 새 게임을 만든다.**

§7.3 (a)의 분류 규칙을 두면 규칙에 걸리지 않게 사실을 서술하는 조작이 생긴다. (d)의 필수 칸을 두면 칸은 채우되 내용이 공허한 문장이 들어온다. (e)의 소멸 검출을 두면 소멸 대신 "닫힘"으로 잘못 표시하는 쪽으로 옮겨간다 — §3.7의 3/3이 그 형태다.

그리고 §7.4의 네 층조차 종점이 아니다. **오라클의 무결성은 오라클로 검증할 수 없다.** 테스트가 통과했다는 사실은 그 테스트가 옳다는 것을 말하지 않는다. 한 수준 위에서 봐야 하고, 그 수준에도 같은 문제가 있다.

그래서 이렇게 적는 것이 정확하다.

> **조작 저항성은 근거의 존재가 아니라 근거의 선행성·불변성·범위에서 온다. 그리고 그것도 완전하지 않다.**

`grep 결과 0건`은 낮춰 부를 수 없지만, 그 grep 패턴을 누가 언제 골랐는지가 남는 문제다. 무한 후퇴를 끊는 것은 논리가 아니라 **사람의 서명**이다 — §6의 세 번째 항목이 여기로 이어진다. 자동 관문은 사람이 봐야 할 것을 줄이는 장치이지, 사람을 대체하는 장치가 아니다.

## 7.6 조작을 측정한다

방어가 작동하는지 보려면 조작 자체가 지표여야 한다. 오른쪽은 이 코퍼스에서 실제로 나온 값이다(§3.6~§3.7).

| 지표 | 정의 | 측정값 |
|---|---|---|
| **소멸률** | 다음 라운드 원장에서 해소·이월 어느 쪽으로도 명시되지 않은 항목 비율 | B **18%** / A **87%** |
| **이관율** | "후속 작업"·"범위 밖"으로 옮겨진 항목 비율 | A **42%** (닫힘 조건 있는 것 11%) |
| **닫힘 근거 유형** | 닫힘 근거가 검증인가 편집 서술인가 | 편집 **91%** / 검증 **3%** |
| **닫힘 근거율** | 닫힘 주장 중 원기록에 닫힘 근거가 있는 비율 | **0/3** (R5 `already_closed_prior`) |
| **범위 미덮음률** | 반박 오라클 자리를 닫힘이 덮었는지 확인 불가한 비율 | **94%** |
| **적합성 심사율** | 닫힘에 적합성 판정이 붙은 비율 | **0%** (필드 없음) |
| **빈칸률** | 잔여 등록 필수 칸이 비었거나 판정 불가능한 비율 | **측정 불가 — 칸이 없다** |
| **하향률** | 같은 항목의 심각도가 낮아진 비율 | **측정 불가 — 식별자가 없다** |

이 지표들이 종료 직전 라운드에서 튀면, 그 종료는 도달이 아니라 포장이다.

마지막 두 줄이 중요하다. **조작 지표 여덟 중 둘은 현재 기록 형식으로 아예 잴 수 없다.** 항목 식별자와 잔여 등록 칸이 없기 때문이다. 방어를 설치하기 전에 **측정 가능한 기록 형식이 먼저**다 — §3.5에서 형식 고정이 측정 가능성의 전제라고 한 것이 여기서 두 번째로 확인된다.

---

# 8. 검증 설계

## 8.1 검증할 명제

§4의 진단은 아직 가설이다. 검증할 것은 "절차만으로는 부족하다"가 아니라 — 그건 §3에서 이미 지지됐다 — **"무엇을 채우면 어느 실패가 줄어드는가"**다.

## 8.2 2×2

두 축을 각각 켜고 꺼야 상호작용이 보인다. 따로 A/B를 두 번 돌리면 안 보인다.

|  | 재진입 = 탐색 | 재진입 = 검증 |
|---|---|---|
| **종료 = 소진형** | 현재 방식 (기준선) | 안정만 개선 |
| **종료 = 임계형** | 종료만 개선 | 둘 다 |

## 8.3 통제

- **대상**: 이미 합의로 끝난 산출물 1개
- **산출물을 조건 간에 전혀 수정하지 않는다.** 이것이 핵심 통제다. 문서가 바뀌면 새 발견이 당연해져서 아무것도 측정할 수 없다
- **반복**: 조건당 5회 = 총 20런
- **형식 고정**: §3.5 때문에 레코드 스키마를 사전에 고정하고, 스키마 위반은 결측이 아니라 **오류**로 처리한다. 이걸 안 하면 계측이 침묵을 수렴으로 읽는다
- **스키마에 반드시 포함**: 항목 식별자(§7.3 e0)와 잔여 등록 칸(§7.3 d). 이 둘이 없으면 조작 지표 여덟 중 둘이 원리적으로 측정 불가다(§7.6)

## 8.4 지표

**종료 축**
1. 종료 도달률 — 20런 중 종료 조건을 만족한 비율
2. 도달까지 라운드 수
3. 잔여 등록 건수

**안정 축**

4. 판정 일치율 — 5회 중 같은 판정이 나온 비율
5. 발견 재현율 — 한 번이라도 나온 발견 중 과반 회차에 나온 비율
6. 누적 발견의 포화 — 회차가 늘 때 합집합이 포화하는가

**조작 축** — §7.6의 여덟. 임계형 종료를 켠 두 칸에서만 의미가 있다.

7. 소멸률 · 8. 이관율 · 9. 닫힘 근거 유형 · 10. 닫힘 근거율 · 11. 범위 미덮음률 · 12. 적합성 심사율 · 13. 빈칸률 · 14. 하향률

13·14는 §8.3의 고정 스키마에 **항목 식별자와 잔여 등록 칸을 넣어야** 측정된다. 기존 코퍼스에서는 잴 수 없었다(§7.6).

임계형 종료 칸의 종료 도달률이 올라갔는데 7~14도 같이 올랐다면, 그 개선은 종료가 아니라 포장이다. **이 대조가 §7 전체의 검증이다.**

**공통**

15. 오라클 허위율 — 인용한 `파일:라인`이 없거나 그 내용이 아닌 비율 (존재 여부는 기계 검증 가능)

## 8.5 예측과 반증 조건

- 기준선 칸: 판정 일치율 낮음, 누적 발견 미포화, 종료 도달률 0에 가까움
- 둘 다 켠 칸이 하나만 켠 칸들보다 좋아야 한다 — **상호작용 항이 §4.3의 예측이다**
- **반증**: 하나만 켜도 충분히 개선되면 §4.3이 틀린 것이다. 그때는 효과가 큰 축 하나만 남긴다
- **반증**: 검증 모드에서도 판정 일치율이 오르지 않으면 §4.2의 진단(재탐색이 원인)이 틀린 것이다
- **반증**: §7.3의 방어를 켰는데 조작 지표가 안 내려가면 그 방어는 효과가 없는 것이다

---

# 9. 이 문서가 하지 못하는 것

- **관찰 2를 직접 측정하지 못했다.** 코퍼스에 재실행 기록이 0건이다. §3.3의 단일 판정 내부 모순은 방증이지 재현성 측정이 아니다.
- **표본이 두 코퍼스뿐이다.** `.ditto`가 다른 곳에 5군데 더 있다. 여기서 본 패턴이 이 둘에 특유한 것인지 일반적인지 확인되지 않았다.
- **역할 간 발견 중복률을 계산하지 못했다.** 위치 기반으로 세었더니 넓은 라인 범위(`L75-255` 같은)가 다른 모든 발견과 겹치는 것으로 집계되어 겹침 수가 발견 수를 넘었다. 정규화하거나 의미 기준으로 다시 세야 한다. 이 값이 필요한 이유는 포획-재포획으로 **잔여 결함을 소진 없이 추정**하기 위해서다 — §6의 "감당 가능한가"를 수치로 뒷받침할 수 있는 도구다.
- **발견 수가 산출물 품질을 재는지 리뷰어의 생성 성향을 재는지 분리하지 못했다.** 적대적 역할은 0건 반환이 임무 실패처럼 보이므로 구조적으로 항상 무언가를 내놓을 유인이 있다. 이건 추론이며 데이터로 분리되지 않았다.
- **§7.3·§7.4의 방어는 하나도 시험되지 않았다.** 조작이 일어났다는 것은 측정했지만(§3.6~§3.7), 방어를 걸면 줄어드는지는 확인되지 않았다. §8이 그 설계다.
- **조작 형태 열 중 셋은 미측정이다.** 분할·조건부 무해화·오라클 훼손은 이 코퍼스에서 세지 않았다. 오라클 훼손은 원본 산출물의 편집 이력이 있어야 잴 수 있는데 이 기록에는 없다.
- **하향률과 빈칸률은 원리적으로 측정 불가였다.** 식별자와 잔여 등록 칸이 없어서다. 하향률의 대용으로 절 단위 앵커를 써 49%를 얻었으나, 진짜 개선과 하향 조작이 분리되지 않아 폐기했다(§3.7).
- **범위 미덮음률 94%는 "안 고쳤다"가 아니라 "기록으로 확인할 수 없다"이다.** `fix`가 산문이라 고친 자리를 다 열거하지 않았을 수 있다.
- **닫힘 근거율 0/3은 표본이 3건이다.** 명시적 닫힘 선언이 코퍼스 전체에 그것뿐이었다.
- **§1.1의 목적 분류는 이 문서에서 검증되지 않는다.** 합의 외의 목적(발산·탐색·판정)에 무엇이 필요한지는 다루지 않았다.
- **§4는 진단이지 검증된 결론이 아니다.** §8이 그것을 위한 설계다.

---

# 10. 핵심 정리

**측정된 사실**

- dialectic 9라운드 전부 `revise`. accept 0회. 반박 수는 14→10→6→**20**→9→7→8→13→11로 단조 감소하지 않는다.
- 병렬 리뷰 15계열 중 발견 수가 단조 감소한 것은 4계열. s4 redteam은 5회 반복 후 시작보다 지적이 많다(8→8→9→7→10).
- **전원 동시 approve는 15회 반복 중 0회.** 개별 approve는 5회 나왔다.
- 승인이 뒤집힌다. s4 architect는 3회 연속 approve 후 재개하자 critical이 나왔고 돌아오지 않았다.
- 오라클은 문서 절 → 라인 → 실행으로 3단계 강해졌는데 종료율은 0으로 동일하다.
- `iter3-security` 한 레코드 안에서 C5·C6이 해결됨과 미해결에 **동시 등재**됐다.
- 원장 필드가 14개 레코드에 있었으나 이월 추적에만 쓰였고 탐색 범위 제한에는 쓰이지 않았다.
- **항목이 사라진다.** 소멸률은 데이터셋 B 18%, 데이터셋 A 87%다. A에서 살아남은 이월은 전부 직전 라운드 것으로, **이월은 한 라운드를 넘기지 못한다.**
- **이관이 소멸의 경로다.** 반박의 42%가 미래·범위 밖으로 넘어갔고, 그중 닫힘 조건이 적힌 것은 11%다. R4에서 이관한 6건 중 5건이 이후 나타나지 않았다.
- **닫힘에는 관문이 없다.** 반박 83건은 100%가 적합성 판정을 받았고 33건이 실제로 기각됐는데, 닫힘 32건 중 적합성 판정을 받은 것은 0건이다 — 필드가 없다.
- **닫힘 근거의 91%가 편집 행위 서술이고, 재검증을 인용한 것은 3%다.** "무엇을 고쳤다"이지 "그래서 해소됐다"가 아니다.
- **닫힘 선언 3건 전부 근거가 없었다.** R5가 "이미 닫힘"이라 한 셋의 원기록은 `fix-needed`, `fix-needed`, `surfaced`였다.
- **닫힘이 반박의 범위를 덮는지 94%가 기록으로 확인 불가하다.**
- 데이터셋 B의 이슈 216건 중 식별자를 가진 것은 0건이고, 원장 참조의 42%가 존재하지 않는 이름을 가리킨다.

**진단**

- 두 개의 독립적인 고장이다. **종료 불가능성**(소진은 도달할 수 없는 목표)과 **안정성 부재**(합의가 상태가 아니라 이벤트로 기록됨).
- 두 고장은 곱해진다. 하나만 고치면 나머지가 그 효과를 없앤다.
- 공통 뿌리는 **라운드를 넘어 살아남는 자료구조가 없다는 것**이다. 부족한 것은 절차가 아니다.
- "모두가 동의할 때 끝낸다"는 종료 조건은 역할 수에 대해 지수적으로 멀어진다.
- 형식 고정은 위생 문제가 아니라 측정 가능성의 전제다 — 형식이 흔들리면 판독기가 침묵을 수렴으로 읽는다.

**대응**

- 목표를 소진에서 **잔여 등록 + 심각도 임계**로 바꾼다. "결함 없음" 대신 "남은 결함이 감당 가능함"을 판정한다.
- 합의를 **오라클에 묶인 명제 집합**으로 동결하고, 재진입의 기본 모드를 탐색이 아니라 검증으로 둔다.
- 감수 판정은 사람이 한다. 에이전트는 잔여 목록까지만 만든다.
- 원리적 한계(소진 불가, 완전 재현 불가)는 인정하고 목표에서 뺀다. 실행 대응 부분만 만든다.

**단서**

- **종료를 가능하게 만들면 조작도 가능해진다.** 임계형 종료는 심각도 라벨에 걸리고, 라벨은 서술이다. 축소·누락·얼버무림·이관으로 "정상처럼 포장"하는 경로가 §6과 함께 열린다.
- 방어는 등급을 규칙의 출력으로 만들고, 하향에만 비용을 붙이고, 빈칸 없는 등록 형식을 강제하고, **닫힘을 명시적 사건으로** 만드는 것이다. 항목은 조용히 사라질 수 없어야 한다. 그 전제로 **항목에 식별자가 있어야 한다.**
- **근거가 참인 채로 통과하는 조작이 따로 있다.** 허위 닫힘, 오라클 훼손, 범위 미달. 주석 처리 후의 `all-green`도 하드코딩으로 통과한 `f(2)==4`도 관찰은 참이고, 거짓인 것은 그로부터의 추론이다. 근거의 **존재**를 요구하는 것만으로는 걸리지 않는다.
- 그래서 닫힘 관문은 네 층이다 — **존재 · 관련성 · 무결성 · 보편성.** 지금 계약에는 넷 다 없다.
- **조작 저항성은 근거의 존재가 아니라 근거의 선행성·불변성·범위에서 온다.** 사후에 고르는 근거는 언제나 주장에 맞춰진다. TDD의 red-first가 이 규칙이고, "테스트를 비활성화·완화하지 말 것"이 그것을 지키는 별도 조항이다.
- 그리고 그것도 완전하지 않다. **오라클의 무결성은 오라클로 검증할 수 없다.** 무한 후퇴를 끊는 것은 논리가 아니라 사람의 서명이다. 자동 관문은 사람이 봐야 할 것을 줄이는 장치이지 사람을 대체하는 장치가 아니다.
- **측정 가능한 기록 형식이 방어보다 먼저다.** 조작 지표 여덟 중 둘(하향률·빈칸률)은 식별자와 잔여 등록 칸이 없어 이 코퍼스에서 아예 잴 수 없었다.
- **합의는 여러 목적 중 하나다.** 발산 과제에 합의 모델의 종료 조건을 걸면 다양성을 죽인다. 이 문서의 진단은 합의 모델에만 적용된다.
